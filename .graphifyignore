# Derived artifacts regenerated by `npm run build` — index the sources, not the outputs.
build/generated/
src/runtime/generated/

# Android RN template glue — not part of the architecture graph we query.
android/
