/** Types for tmp-bundle.mjs (see there): where a runner writes the esbuild bundle it imports. */
export declare const TMP_BUNDLE_DIR: string;
export declare function tmpBundlePath(name: string, root?: string): string;
