## 1. package.json codegen and dev script (HUMAN-BOOTSTRAP)

- [ ] 1.1 In `package.json` `codegenConfig`, add `"ios": {"modulesProvider": {"WhimTone": "WhimToneModule"}}` beside the existing `android` key, leaving `name`, `type`, `jsSrcsDir` and `android` unchanged (design D9).
- [ ] 1.2 Change the `android:release` script to `react-native run-android --mode offline --active-arch-only` (design D4). This needs section 5 merged, since that section adds the `offline` build type.
- [ ] 1.3 Run `./scripts/gate.sh` on the staging branch, then commit `package.json` alone as one human change.

## 2. Native release file and release tooling foundation

- [ ] 2.1 Create `release/whim-release.xcconfig` with the five keys and values in design D1. Its header comment says what reads the file (the Xcode project level, `android/app/build.gradle`, `scripts/release/lib/native-config.ts`), gives the strict grammar, notes the lockstep with `src/host/launcher/release-config.ts`, and names `docs/release/mobile.md`.
- [ ] 2.2 Create `scripts/release/lib/native-config.ts`. It holds the `NativeReleaseConfig` type and `parseNativeReleaseConfig(text)`, which enforces design D1's grammar and every key's shape: a reverse-DNS app id, a 10-character uppercase alphanumeric team id, `MAJOR.MINOR.PATCH`, a positive integer build number, and a lowercase hostname with no scheme or path. It throws `NativeConfigError` carrying the line number. Add `loadNativeReleaseConfig(repoRoot)`, and `scanNativeLiterals(repoRoot, config)`, which reports app id, team id and domain literals in tracked text files under `ios/` and `android/` (via `git ls-files`) and marketing version literals in `project.pbxproj`, `Info.plist` and `android/app/build.gradle`.
- [ ] 2.3 Create `scripts/release/lib/build-number.ts` per design D3: `BUILD_NUMBER_EPOCH_UTC`, `buildNumberAt(date)` (throws before the epoch), and `assertBuildNumberAbove(build, storeLatest)`, whose error names both numbers.
- [ ] 2.4 Create `scripts/release/run.mjs` following `checks/test/run.mjs`: it esbuild-bundles `scripts/release/cli.ts` to a pid-named temp file with `external: ['typescript', 'playwright']` and `tsconfigRaw: '{}'`, imports it, removes the file in `finally`, and propagates the exit code. Create `scripts/release/cli.ts` with a command table (name → `{ summary, run(args) }`) holding only `build-number [--at <iso>]`. An unknown or missing command exits 2 and prints the table.
- [ ] 2.5 Scaffold the release suites. Create `checks/test/release/index.ts` exporting `runReleaseSuites()`, which awaits `run()` from `native-config`, `hermes-entry`, `ios-project`, `android-project`, `assets`, `store-listing`, `release-cli` and `domain-lockstep` (`<name>.suite.ts` each) in that order. Create each suite file other than `native-config` as a stub whose `run()` does nothing and whose header names the owning chain from `chains.md`. Import `runReleaseSuites` in `checks/test/acceptance.ts` and call it at the end of `main()`, after `runHostileCorpus()`.
- [ ] 2.6 Fill `checks/test/release/native-config.suite.ts` using `checks/test/harness.ts`. The parser accepts the real file and rejects `#include`, a `$(...)` value, a duplicate key, a missing key and a malformed team id, each error naming its line. `buildNumberAt('2026-09-14T12:00:30Z')` is `369360`, the epoch gives `0`, and a date before it throws. `scanNativeLiterals` finds nothing in the repo. On temp-directory fixtures it names a literal app id planted in `android/app/build.gradle` (the discriminating case, against a scan that only reads the pbxproj) and a literal marketing version in `Info.plist`.

## 3. Hermes runtime prerequisites

- [ ] 3.1 `git mv src/host/version-store/polyfills.ts src/host/platform/hermes-polyfills.ts` and rewrite it per design D8. Export `HermesGlobal` and `installHermesPolyfills(target = globalThis, platform?)`. Check `TextEncoder`, `TextDecoder`, `Buffer`, `process`, `process.env` and `process.platform` on every call, taking codec constructors from `text-encoding-polyfill`'s exports. Remove the module-level `installed` flag, write the platform only when passed and missing, and keep the side-effect install on load. Keep the #36 D2 / #39 references in the header and note the new location.
- [ ] 3.2 Create `src/host/platform/install-entry-polyfills.ts`, which imports `Platform` from `react-native` and calls the installer with `ios` or `android` (any other `Platform.OS` passes none). Make `import './src/host/platform/install-entry-polyfills';` the first statement of `index.js`; a comment above it is fine.
- [ ] 3.3 Point `src/host/version-store/compaction.ts`, `device-acceptance.ts` and `engine.ts` at `../platform/hermes-polyfills`, keeping each as a side-effect import in the same position. Add `import '../platform/hermes-polyfills';` as the first import of `src/host/storage-engine/marshal.ts`. `npm run vstore:test` and `npm run storage:test` stay green.
- [ ] 3.4 Fill `checks/test/release/hermes-entry.suite.ts`, per specs/hermes-runtime-prerequisites. Parse `index.js` with `ts.createSourceFile` (`ScriptKind.JS`) and assert its first statement is the bare side-effect import of the entry installer. Temp fixtures must fail with the import second and with the import given a binding (`import x from …`). Against fresh plain objects: a bare object gets all globals and a UTF-8 round trip of `héllo ✓` succeeds; an existing `TextDecoder` is kept by identity; a call without a platform leaves `process.platform` missing; a later call with `android` sets it; an existing value is never overwritten.

## 4. iOS project identity, hardening and link delivery

- [ ] 4.1 In `ios/Whim.xcodeproj/project.pbxproj`, add a file reference to `../release/whim-release.xcconfig` in a new `Release` group and set it as `baseConfigurationReference` for the project-level Debug and Release configurations. On the `Whim` target's Debug and Release, set `PRODUCT_BUNDLE_IDENTIFIER = "$(WHIM_APP_ID)"`, `DEVELOPMENT_TEAM = "$(WHIM_APPLE_TEAM_ID)"`, `CODE_SIGN_STYLE = Automatic`, `MARKETING_VERSION = "$(WHIM_MARKETING_VERSION)"`, `CURRENT_PROJECT_VERSION = "$(WHIM_BUILD_NUMBER)"`, `CODE_SIGN_ENTITLEMENTS = Whim/Whim.entitlements` and `TARGETED_DEVICE_FAMILY = 1` (design D1, D6). Make the edits with the `xcodeproj` gem from the root Gemfile (`BUNDLE_PATH=vendor/bundle bundle exec ruby …`), never by regenerating the project. Keep the script out of the repo and record its exact commands in the handoff.
- [ ] 4.2 Create `ios/Whim/Whim.entitlements` with `com.apple.developer.associated-domains` = `["applinks:whim.$(WHIM_DOMAIN)"]` and add it to the project as a file reference outside every build phase.
- [ ] 4.3 In `ios/Whim/Info.plist`, add `ITSAppUsesNonExemptEncryption` = false. Delete the empty `NSLocationWhenInUseUsageDescription` and `UISupportedInterfaceOrientations~ipad`. Leave ATS unchanged (design D5) and keep `CFBundleDisplayName` = `Whim`.
- [ ] 4.4 Update `ios/Whim/PrivacyInfo.xcprivacy` per design D7: keep the three accessed-API entries, add DiskSpace `E174.1`, and declare `NSPrivacyCollectedDataTypeOtherUserContent` and `NSPrivacyCollectedDataTypeDeviceID`, each not linked, not tracking, purpose App Functionality. `NSPrivacyTracking` stays false.
- [ ] 4.5 In `ios/Whim/AppDelegate.swift`, forward `application(_:open:options:)` and `application(_:continue:restorationHandler:)` to `RCTLinkingManager`, keeping `launchOptions` passed to `startReactNative` (specs/app-links §"The iOS app delivers universal links to the launcher").
- [ ] 4.6 Create `scripts/release/lib/ios-project.ts`. It needs a small XML plist reader (dict, array, string, true/false, integer) and extraction of the `Whim` target's build settings per configuration from the pbxproj, plus `checkIosProject(repoRoot, config)` returning findings. Fill `checks/test/release/ios-project.suite.ts`: the repo passes, and temp fixtures fail for an empty usage-description string, `TARGETED_DEVICE_FAMILY = "1,2"`, a literal bundle id, `ITSAppUsesNonExemptEncryption` set to `true` (the discriminating case, against a presence-only check), an entitlement host not built from `$(WHIM_DOMAIN)`, a manifest without DiskSpace, and a manifest declaring tracking.
- [ ] 4.7 If the worktree can run the iOS toolchain (the primary tree's `node_modules` symlinked, network for pods), run `npm run build`, `(cd ios && BUNDLE_PATH=../vendor/bundle bundle exec pod install)` and `xcodebuild -workspace ios/Whim.xcworkspace -scheme Whim -configuration Release -sdk iphonesimulator -derivedDataPath ios/build/sim CODE_SIGNING_ALLOWED=NO build`, and put the resolved `PRODUCT_BUNDLE_IDENTIFIER`, `DEVELOPMENT_TEAM`, `MARKETING_VERSION` and `CURRENT_PROJECT_VERSION` in the report. Otherwise report "not run". Never commit `Podfile.lock` path-only churn.

## 5. Android identity, build types, signing and link filter

- [ ] 5.1 In `android/app/build.gradle`, parse `../../release/whim-release.xcconfig` with design D1's grammar, failing on any other line or a missing key. Set `applicationId`, `versionName` and `versionCode` (the `whimBuildNumber` property if present, else `WHIM_BUILD_NUMBER`), and `manifestPlaceholders.whimWebHost = "whim.${WHIM_DOMAIN}"` as a single-key assignment. Keep `namespace "com.whim"`.
- [ ] 5.2 Per design D4: add `signingConfigs.release` from `WHIM_UPLOAD_STORE_FILE`, `WHIM_UPLOAD_STORE_PASSWORD`, `WHIM_UPLOAD_KEY_ALIAS` and `WHIM_UPLOAD_KEY_PASSWORD` (Gradle property, else environment variable). Make `release` non-debuggable and upload-signed, with a `doFirst` on `preReleaseBuild` that throws a `GradleException` naming every missing value and `docs/release/mobile.md`. Add the `offline` build type (`initWith release`, debug signing, `debuggable true`, `matchingFallbacks = ['release']`) and `sourceSets.offline.res.srcDirs += 'src/debug/res'`. Replace the v0.1 comment with one describing `debug`, `offline` and `release`.
- [ ] 5.3 Replace `android/app/src/main/res/xml/network_security_config.xml` with `<base-config cleartextTrafficPermitted="false"/>`. Move the five dev hosts, with their explanatory comment, to `android/app/src/debug/res/xml/network_security_config.xml` (design D5).
- [ ] 5.4 In `AndroidManifest.xml`, add the specs/app-links Android intent filter to `MainActivity` with `android:host="${whimWebHost}"`, and `android:screenOrientation="portrait"`. Keep `launchMode="singleTask"`.
- [ ] 5.5 Change `appId: com.whim` to `appId: com.anycognition.whim` in the six `demo/android/flows/tea/*.yaml` flows, including the `--guard-app` header comments.
- [ ] 5.6 Create `scripts/release/lib/android-project.ts` with `checkAndroidProject(repoRoot)`: the manifest filter shape (autoVerify, VIEW, DEFAULT, BROWSABLE, `https`, `${whimWebHost}`, `/a/`), `singleTask` and portrait; a main network config that permits cleartext nowhere; a debug config holding the dev hosts. Fill `checks/test/release/android-project.suite.ts`: the repo passes, and temp fixtures fail for a filter without `autoVerify`, a literal host instead of the placeholder, and a main config with a cleartext `domain-config` (the discriminating case, against a check of `base-config` alone).
- [ ] 5.7 If the worktree can run Gradle (JDK 21, the primary `node_modules` symlinked), check three things with no `WHIM_UPLOAD_*` set: `./gradlew :app:assembleDebug :app:assembleOffline` succeeds; `./gradlew :app:bundleRelease` fails naming all four values; the merged release manifest under `android/app/build/intermediates/` contains host `whim.example.com` and no debuggable flag. Report the evidence, or "not run".

## 6. iOS WhimTone

- [ ] 6.1 Create `ios/Whim/WhimToneModule.mm` per design D9: `@interface WhimToneModule : NSObject <NativeWhimToneSpec>` importing `WhimAppSpecs/WhimAppSpecs.h`, `+moduleName` returning `@"WhimTone"`, `getTurboModule:` returning `NativeWhimToneSpecJSI`, a private serial method queue, and `play:`.
- [ ] 6.2 In the same file, add the design D9 tone table and renderer (44.1 kHz, 16-bit mono, the gain formula, 3 ms linear ramps per burst), a minimal RIFF/WAV writer, lazy per-token files under `Caches/whim-tone-v1/`, cached `SystemSoundID`s played with `AudioServicesPlaySystemSound`, unknown tokens played as `tick`, and every error swallowed.
- [ ] 6.3 Add `WhimToneModule.mm` to the `Whim` target's Sources with the `xcodeproj` gem, using the command recorded in `handoff/ios-project.md`.
- [ ] 6.4 Update the header comments of `src/host/cue-backend.ts` and `src/native/NativeWhimTone.ts` to name both native implementations and the iOS system-sound behavior. No code change.
- [ ] 6.5 If the iOS toolchain is available, run `pod install` and the simulator build from 4.7, and report whether codegen generated `NativeWhimToneSpec` and the target compiled. Otherwise report "not run". A missing generated header before the `ios` codegen key exists is a blocker to report, not to work around.

## 7. Brand asset generator

- [ ] 7.1 Create `release/assets/brand.json` (`iconBackground` `#3f3d8f`, `launchBackground` `#fbfaf8`) and the placeholder `release/assets/icon-foreground.svg`: a 1024×1024 viewBox holding a stroked lowercase `w` in `#fbfaf8`, drawn with path data only, inside the central 626 px circle (design D10).
- [ ] 7.2 Create `scripts/release/lib/png.ts` using only `node:zlib`: `readPngInfo(buf)` (width, height, bit depth, color type, alpha), RGBA8 decode covering all five filter types, and RGB8 encode.
- [ ] 7.3 Create `scripts/release/lib/assets.ts`: the design D10 output table as data (path, size, alpha, kind); `generateAssets(repoRoot)`, which renders through Playwright's Chromium, composites opaque outputs over `iconBackground`, and writes the images, the single-size `AppIcon` `Contents.json`, the `LaunchMark` and `LaunchBackground` asset sets, the two `mipmap-anydpi-v26` adaptive icon XMLs, `res/values/brand_colors.xml` and `release/assets/generated.json`; and `checkAssets(repoRoot)`, which returns findings for exactly-one-source, hashes, sizes, alpha, and `launchBackground` against `SHELL_COLORS.paper` from `src/sdk/design-tokens.ts`.
- [ ] 7.4 Register `generate-assets` in `scripts/release/cli.ts`, run it, and commit every output. That replaces the stock robot mipmaps and the multi-slot `AppIcon` set, and adds the Play `icon.png` and `featureGraphic.png` under `release/store/play/en-US/images/`.
- [ ] 7.5 Fill `checks/test/release/assets.suite.ts`: the repo passes `checkAssets`. Temp copies fail when the source changed without regenerating (naming the source and the command), a density's `ic_launcher.png` is deleted (naming the path), the iOS icon is re-encoded with alpha, the feature graphic has the wrong size, and `brand.json` disagrees with `SHELL_COLORS.paper` (showing both). `png.ts` cases: an RGB encode reads back as color type 2 with the same pixels, and a decode of an RGBA fixture using every filter type matches.

## 8. Launch screen wiring

- [ ] 8.1 Rewrite `ios/Whim/LaunchScreen.storyboard`: remove both template labels; set the root view background to the `LaunchBackground` named color; center a 160×160 `LaunchMark` image view with Auto Layout; declare both named resources.
- [ ] 8.2 In `ios/Whim/AppDelegate.swift`, set the root view controller's view background to `UIColor(named: "LaunchBackground")` right after `startReactNative` (design D10).
- [ ] 8.3 Android styles: `AppTheme` gains `android:windowBackground` `@color/launch_background`. Add `Theme.Whim.Launch` (parent `AppTheme`, `android:windowBackground` `@drawable/launch_screen`) to `res/values/styles.xml`. Create `res/values-v31/styles.xml` overriding `Theme.Whim.Launch` with `android:windowSplashScreenBackground` `@color/launch_background`, and `res/drawable/launch_screen.xml` as a layer list of `@color/launch_background` and a centered `@drawable/launch_mark`.
- [ ] 8.4 Set `MainActivity`'s `android:theme` to `@style/Theme.Whim.Launch` in `AndroidManifest.xml`. In `MainActivity.kt`, override `onCreate` to call `setTheme(R.style.AppTheme)` before `super.onCreate`.
- [ ] 8.5 If the toolchains are available, cold-start the simulator Release build and an `offline` build on an emulator. Capture the launch frame and the first launcher frame (`xcrun simctl io booted screenshot`, `adb exec-out screencap -p`) and attach both to the report. Otherwise report "not run".

## 9. Store listing content and privacy answers

- [ ] 9.1 Write `release/store/app-store/en-US/{name,subtitle,description,keywords,promotional_text}.txt`, `release/store/app-store/copyright.txt` (`2026 AnyCognition Inc.`), `primary_category.txt` (`PRODUCTIVITY`) and `secondary_category.txt` (`UTILITIES`), following design D11's copy rules. The data-sent sentences must match compliance design D5.
- [ ] 9.2 Write `release/store/play/en-US/{title,short_description,full_description}.txt` and `release/store/play/en-US/changelogs/default.txt` under the same rules.
- [ ] 9.3 Write `release/store/app-store/app-privacy.json`, `release/store/play/data-safety.json` and `release/store/app-store/age-rating.json` per design D11, and `release/store/answers.md` with every console-only answer (IARC questionnaire, target audience 13+, AI-generated content declaration, Device and Network Abuse exemption, app access with no login, no ads), each citing its source.
- [ ] 9.4 Create `scripts/release/lib/store-listing.ts` with `LISTING_LIMITS`, `PROMO_TERMS` and `checkStoreListing(repoRoot, config)`, per specs/store-listing: required files, limits (keywords in UTF-8 bytes), no URL files, no domain or `whim.` literals, no `!` or promotional terms, committed screenshot sizes and ratios, a 13+ age rating override, and privacy consistency across `app-privacy.json`, `data-safety.json` and `PrivacyInfo.xcprivacy` through design D11's mapping (using the plist reader from `handoff/ios-project.md`).
- [ ] 9.5 Fill `checks/test/release/store-listing.suite.ts`: the repo passes. Temp fixtures fail for a 31-character subtitle, a `support_url.txt`, `revolutionary`, an exclamation mark, a manifest copy without device ID, a 1080×2400 Play screenshot, and keywords of 100 characters but 101 bytes (the discriminating case, against a character count).

## 10. Release CLI: preflight, AAB check, privacy audit, association files, tags

- [ ] 10.1 Create `scripts/release/lib/preflight.ts` per design D12 and specs/store-release-pipeline §"The preflight names every reason it refuses". It holds the `PreflightSnapshot` type, `collectPreflightSnapshot(repoRoot, platform)`, and a pure `evaluatePreflight(snapshot, options)` that returns every failure with its fix. The repo checks it runs are `scanNativeLiterals`, `checkIosProject`, `checkAndroidProject`, `checkAssets` and `checkStoreListing`.
- [ ] 10.2 Create `scripts/release/lib/verify-aab.ts`: fingerprint file parsing (32 colon-separated hex bytes), the signer SHA-256 via `keytool -printcert -jarfile`, and manifest facts via `aapt2 dump xmltree --file base/manifest/AndroidManifest.xml` from the newest build-tools (falling back to `bundletool dump manifest`, and recording which works), plus a pure `aabFindings(facts, expected)` for package, version code, debuggable and fingerprint.
- [ ] 10.3 Create `scripts/release/lib/privacy-audit.ts`: design D7's table as data; Mach-O enumeration inside an `.app`; imported symbols from `xcrun nm -u -j` and selectors from `otool -v -s __TEXT __objc_methname`; pure `categoriesFor(symbols, selectors)` and `auditFindings(binaries, manifests)` applying D7's main-binary and framework rules.
- [ ] 10.4 Create `scripts/release/lib/association-files.ts` (pure AASA and assetlinks builders per specs/app-links, Play signing fingerprint first) and `scripts/release/lib/release-tag.ts` (`release/<marketing>+<build>`, reused when it points at HEAD, an error when it points elsewhere, git through an injected runner).
- [ ] 10.5 Register `check`, `native-config --json`, `preflight --platform ios|android --build <n> --store-latest <n> [--allow-placeholder-domain]`, `verify-aab <path> --build <n>`, `privacy-audit <App.app>`, `association-files [--out <dir>]` and `tag --build <n>` in `scripts/release/cli.ts`. Every command exits 0 on success, 1 on findings (one line each, with the fix) and 2 on usage errors.
- [ ] 10.6 Fill `checks/test/release/release-cli.suite.ts`. `evaluatePreflight` fixtures: a dirty tree plus a 0644 `play-publisher.json` lists both; the placeholder domain refuses, and passes with the override; store latest 369400 against build 369360 names both; Android with no upload values lists all four. `association-files` for team `2B7K4YLS34` gives the exact spec JSON, and a missing Play fingerprint file names it. `categoriesFor`/`auditFindings` classify `_statfs` as DiskSpace and flag it when undeclared. The tag runner reuses a tag at HEAD and refuses one elsewhere. `aabFindings` with the debug fingerprint shows expected and actual.

## 11. fastlane lanes and the mobile release runbook

- [ ] 11.1 Create `fastlane/Fastfile` with `min_fastlane_version("2.237.0")`, a helper that runs `node scripts/release/run.mjs` from the repo root and fails the lane on a non-zero exit, config from `native-config --json`, the build number computed once per lane (or `build:`), and the store-latest lookups (`latest_testflight_build_number`; the maximum of `google_play_track_version_codes` over internal, alpha, beta and production, where an empty track counts as 0).
- [ ] 11.2 Add the iOS lanes per design D12. `testflight`: preflight, `npm run build`, `bundle exec pod install`, `build_app` (Release, app-store export, `WHIM_BUILD_NUMBER` and signing flags in `xcargs` and `export_xcargs`, output in `ios/build/release/`), `privacy-audit` on the archived app, `upload_to_testflight` (with `external:` and `groups:` passed through), then `tag`. `metadata`: `deliver` with the metadata path, age rating file, domain-derived URLs, the review contact JSON, `skip_binary_upload`, and no submission. `privacy`: `upload_app_privacy_details_to_app_store` with `app-privacy.json`.
- [ ] 11.3 Add the Android lanes. `closed`: preflight, `npm run build`, `gradle` `bundle` `Release` with `whimBuildNumber` and the three store ABIs, `verify-aab`, then unless `upload:false`, `upload_to_play_store` to track `alpha` with `release_status` from `status:` (default `draft`) and metadata, images and screenshots skipped, then `tag`. `metadata`: `supply` with `release/store/play` and no binary.
- [ ] 11.4 Write `docs/release/mobile.md`: one-time setup with exact commands and file modes for every credential in design D12; the App Store Connect and Play Console steps; the first manual Play upload; per-release commands with the `build:` hand-off between platforms; replacing the icon artwork; changing the domain (the native file plus `release-config.ts`, held together by the lockstep suite); association files for ops; troubleshooting (the D4 `installOffline` fallback, the D13 arm64 fallback, the D5 http override note, `brew upgrade fastlane`); and what is attended.
- [ ] 11.5 Update CLAUDE.md "Android build & run": `npm run android:release` builds the `offline` build type (debug-signed, dev cleartext hosts); the store AAB comes only from `fastlane android closed`; iOS builds follow `docs/release/mobile.md`. Update the matching line in README.md.
- [ ] 11.6 From the committed worktree with no credentials, run `fastlane lanes` and `fastlane ios testflight`. Report that the lanes parse and that the second stops in the preflight, naming the missing App Store Connect key file and the placeholder domain, before any build.

## 12. Post-compliance reconciliation (after store-launch-compliance)

- [ ] 12.1 Fill `checks/test/release/domain-lockstep.suite.ts`: import `WHIM_DOMAIN` from `src/host/launcher/release-config.ts` and assert it equals the native release file's `WHIM_DOMAIN`. A fixture mismatch must show both values.
- [ ] 12.2 Condense `docs/store/review-notes.md` §1–4 into `release/store/app-store/review_information/notes.txt` (plain text, at most 4000 characters, limit added to `store-listing.ts`) and have `fastlane ios metadata` send it. Replace review-notes §5's draft answers with pointers to `app-privacy.json`, `age-rating.json`, `data-safety.json` and `answers.md`, settling any disagreement in favor of compliance D5 and listing each change in the chain report. In §6's checklist, change "real domain in all three places" to the native release file plus `release-config.ts`.
- [ ] 12.3 Append one entry to `docs/decisions.md` (read the tail for the next free number) recording design D1, D3, D4, D8 (including the #36 D2 / #39 recipe's new location), D9 and D12.
- [ ] 12.4 Add `*.jks`, `*.p8` and `play-publisher*.json` to `.gitignore`.

## 13. Attended device acceptance (human-run, recorded PENDING at merge)

- [ ] 13.1 Prepare in `/Users/davrondjabborov/Work/other/Whim` on the staging tip. `node -v` must print v22. Run `npm ci && npm run build`, `export BUNDLE_PATH=vendor/bundle && bundle install && (cd ios && bundle exec pod install)`. Sign Xcode into the AnyCognition Inc. team (Settings, Accounts). Get the iPhone 16 Pro Max UDID from `xcrun xctrace list devices`; its CoreDevice id is `D9977C08-2DDA-5AEB-8684-F977D1DAB1F2`.
- [ ] 13.2 TextDecoder fix on the iPhone. Set `RUN_STORAGE_PROBE = true` in `App.tsx` without committing. Run `xcodebuild -workspace ios/Whim.xcworkspace -scheme Whim -configuration Release -destination 'platform=iOS,id=<UDID>' -derivedDataPath ios/build/device -allowProvisioningUpdates build`, then `xcrun devicectl device install app --device D9977C08-2DDA-5AEB-8684-F977D1DAB1F2 ios/build/device/Build/Products/Release-iphoneos/Whim.app` and `xcrun devicectl device process launch --device D9977C08-2DDA-5AEB-8684-F977D1DAB1F2 com.anycognition.whim`. Record the on-screen verdict. Repeat with `RUN_BRIDGE_PROBE = true`, then `git checkout App.tsx`. Pass means both probes report their verdicts with no crash; on the simulator before this change, both aborted.
- [ ] 13.3 WebKit containment on the iPhone. Build Debug with an embedded bundle (`FORCE_BUNDLING=1 xcodebuild … -configuration Debug …`, same destination and install commands, or run `npx react-native start` on the Mac and allow Local Network on the phone). Open the developer probe from Home, and record `CONTAINED ✓` with its check count (47/47 on the simulator) and the forgery rejection.
- [ ] 13.4 iOS product checks on the Release install. With the Silent switch off, fire `tick`, `chime` and `alarm` from a mini-app with sound cues (the pour-over timer if seeded, otherwise build "a timer that chimes at each stage"), and compare by ear with the Android emulator. With the Silent switch on, confirm no tone and working haptics. Screen-record a cold start (Control Center) and step through the frames: launch mark on paper, no white frame before Home. Confirm the home-screen icon, the display name `Whim`, and that the app doesn't rotate.
- [ ] 13.5 Android checks. `npm run android:release` installs the `offline` build on the emulator, and it reaches the LAN dev server over http. Set `RUN_STORAGE_PROBE` and then `RUN_BRIDGE_PROBE` locally and confirm both verdicts under Android Hermes. Cold start with no flash on the API 36 image (and API 29 if installed). Check the adaptive and themed icons. Uninstall the old `com.whim` app (`adb uninstall com.whim`).
- [ ] 13.6 Links, only once the real domain and the AASA/assetlinks files are hosted; otherwise record PENDING with these commands. iOS: tap `https://whim.<domain>/a/<installed id>` in Notes, cold and warm. Android: `adb shell pm verify-app-links --re-verify com.anycognition.whim`, `adb shell pm get-app-links com.anycognition.whim`, `adb shell am start -a android.intent.action.VIEW -d https://whim.<domain>/a/<installed id>`.
- [ ] 13.7 Record every result in `openspec/changes/platform-release-readiness/progress.md`. File each failure as a fix-loop finding naming the task and the evidence.

## 14. Attended accounts, credentials and first uploads (human-run, recorded PENDING at merge)

- [ ] 14.1 Create the upload key. `mkdir -p ~/.config/whim && chmod 700 ~/.config/whim`, then `keytool -genkeypair -v -storetype PKCS12 -keystore ~/.config/whim/whim-upload.jks -alias whim-upload -keyalg RSA -keysize 4096 -validity 10000 -dname "CN=AnyCognition Inc., O=AnyCognition Inc."` and `chmod 600 ~/.config/whim/whim-upload.jks`. Add the four `WHIM_UPLOAD_*` lines to `~/.gradle/gradle.properties` and `chmod 600` it. Store the keystore and passwords in the company password manager. Record the public fingerprint with `keytool -list -v -keystore ~/.config/whim/whim-upload.jks -alias whim-upload | awk '/SHA256:/{print $2}' > release/android-upload-cert.sha256` and commit it.
- [ ] 14.2 App Store Connect. Create a Team API key with the App Manager role (Users and Access, Integrations) and save `AuthKey_<KEYID>.p8` to `~/.config/whim/`. Write `~/.config/whim/asc-api-key.json` (`key_id`, `issuer_id`, `key_filepath`) and `~/.config/whim/review-contact.json` (`first_name`, `last_name`, `phone_number`, `email_address`), then `chmod 600 ~/.config/whim/*`. Register `com.anycognition.whim` with Associated Domains in Certificates, Identifiers & Profiles if automatic signing hasn't. Create the app record: iOS, name per design Open Questions, English (U.S.), that bundle id, SKU `whim-ios`. Create the external TestFlight group `Public beta`.
- [ ] 14.3 Play Console. Create the app (Whim, English (United States), app, free) with Play App Signing on. Grant the service account in `play-publisher.json` release permissions for it, and add closed-testing testers. Build with `fastlane android closed upload:false` (add `placeholder_domain:true` only if the domain is still the placeholder) and upload `android/app/build/outputs/bundle/release/app-release.aab` by hand under Testing, Closed testing. Copy the app signing key's SHA-256 from Setup, App integrity into `release/android-play-signing-cert.sha256` and commit it. Run `node scripts/release/run.mjs association-files --out /tmp/whim-association` and hand both files to ops.
- [ ] 14.4 Console forms. Play: the content rating questionnaire and target audience from `answers.md`; Data safety via Export to CSV, filled from `data-safety.json`, imported, and committed as `release/store/play/data-safety.csv`; app access; ads. Apple: run `fastlane ios privacy` (or enter `app-privacy.json` by hand), then `fastlane ios metadata`, confirming App Store Connect accepted the age rating attribute names.
- [ ] 14.5 Screenshots. Capture 4–6 per store (home grid, compose, plan, a running app, the report sheet) against the production server: iPhone 17 Pro Max simulator via `xcrun simctl io booted screenshot` into `release/store/app-store/screenshots/en-US/`, and the emulator after `adb shell wm size 1080x2160` via `adb exec-out screencap -p` into `release/store/play/en-US/images/phoneScreenshots/`. Run `node scripts/release/run.mjs check`, commit, and push metadata with both metadata lanes.
- [ ] 14.6 Upload. Run `fastlane ios testflight`, note the printed build number, and run `fastlane android closed build:<that number>`. Confirm TestFlight processing, install through TestFlight on the iPhone 16 Pro Max, confirm the closed-testing release in Play Console, and record both build numbers and the `release/<version>+<build>` tag in `progress.md`.
