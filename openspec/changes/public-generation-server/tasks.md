# Tasks — public-generation-server

<!-- Section N maps to chain-(N-1) in chains.md. Section 1 is HUMAN-BOOTSTRAP (dependency pins);
     section 15 is attended acceptance. Every other section is dispatchable.
     Sections 16 (chain-15, pages site) and 17 (chain-16, load test) came with the web-host and
     capacity amendment. They run BEFORE section 15's attended acceptance; chains.md has the order. -->

## 1. Harness runtime dependencies (HUMAN-BOOTSTRAP)

- [x] 1.1 Read the lockfile-resolved root versions of `esbuild`, `playwright` and `typescript` from `package-lock.json`. Run `npm install --workspace server --save-exact esbuild@<v> playwright@<v> typescript@<v>`. Confirm `package-lock.json` holds exactly one copy of each package and that `npm run guard:metro` is unchanged (design.md D3).
- [x] 1.2 Update the dependency-budget section of `server/test/contract.suite.ts`. The allowed runtime set becomes `hono`, `@hono/node-server`, `pino`, `@whim/contract`, `esbuild`, `playwright`, `typescript`. Assert that each of the last three is pinned to exactly the lockfile-resolved root version. Keep the `pino-pretty` dev-only and React-adjacent checks (specs/generation-server "Server workspace and runtime").
- [x] 1.3 Run `./scripts/gate.sh` and `npm run guard:metro` on the staging branch, then commit the three files together as one human change.

## 2. Wire shapes, typed config, suite scaffold

- [x] 2.1 Add `ReportReason`, `ReportRequest` and `ReportResponse` zod schemas with their `z.infer` types to `contract/src/index.ts`, exactly per specs/generation-contract "Report request and response shapes".
- [x] 2.2 Add the `ServiceRefusalCode` zod enum (`payload_too_large | daily_limit | device_busy | server_busy | content_policy | policy_unavailable | budget_exhausted`) to `contract/src/index.ts`. Leave `ApiError` untouched.
- [x] 2.3 Extend `server/test/contract.suite.ts` (outside the dependency-budget section) with every scenario from both new contract requirements, including `budget_exhausted`'s membership in the closed vocabulary.
- [x] 2.4 Create `server/src/config.ts` with `loadServerConfig(env, opts?) → ServerConfig`, a pure, frozen, typed loader for every variable in specs/server-admission-control "Admission limits are environment-configurable" — including `WHIM_MIN_CREDIT_USD` (default 0.50, a non-negative decimal, not a positive integer) and `WHIM_CREDIT_CACHE_TTL_MS` (default 60000) — plus `WHIM_SERVER_HOST`, `WHIM_SERVER_PORT`, `WHIM_DATA_DIR`, `WHIM_POLICY_TIMEOUT_MS`, `WHIM_REPORT_RETENTION_DAYS`, `WHIM_LEDGER_RETENTION_DAYS`, `WHIM_DRAIN_TIMEOUT_MS` (default generation max + 30000) and the existing variables. It fails with errors naming the variable. Under `NODE_ENV=production` it applies the refusals in specs/server-deployment "Production configuration refuses dev-only modes". Fill `server/test/config.suite.ts` with those scenarios.
- [x] 2.5 Scaffold: create empty, registered suite modules for later chains and import and sequence them in `server/test/acceptance.ts`. The modules are `ledger`, `resolver`, `admission`, `policy`, `reports`, `admin`, `routes-unary`, `routes-generate`, `disconnect`, `prod-build` and `deploy-config` (`server/test/<name>.suite.ts`, each exporting `run<Name>Tests()` with only a section header). Later chains edit only their own module and never `acceptance.ts`.

## 3. Usage ledger and cost resolver

- [x] 3.1 In `server/src/usage-store.ts`, open the database in WAL mode with a busy timeout. Add the `requests` ledger table and index from design.md D7, then extend `UsageStore` and both implementations with `admit`, `refund`, `settle`, `recordCost`, `summary` and `purgeLedger`. `admit` counts and inserts in one immediate transaction, returning `retryAfterSec` to the next UTC midnight from the injected clock. Keep `credit`/`read` and the `usage` table unchanged.
- [x] 3.2 Fill `server/test/ledger.suite.ts`: device and global limits, device-over-global precedence, the atomic last unit (concurrent admits), UTC rollover on an injected clock, durability across reopen, refund, retention purge, and a marker-text scan proving the usage database file holds no content.
- [x] 3.3 Create `server/src/usage/resolve.ts`, one post-request resolver taking a request id, device id, generation ids, `creditOwned` and an injected stats transport. It enforces bounded attempts, a per-attempt `AbortSignal.timeout` and a total budget. It sums `total_cost` into `recordCost` (`resolved`/`unresolved`) and credits reconciled tokens only when `creditOwned` is false. Reduce `server/src/generation/reconcile.ts` to a wrapper over it, keeping its exported signature so no import site changes. Expose a tracker the drain can await.
- [x] 3.4 Fill `server/test/resolver.suite.ts` with every scenario in specs/server-admission-control "The usage store keeps a content-free request ledger" and specs/generation-pipeline "Aborted runs reconcile their authoritative usage": summed cost, unresolved on exhaustion, a hanging attempt cut off by its timeout, no double credit, and a cancelled run's cost plus a single token credit. The existing `metering.suite.ts` must stay green unchanged.

## 4. Admission core and identity seam

- [x] 4.1 Create `server/src/admission/slots.ts` covering per-device generation exclusivity, the global generation cap, the global unary cap and the `draining` flag. `acquire(kind, deviceId)` returns a handle with an idempotent `release()` or a refusal reason (design.md D8).
- [x] 4.2 Create `server/src/admission/refusals.ts` with builders for each `ServiceRefusalCode` body, using the hint table in design.md D8 and `Retry-After` seconds from an injectable clock. Both `server_busy` variants are covered.
- [x] 4.3 Create `server/src/device-identity.ts` with the `DeviceVerifier` interface and `shapeOnlyVerifier` exactly as in design.md D15. It reproduces today's UUID check and `DeviceIdError` bodies.
- [x] 4.4 Create `server/src/admission/credit.ts`, a route-agnostic primitive over an injectable transport (design.md D6a): `checkCredit({transport, clock, ttlMs, floorUsd}) → {ok:true} | {ok:false, reason:'budget_exhausted'}`. It queries `GET https://openrouter.ai/api/v1/key`, reads `data.limit_remaining` (`null` treated as unlimited, never refusing), caches the result in memory keyed by nothing but time (one key, one server) for `ttlMs`, and refuses only when the cached value is a number below `floorUsd`. A transport error, non-2xx response, or unparseable body returns `{ok:true}` (fail-open) and a caller-visible flag so the route can log a warning. Export `invalidateCreditCache()` for the mid-flight 402 path (chain-8, chain-9, chain-10).
- [x] 4.5 Fill `server/test/admission.suite.ts`: slot exclusivity and caps, idempotent release, draining refusal, each refusal body validating as `ApiError` with a `ServiceRefusalCode` and a hint free of internal identifiers, `Retry-After` at 22:00:00 UTC equal to 7200, verifier accept/refuse cases, and `credit.ts`'s scenarios: below-floor refusal, `null` limit never refusing, a cache hit inside the TTL making one transport call for two checks, a failed lookup failing open with its warning flag set, and `invalidateCreditCache()` forcing the next check to re-query even inside the TTL.

## 5. Content policy

- [x] 5.1 Write `docs/content-policy.md` with a `## Rating rule` section (the 13+ rule the generation prompts carry) and a `## Categories` section covering every category in specs/content-policy "The 13+ content policy has one written source".
- [x] 5.2 Create `server/src/policy/`. It holds the `ContentPolicy` interface, `PolicyUnavailableError`, and `ModelContentPolicy`: roster `rewrite` id, `reasoning: false`, `maxTokens` 48, timeout, a data-framed user message, and parsing via `json-block.ts` plus a strict guard in which anything else is unavailable. It also holds `StubContentPolicy` (`[[refuse]]`, `[[policy-down]]`) and `cachedPolicy` (LRU 1000, TTL 15 min, SHA-256 of the canonical input, never caching unavailable). Each check emits one content-free log record (design.md D9).
- [x] 5.3 Add a canonical policy-input builder per route: prompt and clarification questions and answers for all three routes, plus rewrite `app.name` and collection/field display names. It never includes source, bundle, manifest or schemas.
- [x] 5.4 In `server/src/generation/prompts/inputs.ts`, load the rating-rule and categories sections through a cached loader, following the `loadSdkReference(cwd)` pattern. Append the rating rule to the rewrite and engineer system messages inside `prompts/index.ts` without changing any builder signature, and do not edit `server/src/routes/*`. Extend `server/test/prompts.suite.ts` with the missing-section, both-prompts-carry-it and no-duplicate-in-source tripwires.
- [x] 5.5 Fill `server/test/policy.suite.ts` with every scenario of specs/content-policy that does not need a mounted route: fail-closed cases (timeout, prose, unknown verdict, transport error), input coverage, source exclusion, model id and bounds, cache hit and no caching of unavailable, the stub markers, and log content.

## 6. Report store and operator command

- [x] 6.1 Create `server/src/reports/store.ts` with `ReportStore` and `NodeSqliteReportStore` on `WHIM_DATA_DIR/reports.db`: WAL, `secure_delete=ON`, the exact column set from specs/content-reports, `insert` returning `crypto.randomUUID()`, `list`, `get` and `purgeOlderThan`. Add an in-memory twin for tests.
- [x] 6.2 Fill `server/test/reports.suite.ts`: exact stored fields, retention default and override, and marker text present only in `reports.db` and absent from the usage database.
- [x] 6.3 Create `server/src/admin/cli.ts` with `reports list [--since N] [--limit N] [--json]` (no prompt or source text, sizes only), `reports show <id> [--json]`, `reports purge` and `usage [--days N] [--top N] [--json]` over `UsageStore.summary` (daily totals by kind, top devices by cost, per-generation count/mean/median/p95/max, unresolved count). It opens stores read-only except for purge.
- [x] 6.4 Add the dev runner `server/admin.mjs`, which bundles the CLI like `server/dev.mjs`. Fill `server/test/admin.suite.ts` with output-shape tests for every subcommand, plus a read-while-writing test in which a second connection lists while inserts proceed.

## 7. Synthetic run: sandbox, egress, build isolation

- [x] 7.1 Add `browserLaunchOptions()` as the only launch configuration in `synthrun/session.ts`: `chromiumSandbox: true`, `headless: true`, the dead proxy `{ server: 'http://127.0.0.1:9', bypass: '<-loopback>' }`, `--host-resolver-rules=MAP * ~NOTFOUND`, `--force-webrtc-ip-handling-policy=disable_non_proxied_udp` and `--dns-prefetch-disable`. There is no fallback and no disabling option (design.md D4–D5).
- [x] 7.2 Replace the temp-file and `file://` page load with in-memory delivery. A context-level route fulfills `https://synthrun.invalid/run/<runId>` with the assembled HTML and aborts every other request. Add `routeWebSocket` closing all sockets and `acceptDownloads: false`. Block service workers through the sandboxed realm and the abort-all route, never Playwright's `serviceWorkers: 'block'` (it injects a script into the candidate's realm), and record blocked attempts in the run trace as a bounded count with no URLs. Export a `probeEgressBlocked(session)` helper that the boot self-test uses.
- [x] 7.3 Add an esbuild `onResolve` plugin to `synthrun/builder.ts` that keeps `vc-sdk`/`react`/`react-dom` external, resolves only the entry and the inject shim, and fails everything else with a named error. Extend `checks/passes/import-allowlist.ts` to visit `ExportDeclaration` module specifiers and `ImportEqualsDeclaration`, and add the new specs/static-checks scenarios to the checks acceptance suite.
- [x] 7.4 In `synthrun/test/`, start loopback HTTP, WebSocket and UDP canaries and implement every scenario of specs/synthetic-run "A synthetic run has no network egress", including the per-layer non-vacuity controls, and "Chromium runs with its OS sandbox enabled" (browser process args). Add the builder file-read scenario. Run the whole existing synthrun suite and `npm run server:e2e` locally and keep them green unchanged.
- [x] 7.5 Vendor Playwright 1.60.0's `utils/docker/seccomp_profile.json` as `deploy/seccomp/chromium-playwright-1.60.0.json`. Make the sandboxed browser launch in CI and the devcontainer. If the runner refuses user namespaces, add a `sysctl kernel.apparmor_restrict_unprivileged_userns=0` step to `.github/workflows/invariants.yml` before the Chromium suites, and add `--security-opt seccomp=…` to `.devcontainer/devcontainer.json` `runArgs`. Never add `--no-sandbox`.

## 8. Synthetic run: abort at every wait, crash replacement

- [x] 8.1 Make semaphore acquisition in `synthrun/concurrency.ts` abortable. An aborted waiter leaves the queue without ever holding the slot.
- [x] 8.2 Honor the run signal in `openRun` (before context creation and raced against navigation) and in `awaitMount` (`synthrun/session.ts`, `synthrun/observe.ts`, `synthrun/report.ts`). Close the page and context and release the slot within 5 s, with no duplicate cleanup. Update the `RunOptions.signal` documentation in `synthrun/contract.ts`, which supersedes #56 D8 for these waits.
- [x] 8.3 Detect browser disconnect in `SynthRunSession`. Fail in-flight runs with a named `browser_disconnected` harness error, relaunch lazily with `browserLaunchOptions()` under a mutex before the next `openRun`, and retry on the next run when a relaunch fails.
- [x] 8.4 In `synthrun/test/`, cover abort-while-queued (no context opened), abort 250 ms into a hanging mount (context closed and slot free within 5 s), and recovery after killing the browser process mid-run.

## 9. Generation wall-clock budget

- [ ] 9.1 In `server/src/generation/machine.ts`, add a per-run deadline measured on the injected `Clock` (constructor option `maxRunMs`). On expiry, abort an internal controller linked to the request signal. Record the run's end cause on `RunTrace` as `outcome: 'delivered' | 'failed' | 'expired' | 'aborted'`.
- [ ] 9.2 On expiry, emit exactly one `usage` event and one `failure` whose reason is "This took too long to build. Please try again." A client abort that arrives first emits nothing then or later.
- [ ] 9.3 In the same top-level model-call catch in `machine.ts` (research.md: OpenRouterNetworkError swallowed there today), detect an HTTP `402` from the model client distinctly from an ordinary model failure, skip any repair attempt, call `invalidateCreditCache()` (chain-3, `server/src/admission/credit.ts`), and end the run with one `failure` event whose reason names the generation budget running out (design.md D6b, specs/generation-pipeline "A run ends cleanly when the operator's provider credit is exhausted").
- [ ] 9.4 Thread `maxRunMs` through `server/src/generation/index.ts`'s pipeline construction, and add the specs/generation-pipeline "A run is bounded in wall-clock time" and "A run ends cleanly when the operator's provider credit is exhausted" scenarios plus trace-outcome assertions to `server/test/machine.suite.ts`.

## 10. App options, unary routes, report route, stream probe

- [ ] 10.1 In `server/src/app.ts`, extend `createApp` options with `deviceVerifier`, `config`, `clock`, `slots`, `policy`, `reportStore`, `resolver` and `creditTransport` (the injectable transport `credit.ts` uses to reach `GET https://openrouter.ai/api/v1/key`). The `/v1/*` middleware now calls the verifier and sets `deviceId`. Add the substituted-verifier scenario to `server/test/wire-v2.suite.ts`, and update `server-core.suite.ts` and other existing suites only as far as the new required options demand.
- [ ] 10.2 In `routes/clarify.ts` and `routes/rewrite.ts`, apply the ordered admission: `hono/body-limit` 413, validation, prompt cap, operator credit (`checkCredit` from chain-3's `server/src/admission/credit.ts`, `503 budget_exhausted`), drain, unary cap, daily unit, policy (with refund on unavailable), then work. On a `402` from the model call, map it to `503 budget_exhausted` in place of the route's existing `502`, call `invalidateCreditCache()`, and skip nothing else in the existing error path. Combine the request signal with the unary model timeout, capture generation ids, settle the ledger row, and hand every ending to the resolver.
- [ ] 10.3 Add `routes/report.ts`, mounted with `app.route('/v1/report', …)`: body-limit, `ReportRequest` validation, prompt and source byte caps, drain, `report` daily units (device and global), insert, `202 { reportId }`, and one content-free log record. No policy check, no model call, and no credit check (specs/server-admission-control's fixed order excludes `/v1/report` from the credit check, matching its existing model-free path).
- [ ] 10.4 Add `GET /healthz/sse` outside `/v1`: three SSE comment frames one second apart, then close, counted against the global unary cap.
- [ ] 10.5 Fill `server/test/routes-unary.suite.ts` with the clarify, rewrite and report scenarios from specs/server-admission-control, specs/content-policy and specs/content-reports: order of checks including the credit check ahead of drain and daily accounting, no model call before admission, chunked-body 413, every refusal validating with `Retry-After` only when knowable, a stalled rewrite timing out with a released slot, a refused rewrite making only the classifier call, a `402` mid-call mapping to `503 budget_exhausted` with the cache invalidated for clarify and rewrite, report acceptance, bounds and gating, and the `/healthz/sse` frame spacing.

## 11. Generate route and real disconnect proof

- [ ] 11.1 In `routes/generate.ts`, apply the ordered admission: body-limit, validation, prompt cap, operator credit (`checkCredit`, `503 budget_exhausted`), drain, device exclusivity, global cap, daily units with the global ceiling, then policy before the stream opens.
- [ ] 11.2 Release the generation slot from the stream's single teardown path (terminal, cancel, `Request.signal`, error, expiry, drain abort, and the machine's `402` ending from chain-8). Settle the ledger row from `RunTrace.outcome`, and replace the route's direct reconciliation with the resolver, keeping `creditOwned` semantics.
- [ ] 11.3 Compose the new dependencies in `server/src/main.ts`: config, both stores, slots, the model or stub policy wrapped in the cache, the resolver with the existing stats transport, `maxRunMs`, `WHIM_MIN_CREDIT_USD` / `WHIM_CREDIT_CACHE_TTL_MS` and the real `fetch`-backed credit transport, and synthrun concurrency from config. Keep the existing startup behavior otherwise, since lifecycle refactoring is section 12.
- [ ] 11.4 Fill `server/test/routes-generate.suite.ts`: ordering including the credit check ahead of drain and concurrency, `device_busy` without charge, `server_busy` at the cap, `budget_exhausted` refusing before any slot is acquired, daily limit and global ceiling with `Retry-After`, UUID rotation capped by the ceiling, refund on `policy_unavailable`, slot freed after terminal, cancel and policy refusal, a mid-run `402` ending the stream in one `failure` event and invalidating the credit cache for the next request, and the data-directory marker scan from specs/generation-server "Server state is the usage store and user-sent reports".
- [ ] 11.5 Fill `server/test/disconnect.suite.ts`. `serve()` the app on `127.0.0.1:0` with a scripted model whose transport blocks until aborted. A raw `net.Socket` posts generate, reads the first event and destroys itself. Within 5 s assert the transport observed the abort, both slots are free and a follow-up generation from the same device is admitted. Repeat for clarify, including a free unary slot.

## 12. Lifecycle, drain, production build

- [ ] 12.1 Create `server/src/runtime-assets.ts` (the one exported asset list from design.md D1) and `server/src/preflight.ts`, which checks assets, `createRequire`-resolves `esbuild`/`playwright`/`typescript` and verifies `WHIM_DATA_DIR` is writable, exiting non-zero with the first missing item named. Create the data directory with mode 0700 under process umask 0077.
- [ ] 12.2 Move composition into `server/src/lifecycle.ts`, leaving `main.ts` as a thin shell. The order is config, preflight, stores (with purge timers), then session plus self-test (real pipeline only: `tip-splitter` fixture contained with no error diagnostics, plus `probeEgressBlocked`), then listen on `WHIM_SERVER_HOST:WHIM_SERVER_PORT`. Any failure exits non-zero with a named reason. Export the composition as `startServer({ env, overrides?, listen? })` (design.md D26): `env` is the environment as data, `overrides` may replace the model client and roster, the generation-stats transport, and the credit transport, and may wrap the Hono app before it is served; `listen` may override host and port (port 0 allowed); the result exposes the bound URL and the drain. Signal handlers and `process.exit` stay in `main.ts`, which calls `startServer` with no overrides.
- [ ] 12.3 Implement the drain from design.md D16: a tracked in-flight registry, `draining` refusals, `server.close()`/`closeIdleConnections()`, waiting to `WHIM_DRAIN_TIMEOUT_MS`, aborting the rest, a 10 s resolver window, closing session and stores, `exit(0)`, and a second signal skipping the wait.
- [ ] 12.4 Add `server/build.mjs`, which writes `server/dist/app/` (bundled `server/main.mjs` and `server/whim-admin.mjs`, linked sourcemaps, externals equal to the declared runtime packages, assets copied from `runtime-assets.ts`). Add `server/dist/` to `.gitignore`. Export the single-entry bundling step as `bundleServerEntry({ entry, outfile, write? })`, returning the esbuild metafile's input paths, and use it for both bundles, so another entry point bundles with identical settings (design.md D26). Running the file directly still builds the tree.
- [ ] 12.5 Fill `server/test/prod-build.suite.ts`. Build the tree and assert its exact file list. Spawn `node server/main.mjs` from the tree root in stub mode and check `/healthz` identity. Assert a removed asset and an unwritable data directory exit non-zero by name, and that `NODE_ENV=production` with `WHIM_PIPELINE=stub` refuses. Then test the drain: SIGTERM mid-stream completes the stream and exits 0, a short drain timeout aborts and exits 0, and a second signal skips the wait.
- [ ] 12.6 In `server/test/e2e.ts`, add the boot self-test (passes on a healthy session) and the real-TCP disconnect in which a hanging-mount candidate's browser context is closed and its slot free within 5 s. Correct the stale "not wired into gate-full" header comments in `e2e.ts` and `e2e.run.mjs`.

## 13. Container image and GCP deployment artifacts

- [ ] 13.1 Write `deploy/Dockerfile` (three stages per design.md D2: Node 22 bookworm-slim, pinned Playwright `--only-shell` Chromium install, uid 10001, production closure only, `CMD node --enable-source-maps server/main.mjs`), root `.dockerignore`, root `.gcloudignore` and `deploy/cloudbuild.yaml` (linux/amd64, tag `:$COMMIT_SHA` to `northamerica-northeast1-docker.pkg.dev/$PROJECT_ID/whim/server`).
- [ ] 13.2 Write `deploy/compose.yaml` with every server hardening setting and the fixed bridge subnet from design.md D17, plus Caddy pinned by digest with a persistent data volume. Every interpolated value (`WHIM_IMAGE`, `WHIM_API_HOST`, `WHIM_WEB_HOST`, `WHIM_SERVER_MEM_LIMIT` as `mem_limit`, `WHIM_SERVER_SHM_SIZE` as `shm_size`) is written `${NAME:?}` and comes from the compose project's `.env` in `/opt/whim` (design.md D24). The server's `env_file` entries are `/etc/whim/config.env` and `/etc/whim/server.env`. Caddy gets both hostnames through `environment:` and mounts `/mnt/disks/whim-data/site` read-only at `/srv/site`. Write `deploy/Caddyfile` with two site blocks. The API block is `{$WHIM_API_HOST}` with `request_body max_size 2MB` and `reverse_proxy whim-server:8787 { flush_interval -1 }`, no `encode`, no `log`, no file serving. The pages block is `{$WHIM_WEB_HOST}` with root `/srv/site/current` and exactly the D21 route table: internal rewrites to flat files, `Content-Type: application/json` on both `.well-known` paths, `404` with `not-found.html` for everything else, the D21 `Content-Security-Policy` and `X-Content-Type-Options` headers, and no `reverse_proxy`, `templates`, `encode` or `log`. Write `deploy/defaults.env` with the D20 values (`WHIM_GCP_PROJECT=anycognition-whim`, `WHIM_GCP_REGION=northamerica-northeast1`, `WHIM_GCP_ZONE=northamerica-northeast1-a`, `WHIM_STATIC_IP=34.118.191.193`, `WHIM_API_HOST=api.whim.anycognition.ca`, `WHIM_WEB_HOST=whim.anycognition.ca`), and `deploy/server.env.example` and `deploy/operator.env.example` with variable names only.
- [ ] 13.3 Write `deploy/vm/bootstrap.sh` (Docker Engine and compose plugin, Artifact Registry credential helper, persistent-disk format-once and fstab mount at `/mnt/disks/whim-data`, owned data directories including `site/` owned by root and readable by the Caddy container, a user-namespace assertion, `/etc/whim` with root-only mode, and `/opt/whim` owned by root), `deploy/vm/whim-egress.sh` (the DOCKER-USER rules from D17, idempotent) and `deploy/vm/whim-egress.service`.
- [ ] 13.4 Write `deploy/provision.sh`: idempotent `gcloud` commands for APIs, the Artifact Registry repo, adopting the reserved static address whose IP equals `WHIM_STATIC_IP` (fail if none, never create one), minimal service account and roles, the Debian 12 VM with persistent disk on the `standard` profile's machine type (or `--profile <name>`'s), firewall rules (80, 443/tcp+udp, IAP-only SSH) and the empty Secret Manager secret. It never adds a secret version. It reads project, region and zone from the D24 value chain. Tasks never run it. Write `deploy/deploy.sh [--tag <sha>] [--site-only]` (design.md D21, D24). The preflight loads `deploy/defaults.env`, then `~/.config/whim/deploy.env`, then the environment. It refuses naming each missing required value (`WHIM_SUPPORT_EMAIL`, `WHIM_ENGINEER_MODEL`, `WHIM_REWRITE_MODEL`), a `WHIM_API_HOST` that isn't `api.` + `WHIM_WEB_HOST`, a Node major other than 22, and a dirty or unpushed tree. Unless `--site-only`, it then reads the latest enabled version of the OpenRouter secret and exits non-zero naming the secret when it is missing, has no enabled version, or is empty, all before Cloud Build. Full deploy after that: Cloud Build, upload compose, Caddyfile and seccomp to `/opt/whim` over IAP, write `/etc/whim/server.env` from the secret, write `/etc/whim/config.env` and the compose `.env` from the profile matching the VM's actual machine type (refusing when none matches, 13.6), build the site with `node server/site.mjs build --out <stage>` (handoff/web-site.md) and publish it as a new release directory swapped in with `mv -T` on the `current` symlink, pull, `up -d`, wait healthy, run smoke. `--site-only` builds and publishes the site, uploads the Caddyfile, reloads or starts only Caddy, and runs the pages smoke checks, with no image, no secret and no server recreate. Write `deploy/smoke.sh`: both hostnames resolve to `WHIM_STATIC_IP` with no AAAA record (checked first); TLS `/healthz` answers exactly `{ ok: true, service: 'whim-server' }`; `/v1` without header returns 400; `/healthz/sse` frame spacing; metadata-server egress failing from inside the container; no `react-native` in the image's `node_modules`; `/privacy`, `/support` and `/a/x` return `200` HTML without a redirect and `/nope` returns `404`; each `.well-known` path either returns `200`, `application/json`, no redirect and bytes equal to the published site's file, or returns `404` when the site has none. A `--pages-only` flag runs just the DNS and pages checks.
- [ ] 13.5 Fill `server/test/deploy-config.suite.ts`, keeping the web-site sub-suite call chain-15 added: the Dockerfile Playwright pin and seccomp filename version equal the lockfile version; the final user is non-root; there is no `.env` copy and no secret-named variable with a value in any deploy file; no sandbox-disabling flag in any deploy artifact; in the API block `flush_interval -1` is present, `encode` and `log` are absent, and no file serving; the pages block has `Content-Type: application/json` on both `.well-known` paths and none of `reverse_proxy`, `templates`, `encode`, `log`; every `${…}` in `compose.yaml` uses the `:?` form; no published server port; `stop_grace_period` is at least drain default plus 30 s; `deploy/defaults.env` satisfies `WHIM_API_HOST == "api." + WHIM_WEB_HOST`; no file under `server/src/` contains `anycognition.ca` or `sslip.io`, and no deploy file contains `sslip.io`; no deploy file writes an association file's content; and `bash -n` passes for every `deploy/**/*.sh`.
- [ ] 13.6 Write `deploy/profiles/standard.env` and `deploy/profiles/event.env` exactly as the design.md D25 table, and `deploy/resize.sh --profile <name>` running D25's steps in order: quota check before any change, `docker compose stop whim-server`, instance stop, `set-machine-type`, start, confirm the machine type, `deploy.sh --tag <running tag>`. On a failed type change or start it starts the VM on its current type, redeploys that type's profile and exits non-zero naming the step. In `deploy.sh`, select the profile from the VM's machine type and write its server keys and the model ids to `/etc/whim/config.env` and its `WHIM_SERVER_*` keys and name to the compose `.env`. Extend `deploy-config.suite.ts`: profile keys are limited to `WHIM_PROFILE_MACHINE_TYPE`, `WHIM_SERVER_MEM_LIMIT`, `WHIM_SERVER_SHM_SIZE` and variables `loadServerConfig` reads; each profile's server keys load through `loadServerConfig` without error; machine types are unique; `standard` sets no server key; no profile sets a daily limit or ceiling, a retention variable, `NODE_ENV`, `WHIM_PIPELINE`, `WHIM_DEV_LOG_SINK` or a secret-named key; `WHIM_SYNTHRUN_CONCURRENCY` never exceeds the machine type's vCPU count or the profile's generation cap; `deploy.sh` has no `--profile` option. Discriminating red-check: an `event.env` setting `WHIM_LIMIT_GENERATIONS_PER_DAY` must fail.

## 14. Runbook and records

- [ ] 14.1 Write `docs/deploy.md` in the order required by specs/server-deployment "A short runbook covers operating the public server". Include the D6 defaults and derivation, the $50 starting credit limit and how the owner raises it (D6/D6a; no step sets it), the week-1 recalibration from `whim-admin usage`, and persistent-disk snapshot scheduling. Also include: the DNS records at GoDaddy for both hostnames and the smoke DNS check (D20); `~/.config/whim/deploy.env` and each value in it (D24); the pages host, `--site-only`, and D22's association-file order as a numbered list tied to platform-release-readiness tasks 14.1–14.3 and 14.6, with the `WHIM_DOMAIN` precondition; the event profile table marked as estimates with D25's derivation; the demo-night checklist (resize up the day of the event, run the load test, resize down after); `resize.sh`; and `deploy/loadtest/run.sh` `start`/`drive`/`stop` with what each report field means. No sslip.io cutover section.
- [ ] 14.2 Extend `server/test/deploy-config.suite.ts` with the runbook check: every script path `docs/deploy.md` references exists, and every `WHIM_*` variable it names is read by `server/src/config.ts`, a deploy script, a deploy profile or env file, `server/src/site/build.ts`, or `server/src/loadtest/`.
- [ ] 14.3 Append `docs/decisions.md` #64 recording the public server posture: sandbox on with no egress (layers and proof), the dependency budget amendment, the Node 22 base instead of the Playwright image and why, spend controls and defaults, the reports exception to #33, #56 D8 superseded for the three synthrun waits, the attestation seam, the real hostnames and the pages host on the same Caddy (D20–D21), association files only from the release tooling and gated on the Play fingerprint (D22), the policy parity tripwire (D23), capacity profiles (D25), and the load test's no-credit guarantees (D26).
- [ ] 14.4 Rewrite the Purpose paragraph of `openspec/specs/generation-server/spec.md` (drop "LAN dev only — no deployment or TLS yet"; describe the public service behind the deployment's TLS proxy). Update the generation-server row and add rows for `server-deployment` (naming the pages host, capacity profiles and the no-spend load test), `server-admission-control`, `content-policy` and `content-reports` in `docs/capabilities.md`, and add a README pointer to `docs/deploy.md`.

## 15. Attended acceptance (human-run, recorded PENDING at merge)

- [ ] 15.1 On a Linux Docker host, build the image and run it with the compose security settings in production mode against a real key. Check that the boot self-test passes, `/healthz` and `/healthz/sse` answer, and that one real generation through local Caddy streams unbuffered and records a resolved cost visible in `whim-admin usage`.
- [ ] 15.2 After the orchestrator provisions GCP, and once the owner has added the OpenRouter secret version, chosen the model pair and the support email, and filled `~/.config/whim/deploy.env`: run `vm/bootstrap.sh` and `deploy.sh`, and confirm `smoke.sh` passes against `api.whim.anycognition.ca` and `whim.anycognition.ca`, including DNS and pages checks. Confirm the build reported association files as absent with link verification PENDING (unless the Play signing fingerprint is already committed). Record the image tag, both hostnames and the association state in `docs/decisions.md` #64's status tag. Also confirm the missing-key failure once: before the secret has a version, `deploy.sh` exits naming the secret and starts no build.
- [ ] 15.3 From a device on cellular data, run one generation against the deployed server and kill the app mid-generation. Confirm in the server log that the stream aborted, the slot was released, and the ledger row settled as aborted with its cost resolved.
- [ ] 15.4 Event-profile load test, before demo night (2026-09-24). Run `deploy/resize.sh --profile event` and confirm smoke passes on `e2-standard-8`. Run `deploy/loadtest/run.sh start`, then `run.sh drive --devices 15 --cap 15`, then `run.sh drive --devices 16 --cap 15`, then `run.sh stop` (smoke must pass on the production identity afterwards). Record in `progress.md` under "Event profile load test", for each drive: p50 and p95 time to first event, p50 and p95 total time, terminal counts, `server_busy` count (0 expected at 15 devices, exactly 1 at 16), peak container CPU (percent of one vCPU, out of 800) and peak memory, the leak probe verdict for both rounds, and the image tag. Confirm the OpenRouter activity page shows no request during the test. If peak memory exceeds 75% of `WHIM_SERVER_MEM_LIMIT`, CPU stays above 85% for more than a minute, or any run ended in `failure`, file a fix-loop finding to retune `deploy/profiles/event.env` (never edit it during the attended run). Resize back with `resize.sh --profile standard` unless the event is within a day.
- [ ] 15.5 Association files go live. Recorded PENDING until platform-release-readiness task 14.3 commits `release/android-play-signing-cert.sha256`. Then run `deploy.sh --site-only` from a clean checkout containing it. Confirm smoke's association checks pass, and confirm by hand with `curl -sI https://whim.anycognition.ca/.well-known/apple-app-site-association` and `…/assetlinks.json` (both `200`, `application/json`, no `Location` header). Confirm the assetlinks fingerprints read Play signing first, then upload. Record the date in `progress.md`, before the closed-testing rollout and the first TestFlight build, so platform-release-readiness task 13.8 can run.

## 16. Pages site: sources, build, parity tripwire

- [ ] 16.1 Write `deploy/site/privacy.html` with the content design.md D23 lists, in plain HTML with one inline `<style>` and no script. Quote the consent screen's disclosure strings verbatim from `src/host/launcher/copy.ts` (every `consent…` key outside the D23 allowlist), under headings that reuse the consent screen's own section titles. Name AnyCognition Inc. and use `{{WHIM_SUPPORT_EMAIL}}`, `{{WHIM_ENGINEER_MODEL}}` and `{{WHIM_REWRITE_MODEL}}` placeholders. State report and ledger retention as the number of days in `loadServerConfig({})` (90 today), and include a "Last updated" date. Make no claim the D23 list doesn't make.
- [ ] 16.2 Write `deploy/site/support.html` (support email, reporting an app from inside Whim, turning AI features off in Settings, a link to `/privacy`), `deploy/site/app-link.html` (the app opens only on the phone that made it, plus a store-links block delimited so the build can drop it when neither store URL is set) and `deploy/site/not-found.html` (links to `/privacy` and `/support`). Use the same inline style and no script. Take user-facing wording from store-launch-compliance design D15 and D17. Use no mechanism vocabulary.
- [ ] 16.3 Create `server/src/site/build.ts` and the runner `server/site.mjs` (bundles like `server/dev.mjs`): `node server/site.mjs build --out <dir>`. Pure pieces: `renderPage(source, values)` with the closed placeholder set from design.md D23 (HTML-escaping; failing by name on an unknown placeholder, a missing or malformed required value, or a leftover `{{`; the store-links block dropped when both store URLs are unset), and `associationState(repoRoot)` returning `present` or `absent` plus the first missing fingerprint path. `buildSite({ repoRoot, env, outDir, runAssociationFiles })` renders into a temp directory. When the state is `present` it calls the injected runner, which wraps `node scripts/release/run.mjs association-files --out <stage>`, and copies exactly the two files that `handoff/release-cli.md` names into `.well-known/` byte for byte, failing on a non-zero exit or any other file set. It moves the temp directory to `outDir` only on success and prints one line: `association files: present` or `association files: absent (<path>); app link verification stays PENDING`. Exit 0 on success, 1 on a build failure (one line naming the cause), 2 on a usage error.
- [ ] 16.4 Create `server/test/web-site.suite.ts` exporting `runWebSiteTests()`, and add its import and one awaited call at the top of `runDeployConfigTests()` in `server/test/deploy-config.suite.ts`. Change nothing else in that file. Cases: the D23 parity rule over the real `COPY` and the rendered policy, naming each missing key, with the allowlist exactly as D23 states; retention in the rendered policy equals `loadServerConfig({})`'s report and ledger defaults; placeholder failures by name; escaping of `<` in a value; store links absent without URLs and present with both; `associationState` on temp-directory fixtures (neither file, upload only, both); `buildSite` with a fake runner writing two files copies them byte-identically, fails on a non-zero exit, and leaves no `outDir` on failure; no `<script` in any rendered page. Discriminating red-checks, each recorded in the report: a policy missing `consentWhatSentDevice`'s text must fail; a `COPY` fixture with an extra `consentWhatSentReports` key that the policy doesn't quote must fail (against a variant that checks a hand-kept key list); a policy saying 30 days must fail.

## 17. Load test: replay server, image, driver

- [ ] 17.1 Create `server/src/loadtest/replay-model.ts` and `server/src/loadtest/server.ts` per design.md D26. The replay `ModelClient` imports no transport and no `fetch`. It identifies the role from the roster id, answers every call the real machine, summariser, clarify, rewrite and `ModelContentPolicy` make with deterministic turns (the classifier always allows), paces engineer-role turns by `WHIM_LOADTEST_ENGINEER_TURN_MS` (default 15000) and rewrite-role turns by `WHIM_LOADTEST_REWRITE_TURN_MS` (default 1000) with abort honored during the wait, and rotates generated sources across the top-level `fixtures/*.app.tsx` that pass `runStaticChecks` with no error diagnostic. `runLoadtestServer({ env, listen?, start? })` (`start` defaults to `startServer` and exists for the suite) refuses naming `OPENROUTER_API_KEY` when it is set in `env`, before calling `start`. It then installs a counting `fetch` trap that throws (restored on stop), and calls chain-11's `startServer` with the replay model and roster `loadtest/engineer` / `loadtest/rewrite`, a zero-cost stats transport, a no-limit credit transport, and an app wrapper whose `/healthz` answers `{ ok: true, service: 'whim-server-loadtest' }`, passing `NODE_ENV=production`, `OPENROUTER_API_KEY=loadtest-no-network` and those roster ids to the config loader. Add `server/loadtest-server.entry.mjs` (calls `runLoadtestServer` with `process.env`) and `server/loadtest-build.mjs`, which bundles that entry to `server/dist/loadtest/server/loadtest-server.mjs` through `bundleServerEntry`.
- [ ] 17.2 Create `server/src/loadtest/drive.ts` and the runner `server/loadtest.mjs` (bundles like `server/dev.mjs`): `node server/loadtest.mjs --target <https url> --devices <N> --cap <C> [--json <file>]`. Implement the driver exactly as design.md D26 describes (fresh UUID per device, a device-unique prompt, SSE read to the terminal event, time to the first `data:` event rather than a comment frame, the two-round leak probe with 5 s between rounds, p50/p95, counts, the exit rule). It accepts a sampler summary (`--stats <file>`, the CSV `run.sh` collects) and folds peak CPU and memory into the report. Pure pieces are exported for the suite: SSE frame parsing, percentile, the report builder, the verdict.
- [ ] 17.3 Create `deploy/loadtest/Dockerfile` (build stage runs the same `npm ci --ignore-scripts`, `npm run build` and `node server/loadtest-build.mjs` as the production build stage; final stage `FROM ${SERVER_IMAGE}` copies only the bundle and its sourcemap to `/app/server/`, keeps user 10001, and sets `CMD ["node","--enable-source-maps","server/loadtest-server.mjs"]`), `deploy/loadtest/cloudbuild.yaml` (`SERVER_IMAGE=…/whim/server:$COMMIT_SHA`, pushes `…/whim/server-loadtest:$COMMIT_SHA`), `deploy/loadtest/compose.loadtest.yaml` (overrides only `whim-server`: the load-test image, `env_file` reset to `/etc/whim/config.env` alone, the loadtest env pacing values, and `/mnt/disks/whim-data/loadtest:/data`), and `deploy/loadtest/run.sh start|drive|stop` per D26. `start` refuses unless HEAD equals the deployed tag, builds the image when Artifact Registry lacks it, uploads the override to `/opt/whim/loadtest/`, wipes and re-owns the loadtest data directory, runs `docker compose stop whim-server` and then `up -d whim-server` with both files, and waits for `/healthz` to report `whim-server-loadtest`. `drive` runs a `docker stats` sampler on the VM over IAP every 2 s, runs the driver against `https://$WHIM_API_HOST`, then stops the sampler and prints the report. `stop` runs `up -d whim-server` from `compose.yaml` alone and then `deploy/smoke.sh`.
- [ ] 17.4 Create `server/test/loadtest.suite.ts` exporting `runLoadTestTests()`, and add its import and one awaited call at the end of `runDeployConfigTests()`. Cases: `runLoadtestServer` with `OPENROUTER_API_KEY` in `env` rejects naming it and never calls the injected `start`; the replay model answers each role and every rotated fixture passes `runStaticChecks` with no error; a full `GenerationMachine` run over the replay model with a stub run stage ends in `result`, and an abort during a paced turn ends it with no terminal; `bundleServerEntry` over `server/src/main.ts` (write false) lists no input under `server/src/loadtest/`; `deploy/Dockerfile`, `deploy/cloudbuild.yaml`, `deploy/compose.yaml`, `deploy/deploy.sh` and `deploy/resize.sh` contain no `loadtest`; the override's `env_file` doesn't name `server.env`; driver pieces (SSE framing across chunk boundaries, keep-alive comments not counted as first event, p50/p95, and a verdict that fails on a `failure` terminal, on a refusal with N ≤ C, and on a leak-probe `server_busy`). Discriminating red-check: a production entry that imports the replay model must fail the metafile check (against a variant that only greps `server/dist`).
- [ ] 17.5 In `server/test/e2e.ts`, add a browser-backed case. Start `runLoadtestServer` in-process on `127.0.0.1:0` with a real session, `WHIM_MAX_CONCURRENT_GENERATIONS=3`, `WHIM_SYNTHRUN_CONCURRENCY=2`, turn pacing 300 ms engineer and 50 ms rewrite, and a temp data dir. Drive it with the driver's exported functions: three concurrent generations all end in `result`; a fourth started while they run is refused with `server_busy`; the two-round leak probe passes afterwards; the `fetch` trap counted zero calls; `/healthz` reports `whim-server-loadtest`. Stop the server and confirm `fetch` is restored. Run `npm run server:e2e` and the fast gate locally and keep both green.
