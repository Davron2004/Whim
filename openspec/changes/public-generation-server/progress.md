# Progress ledger: public-generation-server

Staging branch: integration/store-launch (shared launch run; MAIN_TIP 3a66cca)

## Session 4 — 2026-09-15 acceptance (in progress)

- Resumed clean staging tip `f08ad63de49eb8b8190d48087a88d518a5498ac8`. Full local gate passed; production `deploy/smoke.sh` exited 0 with all checks passing, including both intentionally absent association files.
- Task 15.3 remains pending a physical phone on cellular. The available Android emulator and iOS simulator do not satisfy that condition. Owner's mail/Apple checklist remains untouched.
- Task 15.4 started from a clean detached checkout at deployed image `2acb69cfe0b079d811199b4be1e8085d04b306ab`, `.claude/worktrees/store-launch-loadtest-20260915`. `resize.sh --profile event` passed quota, drain, stop, machine-type change and start, then exited 1: IAP could not connect to port 22 immediately after restart (4003). Its recovery also failed at that point. The documented same-tag `deploy.sh --tag 2acb69cfe0b079d811199b4be1e8085d04b306ab` retry exited 0 and restored healthy production on `e2-standard-8`; all smoke checks passed. This restart-readiness failure needs a finding.
- Load-test image preparation started (Cloud Build `4731e19b-1e57-4a5c-b371-1fda0775b3cb`). No drive result yet. Mandatory cleanup is `deploy/loadtest/run.sh stop`, then `deploy/resize.sh --profile standard`, with production smoke verified afterwards.
- Cloud Build succeeded. `run.sh start` then stopped production and exited 1 because nested sudo removed `WHIM_LOADTEST_IMAGE` before Compose interpolation. `run.sh stop` exited 0 and restored production with all smoke checks passing. Resize back to standard is in progress. No 15/16-device drive ran; OpenRouter's before/after counters were unchanged.
- Dispatched `chain-acceptance-fixes`, pinned BASE `f08ad63de49eb8b8190d48087a88d518a5498ac8`, branch `chain/server-acceptance-fixes`, worktree `.claude/worktrees/server-acceptance-fixes`. Scope and required behavioral red/green tests are in `acceptance-fixes.md`; independent review is required before merge. Fixes cover SSH readiness after VM start, preserving the replay image through sudo, and restoring production after failed load-test start.
- Standard-profile restoration completed: the return resize reproduced the same post-start IAP 4003 failure, then the documented same-tag deploy retry exited 0. Production is healthy on `e2-standard-2` at `2acb69c`, all smoke checks passing. No replay service remains active. Task 15.4 remains incomplete until the fixes are reviewed and the actual drives pass.
- Repair commit `b042aed` passed the worker's fast gate and protected-file integrity, but independent review REJECTED it (report mismatch). Recovery did not cover post-stop data-directory preparation failures, exact original exit codes were collapsed, and required failed-recovery/persistent-readiness tests were absent. Correction pass 1 is in the same worktree: one cleanup path for every post-stop failure, visible original and recovery errors, a hard bound for the readiness probe, and executable tests for startup/preparation/health/recovery failures plus persistent and recovery-start readiness. Nothing from this chain has merged.

## Earlier sessions

- 17:07 dispatched chain-6 BASE 889bc2d worktree .claude/worktrees/public-generation-server-6
- 17:29 chain-6 report: blocked, class B — serviceWorkers:block injects an init script into the sandboxed candidate iframe (SecurityError → 20 synthrun checks fail; harness code inside the untrusted realm). Adjudicated option 1: drop the Playwright option; SW blocked by the opaque-origin sandbox + abort-all route, with a non-vacuous suite case; implementer amends D5 / 7.2 / spec in-chain; scope += .devcontainer/run-loop.sh seccomp arg. SendMessage revision 1/2. Carry to chain-14: CI never runs synthrun/e2e, so the runner sysctl step is unverified.
- 18:03 chain-6 report: complete, GATE PASS (synthrun 282, e2e 40), class-A x5 (aborted vs blockedbyclient; erased unused TS import; egress count as capped trace entry; browserProcessId + env.d.ts; CI userns step unverifiable) · integrity OK · merged · regate-pass. Carry: chain-7 single launch call site; chain-12 compose needs security_opt seccomp. RUNTIME FINDING: webkitRTCPeerConnection survives neutralize.js (on-device WebRTC UDP egress) → security fix dispatched.
- 22:54 chain-0 (HUMAN-BOOTSTRAP): owner ran the server workspace install (esbuild 0.25.12, playwright 1.60.0, typescript 5.9.3, one lockfile copy each); parked contract-suite patch re-applied · guard:metro OK · committed · FAST GATE PASS. Resumed in a fresh orchestrator session after the usage cutoff.
- 22:55 dispatched chain-1 BASE 99ed045 worktree .claude/worktrees/public-generation-server-1
- 22:55 dispatched chain-7 BASE 99ed045 worktree .claude/worktrees/public-generation-server-7
- 23:08 chain-1 report: complete, GATE PASS (server 923), class-A x4 (loadServerConfig opts {now}; defaults for POLICY_TIMEOUT/REPORT+LEDGER_RETENTION from their specs; fail-fast on first invalid var; worktree node_modules needs per-entry links with @whim/* → own workspaces) · integrity OK · merged · regate-pass
- 23:08 dispatched chain-2 BASE 652f4fe · dispatched chain-4 BASE 652f4fe (chain-3 held for a frontier slot: concurrency + credit fail-open)
- 23:26 chain-7 report: complete, GATE PASS (synthrun 320 = 282+38, e2e 40), class-A x7 (synthrun/abort.ts helpers; resilience suite; isolation exports; env.d.ts process.kill; AbortError without signal.reason under RN AbortSignal types; openContextCount(); browser_launch_failed SessionError; post-openRun abort still resolves a discarded report) · red-checks x6 · single launch call site kept · integrity OK · merged · regate-pass. Carry to chain-11: e2e testCancellationDisposesAndReleasesSlot aborts 250 ms after beforeNavigate and would flip to AbortError if navigation ever exceeds 250 ms (about 30 ms today); openContextCount() is available for 12.6.
- 23:26 plan amendment dispatched on plan/web-host-and-capacity: whim pages host on the same Caddy, real hostnames (anycognition.ca), demo-night capacity profile and load test, backlog note on per-candidate Chromium.
- 23:30 chain-2 report: complete, GATE PASS (server 984), class-A x2 (server/test/e2e.ts fake UsageStore gained stubs for the six new methods, compile fix only; the cost-aware OpenRouter generation-stats transport does not exist yet) · integrity OK · merged · regate-pass. Carry to chain-9/10: compose a cost-aware transport (total_cost) for resolveRequestUsage; openRouterGenerationStatsTransport returns tokens only. Test-double gotcha: {...classInstance} drops prototype methods.
- 23:31 dispatched chain-5 BASE 44032ad
- 23:32 chain-4 report: complete, GATE PASS (server 1097), class-A x4 (check(input, route, signal?) so cachedPolicy can log route; rating rule on rewrite + generate prompts only, not repair/plan (reviewer to judge); classifier token usage has no return channel, so ledger crediting is left to chain-9/10; prompts/index.ts header notes the memoized policy-doc load) · integrity OK · merged · regate-pass. Carry to chain-9/10: always wrap cachedPolicy(...) (the only logging site); plumb the classifier call metering into the ledger. Stub-in-production already refused by loadServerConfig. chain-8 now eligible once chain-3 merges.
- 23:42 chain-3 report: complete, GATE PASS (server 1069), class-A x4 (credit transport in credit.ts, 5 s fail-open timeout, no config var; shared in-flight lookup, failed lookups never cached, epoch guard vs invalidation; RangeError on a non-positive cap; D8 hints kept verbatim though two sentences) · red-checks x8 · integrity OK · merged · regate-pass. Carry to chain-9: switch app.ts to shapeOnlyVerifier and delete the duplicate UUID middleware. Carry to chains 8/9/10: call invalidateCreditCache() at suite start (module-level cache). knip may flag the new admission exports until 9/10 wire them.
- 23:42 dispatched chain-8 BASE 1050db5
- 23:48 chain-5 report: complete, GATE PASS (server 1032), class-B adjudicated: the subagent Write guard blocks a handoff filename containing "report", so the contract landed as handoff/admin-contract.md and chains.md was repointed (chain-5 writes, chain-9 reads). Class-A x2 (admin/main.ts process entry split from pure cli.ts, mirroring app.ts/main.ts; list/usage defaults 50/7/10) · integrity OK · merged · regate-pass. DNS 2FA postponed by owner to 2026-09-15; first deploy may use the sslip.io stand-in.
- 23:48 dispatched chain-9 BASE 3d259e7
- 00:01 plan amendment merged (fee1b9a via 6f67b5b), validate --strict OK (repo 40/40). D20-D26: real hostnames in deploy/defaults.env (sslip.io only as break-glass); pages host as a second Caddy site with atomic symlink-swap publish and deploy.sh --site-only; association files only from PRR association-files, served only once BOTH fingerprints are committed (404 before that; Android and iOS link verification PENDING until PRR 14.3); pages rendered from repo sources with a consent-parity tripwire; operator values defaults.env → ~/.config/whim/deploy.env → env; capacity profiles standard|event (event: e2-standard-8, 15 generations, 6 synthrun contexts, unary 32, mem 16g, shm 3gb; to be measured) with resize.sh; a no-spend load test (replay model image, no key allowed, fetch trap, metafile and tripwire exclusion from production). Chains: 11 exports startServer/bundleServerEntry; 12 = 13.1-13.6 after 11+15; 13 after 16; 14 adds 15.4 load test and 15.5 association go-live; NEW 15 web-host-site (§16, needs PRR chain-9 merged for file names); NEW 16 load-test-harness (§17, after 12). Owner questions: support email (required at deploy), production model pair (default: the current .env pair).
- 00:08 chain-8 report: complete, GATE PASS (server 1458), class-A x4 (touched openrouter.ts + generation/model.ts: OpenRouterCreditError status 402 and structural isCreditExhaustedError, since the real client hid 402 as a network error; optional Clock.setTimer with an unref host-timer fallback; one machine.suite assertion re-pinned to "caller abort reaches every model call" per D13; endOnThrow extracted for complexity) · red-checks x10 · integrity OK · merged · regate-pass. Carry to chain-10: main.ts must pass config.generationMaxMs. Carry to the chain-9 follow-up: swap chain-9 local status===402 detection for isCreditExhaustedError. Known gap per contract: a 402 from the summariser is still swallowed without cache invalidation.
- 00:10 dispatched chain-15 (web-host-site) BASE d42ca84 (platform chain-9 merged, so association file names are fixed)
- 00:21 chain-9 report: complete, GATE PASS (server 1485), class-B adjudicated: classifier-call metering (content-policy "metered and observable without content") not implemented because ContentPolicy.check() has no usage return channel → fix chain 9b (widen check() to return {usage?, generationIds?}, meter it on clarify/rewrite, swap the local isProviderBudgetExhausted for chain-8 isCreditExhaustedError), dispatched before chain-10 so the generate route is built on the final interface. Class-A x2 (AppOptions: only pipeline/usageStore required, rest defaulted, so no existing suite edits; admitUnaryRequest + isProviderBudgetExhausted live in routes/clarify.ts, imported by rewrite.ts) · integrity OK · merged · regate-pass.
- 00:21 dispatched chain-9b (policy metering + shared 402 helper) BASE 70fdfcb
- 00:56 USAGE CUTOFF (session limit) killed every running agent; resumed at low priority by messaging the same agents (transcripts intact). Worktree state at resume: server-15 substantial uncommitted site work; platform-10 Fastfile only; server-9b and compliance-7 empty; iOS planner partial drafts.
- 01:05 chain-15 report: complete, GATE PASS (server 1549), class-A x4 (buildSite is pure and site.mjs owns stdout and exit codes; REPO_ROOT from process.cwd() per suite convention; regex replaced by indexOf/sort comparators for sonarjs; two independent IF blocks for store links) · integrity OK · merged · regate-pass. Privacy page h2s: What leaves your phone / What gets sent / What never gets sent / Reports / Usage ledger / What Whim does not do / Your choices / Age rating; all 8 non-allowlisted consent strings quoted verbatim; retention 90 days read from config defaults. Today's real build prints "association files: absent ... PENDING" and exits 0; missing WHIM_SUPPORT_EMAIL exits 1.
- 01:12 chain-9b report: complete, GATE PASS (server 1647), no deviations · integrity OK · merged. ContentPolicy.check() returns PolicyCheckResult {verdict, usage?, generationId?}; admitUnaryRequest credits classifier usage on allow and refuse; isProviderBudgetExhausted deleted for isCreditExhaustedError; red-check removing the credit call failed exactly the 2 new metering assertions. Orchestrator updated handoff/app-options.md (stale gap notes). Carry to chain-10: the generate route meters the classifier the same way.
- 01:12 gate-full on 50c443e: FAILED only at knip (everything else PASS, openspec 40/40). Findings: unused server/src/admin/main.ts (process entry), unlisted zod via src/host/launcher/contract-mirror.ts, 5 stale knip.json hints. Orchestrator applied the Class-2 knip.json fix (admin entry, stale entries dropped); a fix chain deletes contract-mirror.ts; gate-full reruns after.
- 01:25 dispatched chain-10 BASE 4d4566d (Opus; generate route, composition, real-TCP disconnect proof; carries maxRunMs, cost-aware transport, classifier metering, slot release on 402).
- 02:04 chain-10 report: complete, GATE PASS (server 1991), class-A x6 (AppOptions: reconcile + NO_OP_STATS_TRANSPORT removed, inFlight added, recorded in composition.md; server-core double-credit tests rebuilt on resolver; token-only openRouterGenerationStatsTransport deleted (knip); generate uses two resolver calls so classifier tokens are not double-counted; stream guards forward nothing after abort or the first terminal, policy errors fail closed; disconnect suite restores the Request/Response globals node-server replaces) · red-checks x8 incl. real-TCP · integrity OK · merged · regate-pass · knip clean. Found bug → fix chain 9c: clarify/rewrite resolve [policyGenerationId, ...ids] with creditOwned=false on failure/timeout/402, double-crediting uncached classifier tokens. Loose ends: reconcile.ts header lists stale import sites; a policy_unavailable row keeps cost_state pending (no generation id), same as clarify. Carry to chain-11: an in-process serve() must save/restore the Request/Response descriptors; add a test for main.ts openRouterUsageAndCostTransport when composition moves to lifecycle.ts.
- 02:04 dispatched chain-9c (classifier double-credit on clarify/rewrite failure paths) BASE fb9932a · dispatched chain-11 BASE fb9932a
- 02:19 chain-9c report: complete, GATE PASS (server 2023), class-A x2 (shared resolveUnaryUsage in clarify.ts, imported by rewrite.ts; the suite's testApp takes an injected usageStore/resolver) · integrity OK · merged · regate-pass. Bug confirmed in clarify AND rewrite for model failure, timeout and 402; red-check (single-call shape) failed exactly the 5 new failure-path assertions (e.g. 21/8/29 credited vs 16/6/22 expected).
- 02:46 chain-11 report: blocked class-B (one item), 6/6 tasks in code, GATE PASS, server:e2e 56/0, knip clean, commit 86cd7cd. B: Playwright chromium.launch defaults install SIGTERM/SIGHUP (close browsers) and SIGINT (exit 130) handlers, so a production drain would kill in-flight runs. Adjudicated option 1 (revision 1/2): add handleSIGINT/SIGTERM/SIGHUP false to browserLaunchOptions() in synthrun/session.ts (scope granted, sandbox and egress unchanged), amend handoff/synthrun-launch.md, add a real-mode SIGTERM e2e case with a red-check, rerun the synthrun isolation suite. Class-A x7 recorded in the report (admin bundle from admin/main.ts; usage/openrouter-stats.ts with a test; build.d.mts + buildRuntimeTree; closeAllConnections in drain; transports keyed on OPENROUTER_API_KEY; e2e 250 ms abort timed from page load; spawned no-browser launch-failure case). Orchestrator applied the Class-2 .eslintignore server/dist/ entry. Follow-up (unowned): server/dev.mjs exits immediately on signals, so dev mode does not drain. Carry to chain-16: install the fetch trap before startServer (the credit transport captures globalThis.fetch at creation).
- 03:00 chain-11 revision 1 report: complete, GATE PASS, server:e2e 68/0, synthrun 320 (sandbox + egress checks all PASS with the flags), knip clean, commit baba688 on 86cd7cd · integrity OK · merged · regate-pass. browserLaunchOptions gains handleSIGINT/SIGTERM/SIGHUP false; handoff/synthrun-launch.md + runtime-tree.md amended. New real-pipeline SIGTERM e2e case (child process, 4 s drain): Chromium stays alive, the context stays open, no terminal event until the deadline, then exit 0 with ledger ['aborted']; red-check without the flags: browser exited 21 ms after SIGTERM, ledger ['failed'], 4 fails.
- 03:00 dispatched chain-12 BASE c0a53f2 (Opus; container + GCP deploy artifacts, amended by D20-D26)
- 03:19 chain-12 report: blocked class-B (measured with a local docker build + run). The spec's cap_drop ALL + the vendored Playwright seccomp profile kills Chromium's sandbox at boot (seccomp allows chroot only with CAP_SYS_CHROOT; Chromium chroots inside its userns → sys_chroot check failed → SIGTRAP → browser_launch). With cap_add SYS_CHROOT only: self-test passes, sandbox on, egress blocked; /proc shows CapEff=CapPrm=CapAmb=0, CapBnd=SYS_CHROOT only, NoNewPrivs=1, Seccomp=2. ADJUDICATED option 1 by the orchestrator (security-relevant, flagged to the owner): cap_drop [ALL] + cap_add [SYS_CHROOT], the tripwire pins exactly that, spec (hardened container requirement) + D17 amended in the same chain; option 2 (a derived seccomp profile) rejected as it breaks the byte-identical vendored profile. Verified pins: node:22.23.2-bookworm-slim@sha256:83f487e0…, caddy:2.11.4@sha256:13ba145c…, gcr.io/cloud-builders/docker@sha256:e0360913…
- 03:59 chain-12 revision 1 report: complete, GATE PASS, commit 78f4121 · integrity OK · merged · regate-pass · knip clean · validate --strict OK. SYS_CHROOT ruling applied (spec scenario "The capability set is pinned", D17 measured note, compose cap_add, tripwire pins). Class-A: deploy/lib.sh shared helpers; cloudbuild _REGION; deploy.sh resolves profile + builds site before Cloud Build; provision.sh creates VPC whim-net (default-allow-ssh would break IAP-only); HOME=/tmp; resize recovery covers drain/stop/confirm; --site-only Caddy-only start. Local verification on the real image: CapEff=0, NoNewPrivs=1, Seccomp=2, self-test pass, SSE frames 6/1014/2021 ms through Caddy, drain exit 0, pages 200/404 without redirect, .well-known 404 → 200 application/json after the symlink swap; 49 planted-weakening controls in the suite. Not verifiable here: real gcloud flags, the Debian GCE image, amd64 build, shellcheck. Carry to chain-13: docs/deploy.md needs an "OpenRouter key" section (deploy.sh names it). Carry to chain-16: its own build context or <Dockerfile>.dockerignore (the root .dockerignore excludes deploy/ and *.env); install the fetch trap before startServer.
- 03:59 ATTENDED provision/deploy sequence (owner gcloud auth): 1) deploy/provision.sh 2) gcloud compute scp --tunnel-through-iap --recurse deploy/vm whim-vm:/tmp/whim-vm 3) gcloud compute ssh whim-vm --tunnel-through-iap --command 'sudo bash /tmp/whim-vm/bootstrap.sh --region northamerica-northeast1' 4) ~/.config/whim/deploy.env with WHIM_SUPPORT_EMAIL, WHIM_ENGINEER_MODEL, WHIM_REWRITE_MODEL 5) after DNS A records: deploy/deploy.sh --site-only 6) after the owner adds a secret version to whim-openrouter-api-key: deploy/deploy.sh 7) demo day: deploy/resize.sh --profile event, then back to standard. Rollback: deploy/deploy.sh --tag <sha>.
- 03:59 dispatched chain-16 BASE 279eeb6
- 04:49 chain-16 report: complete, GATE PASS (server 2307, e2e 74), knip clean, commit 26263a5 · integrity OK · merged · regate-pass. Class-B resolved in-chain: loadtest/server.ts cannot value-import startServer (lifecycle → playwright breaks the fast-suite bundle), so RunLoadtestServerOptions.start is REQUIRED and injected by loadtest-server.entry.mjs; LoadtestConfigError is local; contract documents it. Class-A: the driver captures the pre-trap fetch at module load; stats as docker-native percentages; Dockerfile.dockerignore. Attended 15.4: resize event → run.sh start → drive --devices 15 --cap 15 (0 server_busy) → drive --devices 16 --cap 15 (exactly 1) → run.sh stop (restores production + smoke) → record → resize standard. The run.sh docker-stats sampler over IAP is best-effort and untestable outside the VM.
- 04:49 dispatched chain-13 BASE faf21e3 (runbook and records; last dispatchable chain before attended chain-14)
- 05:01 chain-13 report: complete, GATE PASS (server 2316), knip clean, validate OK, commit 3a7aae3 · integrity OK · merged. docs/deploy.md runbook (incl. "OpenRouter key"), decision #68 (not #64 as the stale task text said), capabilities.md rows for the four new capabilities (point at openspec/specs/* paths that land at archive), generation-server spec Purpose rewritten (task 14.4), README pointer; runbook↔script tripwire with discriminating red-checks. Event-profile numbers in the runbook are estimates until attended 15.4. ALL DISPATCHABLE CHAINS DONE; remaining: chain-14 (attended). Next: gate-full + whole-change reviewer.
- session 3: the 05:xx whole-change reviewer was lost with session 2 (never ledgered); re-dispatched (Opus) over 5b74787..staging tip. CHANGES-REQUESTED. Ledger spot-checked accurate. Solid: single teardown path in generate.ts, idempotent SlotHandle.release, BEGIN IMMEDIATE admit, credit-cache epoch guard, /v1/* gated once, key never on a command line, discriminating deploy/loadtest tripwires; Class-1 scan clean. Queued notes: (a) rating rule SHOULD cover repair (spec says "engineer"); (b) SYS_CHROOT minimal, correct, pinned exactly; (c) summariser 402 still swallowed, LOW; (d) chain-9/10 carry-forwards all verified; (e) serve() descriptors restored, 250 ms abort re-timed off page load; (f) no-spend layers hold. Findings: 1 HIGH BLOCKING clarify/rewrite have no globalLimit (UUID rotation → unbounded flash spend until the $50 key trips and kills generate); 2 MED BLOCKING repair prompt lacks ratingRuleAppendix; 3 MED partial usage resolution stamped resolved; 4 MED /healthz/sse holds shared unary slots unauthenticated; 5 MED admitUnaryRequest leaks the slot if the store throws; 6 MED mid-stream OpenRouter error frames dropped (402 never mapped); 7 LOW summariser 402 no cache invalidation; 8 LOW deploy.sh --tag rollback republishes today's site; 9 LOW capabilities.md rows point at specs that exist only after archive; 10 LOW settle uses Date.now(); 11 LOW egress rules IPv4-only; 12 LOW drain aborts emit no terminal.
- session 3 dispatch: server-fixes-a (Opus; findings 1,4,5,10; owns docs/deploy.md) and server-fixes-b (Opus; findings 2,3,6,7) in parallel at the staging tip; server-fixes-c (8, 11) after a merges. Backlog: 12 (spec change, not for launch); 9 resolves at archive. Orchestrator call, flag to owner: WHIM_LIMIT_UNARY_PER_DAY default 2000 (4x the 400 generation ceiling, matching the per-device 60:15 ratio); healthz probe gets its own cap of 2.
- session 3: server-fixes-a report: complete, GATE PASS (server 2348), commit 9037ede · integrity OK · merged · regate-pass. F1 WHIM_LIMIT_UNARY_PER_DAY (default 2000) counted ACROSS clarify+rewrite via a new additive `AdmitParams.globalKinds` (both stores counted the global total per kind, so globalLimit alone would have given 2000 each); refuses server_busy with Retry-After to UTC midnight; red-check: without it a fourth rotating device gets 200. F5 slot released on a store throw (red-check: count stuck at 1 without the catch). F4 probe pool `'probe'` cap 2 in slots.ts, spec scenario in server-deployment. F10 settle takes optional `now` (report.ts, out of scope, still stamps Date.now()). Class-A: server-deployment spec touched (where the probe requirement lives); generate.ts three settle sites gained `now: clock()`; admission.suite counts gained probes:0. Carried to server-fixes-c: refresh handoff/admission-core.md (SlotKind 'probe', MAX_CONCURRENT_PROBES, SlotCounts.probes) and handoff/usage-ledger.md (globalKinds, SettleParams); design.md D6 table row for the unary ceiling; lifecycle drain waits on generations+unary only, not probes.
- session 3: server-fixes-b report: complete, GATE PASS (server 2369), validate --strict OK, commit c404152 · integrity OK · merged · regate-pass. F2 repair prompt carries ratingRuleAppendix (plan deliberately not); spec enumerates rewrite + generate + repair; tripwire asserts all three and absence from plan. F6 one status→error mapping (`statusError`) for pre-stream HTTP and in-stream `{"error":{"code":402}}` frames; emitFrame throws the typed error; machine test proves budget_exhausted, no check on the truncated candidate, cache re-queried. F7 summariser 402 invalidates the cache at BOTH swallowing layers (summarise.ts turn catch + machine summariseDelivery). F3 ids resolve concurrently under the shared deadline; `resolved` only when every id resolved, else `unresolved` with partial COST discarded (partial tokens still credited when creditOwned=false; a 'partial' CostState would be a usage-store + admin-report change, not taken). Red-checks for all four recorded in the report (F3's first red-check passed spuriously; bounds tightened). Class-A: summarise.ts edited (conditional scope); responseError routed through statusError; nested template split for sonar lint. MEMORY (kept in ledger, not saved): a 402 arrives three ways (pre-stream status, in-stream error frame, summariser); all must reach invalidateCreditCache(), only the first two end the run. Stale contracts (content-policy.md, pipeline-budget.md, usage-ledger.md resolvedAll) handed to server-fixes-c.
- session 3: reviewer (Opus) over server-fixes-a+b (3f1eb70..05fb4be): APPROVE-WITH-FINDINGS, all POST-LAUNCH; server 2401 pass from the primary tree; no vacuous test; spec conformance confirmed; both stores agree on the ceiling predicates; refusals consume no unit; F6 throw unwinds the body iterator and every StreamResult consumer attaches a rejection handler. Findings: (1 MED) an `unresolved` cost row is terminal (recordCost is WHERE pending; no sweeper) so the all-or-nothing stamp loses cost data and biases the operator's per-generation stats low; (2 MED) the F5 outer catch releases the slot but never settles the already-inserted ledger row → a throwing store burns the daily ceiling invisibly, and no app.onError so the client gets Hono's plain 500; (3 LOW, unverified) any in-stream error frame is fatal even after a complete candidate; (4 LOW) unbounded resolver fan-out; (5 LOW) MAX_CONCURRENT_PROBES is the only cap not env-read; (6 LOW) design.md D6 row, already with chain C; (7 LOW) `globalKinds: []` would render `kind IN ()`. Plan: server-fixes-d after C merges for 1, 2, 4, 5, 7; 3 to backlog.
- session 3: server-fixes-c report: complete, GATE PASS (server 2353), commit cb3073d · integrity OK · merged · regate-pass. (8) `--tag` rollback is server-image-only, no site build/publish; runbook says `--site-only` moves the site separately; red-check plants the publish call back. (11) ip6tables chain (fd00:ec2::254 dropped, 443/53 allowed, drop-all, `-i br-+` jump because the whim bridge has no IPv6 subnet to match; safe only while this VM runs no other bridge) + `assert_bridge_no_ipv6` in bootstrap (no-op before the network exists); red-checks for both. Contracts refreshed: admission-core.md (120 lines), usage-ledger.md, content-policy.md, pipeline-budget.md (the last two written from the orchestrator's description of chain B, not its diff — verify at the next review). design.md D6 row added. Drain waits on probes (lifecycle.ts:255) WITHOUT a test: a real-startServer Node suite cannot bundle under server/test/run.mjs (its external list omits playwright-core, which lifecycle → synthrun/session pulls in); only e2e.run.mjs externalizes it. Carried to server-fixes-d: put the probe-drain case in e2e.ts.
- session 3: server-fixes-d report: complete, GATE PASS (server 2480, e2e 81, knip 0, validate --strict OK), commit 6ddb4a4 · integrity OK · merged 816bd33 · regate-pass. D1 `recordCost` upgrades unresolved→resolved (resolved stays final); schema `requests.generation_ids TEXT` with an idempotent ALTER + a pre-change-file migration test; resolver registers ids on the pending row before its attempts; `runCostResolutionSweep` (≤50 rows oldest-first, same shared deadline, skipped while draining, never overlapping) scheduled by lifecycle 30 s after boot then every 5 min on unref'd timers, only when a stats transport exists; sweeps COST only, never re-credits tokens; stale pending measured from ended_at. D2 both admissions settle the row `error` on a store throw (no refund) then release; one `app.onError` → 500 internal_error ApiError body, real message only in the log. D4 worker pool of 4. D5 `WHIM_LIMIT_PROBE_CONCURRENCY` (default 2) through loadServerConfig → SlotLimits.maxConcurrentProbes; docs + spec rows. D6 e2e `testDrainWaitsForProbes` (regression net, not a discriminating red-check: the drain's connection wait would also cover the probe socket). D7 empty globalKinds → [kind] in both stores (node:sqlite accepts `IN ()` as always-false, so the symptom was a ceiling that never binds). D8 chain C's two contract paragraphs verified correct. Five red-checks recorded (15/2/7/1/1 failures). Backlog: reviewer finding 3 (in-stream error frame fatal after a complete candidate).
- session 3: reviewer (Opus) over server-fixes-d (5887792..816bd33): APPROVE-WITH-FINDINGS, all POST-LAUNCH; no double count (cost written not accumulated; resolved-is-final enforced at the write in both stores, bindings checked); migration idempotent both ways; settle idempotent so no double settle; app.onError cannot change an existing status or touch the SSE paths; every new test discriminating except the disclosed e2e drain net (judged acceptable). Findings: (1 MED) the sweep has no give-up rule: oldest-first LIMIT 50 with no attempt counter or max age, so 50 permanently dead rows starve every newer row forever while burning stats traffic; (2 LOW) settleFailedAdmission runs before handle.release(), gating slot release on a store that just failed; (3 LOW) CostSweepCandidate.deviceId is carried but never read; (4 LOW) the resolve.ts comment claims every id gets an attempt, but ids past the first four can get none under a slow provider; (5 LOW) an in-flight sweep pass is not registered with the drain window, so shutdown may log a meaningless sweep-failed warning; (6 LOW) SQLite applies LIMIT before the JS empty-ids filter. Dispatch server-fixes-e (Sonnet, mechanical) for 1, 2, 3, 4, 6; 5 backlog.
- session 3: server-fixes-e report: complete, GATE PASS (server 2486), commit d25c81c · integrity OK · merged · regate-pass. E1 sweep candidates bounded by `ended_at` within 24 h (`DEFAULT_SWEEP_MAX_AGE_MS`, both stores; red-check: 5 new tests fail without it, incl. 50 dead rows crowding out one fresh row). E6 SQL excludes `'[]'` before LIMIT. E2 release before settle. E3 deviceId dropped from CostSweepCandidate. E4 comment softened. All review fix chains merged; public-generation-server has no open buildable work; remaining: chain-14 attended acceptance. Backlog (post-launch, ledgered above): in-stream error frame fatal after a complete candidate; in-flight sweep pass not registered with the drain window.
- session 3 ATTENDED (chain-14 acceptance begins): owner ran `deploy/provision.sh` (classifier refuses production deploys from the agent): static IP adopted, services, registry `whim`, SA whim-vm, network whim-net + subnet 10.10.0.0/24 (IPv4 only), firewall web + IAP-SSH, disk whim-data 20 GB, VM whim-vm e2-standard-2 RUNNING on 34.118.191.193, empty secret whim-openrouter-api-key. IAP scp/ssh failed twice with 4047 "Failed to lookup instance" in the first ~2 min after creation (propagation), then worked. bootstrap.sh exit 0: Docker + compose, registry credentials, data disk formatted/mounted, unprivileged userns OK, whim-egress.service enabled. Both A records resolve (Google, Cloudflare, authoritative). `~/.config/whim/deploy.env` written (support@anycognition.ca, dev model pair, Play URL).
- session 3 ATTENDED: staging branch pushed to origin (deploy.sh refuses unpushed commits). `deploy/deploy.sh --site-only` at 91def0c: site built, uploaded, published, Caddy reloaded; the in-script smoke FAILED 5 checks with TLS "internal error" because it ran ~10 s before Let's Encrypt finished issuing (both certs obtained at 15:56:06-07 UTC per the Caddy log). `deploy/smoke.sh --pages-only` rerun: all checks passed (privacy, support, /a/x, 404, both association files 404 as expected). BACKLOG: deploy.sh's post-publish smoke should retry TLS failures for ~60 s on a first deploy instead of failing the run. Scanners hit api.whim within seconds of the cert (expected 502 until the server is deployed). Next: owner adds the OpenRouter key as a secret version, then `deploy/deploy.sh`.
- session 3 (owner request): site-model-wording 65f6b3a+d75779b · integrity OK · merged · regate-pass. The privacy page names OpenRouter as the processor and says models can change and may be processed outside Canada; WHIM_*_MODEL are no longer page placeholders (build.ts allowlist, web-site suite, D23, server-deployment spec, handoff/web-site.md, docs/deploy.md all updated). Rationale: the compliance spec requires disclosing "third-party AI model providers through OpenRouter", never model ids; naming ids was D23's own stricter default. Owner also switched the engineer model to deepseek/deepseek-v4.1-flash in deploy.env (probe: same wire shape, reasoning flag only unhides, ~14x cheaper on a toy prompt, but it fences code → chain `source-fence` in flight).
- session 3: source-fence c4867c8 · integrity OK · merged · regate-pass (server 2501). `unwrapSourceFence` (source-block.ts, mirrors json-block.ts: leading fence only, optional ts/tsx/typescript/javascript/jsx tag, missing closer tolerated, ≤3-line prose preamble before a fence, unfenced input returned as the same object) applied at the generate and repair seams before the check stage; machine test proves a fenced engineer reply delivers with zero repair rounds; spec scenario in generation-pipeline. Motivation: deepseek-v4.1-flash fences code despite "no markdown fence".
- session 3 ATTENDED: owner added secret version 1 (73 bytes); owner ran `deploy/deploy.sh` at 2acb69c: Cloud Build image `server:2acb69c…`, upload 20260915T165817Z, compose/seccomp/config/site installed, /etc/whim/server.env written, pull + restart through the drain, health OK, `smoke.sh: all checks passed` (healthz, generate-without-device → 400, healthz/sse 3 frames, metadata egress blocked, no react-native in node_modules, pages, association 404s). Task 15.2 ticked. 15.1's Linux-host run is covered by the VM run except the real generation, which is 15.3. Engineer model in production: deepseek/deepseek-v4.1-flash.

- Server acceptance correction 1 (`5a7a08d`, `a6aee87`) passed 2515 server checks and the fast gate, but independent review found missing proofs: successful smoke was not asserted, the replay-stop assertion counted the initial stop, install failure and hanging-IAP timeout were untested, and the advertised readiness deadline could overrun. Correction pass 2 covers those exact cases and bounds probe/sleep by the remaining deadline. Production remains healthy on standard profile at `2acb69c`; no acceptance fix has merged or deployed yet.

- Correction pass 2 `c08fd0b` passed 2520 server checks, fast gate and six-file integrity. Independent review accepted the cleanup behavior/proofs but requested two final test changes: the transient readiness fixture must actually emit IAP 4003, and the 2-second deadline test must reject the previous 5-second oversleep (its current 8-second allowance cannot). The chain has reached the runbook's two-revision cap. Root requested owner approval for one extra pass limited to those tests; no further changes, merge or deployment until that answer. Other native work continues independently.

- With no answer yet to the extra-pass question, `scripts/fixloop.sh park` preserved `c08fd0b` as `wip/server-acceptance-fixes`; worktree remains `.claude/worktrees/server-acceptance-fixes`, resume note `.claude/fixloop/wip-server-acceptance-fixes.md`. Do not merge or deploy this repair until the two remaining tests pass independent review. Production remains standard profile at `2acb69c`, previously restored and smoke-verified. No 15/16-device drive ran in this session.

- Owner explicitly approved both pending actions ("green light on both"). Root resumed the server branch as `chain/server-acceptance-fixes` and dispatched the extra pass, limited to the actual IAP 4003 fixture and a deadline assertion that rejects the former oversleep. Independent review and normal gates still apply; no further approval is needed for this pass.

- Approved extra pass `51c7d1f` changes only the test suite: actual IAP 4003 fixture plus an executable old-sleep mutant that fails the tighter deadline. Server checks 2522/2522 and worker fast gate passed; independent review APPROVE; exact six-file integrity passed. Merged as `1afc1fc`; full primary gate exited 0 (`/tmp/whim-gate-full-server-acceptance-merged.log`). Tasks 18.1–18.4 are complete. Live task 15.4 still awaits deployment of the matching repaired tip and both drives. Fresh pre-deploy production smoke exited 0 with all checks passing.

- Full deployment of pinned `1e02aa3b319623e986e9b1faedd0835033c375fb` exited 0 with all smoke checks passing. Cloud Build `68ecf5f0-e60d-4553-ab13-9afe10a5cb63` succeeded; production image digest `sha256:a8cb5d4f05659e7e0a50cf47027c792ed73428307bfd0e6cbeb8a5234bf5637e`. Log: `/tmp/whim-production-deploy-resumed.log`. The operational checkout pins that SHA independently of subsequent staging documentation/host changes.
- Repaired event resize exited 0: `resize.sh: whim-vm runs e2-standard-8 with profile event`; tag redeploy and all smoke checks passed without the previous SSH-readiness failure. Log: `/tmp/whim-resize-event-resumed.log`. OpenRouter before snapshot at `2026-09-15T20:24:59Z`: 281 requests, $2.59, 1.72M tokens, 4.2% cache hits. Replay image build/start is in progress (`ae8cf272-e935-4cf0-838d-1703692a58b6`); no drive has started yet. The local sampler observer is running and retains temporary CSVs from the two round-specific TMPDIRs.

- ATTENDED task 15.4 replay start at deployed `1e02aa3b319623e986e9b1faedd0835033c375fb`: replay image built and started, then Compose reported `whim-server` unhealthy. Post-stop cleanup restored the base production service and smoke passed; the VM was resized back to `e2-standard-2`; no drive ran and OpenRouter before/after activity was unchanged. The replay container was removed during recovery, so its log is unavailable. Read-only Compose rendering showed the load-test override retained base `/etc/whim/server.env` under ordinary sequence merge; that file's provider key triggers the existing fail-closed replay refusal. VM Docker Compose is `v5.5.1`; local `v5.4.0` parses `!override`. Planned chain `chain-replay-compose-env` owns only the override, its Node structural tripwire and `handoff/loadtest.md`; task 19.4 remains attended before retrying task 15.4.
- Dispatched `chain-replay-compose-env` at pinned BASE `6a7c3832a88678e76e4484b6a8992925a883458b`, branch `chain/server-replay-compose-env`, worktree `.claude/worktrees/server-replay-compose-env`. Exact three-file scope and red/green proof are in `replay-compose-env-fix.md`. Root caught and corrected an unchecked success echo in the planned receipt before dispatch; both expected outcomes now explicitly control its exit. No live retry starts until independent review, integrity and merged full gate pass.
- Replay correction reproduced the ordinary-list structural failure, then its synthetic Compose receipt passed with explicit expected statuses 42 and 0 (`/tmp/whim-replay-compose-receipt-statuses.log`). The first post-fix server suite exposed an existing flaky negative control: old elapsed 1062 ms failed the expected oversleep assertion. Independent diagnosis found the hanging probe could consume the total integer-second deadline before reaching the mutated sleep; the later 2525/2525 rerun does not resolve that defect.
- Dispatched separate test-only `chain-readiness-test` at pinned BASE `a7a3c6f91137644e1f999db41b2db533da542cdb`, branch `chain/server-readiness-test`, worktree `.claude/worktrees/server-readiness-test`. Scope is the deployment test suite and its short readiness-test handoff. `readiness-test-fix.md` preserves the hanging-child case and requires a separate immediate-failure fixture with proof that the old five-second sleep actually executed. Its files are disjoint from the replay correction. Both chains must pass review and merged gates before live retry.
- Replay correction `7845a150d253c2c7c1b3d7286b238dacc1c7a874` passed final independent review, 2525 server checks, lint and fast gate. The first gate's regex-space lint findings were corrected within scope. Root staged and committed the exact three files after reused-agent Git staging was denied; no source edits by root. Integrity passed, then merge `bf3ea0197d339863568ec733370e327541954e66` started its primary fast regate (`/tmp/whim-gate-replay-compose-merged.log`). The flaky readiness test remains open in its separate chain; no live retry yet.
- Replay primary fast regate exited 0. Independent no-daemon receipt on the merged primary tree also exited 0, recording ordinary-list status 42 and corrected status 0 (`/tmp/whim-replay-compose-primary-receipt.log`, companion `.exit`). Tasks 19.1–19.3 complete. Task 19.4 and live 15.4 remain pending; the readiness-test correction and final full gate still precede live retry.
- Readiness-test correction `89614e7` passed independent final review, exact two-file integrity, 2526 server checks and the worker fast gate. The normal helper measured 1327 ms and the mutant 5078 ms; shell trace proves `sleep 5` executed. Root staged and committed the two files after the worker's staging command was denied; no source edits by root. Merged as `d6c32ae3a6420ba9601ff9faff7484012690a22a`; primary fast regate exited 0 (`/tmp/whim-gate-readiness-merged.log`). Tasks 20.1–20.3 complete. Full gate and matching-tip deployment still precede live retry.
- Combined full gate and final integration review passed. The clean operational checkout now pins `420be203612d3eb65b91f4a204188dfb84dda205`, also the source of the final normal iOS build. Full production deployment exited 0 with all smoke checks passing (`/tmp/whim-production-deploy-envfix.log`): Cloud Build `7cb160ab-a38c-45ef-a68f-e04397927e06`, image digest `sha256:c31fcf19968c30f59e1d2528c48daa373e44f9a15cf7c9c7dcc98a8517de4f16`. Event resize is running before the live replay retry. Read-only image-contract preflight confirmed the inherited production tree includes the five fixture sources the replay startup reads; this is not a remote image receipt.
- Event resize exited 0 with all smoke checks and `e2-standard-8` confirmed (`/tmp/whim-resize-event-envfix.log`). Fresh OpenRouter baseline at `2026-09-15T21:17:44Z`: 281 requests, $2.59, 1.72M tokens, 4.2% cache hits; screenshot `~/.cache/whim-launch-2026-09-14/acceptance-2026-09-15/openrouter-before-envfix-20260915T211744Z.png`. Replay image build/start now runs from the pinned checkout, Cloud Build `5592a377-831c-4fae-84c2-7c402a146cfe`, log `/tmp/whim-loadtest-start-envfix.log`. No drive yet.
- Replay startup exited 0 and reached its expected health identity. Both capacity reports then passed their strict JSON predicates: 15 devices yielded 15 results, no failures/missing terminals/refusals; 16 yielded 15 results and exactly one `server_busy`; both leak probes passed. First-event p50/p95 were 1173/1240 ms and 1149/1208 ms; total p50/p95 were 35076/42243 ms and 35302/42427 ms. CPU peaks were 295.79 and 397.49 Docker percent (36.97% and 49.69% across eight vCPUs); memory peaks 3.33% and 3.09% of the 16g limit. Reports: `~/.cache/whim-launch-2026-09-14/acceptance-2026-09-15/loadtest-envfix-round15.json` and `loadtest-envfix-round16.json`.
- Both wrappers nevertheless exited 1 after JSON with `sampler_pid: unbound variable`. The EXIT trap referenced locals after `cmd_drive` returned, before cleanup. The raw CSVs were recovered from the actual macOS temporary directory (plain `mktemp` ignored the supplied round-specific TMPDIR); they match both report peaks. Retained copies, source identification, hashes and timestamps are in `loadtest-envfix-round15-samples.csv`, `loadtest-envfix-round16-samples.csv` and `loadtest-envfix-samples-provenance.json` under the same evidence directory. The first sampler outlived the JSON report by about 22 seconds; raw row counts are not exact report-window durations. Both have three consecutive rows above raw 85 and none above raw 680, but no per-row timestamp claim. The earlier directory watcher retained zero files. After byte-equality verification, only the two identified temporary CSVs were removed; no matching local sampler process remained.
- `run.sh stop` exited 0 with all production smoke checks (`/tmp/whim-loadtest-stop-envfix.log`); standard resize also exited 0 with smoke and `e2-standard-2` confirmed (`/tmp/whim-resize-standard-envfix.log`). OpenRouter totals at 21:28:32Z were unchanged from 21:17:44Z: 281 requests, $2.59, 1.72M tokens, 4.2% cache hits; after screenshot `openrouter-after-envfix-20260915T212832Z.png`; browser space 196 closed. Server behavior and no-spend evidence passed, but task 15.4 remains pending for clean wrapper exits.
- Dispatched `chain-sampler-cleanup` at BASE `37ed341602f37bcb54167cf66e85f605010ba0e5`, branch `chain/server-sampler-cleanup`, worktree `.claude/worktrees/server-sampler-cleanup`. Its three-file client wrapper/test/contract scope is in `sampler-cleanup-fix.md`. Independent CLI audit confirms the next retry can use clean operational checkout `420be203` for start/stop (preserving its deployed-tag guard) and the corrected primary checkout for drive; server image, replay code, driver and Compose remain unchanged. No image rebuild or tag-guard bypass is needed for this client-only fix.
- Sampler correction `0f4ed9f` merged as `de57566`. Regression tests reproduced the expired-local error and the surviving gcloud child. The fix keeps cleanup inside the function's lifetime, terminates the sampler's owned process group, preserves driver statuses 0 and 23, and stops the remote loop on collection/output failure. Tests prove a nonempty CSV existed before removal and catch a single-PID-kill mutant. Server checks: 2530 passed, 0 failed; fast gate passed; independent reviewer APPROVE; exact three-file integrity passed; merged full gate exited 0 (`/tmp/whim-gate-full-sampler-cleanup.log`, 41 OpenSpec items). Tasks 21.1–21.3 complete. The merged worktree and branch were removed. Final live retry started with event resize from pinned `420be203`; OpenRouter baseline at 21:49:40Z remains 281 requests/$2.59/1.72M tokens/4.2% cache, own space 197 open for the after snapshot.
