## 1. Exit logic and the system-back seam

- [x] 1.1 Create React Native-free `src/host/launcher/system-back.ts` per design D9: export `BackHandlerLike` (`addEventListener('hardwareBackPress', listener: () => boolean) => { remove(): void }`) and `bindSystemBack(api, current: () => (() => void) | null): () => void`. Create `src/host/launcher/use-system-back.ts`, the only new file importing `BackHandler`: `useSystemBack(handler: (() => void) | null)` keeps the handler in a ref assigned on every render and calls `bindSystemBack(BackHandler, () => ref.current)` in a mount-only effect whose cleanup unsubscribes. Create `src/host/launcher/test/screen-exits.suite.ts` with `bindSystemBack` cases against a fake api: one registration across three handler swaps, the latest handler runs, the listener returns `true` with a handler and `false` with `null`, and the unsubscribe removes the listener.
- [x] 1.2 In `src/host/launcher/prompt-flow.ts` add `planBackAction(editingRow: boolean): 'cancel-edit' | 'leave'` beside `buildBackAction` (design D4). Add both cases to the suite that already covers `buildBackAction` (`test/prompt-flow-screens.suite.ts`).
- [x] 1.3 In `src/host/launcher/consent-flow.ts` add `consentControls(mode: 'ask' | 'review', consentOn: boolean)` returning `{ primary, plain }`, each `{ action: 'agree' | 'close' | 'turn-off'; label: keyof typeof COPY }` (type-only `COPY` import), exactly per the table in design D6. Add no `COPY` key. Cases in `test/consent-flow.suite.ts`: every one of the three states has exactly one `close`; review-on's `close` is the primary; review-off's plain is `close` labelled `consentDecline`; ask's labels are `consentAgree` and `consentDecline`.
- [x] 1.4 Create React Native-free `src/host/launcher/screen-exits.ts` with `ScreenKind`, `ExitControl`, `ScreenExit`, `SCREEN_EXITS`, `FALLBACK_EXIT` and `frameEdgesFor`, exactly as design D8 and D10 give them (13 rows; `FALLBACK_EXIT.controls` is `ScreenErrorFallback.tsx` `screenErrorBack`). Add that key to `copy.ts` here, as `screenErrorBack: 'Back to your apps'` beside `screenErrorRetry`, so the table typechecks before task 3.1 renders it. Add `frameEdgesFor` cases to `test/screen-exits.suite.ts`: `app` and `dev` get `['top']`, each of the other eleven kinds gets `['top', 'bottom']`.
- [x] 1.5 In `src/host/launcher/ScreenBoundary.tsx` add optional `onLeave?: () => void` to `ScreenBoundaryProps` and `ScreenFallbackProps` and pass it through to the fallback unchanged (design D7). Add `react-test-renderer` cases to `test/screen-exits.suite.ts`, following `observability-ui.suite.ts`'s render idiom: a throwing child with `onLeave` given hands the fallback that exact function; without it the fallback gets none; invoking it calls the spy once; a changed `screen` still resets the boundary. Register `runScreenExitsTests` in `test/acceptance.ts`.

## 2. Screens on the seam

- [x] 2.1 Replace the hardware-back `useEffect` in `ComposeStep.tsx`, `ClarifyStep.tsx`, `SettingsScreen.tsx`, `HistoryScreen.tsx`, `FailureScreen.tsx` and `AppLinkMissingScreen.tsx` with `useSystemBack(<the prop the effect called>)` and drop their `BackHandler` imports. Re-pin, never delete, the assertions that read those listeners: `test/failure-screen.suite.ts:98`, `test/app-link-ui.suite.ts:114` and `test/prompt-flow-screens.suite.ts:570`, each to the `useSystemBack(<prop>)` call.
- [x] 2.2 In `PlanStep.tsx` build one `handleBack` from `planBackAction(editingIndex != null)` that cancels the row edit or calls `onBack`, and pass it to both `useSystemBack` and `<FlowHeader step="plan" onBack={handleBack} />` (design D4).
- [x] 2.3 In `BuildStep.tsx` remove the `onLeaveRunning` prop: `Leave it running` and `useSystemBack` both take `onBack`. In `LauncherRoot.tsx` pass only `onBack={onBuildBack}`; `onLeaveRunning` itself stays for `onBuildBack` and the link exit. Re-pin `test/prompt-flow-screens.suite.ts:557` to `COPY.buildLeaveRunning` plus `onPress={onBack}`; `test/prompt-flow-wiring.suite.ts:458-479` must stay green untouched.
- [x] 2.4 In `DoneStep.tsx` call `useSystemBack(onBackToApps)` (design D4).
- [x] 2.5 In `ConsentScreen.tsx` render the primary and plain buttons from `consentControls(mode, consentOn)` through a local `ConsentAction` component that receives the control with `onAgree`, `onClose` and `onTurnOff` as JSX props (design D6), keep the existing button styles for primary and plain, and call `useSystemBack(onClose)`. `ConsentScreenForShell` in `LauncherRoot.tsx` needs no change: review mode's `onClose` is already `onConsentReviewClose`.
- [x] 2.6 In `flow-chrome.tsx` raise `FlowHeader`'s `Back` `hitSlop` from 10 to 16 (design D11). No style change.

## 3. Error-screen exit, bottom inset, and the exit check

- [x] 3.1 In `ScreenErrorFallback.tsx` render, only when `onLeave` is given, a child component that calls `useSystemBack(onLeave)` and shows a plain `COPY.screenErrorBack` action bound `onPress={onLeave}` under `Try again`, tokens only. In `LauncherRoot.tsx` read `const exit = SCREEN_EXITS[screen.kind]` and pass `onLeave={exit.back === 'root' ? undefined : goHome}` to `ScreenBoundary` (design D7).
- [x] 3.2 In `LauncherRoot.tsx` give the root `SafeAreaView` `edges={frameEdgesFor(screen.kind)}` (design D10). Change nothing in `Orb.tsx`, `SheetModal.tsx`, `RunDetailsSheet.tsx` or `DevProbeScreen.tsx`.
- [x] 3.3 Add the scanner to `test/screen-exits.suite.ts` per design D8: a pure `scanScreenExits(files: Record<string, string>): { rule: 'seam' | 'declared' | 'bound' | 'labelled' | 'real-kinds'; file: string }[]` and a loader over the top-level non-test `.ts`/`.tsx` files of `src/host/launcher/` (read with `fs`, never imported). Cases: the real tree returns `[]`; one inline fixture per rule returns exactly that violation and file. The "bound" fixture is a plan step that passes `handleBack` to `useSystemBack` but `onBack` to `FlowHeader`; the "seam" fixture is a new screen calling `BackHandler.addEventListener`; the "declared" fixture is a file calling `useSystemBack` that no row names; the "labelled" fixture drops `COPY.failureBack` from a failure-screen source; the "real kinds" fixture is a table key with no `kind:` literal.
- [x] 3.4 Red-check and record both in the chain report. Typecheck coverage: add `| { kind: 'about' }` to `Screen` in the worktree, confirm `npx tsc --noEmit` fails at the `SCREEN_EXITS[screen.kind]` read, and revert. Behavior: revert only `PlanStep.tsx` to pass `onBack` to `FlowHeader` and confirm the real-tree case turns red with a "bound" violation on `PlanStep.tsx`, then restore.
- [x] 3.5 Append one entry to `docs/decisions.md` (read the tail for the next number; `store-launch-compliance` chain-7 appends just before): launcher screens are left through visible controls on both platforms, system back shares their handler through `useSystemBack`, no edge swipe or navigation library until a native stack exists, the orb's `Home` is the iOS exit from a mini-app, and the exit table is gate-enforced. One line of why each. Apply the `unslop` skill.

## 4. Attended device walk (orchestrator with the owner; not dispatchable)

- [ ] 4.1 iPhone, release build from the staging tip (the `platform-release-readiness` chain-12 recipe; the same session is fine). Using only taps, leave each of these and confirm where it lands: Settings (chevron → Home); AI features review with consent off (`Not now` → Settings, still off); review with consent on (`Keep AI features on` → Settings); History (chevron → Home) and its confirm sheet (`Cancel`) and report sheet (`Cancel`); consent ask from the composer (`Not now` → Home); compose, clarify and plan (`Back` each step, text and answers kept); plan while editing a row (`Back` cancels the edit and stays); build (`Leave it running` → Home with a building ghost) and its details sheet (`Close`); done (`Report this app` → `Cancel`, then `Back to your apps`); failure, reached by force-quitting during a build and tapping the interrupted ghost (`Back to your apps`, ghost kept); a running mini-app (orb → `Home`) and its report sheet (`Cancel`); tile long-press sheet (`Cancel`) and `App link` sheet (`Done`); link-missing, only if association files are live (else record N/A and why). Screenshot the build, failure, consent and compose screens and confirm every bottom control sits fully above the home indicator and the orb didn't move. If `launcher-polish-fixes` has merged, also confirm the ⚙ renders monochrome and VoiceOver reads a tile as its name once.
- [ ] 4.2 Android emulator, `npm run android:release`. Repeat 4.1 by tapping, then again with system back on each screen, which must land exactly where the tapped control did. The done step now returns Home and Whim stays in the foreground; back mid-row-edit on plan cancels the edit. Screenshot the same four screens and compare bottom spacing with the previous build.
- [ ] 4.3 Record each row's result and the screenshots' paths in `progress.md`. Any failure becomes a finding in a fix batch, never an inline edit.
