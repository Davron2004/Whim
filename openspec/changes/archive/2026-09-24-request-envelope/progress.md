# Progress ledger: request-envelope

- run-start: staging `integration/request-envelope`, MAIN_TIP `6d16899cc3e4290f58f70b2b99c1a40ed10372dd` (2026-09-23)
- dispatched chain-1: BASE `6d16899cc3e4290f58f70b2b99c1a40ed10372dd`, worktree `.claude/worktrees/request-envelope-1`, branch `chain/request-envelope-1` (@whim symlinks pre-created)
- dispatched chain-3: BASE `6d16899cc3e4290f58f70b2b99c1a40ed10372dd`, worktree `.claude/worktrees/request-envelope-3`, branch `chain/request-envelope-3`
- chain-1 report: STATUS blocked, class B. Adding `update_required`/`consent_required` to `ServiceRefusalCode` breaks the phone's exhaustive `REFUSAL_RULES` (src/) and `refusal-landing.suite`'s code lists. Adjudicated (SendMessage revision 1/2): chain-1 may add both codes as interim `{landing:'sender', tone:'neutral'}` rules and add them to `SENDER_LANDING_CODES`; chain-4 revises. Also told it the spec's "every log line" wins over 1.3's "terminal line" wording: step-level pipeline lines carry `requestId` too.
- chain-3 report: STATUS complete, GATE PASS, commit `b0ebc63`. Tasks 3.1–3.2 done; 3.3 DEFERRED (Metro can't bundle from a worktree: `Unable to resolve module @babel/runtime/helpers/interopRequireDefault`), to run from the main tree post-merge. Class-A deviations: iOS registers via `RCT_EXPORT_MODULE(WhimAppInfo)` instead of a `codegenConfig.ios.modulesProvider` entry (package.json frozen), runtime lookup unverified until an iOS run; build passed as raw OS string, validated in `app-info.ts`; Kotlin module in `com.whim.tone` (codegen package); offline build is `:app:assembleOffline -PwhimBuildNumber=N`. iOS simulator Release compile: BUILD SUCCEEDED.
- chain-3 integrity: exit 0 (11 files). Merged; regate-pass. Worktree and branch removed.
- follow-up for the end fix chain: `app-info.suite.ts` passes value-returning lambdas to `h.throws` (typed `() => void | Promise<void>`); the dir is tsc-excluded so the gate is blind to it, but the IDE flags 8 errors.
- task 3.3 (run from the main tree by a verifier, temporary `App.tsx` probe reverted):
  - Android: `:app:assembleOffline -PwhimBuildNumber=381907`; bundle grep `WHIM_APP_INFO_PROBE` = 1; badging `package: name='com.anycognition.whim' versionCode='381907' versionName='1.0.0'`; logcat `ReactNativeJS: WHIM_APP_INFO_PROBE {"platform":"android","version":"1.0.0","build":381907}`. PASS. Launcher home renders normally on emulator-5554.
  - iOS simulator (extra, de-risks 6.2): Release sim build, iPhone 17; Info.plist `CFBundleShortVersionString=1.0.0`, `CFBundleVersion=1` (local builds get no fastlane build number); unified log `[com.facebook.react.log:javascript] WHIM_APP_INFO_PROBE {"platform":"ios","version":"1.0.0","build":1}`. PASS: `RCT_EXPORT_MODULE(WhimAppInfo)` resolves under bridgeless without a `modulesProvider` entry. JS logs on the sim only show with `log show --info` (subsystem `com.facebook.react.log`, category `javascript`).
- chain-1 report (after revision 1): STATUS complete, GATE PASS, commit `a117c42`, 6/6 tasks. Class-A: pipeline logs go through a per-run `state.log` from `RunTrace.requestId` (widened `logStage`, `throwLoggedModelCallFailure`, `RunBudget`); consent static check lives in `server/test/request-edge.suite.ts` (server-only concern); wire-v2 "gate mounted once" test now asserts prefix mounting (three `/v1/*` middlewares); legacy `appVersion: 'unknown'`; drivers use `server/src/bench-envelope.ts` (imports `scripts/release/lib/build-number.ts`; drivers only, not the production entry). Red-checks against weaker variants reported. Spec gap reported honestly: OpenRouter "model call", content-policy "check" and cost-resolver lines still lack `requestId` → new chain-1b.
- chain-1 integrity: exit 0 (28 files). Merged; regate-pass. Worktree and branch removed.
- tripwire candidate: none yet (the refusal-code/phone-rule split is a planning lesson, not a repeated implementer deviation).
- chains.md amended: chain-4 now writes handoff/refusal-routing.md (read by chain-5); new chain-1b after chain-2.
- dispatched chain-2: BASE `b115224dc7c52bb1c64e3cbd1766097e3e4f5a99`, worktree `.claude/worktrees/request-envelope-2` (@whim symlinks pre-created)
- dispatched chain-4: BASE `b115224dc7c52bb1c64e3cbd1766097e3e4f5a99`, worktree `.claude/worktrees/request-envelope-4` (@whim symlinks pre-created)
- chain-6 timing note: a legacy client counts as build 0 on both platforms, so raising EITHER minimum above 0 refuses the demo phone's TestFlight 381237. 6.2's temporary raise must happen after the 2026-09-24 evening demo, or with the owner's explicit OK.
- chain-2 report: STATUS complete, GATE PASS, commit `a33cf5a`, 4/4 tasks. `server/src/min-build.ts` (`minimumBuildGate`) at chain-1's mount point; legacy refused once either minimum > 0; strict `^(0|[1-9]\d{0,14})$` parse with `ServerConfigError`, mirrored in `deploy.sh` preflight (8-value agreement test); `/healthz` = `{"ok":true,"service":"whim-server","minBuild":{"ios":N,"android":N}}`; `smoke.sh` builds its expected body from the operator values. Class-A: deploy-config suite refuses `WHIM_MIN_BUILD_*` in capacity profiles; smoke.sh verified by curl-stubbed test (https-only); load-test server's `/healthz` untouched. Red-checked against 4 weaker variants. Unverified: `docker compose up` recreating the container on a `config.env`-only change (smoke catches it; chain-6 sees it live).
- chain-2 integrity: exit 0 (13 files). Merged; regate-pass. Worktree and branch removed.
- dispatched chain-1b: BASE `05ed9e8da3fc1d89743aec35c3d276da4abba005`, worktree `.claude/worktrees/request-envelope-1b` (@whim symlinks pre-created)
- chain-4 report: STATUS complete, GATE PASS (+ knip clean), commit `5e0b03e`, 3/3 tasks + `handoff/refusal-routing.md`. Class-A: `ClientOptions` carries `appInfo: () => AppInfo` read per request (not at options creation, so a missing native module fails one request instead of crashing the shell render); new `GenerationClientErrorKind` `'client'` (nothing sent, never a partial envelope); `ConsentStatus.granted` carries `version`; `LauncherRoot` optional `appInfo` prop (tests pass `testAppInfo`); copy key `permissionRequiredLine` (web-site suite treats `consent*` keys as privacy-page disclosure); detached-build `consent_required` opens no screen, Retry leads to consent; agreeing on a refusal-opened consent screen returns to the sending step intact, no auto-resend. Header lockstep check at `checks/test/repo/header-lockstep.suite.ts`, red-checked. Six weaker variants red-checked.
- chain-4 integrity: exit 0. Merged; regate-pass. Worktree and branch removed.
- memory candidate (chain-4): launcher Node suites are tsc-excluded, so a fixture missing a new required field fails only at runtime; `consent*` copy keys must appear verbatim in privacy.html unless allowlisted.
- dispatched chain-5: BASE `8697e6ec1246e03de49a10e2d8bd7951812675d4`, worktree `.claude/worktrees/request-envelope-5` (@whim symlinks pre-created)
- merged main (`f808b97`, #76 legal-surface-v2 proposal) into staging: clean, no overlapping paths; regate-pass. Chains 1b and 5 keep their BASEs as staging ancestors.
- chain-1b report: STATUS complete, GATE PASS, commit `0119524`. `ModelRequest.logger` threaded through OpenRouter, the policy check (and cache), the summariser, and clarify/rewrite/pipeline model turns (one pattern). Class-A: no per-request cost-resolver log line exists (`resolveRequestUsage` logs nothing); the background `cost resolution sweep` spans many requests, left on the module logger. Four red-checks (dropped logger at each of four call sites) failed by name.
- chain-1b integrity: exit 0 (11 files). Merged; regate-pass. Worktree and branch removed. `handoff/envelope.md` updated (the "not yet carrying it" line).
- chain-5 report: STATUS complete, GATE PASS (+ knip clean), commit `5ae1bf9`, 4/4 tasks. New `update-gate.ts`, `UpdateRequiredScreen.tsx` (copies `AppLinkMissingScreen` layout, ConsentScreen buttons). Class-A: "Not now"/system back go Home carrying the typed prompt in memory (`heldPrompt`, per app, not persisted); launch check and running-build refusals open the screen only over Home/compose, else the ghost keeps the reason; store link picked by `Platform.OS`; `renderScreenContent` complexity refactor; lockstep check `STORE_IDS.playPackage` == `WHIM_APP_ID`. "Next AI action shows it again" comes from the server's 426 (no remembered verdict). 25 new cases; 8 weaker variants red-checked.
- chain-5 integrity: exit 0. Merged; regate-pass. Worktree and branch removed. All dispatchable chains merged (1, 1b, 2, 3, 4, 5); chain-6 is attended.
- gate-full on staging tip `2491602`: FULL GATE PASSED (knip, guard:metro, invariants + negative controls, bridge-invariants, synthetic-run, generation-e2e, deliver-by-source, codex-sync, openspec 45/45). Reviewer dispatched on `main...integration/request-envelope`; visual device check of the update screen dispatched in parallel.
- visual device check (update screen forced via a temporary initial-state edit, reverted): Android offline APK (bundle grep "Update Whim" = 1) and iOS sim Release both render "Whim needs an update" + body + "Update Whim" + "Not now" with no clipping or safe-area overlap; dispatcher reviewed the screenshots. Android: "Update Whim" opened Google Play (`com.android.vending`); "Not now" landed on Home with example tiles. iOS taps not driven (no tapper). Light-only rendering matches the rest of the launcher (no dark-mode support anywhere in `src/host/launcher`).
- reviewer verdict: FINDINGS (none demo-blocking). H1 rollback to a pre-change image fails smoke on the `/healthz` body; M1 launch check doesn't gate typing (spec scenario "before the user can type"); M2 new type errors in tsc-excluded suites; L1 duplicate `scope` key on model-call lines; L2 wire-v2 assertion weakened to `> 0`; L3 phone accepts versions the server rejects; L4 in-memory ledger overwrites duplicate ids; L5 rewrite model-call requestId untested; L6 report-exempt ledger/log writes undocumented; L7 hand-written fixtures + duplicated test setup; L8 layering OK. Report mismatches: 5.3 ticked vs its wording; chain-1's wire-v2 claim.
- adjudication: M1 resolved by amending the scenario, task 5.3 and one proposal phrase to the implemented behaviour (update screen replaces home/compose once the check reads the minimum; the 426 covers earlier sends), not by blocking input on a network probe. Surfaced to the owner at closure. H1, M2, L1–L7 → fix chains 7a (server/deploy) and 7b (phone/contract/checks).
- dispatched chain-7a: BASE `f956c544028cc1b4a9092e2da4ceb2eba847b850`, worktree `.claude/worktrees/request-envelope-7a`
- dispatched chain-7b: BASE `f956c544028cc1b4a9092e2da4ceb2eba847b850`, worktree `.claude/worktrees/request-envelope-7b`
- filed #79 (ai-proposed): Node acceptance suites are tsc-excluded, so stale fixtures fail only at runtime (M2's root cause).
- harness feedback: orchestrator + 9 agent debriefs + README tally committed under `docs/harness-feedback/` (owner request); chains 7a/7b debrief when they finish.
- chain-7b report: STATUS complete, GATE PASS, commit `8392fe9`. M2: 13 targeted type errors gone (scratch tsc 51→38; the rest pre-existing env/RN noise). L3: `APP_VERSION_PATTERN` exported by the contract, phone copy in `app-info.ts`, lockstep + agreement check. L7: refusal fixtures from the server's builders (`/healthz` stays hand-written; inline in `createApp`). M1: renamed test + slow-/healthz and send-before-check cases. Red-checks reported per fix.
- chain-7b integrity: exit 0 (9 files). Merged; regate-pass.
- chain-7a report: STATUS complete, GATE PASS, commit `4a42cba`. H1: smoke checks `/healthz` structurally (absent `minBuild` passes with WARN only when both minimums are 0); `deploy.sh --tag` rollback tests for both cases; rollback docs. L1: request-only logger to model calls, raw-line single-scope tests. L2: exact 4 prefix middlewares. L4: in-memory ledger throws `UNIQUE constraint failed: requests.id`. L5: rewrite case. L6: comment. L7: `forbiddenModelApp` helper. Red-checks reported per fix.
- chain-7a integrity: exit 0 (11 files). Merged; regate-pass. Handoffs updated (min-build smoke wording, app-info version row). Worktrees and branches for 7a/7b removed.
- closure: ruleset probe OK; pushed `integration/request-envelope`; draft PR #80; checks SETTLED PASS (isolation-suite, quality-gate); Sonar round 1: gate OK, 4 issues (`findings-sonar-1.md`: S1 smoke.sh:150 S7688, S2 update-gate.ts:14 S6551, S3 app-info.ts:58 S7786, S4 rewrite.ts:102 S107).
- sonar round 1 dispatched as ONE fix-worker in `.claude/worktrees/request-envelope-sonar1` (BASE `4a892b6d8a367a305674e1a45d65fcabd1541ed4`), condensed from the per-finding fix-loop (owner's deploy-tonight timeline; four mechanical nits): integrity with allowlist, gate-full, Sonar re-analysis remain the checks.
- chain-6 prep (2026-09-23 23:51 EDT): production runs `server:a9b03c47eef3ae33948aedf9ec38c2c56fe2147f` (whim-whim-server-1, Up 12 hours, healthy). Rollback target: `deploy/deploy.sh --tag a9b03c47eef3ae33948aedf9ec38c2c56fe2147f`. Owner chose to run chain-6 tonight.
- sonar round 1 fix: commit `b45afc4` (S1 `[[`; S2 formatter without `String()` on non-Errors; S3 `TypeError` for the two shape checks, messages unchanged; S4 `RewriteRetryContext` groups the five request-wide deps). Integrity exit 0 with allowlist (4 files). Merged; gate-full PASSED on the merged tip.
- owner review of S4: `rewriteWithRetry` had 7 params on main; chain-1b's `requestLog` made 8. The grouping is behaviour-neutral but cosmetic (`applyRewriteAttempt` has 11, `runClarifyWork` 9, both over the limit on main). Proper refactor filed as #81 (ai-proposed).
- cleanup lane: pinned TARGET_SHA `b9d6c57`, TARGET_TREE `d3ff940`, BEFORE_COUNT 316 (39 over main); grant minted; backup `backup/pre-cleanup-integration-request-envelope`; lane worktree created.
- PR #80 merged by owner (rebase-merge, main 672e4e4, 2026-09-24 04:37 UTC); tree identical to cleaned tip d3ff940; staging, backup and cleanup branches deleted (remote auto-deleted by GitHub).
- chain-6 deploy (owner go): deploy/deploy.sh from main 672e4e4, both minimums 0 → exit 0, built-in smoke all passed, "done". Standalone deploy/smoke.sh: all checks passed; /healthz = {"ok":true,"service":"whim-server","minBuild":{"ios":0,"android":0}}. Includes f542669 (OpenRouter data_collection deny) and 5b04360. Rollback target recorded: a9b03c47eef3ae33948aedf9ec38c2c56fe2147f.
- 6.1 (owner: emulator stands in for the real device; owner checks 381237 later): one real generation via flowbench tip-splitter-p1: clarify 200/2983 ms, rewrite 200/2578 ms, generate 200/21079 ms, outcome result; lines carry android/1.0.0/383324/consent 1 + requestId, no device id, one `scope` key.
  - Emulator LEGACY APK (f808b97, pre-envelope; stands in for 381237): consent → clarify → plan → build → Tip Splitter works. Server lines 00:48–00:49: clarify/rewrite/generate 200, `platform: unknown, appVersion: unknown, build: 0, consent: 1`, each with a requestId.
  - Emulator NEW APK (672e4e4, build 382100) installed over it: data + consent grant survived, no update screen at minimum 0, no consent re-ask; plan → build → Water Counter works. Lines 00:53–00:54: `android, 1.0.0, 382100, consent 1`.
  - Smoke's no-device-header probes: 400 with requestId.
  - Side findings: orb covers the bottom of generated mini-apps → #82 (ai-proposed); plan-screen footer "clipping" was scroll position, not a bug (button is a flex sibling).
- 6.2 raise: `WHIM_MIN_BUILD_ANDROID=382101 deploy/deploy.sh` from the deploy worktree (main 672e4e4, image already built) → exit 0; smoke passed incl. /healthz = {"ok":true,"service":"whim-server","minBuild":{"ios":0,"android":382101}} → config.env-only change DID recreate the container (resolves chain-2's unverified note). Emulator (build 382100) check dispatched.
- 6.2 emulator (build 382100 vs Android min 382101): cold start → update screen with no /v1 request (server lines: none at launch); "Not now" → home, Water Counter opens and runs; clarify → `426 update_required` (build 382100, requestId) → update screen; typed prompt "a note taking app" kept across the round trip; "Update Whim" → Google Play (`com.android.vending`). Minimum lowered back: plain deploy → /healthz minBuild {0,0}, smoke passed, done. iOS half of 6.2 (new iOS build's envelope) deferred to the owner's device check.

## Closing summary

- Chains: 1, 1b (added at dispatch: request id on model/policy lines), 2, 3, 4, 5, then review-fix chains 7a (server/deploy) and 7b (phone/contract), then one Sonar round (4 issues, one fix-worker). Chain-6 ran attended on production with the emulator.
- Redispatches: 0. SendMessage revisions: 1 (chain-1, class B: a refusal code and its phone rule split across chains).
- Deviations: class A across chains, logged per chain above. Class B: 1. Class C: 0. Regate failures: 0. Integrity failures: 0.
- Reviewer: FINDINGS. 1 high (a rollback smoke false-fail), 2 medium (the launch-check wording, amended in the spec; 13 type errors in tsc-excluded suites), 8 low. All fixed in 7a/7b except the scenario wording, which was amended.
- Issues filed: #79 (tsc-excluded suites), #81 (9–11 argument helpers), #82 (the orb covers the mini-app bottom), #83 (log lines over 16 KiB).
- Harness feedback: `docs/harness-feedback/2026-09-23-request-envelope/` (86 entries from 12 authors).
- Production: `672e4e4` deployed 2026-09-24 with both minimums at 0. The minimum-build gate was proven live by a raise to 382101 and back.
