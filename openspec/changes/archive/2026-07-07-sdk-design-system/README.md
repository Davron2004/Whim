# sdk-design-system

A user-customizable theme (six presets, accent + shape knobs) spanning the launcher shell and every mini-app, plus the full SDK component kit (forms, toggles, lists, cards, modal, progress) and a seeded gallery fixture — roadmap change #3 with the launcher half that makes it visible.
