# Progress ledger: eval-harness

## Run metadata
- run-start: 2026-07-30 (overnight orchestrator, attended=false); staging branch v1-sprint (sprint-wide, see synthetic-run-harness/progress.md)
- chain DAG: A → {B → C, D} → E → F; G (class2-bootstrap-record) floats, HUMAN-BOOTSTRAP-adjacent (record-only)
- holdout eval set is USER-HELD: never sought, never created; loader refuses without --eval-set/WHIM_EVAL_SET (locked, decision #42)
- chain-B depends on synthetic-run-harness interfaces via the single adapter; dispatch chain-B only after synthetic-run-harness chains merge far enough to expose its report shape (or per handoff/harness-core.md + observe-api.md)

## Dispositions (append-only)
- chain-A (contract-loader-corpus) DISPATCHED: BASE=ffef2db, worktree=.claude/worktrees/eval-harness-A, branch=chain/eval-harness-A, implementer=sonnet; brief includes holdout constraints + repo gotchas.
- chain-A REPORT: complete 6/6, GATE PASS + evals suite 39/39 (run directly; gate wiring is chain-G/Class-2), commit 1af7264, contract handoff/eval-contract.md (120 lines, at cap). Deviations (A): file layout + type field shapes designed by implementer and pinned in contract; sonarjs/no-empty-test-file needs per-file configuration comment (documented in contract); corpus slugs chosen + Slug column added to docs/app-corpus.md Tier-0 rows (design D11).
- chain-A INTEGRITY: exit 0, scope as declared. MERGED onto v1-sprint, tasks 1.1-1.6 ticked, REGATE PASS.
- chain-D (tier-c-judge-and-rubric) DISPATCHED next (needs only chain-A); chain-B HELD until synthetic-run-harness chain-2 (observe-api contract) merges.
- chain-D REPORT: complete 5/5, GATE PASS + evals 69/69, commit 2c72319, contract handoff/judge.md (93 lines). Deviations: B-resolved-in-scope — live judge cannot statically import server/src (RN vs Node tsconfig global conflict, even import type); construction-gating needs no import; score() shells to esbuild CLI + dynamic import (documented in judge.md closing section); A — rubric criteria originated (intent-fidelity/usability/robustness/polish, 1-5) per D8.
- chain-D INTEGRITY: exit 0. MERGED onto v1-sprint, tasks 4.1-4.5 ticked, REGATE PASS, evals suite green on merged tip.
- chains B/C/E/F: HELD pending synthetic-run-harness chain-5 (run-report shape) per DAG + adapter design.
- chain-B REPORT: complete 5/5, GATE PASS + evals 82/82, commit 46b4551, contract handoff/run-observation.md (73 lines). Deviations (A): run.mjs external:['typescript'] (pre-authorized); missing RuntimeDiagnostic.line defaults to 0 sentinel (documented); computeCaseVerdict excludes TierCResult by type signature ("Tier C never gates" compiler-enforced).
- chain-B INTEGRITY: exit 0. MERGED onto v1-sprint, tasks 2.1-2.5 ticked, REGATE PASS, evals green on merged tip.
- chain-C (tier-b-assertions) DISPATCHED next (after B).
- INTERRUPTION (monthly API spend limit): chain-C implementer killed just after start; no work lost, worktree+branch cleaned. Resume: dispatch chain-C fresh (block scratchpad/chain-blocks/eval-C.md or re-assemble), then E→F→G per chains.md. See HANDOFF-v1-sprint.md.
- RESUME RUN (2026-07-31, attended): chain-C (tier-b-assertions) DISPATCHED fresh: BASE=336daa7, worktree=.claude/worktrees/eval-harness-C, branch=chain/eval-harness-C, implementer=opus; block=scratchpad/chain-blocks/eval-C.md; @whim symlinks + npm run build pre-created by dispatcher.
- chain-G (class2-bootstrap-record, RECORD-ONLY) DISPATCHED in parallel: BASE=336daa7, worktree=.claude/worktrees/eval-harness-G, branch=chain/eval-harness-G, implementer=sonnet; writes openspec/changes/eval-harness/pending-class2.md ONLY, applies nothing (dependency-free, disjoint from chain-C's scope).
- DISPATCHER CONSTRAINT (recorded 2026-07-31, resume run): eval-harness chain-F and generation-loop chain-7 (task 7.7) both append to docs/capabilities.md. Serialize the pair — the second is cut from the staging tip only after the first merges. See generation-loop/progress.md for the same note.
- chain-G REPORT: complete 1/1, GATE PASS, commit 1a05cd2. Records exact before/after context for all four Class-2 edits (package.json scripts after checks:test; tsconfig.json exclude after checks/test; scripts/gate.sh `check "corpus-eval" npm run -s evals:test` after static-checks and before sonar ingestion; knip.json "." entry += evals/test/** + evals/cli.mjs, project += evals/**/*.ts), plus apply-order dependency (a before c) and partial-application consequences. No deviations, no contract.
- chain-G INTEGRITY: exit 0, sole changed file = openspec/changes/eval-harness/pending-class2.md (verified against BASE 336daa7, no protected file touched). MERGED onto v1-sprint --no-ff, task 7.1 ticked, REGATE PASS (fast gate green on merged tip). NOTE: the four edits remain UNAPPLIED and human-only.
- chain-C REPORT: complete 5/5, GATE PASS, evals 118/118, commit 32d312c, contract none (as declared). Non-vacuity EVIDENCED, not asserted: implementer temporarily broke `screen-reachable` and `storage-roundtrip`, confirmed the paired red fixtures genuinely failed, restored byte-identical and re-ran green.
- chain-C DEVIATION (A, adjudicated ACCEPT — but a real task-text divergence, flagged for the reviewer): task 3.3 says "Distinguish required from advisory assertions, since only required ones gate the verdict." No such distinction was implemented. Chain-A's merged `evals/contract.ts` carries no `required`/`advisory` field on `EvalAssertion`/`TierBAssertionResult`, and chain-B's merged `evals/tiers/case.ts` already gates on `.every(...)` over all evaluated assertions — both out of chain-C's file scope. DISPATCHER RULING: accept. The distinction is vacuous today because `ASSERTION_KINDS` is a closed vocabulary with no advisory kind, so no corpus spec can express one; and treating every assertion as required is the strictly SAFER default (an advisory assertion that silently fails to gate is a weaker guarantee than one that does). Implementer documented the choice in both assertions.ts and tier-b.ts. Task 3.3 is ticked with this divergence on the record — if an advisory field ever becomes load-bearing it is a contract change to `evals/contract.ts` + `case.ts`, not a chain-C fix.
- chain-C DEVIATION (A): `evals/eval-set.ts` validates an assertion's `target` type but not its per-kind presence (chain-A scope). `evals/assertions.ts` handles a missing `target` defensively — returns a `fail` naming the defect rather than throwing — so a malformed corpus spec surfaces as a red assertion, not a crashed tier.
- chain-C INTEGRITY: exit 0 vs BASE 336daa7, scope exactly as declared (evals/assertions.ts, evals/tiers/tier-b.ts, evals/test/tier-b.test.ts, evals/sets/visible/manifest.json). MERGED onto v1-sprint --no-ff, tasks 3.1-3.5 ticked, REGATE PASS + evals 118/118 re-run on the merged tip (the evals suite is NOT in gate.sh until chain-G's Class-2 lines are human-applied, so the dispatcher runs it by hand at every merge).
- chain-E REPORT: complete 5/5, GATE PASS, evals 163/163 (baseline 118 + 45 new checks), commit 09aaa67, contract handoff/report.md (105 lines, under cap). Deviations: none.
- chain-E REDACTION PROPERTY (the load-bearing one, D3) — structural, not incidental: `buildCaseResult` in serialize.ts is the ONLY constructor of a CaseResult and unconditionally routes caseId/prompt/expectation/candidateSource through redact.ts's redactCase(..., visibility). summary.ts / diff.ts / compare.ts never receive raw case inputs and reference none of those fields — they read only already-built EvalRunReport/CaseResult. So no in-memory unredacted object survives buildReport for a future output path to leak.
- chain-E NON-VACUITY EVIDENCED (real edit-run-revert, not a claim): implementer changed buildCaseResult to call redactCase(..., 'visible') unconditionally, reran the suite, got 156 passed / 7 FAILED — the key-shape check plus 6 "no output surface contains <secret>" checks across serializeReport/renderSummary/diff went red with the leaked strings printed in the failure detail — then reverted and confirmed 163/0 restored.
- chain-E INTEGRITY: exit 0 vs BASE e219ad5, scope exactly as declared (4 report modules + report.test.ts + handoff/report.md). MERGED onto v1-sprint --no-ff, tasks 5.1-5.5 ticked, REGATE PASS + evals 163/163 re-run on the merged tip.
- chain-E NOTE FORWARD (for chain-F): compare.ts's alarm is strictly `divergence > threshold` (exact-threshold stays quiet, per spec "no more than the threshold"). The CLI's file-write (default output dir, no-overwrite-of-a-tracked-file) and argument parsing do NOT exist yet — explicitly out of 5.1-5.5 and recorded in handoff/report.md's closing section so chain-F does not assume them.
- chain-F REPORT: complete 5/5 (6.1-6.5), GATE PASS, evals 196/196 (baseline 163 + 33 new in cli.test.ts), commit aac747e, contract none. docs/capabilities.md verified by dispatcher as a PURE single-row append (git diff --stat = 1 insertion) so it cannot conflict with generation-loop chain-7's own append.
- chain-F SELF-CAUGHT BUG (good sign, recorded): its --out guard originally checked only the exact output FILE path, so a test write actually landed inside a git-tracked directory. Implementer noticed the stray artifact, widened the guard to refuse the whole target DIRECTORY when it holds tracked content, re-verified, and removed the artifact before committing.
- chain-F CLI HAND-EXERCISE (observed, not claimed): (1) no --eval-set/WHIM_EVAL_SET => refuses, exit 2, no Chromium; (2) real run vs the committed visible set under real Chromium => tip-splitter-p1 genuinely passes Tier A 1/1 + Tier B 1/1, other 21 recorded as missing-source sourcing errors excluded from `cases`, exit 1; (3) --generate against the real createStubPipeline => sourced, ran real Chromium, correctly failed on genuine manifest_not_static diagnostics (proves real wiring, not simulation); (4) --out into a tracked dir => refused, exit 2.
- chain-F deviations, all Class A: CandidatePipeline declared locally (cross-tsconfig boundary rule); cli.mjs orchestration lives in an embedded TS facade string bundled via esbuild at runtime (mirrors server/dev.mjs, evals/test/run.mjs, evals/judge/live.ts) because the file scope was exactly three files; diff exit codes (0 clean / 1 regression / 2 unreadable) and the tracked-DIRECTORY write refusal chosen by the implementer as handoff/report.md explicitly left them to chain-F; Tier C always no_judge_configured in the CLI (no --judge flag was ever asked for; wiring judges would be scope creep).
- chain-F INTEGRITY: exit 0 vs BASE 9c13632, 7 files, scope as declared. MERGED onto v1-sprint --no-ff, tasks 6.1-6.5 ticked, REGATE PASS + evals 196/196. **eval-harness tasks.md is now 0 unticked.**
- DISPATCHER FIX (not a chain — a defect the dispatcher found reviewing chain-F's report): chain-F's gate-configuration check asserted `!gateContents.includes('"corpus-eval"')`, i.e. that the suite entry is ABSENT. Correct today, but pending-class2.md(c) records that exact line for a human to apply, and the handoff instructs them to apply it — so the check would have gone RED the moment the human did what they were told, at the worst possible moment (that same line is what first makes the evals suite run INSIDE the gate, so their first gated run of the suite would fail, reading as "I wired it up and it immediately broke"), with the remedy being a hand-edit of a test mid-closure. Chain-F's choice was the better of the two options it could see — asserting a not-yet-true scenario would have been worse — the trap is only visible from the dispatcher's vantage.
- FIX SHIPPED as branch fix/gate-config-test-self-healing, commit d08f13c, integrity exit 0 (sole file evals/test/cli.test.ts), merged --no-ff, REGATE PASS + evals 196/196 (count unchanged — one predicate swapped in place). The check is now TRI-STATE and self-healing: (1) gate.sh contains a WELL-FORMED `check "corpus-eval" npm run -s evals:test` => PASS; (2) gate.sh lacks it AND pending-class2.md still records the obligation => PASS (today's true pre-bootstrap state); (3) gate.sh lacks it AND the record is gone => FAIL, obligation silently dropped. A `"corpus-eval"` marker present but MALFORMED also fails, so a botched application is caught rather than waved through. All four states were exercised against synthetic string inputs in a scratch script (deleted, never committed) and observed to behave as specified. Net: green in both legitimate states, red only in the illegitimate one, and it now covers a case nothing previously did (obligation deleted without ever wiring the gate).

## Reviewer pass (2026-07-31, resume run) — eval-harness
- REVIEWER VERDICT: findings. Report honesty: **discrepancies found** (see finding 2) — everything else in this ledger cross-checked clean against the diff (per-chain task counts, suite counts, the self-healing gate-config fix, the Class-2 record, docs/app-corpus.md's slug column), with 196/196 and tsc --noEmit independently re-confirmed.
- **DISPATCHER CORRECTION to this ledger's own chain-E entry.** That entry claimed "no in-memory unredacted object survives buildReport for a future output path to leak." That is TRUE ONLY FOR THE `case` FIELD and FALSE for the tier results. The dispatcher recorded chain-E's structural claim without checking its coverage, and repeated it to the user. Corrected here rather than left standing.
- **FINDING 1 — HIGH, CONFIRMED (dispatcher read serialize.ts + redact.ts directly): tier results bypass redaction entirely.** buildCaseResult routes only `case` through redactCase and copies tierA/tierB/tierC through VERBATIM; redactCase only touches caseId/prompt/expectation/candidateSource. TierAResult.diagnostics carries Diagnostic.message/hint/symbol from runStaticChecks — checks/contract.ts documents `symbol` as "the offending identifier/specifier/field name", and passes like import-allowlist.ts interpolate the raw specifier into the message. Reviewer reproduced it concretely: a candidate importing 'THE-SECRET-PROMPT-TOKEN-xyz789-not-a-real-module' yields a diagnostic containing that token verbatim in message/hint/symbol. A holdout candidate is LLM-GENERATED FROM THE SECRET PROMPT and models routinely echo prompt wording into identifiers/strings/comments, and Tier A ALWAYS runs — so holdout prompt-derived content reaches the emitted "redacted" report. Violates the spec's "SHALL NOT contain prompt text, expectation prose, or generated candidate source" and decision #42.
- SAME ROOT CAUSE, two further surfaces: TierBAssertionResult.observed (chain-C deliberately carries "the concrete observed value", which is candidate-derived) and TierCResult -> JudgeVerdict.criteria[].rationale (free judge prose; unreachable TODAY only because the CLI always passes judge: undefined, but docs/evals.md's holdout protocol explicitly anticipates wiring a real judge — a latent trap, being fixed now rather than left).
- WHY THE TESTS MISSED IT: report.test.ts's redaction tests are non-vacuous for the field they cover but blind to this channel — the SECRET_CANDIDATE_SOURCE fixture only ever lands in CaseInput.candidateSource, which IS redacted. Chain-E's edit-run-revert red-check was genuine and still only exercised the covered channel. A red-check proves a test can fail; it does not prove the test's INPUT SURFACE is complete.
- **FINDING 2 — MEDIUM, CONFIRMED, and it was UNDISCLOSED in this ledger:** evals/cli.mjs:65's embedded facade imports SynthRunSession/createRunCandidate directly from '../synthrun', making it a SECOND evals/ module importing the harness — contradicting design D6 and the explicit "the ONLY module ..." claims asserted in evals/adapters/synthetic-run.ts:2 and handoff/run-observation.md:15. The specific hazard D6 guards (harness field names spreading into the tiers) did NOT materialize — the facade passes the report opaquely into observationFromRunReport — but a false invariant asserted in a code comment is worse than either being true or being honestly documented.
- ROOT CAUSE OF FINDING 2 IS A PLANNING DEFECT, NOT IMPLEMENTER CARELESSNESS: chains.md:144-147 lists chain-F's `reads:` as eval-contract.md, report.md, judge.md — and OMITS handoff/run-observation.md, the one document stating the sole-importer rule. The constraint was never propagated to the implementer, so the deviation was never surfaced. Lesson for future chain planning: a chain that TOUCHES a subsystem needs the contract carrying that subsystem's invariants in its reads list, not merely the contracts whose types it consumes.
- FINDING 3 — MEDIUM (Tier-C rationale) — folded into the finding-1 fix.
- FINDING 4 — LOW, CONFIRMED: pending-class2.md's (b) tsconfig.json rationale claims omitting the `evals/test` exclude makes typecheck "fail on its process/dynamic-import usage". Reviewer verified `npx tsc --noEmit -p tsconfig.json` currently exits 0 with evals/test/** still INCLUDED — evals/env.d.ts's ambient declarations already satisfy the checker and nothing under evals/test uses dynamic import(). The patch is harmless but the justification a human will read and trust is wrong. Being corrected (text only; the recorded diff itself is untouched and still unapplied).
- FINDING 5 — LOW, informational, NOT actioned: evals/judge/index.ts is imported by nothing in the merged tree (everything imports ./judge / ./scripted / ./replay / ./live directly). Dead today, an explicit task-4.2 deliverable mirroring the storage-engine barrel precedent, and will gain a consumer when Tier C is wired into the CLI. Knip does not catch it because knip is not wired to evals/ yet (Class-2 pending — correctly).
- ACTION: findings 1+2+3+4 converted into fix chain `fix/redaction-tier-results`, BASE=8407427, per apply.md step 11. Dispatcher-set design constraint: extend redaction at the SAME single choke point (buildCaseResult) so "one constructor, redaction cannot be bypassed" becomes actually true; for holdout, closed-vocabulary and numeric fields survive (diagnostic kind + severity — enum values from a closed table, and what makes reports diffable) while free text and candidate-derived strings (message, hint, symbol, Tier-B observed, Tier-C rationale) do not; visible sets unchanged; diff/compare/determinism must keep working. Required deliverable: tests placing a secret token in a Tier-A diagnostic, a Tier-B observed value, and a Tier-C rationale, FAILING against the pre-fix code.
- CLEARED by the reviewer (verified, non-vacuous): holdout-location absence everywhere incl. .gitignore and docs (docs/evals.md uses an obvious <your-own-holdout-path> placeholder); inert-data refusal proven with a live side-effect sentinel that never flips; Tier C structurally cannot gate (computeCaseVerdict has no TierCResult parameter — compiler-enforced); untrusted vantage always fails regardless of `contained`; zero `evals` mentions in package.json/tsconfig.json/knip.json/scripts/gate.sh; live judge throws synchronously on both missing opt-in and missing credential before any I/O; all 6 ASSERTION_KINDS have both green and red fixtures; the tri-state gate-config check verified algebraically correct for all four states; compare refuses mismatched schema AND rubric versions; determinism holds with timings excluded; CLI resolution order with no built-in default; two/zero-terminal streams both classified runner-error; no protected file touched by this change; no secrets committed.
- **FIX 2 SHIPPED (redaction bypass + single-importer + rationale)** — branch fix/redaction-tier-results, three stacked commits 6eff55c -> f428133 -> 004f8b6, BASE 8407427, integrity exit 0 (10 files, no protected file), merged --no-ff, REGATE PASS. evals **254/254** (196 baseline -> 240 -> 249 -> 254). Took THREE dispatcher-driven rounds; each round closed a genuinely DIFFERENT channel of the same property, and each channel was invisible to the tests that existed for a different structural reason.
- ROUND 1 (the reviewer's finding): tier free-text in serializeReport. Pre-fix 11 failures with the tokens visible verbatim in serialized bodies (e.g. `"symbol": "THE-SECRET-TIER-A-SYMBOL-token111"`, `"rationale": "mentions THE-SECRET-TIER-C-RATIONALE-token555"`). Design: redaction extended at the SAME choke point (buildCaseResult) via redactTierA/B/C in redact.ts, so "one constructor, redaction cannot be bypassed" is now actually true. Report-facing types (TierAReportResult etc.) added to evals/contract.ts with free-text fields optional and DROPPED (never hashed, never truncated) under holdout; closed-vocabulary/numeric fields (kind, severity, status, line, column, criterion, score, rubricVersion, judgeIdentity) unconditional, so diff/compare/pass-rates/determinism are unaffected. Evaluator-facing types unchanged. Implementer went beyond the brief on its own judgement and also redacted Tier-C error.message — same class of judge-produced free text as rationale. Self-discovered bundling regression: the finding-2 re-export pulled synthrun's real esbuild+playwright imports into the evals test bundle; fixed by externalizing both, matching synthrun/test/run.mjs's existing precedent.
- ROUND 2 (dispatcher REJECTED the implementer's justification): round 1 kept TierBAssertionResult.english under holdout, reasoned as "assertions are inert eval-set data, never candidate output". Dispatcher ruling: that conflates INERTNESS (never execute eval-set data) with DISCLOSURE (never print holdout content). The assertion's English statement IS expectation prose — spec:38-40 forbids "expectation prose" and the scenario at :44-48 forbids any "expectation string" appearing — and chain-C built the tier English-first precisely so that sentence states what the app should do. Clinching argument was internal inconsistency: redactCase ALREADY drops EvalCase.expectation for holdout, so keeping the assertion's restatement three fields away contradicts the code next to it. Sent back. The implementer then found a DEEPER leak than the dispatcher had flagged: diff.ts's TierBAssertionRegression embedded after.english verbatim into BOTH the JSON diff and renderDiff's rendered human-readable line — and the old suite could never have caught it, because its only holdout fixture went fail->pass and therefore never constructed a regression object at all. Pre-fix 4 failures, 2 of them that diff vector.
- ROUND 3 (dispatcher verified a disclosed gap rather than accepting the conditional): round 2's sweep named the CLI console path as untouched, conditionally ("if a sourcing/generation failure message ever echoes content"). Dispatcher checked and it can — producer.ts:107 `pipeline threw while generating case "X": ${detail}` and :126 `generation failed for case "X": ${terminal.reason}` both carry model/candidate-derived text under --generate, printed by cli.mjs's `console.error(CASE ERROR ...)`. Spec:37-40 enumerates "and console output" explicitly, so this was in scope of the same sentence. Fixed via redactSourcingError in redact.ts (keeping redaction rules in ONE module), applied inside runEvalSet where visibility is already known, before the plain-JS print loop ever sees the message; holdout emits caseId+kind only, visible unchanged. Pre-fix 1 failure with the secret path token on the console surface.
- ROUND-3 TEST SUBSTITUTION, DISPATCHER-VERIFIED: the test injects its secret via the `missing-source` kind rather than a real --generate failure, because the stub pipeline emits a FIXED failure-reason constant that cannot carry an injected token (and server/ was owned by another agent at the time). Dispatcher read redactSourcingError directly to confirm the substitution is faithful: it branches only on `visibility === 'holdout' && kind !== undefined` and returns {caseId, kind} for ALL three kinds uniformly — same field, same code path — so the test genuinely exercises the production channel rather than a lookalike.
- FINDING 2 CLOSED: exactly one module imports the harness — `grep -rn "from '\.\./synthrun'|from '\.\./\.\./synthrun'" evals/` returns only evals/adapters/synthetic-run.ts:14 (dispatcher re-ran this on the merged tip). cli.mjs now imports SynthRunSession/createRunCandidate from the adapter's re-export; both docstrings corrected to describe the real path, so the "ONLY module" comment is true again rather than aspirational.
- FINDING 4 CLOSED: the real reason the tsconfig `evals/test` exclude is wanted is DEFENSIVE/STYLISTIC (matching the established per-suite idiom), NOT a live failure — `npx tsc --noEmit -p tsconfig.json` exits 0 today without it, because evals/env.d.ts's ambient declarations are picked up by the same root `**/*.ts` include glob (a .d.ts is still a .ts) and nothing under evals/test uses dynamic import(). pending-class2.md's (b) rationale rewritten to say so; the recorded diff itself is unchanged and still unapplied.
- RESIDUALS, NAMED NOT FIXED (dispatcher decisions, for the human): (a) EvalRunReport.evalSet.location is unconditional — a PATH, not content; not in the spec's forbidden list; reports land in a gitignored dir. Left deliberately; the human decides, not the dispatcher. (b) A corrupted report file passed to diff/compare can surface a JSON.parse SyntaxError whose message may include a short snippet of the invalid JSON, printed by cmdDiff / main()'s catch. Requires an already-corrupted file, and there is a genuine chicken-and-egg problem — visibility is unknown until the parse succeeds. (c) TierBAssertionResult.english is now dropped for holdout, but an eval-set author who embeds prompt text into an assertion's English statement is an authoring-discipline matter, not a code-level gap. (d) judgeIdentity / appSlug / caseId retained — closed-vocabulary or registry identifiers, agreed safe.
- METHOD NOTE worth keeping: all three rounds' suites were non-vacuous AND red-checked before this pass, and all three still missed their channel — round 1's fixture only entered via candidateSource, round 2's leak lived in a branch the only holdout fixture could not construct, round 3's surface had no assertions at all despite the spec naming it. **A red-check proves a test is SENSITIVE; it says nothing about whether its INPUT SURFACE is complete.** That distinction is the difference between "we verified redaction" and "redaction is verified".
