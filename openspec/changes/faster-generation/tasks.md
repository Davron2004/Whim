## 1. Explicit reasoning, per-role roster, provider routing, per-call timing (server)

- [x] 1.1 Roster: in `server/src/generation/model.ts` add `ReasoningSetting`, `ModelRole`, `RoleSetting` and `ModelRoster = Record<ModelRole, RoleSetting>` (design D2); `modelRosterFromEnv` reads the two required model vars, the optional `WHIM_CLARIFY_MODEL`/`WHIM_SUMMARY_MODEL`/`WHIM_PLAN_MODEL` overrides (empty = unset) and the five `WHIM_<ROLE>_REASONING` vars with their defaults; a value outside the set throws an error naming the variable and the allowed values, surfaced through configuration loading like every other `WHIM_*` error. Add a test helper that builds the full default roster from two model ids.
- [x] 1.2 Wire mapping: `ModelRequest` gains a required `role` label and a required `reasoning: ReasoningSetting`; `server/src/openrouter.ts` maps it per design D1 (`off`/`on`/effort/`default`); `WHIM_PROVIDER_SORT` is loaded and validated in `server/src/config.ts` and passed to the OpenRouter client at the composition root, which adds `provider: { sort }` to every request when set.
- [x] 1.3 Per-call timing: the wrapper emits exactly one `model call` log line per completion when its stream settles (completed, failed, aborted) with the design D4 fields — provider from the SSE chunk's `provider` field, reasoning and cached tokens from the usage details when present — and no message content.
- [x] 1.4 Call sites: classifier (`server/src/policy/policy.ts`: rewrite model, `off`, role `policy`), clarify (`routes/clarify.ts`: clarify role), rewrite (`routes/rewrite.ts`: rewrite role), summariser (`generation/summarise.ts`: summary role), plan and generate/repair (`generation/machine.ts`: plan role; engineer role with `generate`/`repair` labels), and the composition root (`lifecycle.ts`).
- [x] 1.5 Tests in the existing server suites (no new suite file, no edit to `server/test/acceptance.ts`): wire bodies for `off`/`on`/`low`/`default` and provider sort set/unset (`openrouter.suite.ts`); roster defaults, overrides, empty-as-unset and a rejected value naming its variable (`config.suite.ts`); per-call-site requests through the scripted client — classifier/clarify/rewrite/summary carry `off` by default, plan/generate/repair carry `on` and still yield `thinking` events, overrides route only their role, the classifier stays `off` under `WHIM_REWRITE_REASONING=on` (`policy.suite.ts`, `routes-unary.suite.ts`, `machine.suite.ts`); one timing line per completion including an aborted one, with no message text in the log (`openrouter.suite.ts` or `logging.suite.ts`). Red-check: the classifier-reasoning and summary-reasoning cases fail on the pre-change code.
- [x] 1.6 `docs/deploy.md` env reference: the new model, reasoning and provider variables with defaults and fallbacks; the rewrite-side model must accept `reasoning: { enabled: false }` (measured refusers: `z-ai/glm-5.3-flash`, `stepfun/step-3.5-flash`), or every classifier call fails closed; `default` restores the pre-change wire behavior.

## 2. Flow benchmark

- [x] 2.1 `server/src/flowbench/` (driver + report) and the `server/flowbench.mjs` entry, bundled at run time the way `server/loadtest.mjs` bundles `server/src/loadtest/drive.ts`: `--url`, `--eval-set`, `--cases`, `--parallel`, `--retries`, `--save-sources`, `--json`; fresh device id per case; HTTP only; exit codes `0`/`1`/`2` per the flow-benchmark spec.
- [x] 2.2 `server/test/flowbench.suite.ts`, registered in `server/test/acceptance.ts`, against an in-process fake HTTP server that scripts clarify/rewrite/generate responses: request shapes (answers forwarded, rewritten prompt sent to generate, one device id per case), stage and tail durations from scripted stage events, a `failure` terminal with reason and exit `1`, `--retries` over a `503 policy_unavailable` then `200`, no retry without the flag, sources saved byte for byte and absent for a failed case, exit `2` on missing arguments.
- [x] 2.3 `docs/evals.md` model-bakeoff protocol: per candidate roster, start the server with that roster, run the flow benchmark with `--save-sources` and `--json`, then score with `evals/cli.mjs run --source-dir`; say that `run --generate` remains stub-only.

## 3. Review fixes and deploy plumbing

- [x] 3.1 Boot validation: a malformed `WHIM_*_REASONING` fails boot in every pipeline mode. Narrow `server/src/lifecycle.ts`'s stub-mode catch around the model roster to the "no model configured" errors only (`ModelRosterEnvError`, a missing API key), so `ModelRosterReasoningError` always propagates; test that `WHIM_PIPELINE=stub` with `WHIM_PLAN_REASONING=fast` fails boot naming the variable.
- [x] 3.2 Deploy plumbing (design D6): add `WHIM_CLARIFY_MODEL`, `WHIM_SUMMARY_MODEL`, `WHIM_PLAN_MODEL`, `WHIM_CLARIFY_REASONING`, `WHIM_REWRITE_REASONING`, `WHIM_SUMMARY_REASONING`, `WHIM_PLAN_REASONING`, `WHIM_ENGINEER_REASONING` and `WHIM_PROVIDER_SORT` to `deploy/lib.sh`'s `WHIM_VALUE_KEYS`; validate each in `deploy/deploy.sh` preflight when set; append each non-empty one to the staged `config.env`; delete the eight unused `ServerConfig` fields from `server/src/config.ts`. Tests in `server/test/deploy-config.suite.ts`: a set knob reaches `config.env`, an unset one does not appear, and an invalid value fails preflight naming its key. `docs/deploy.md` says these are settable in `deploy.env`.
- [x] 3.3 Flow benchmark exit codes at the process level: spawn `server/flowbench.mjs` against the fake server for a run whose generate ends in `failure` (exit `1`) and with a missing or manifest-less `--eval-set` (exit `2`, zero requests received).
- [x] 3.4 `server/src/loadtest/replay-model.ts` module comment: describe the roles by which roster model they default to, not as fixed aliases of `roster.engineer`/`roster.rewrite`.

