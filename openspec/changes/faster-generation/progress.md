# Progress ledger: faster-generation

Staging branch: integration/faster-generation · MAIN_TIP 780923c775601b0240b7b8b06df2a8eb90368981

## 2026-09-22

- run-start: staging branch cut from MAIN_TIP; planning artifacts committed as b7b04f5. Note: a leftover local `integration/store-launch` exists; its tree is identical to `origin/main` (pre-cleanup history of the merged launch run, never torn down), so it is not an active run. Left untouched for the owner.
- baseline (pre-change code, production roster `deepseek/deepseek-v4-flash-0731` + `deepseek/deepseek-v4.1-flash`, local server, 3 cases): clarify 3.5/29.5/32.0 s, rewrite 17.3/29.6/34.5 s, generate 44.6/122.6/225.9 s; the summariser tail is 20.0 s in every run. First pass without retries: `503 policy_unavailable` on 2/3 clarifies and 3/3 generates, one clarify `502` at 61.7 s. Numbers and method in design.md § Context.
- dispatch plan: chain-1 (server-roster) → Claude `implementer` (sonnet); chain-2 (server-flowbench) → Codex `gpt-6-luna` (owner asked to try Codex workers), gated and committed by the orchestrator because Codex's sandbox cannot write the linked-worktree index.
- dispatched chain-1 (Claude `implementer`, sonnet) and chain-2 in parallel, BASE b7b04f5, worktrees `.claude/worktrees/faster-generation-{1,2}`. Chain-2's first Codex attempt on `gpt-6-luna` failed at request time ("not supported when using Codex with a ChatGPT account"; the CLI was stale) and was redispatched on `gpt-5.6-luna`.
- chain-2 report (Codex `gpt-5.6-luna`, ~217k tokens): STATUS failed-gate, TASKS 3/3, KNIP clean, DEVIATIONS none. The gate failure is environmental: Codex's workspace-write sandbox forbids listening on 127.0.0.1 (EPERM), which breaks the existing server and netdeny suites too. Orchestrator re-gating unsandboxed in the worktree. `codex update` (0.153.4 → 0.156.0) then made `gpt-6-luna` usable.
- round-1 engineer screen (pre-change code, reasoning on, `:nitro` routing, 3 cases, generate only): `v4.1-flash` 3/3 delivered, 95.0 s median; `v4-flash-0731` 3/3 first-pass, 134.6 s median (a 23k-token reasoning pass on flashcards); `qwen3.8-27b` 1/3 (two mid-stream `502 Connection closed` from its fastest provider, DekaLLM; the pipeline does not retry an upstream 5xx on an engineer turn).
- chain-2 fix round (Codex `gpt-6-luna`, ~80k tokens, workspace-write with network so it could bind 127.0.0.1): root cause was the test, not the driver — a fake-server counter shared across runs had already consumed the scripted 503. Class-A deviation accepted after reading the retry test: attempts are now keyed by path+prompt and the no-retry case asserts status 503 and exactly one clarify post (a retrying driver fails both). Also: I had not pre-created the worktrees' `node_modules/@whim/*` symlinks, which made prod-build's "contract is bundled" check fail environmentally; created for both worktrees and chain-1 told.
- chain-2: orchestrator gate in the worktree → FAST GATE PASSED; committed b2eb6c6 on chain/faster-generation-2; integrity (allowlist = chains.md file scope) → INTEGRITY OK.
- chain-2 merged as 4a804a9 (--no-ff) → tasks 2.1–2.3 ticked → regate on the staging tip: FAST GATE PASSED; worktree and chain branch removed.
- dispatched chain-2b (fix: flowbench.mjs calls drive.formatMarkdownReport, which the drive.ts entry never exports — found by a live run against a local server; the suite never exercised the .mjs entry), Codex gpt-6-luna, BASE a824a5caad6b2c828a6dfd6d9c45e75c9bbfd71b, worktree .claude/worktrees/faster-generation-2b.
- chain-2b report (Codex gpt-6-luna, ~87k tokens): complete, red-check done (the new CLI test fails with the export removed). Orchestrator gate in the worktree: FAST GATE PASSED; committed; INTEGRITY OK.
- chain-2b merged → regate on the staging tip: FAST GATE PASSED; worktree and branch removed.
- chain-1 report (Claude implementer, sonnet, ~677k tokens, 45 min): complete, GATE PASS, commit daafed8, 6/6 tasks. Red-check done: with the pre-change wire mapping restored, 8 server tests fail, including the classifier and summariser reasoning cases. Class-A deviations accepted: `server/src/generation/index.ts` (the one place the real OpenRouterClient is built, to thread the provider sort), mechanical roster renames in `server/src/loadtest/{replay-model,server}.ts`, and ten more suites/helpers that build roster literals or raw ModelRequests. ScriptedModelClient now matches turns by the request's role label (model-id inference is ambiguous once roles share a model) and keeps the model-id cross-check. Integrity with the widened allowlist: INTEGRITY OK; acceptance.ts untouched.
- chain-1 merged as bcb2ee7 (--no-ff) → tasks 1.1–1.6 ticked → regate on the staging tip: FAST GATE PASSED; worktree and branch removed. All chains merged.
- review 1 (reviewer, sonnet, whole diff b7b04f5..b7ff2b7): changes-requested, 3 medium + 1 low (review-1.md). Adjudicating finding 3 exposed the deploy whitelist gap (design D6). All folded into chain-3 (tasks 3.1–3.4).
- dispatched chain-3 (review fixes + deploy plumbing), Codex gpt-6-luna, BASE 56e18b31ffa3ad8e5c486ff740dea981a0cd3a50, worktree .claude/worktrees/faster-generation-3.
- chain-3 report (Codex gpt-6-luna, ~156k tokens): complete 4/4, red-checks done (stub boot test and CLI exit-1 test fail with the fixes reverted). Orchestrator read the deploy.sh/lib.sh/lifecycle.ts diffs, gated in the worktree: FAST GATE PASSED; committed; INTEGRITY OK.
- chain-3 merged → tasks 3.1–3.4 ticked → regate on the staging tip: FAST GATE PASSED; worktree and branch removed.
- gate-full on the staging tip 33b33fe (after chain-3): FULL GATE PASSED (knip, metro-guard, invariants, bridge-invariants, synthetic-run, generation-e2e, openspec 42/42).
