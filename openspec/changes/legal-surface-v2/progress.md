# Progress ledger: legal-surface-v2

- run-start: staging `integration/legal-surface-v2`, MAIN_TIP `672e4e4a2978c8b3b5282ca66f75fd9bf81258ee` (2026-09-24). request-envelope applied and deployed (main 672e4e4). Owner granted: run chain-11 console/account steps myself (no payments, no binding signatures), merge the closure PR myself, and deploy.
- DAG: wave 1 {chain-1, chain-10}; after chain-1 {chain-2, chain-3, chain-8}; chain-4 after 3; chain-7 after 2+3; chain-5 after 4; chain-6 after 5; chain-9 after 6+8. chain-11 attended (orchestrator).
- dispatched chain-1: BASE `672e4e4a2978c8b3b5282ca66f75fd9bf81258ee`, worktree `.claude/worktrees/legal-surface-v2-1` (@whim symlinks)
- dispatched chain-10: BASE `672e4e4a2978c8b3b5282ca66f75fd9bf81258ee`, worktree `.claude/worktrees/legal-surface-v2-10` (@whim symlinks)
- chain-10 report: STATUS complete, GATE PASS, commit `b481eca`, 5/5 tasks (breach runbook + log, Quebec s.17 draft, UK Children's Code DPIA draft, lawyer brief, change process). Class-A: log retention stated correctly (VM json-file today; Cloud Logging `_Default` 30 days when developer-observability ships; 90 days is the published cap); points outside the research labelled "general knowledge" and listed as lawyer-brief gaps G1–G5. 17 `TODO(owner)` lines, carried to chain-11. Notes: the repo is public, so the breach log must never hold personal info (owner may prefer it private); "error details off for EU/UK" has no location signal to build on (DPIA §5, G5); Texas §121.053 draft rule in change-process §8 feeds 11.10.
- chain-10 integrity: exit 0 with allowlist `docs/legal/**`. Merged; regate-pass. Worktree and branch removed.
- chain-1 report: STATUS complete, GATE PASS (+ knip clean), commit `d6c91b1`, 6/6 tasks + `handoff/disclosure-manifest.md`. Class-A accepted: `CONSENT_WHATS_NEW` is a separate export beside `COPY` (every `COPY` reader assumes string values); keep rule gained `after: 'collection' | 'last-use'`; release-check defaults to live manifests (knip can't see `scripts/` imports); `disclosure-check` CLI command wired into `deploy.sh` (2-line hook after `preflight_node`) and the release `check`; v1 store mapping records v1's actual "not linked". Red-checks table in the report, each failing a named test.
- chain-1 adjudication (SendMessage revision 1/2): REVERSED one class-A deviation. The v1 baseline had included the `authorities`/`successor` roles and `legal` purpose so the v1→v2 diff matched draft-copy's what's-new line. main's v1 `privacy.html` and consent copy never name them, so that recorded a false v1 and hid three widenings. Told chain-1 to remove them from v1 (and `released/v1.json`), extend `CONSENT_WHATS_NEW.en[1]` text + `covers`, and report whether the spec scenario "A version-1 grant asks again" needs amending.
- ordering note from chain-1: don't ship an app build between chain-1 and chain-3 (v2 consent version on the v1 screen).
- chain-1 revision 1 result: commit `167dba7`. v1 baseline no longer lists authorities/successor/legal; v1→v2 now also widens `role:authorities`, `role:successor`, `purpose:<cat>:legal` ×5, `purpose:usage-records:safety`; the what's-new line covers all 13 ids. Chain-1 then flagged three more v1 entries unsupported by v1's text (request material for `operate`, `service-providers` receiving phone ID/usage/logs/reports, connection logs for `operate`/`safety`) → revision 2/2 sent: remove them from v1 too and cover the resulting widenings.
- spec amendment (orchestrator): ai-data-consent scenario "A version-1 grant asks again" now requires the what's-new line to name every v1→v2 widening, not a fixed subset of three.
- 11.10 research (orchestrator, web): Texas Bus. & Com. Code §121.053 (SB 2420, eff. 2026-01-01) requires notice to each store BEFORE a significant terms/policy change (a new data category; an age-rating change; new monetization; a material functionality change). No lead time or format is set. Enforceable as of 2026-09-24: CCIA v. Paxton PI (2025-12-23) stayed by the 5th Cir. (~June 2026); SCOTUS declined to vacate the stay (~2026-07-06). Channels: **Google Play** = Play Console "Age Signals" page, "notify a significant change" without a new release (support.google.com/googleplay/android-developer/answer/16569691); **Apple** = PermissionKit "Significant Change API" alongside Declared Age Range (developer.apple.com/news/?id=2ezb6jhj, ?id=sg176nne), no console form. Fill `docs/legal/change-process.md` §8's channel TODO with these.
- chain-1 revision 2 result: commit `d009209`. v1 also drops request-material `operate`, `service-providers` on phone ID/usage/logs/reports, and connection-log `operate`/`safety` (connection logs keep AnyCognition as holder with an empty purpose list; `DataUse` allows `purposes: []`). Final v1→v2 widenings (20): `category:app-integrity`, `category:error-details`, `keep:reports`, `keep:usage-records`, `purpose:connection-logs:{legal,operate,safety}`, `purpose:phone-id:legal`, `purpose:reports:legal`, `purpose:request-material:{legal,operate}`, `purpose:usage-records:{legal,safety}`, `recipient:{connection-logs,phone-id,reports,usage-records}:service-providers`, `role:{authorities,platform,successor}`. The what's-new line covers all 20. Red-checks by name.
- carried to the final review (revision cap reached): (1) v1 `usage-records` for `operate` is still unsupported by v1's text; (2) diff blind spot: v2's `service-providers` role is broader than v1's (hosting/logging/email vs AI companies) under the same id, so request material reaching hosting/logging providers isn't a visible widening. Candidate fix: split the role id (e.g. `ai-providers` vs `service-providers`) and name request material in the what's-new line.
- chain-1 integrity: exit 0. Merged; regate-pass. Worktree and branch removed.
- dispatched chain-2: BASE `a6b63c1bf2032ffd9d72df21ab0c0c89bc9b6d23`, worktree `.claude/worktrees/legal-surface-v2-2`
- dispatched chain-3: BASE `a6b63c1bf2032ffd9d72df21ab0c0c89bc9b6d23`, worktree `.claude/worktrees/legal-surface-v2-3`
- dispatched chain-8: BASE `a6b63c1bf2032ffd9d72df21ab0c0c89bc9b6d23`, worktree `.claude/worktrees/legal-surface-v2-8`
- chain-3 report: STATUS complete, GATE PASS (+ knip), commit `75993df`, 5/5 tasks + `handoff/legal-copy.md`. Class-A: `consentWho` split into three keys (one per screen-named role) so the coverage check can catch a dropped app-integrity sentence at key level; edited `deploy/site/privacy.html` (outside scope) to quote the v2 strings word for word, keeping the web-site parity check unweakened, so the page is a v1/v2 mix and must not deploy before chain-7; re-pointed one web-site red-check from a deleted key; `ConsentStatus.outdated` carries `version` (`outdatedFrom` prop); Settings privacy link goes through the language hook; "what gets sent" as four bullet rows. Notes for chain-7: 18 strings to quote verbatim (in the contract); the web-site suite also requires `/deleted after (\d+) days/` and `/kept for (\d+) days/` matching server retention defaults, but draft §2 says "within 12 months". Visual check of the new consent layout on device is still pending.
- chain-3 integrity: exit 0. Merged; regate-pass. Worktree and branch removed.
- tripwire candidate: 2 chains so far (request-envelope chain-4, legal-surface-v2 chain-3) were forced into `server/test/web-site.suite.ts` by copy changes in `src/`: the `consent*` → privacy.html parity rule is a cross-workspace coupling chain plans don't see.
- dispatched chain-4: BASE `e1ca4dc1871565ef7875a09787e77357d664036a`, worktree `.claude/worktrees/legal-surface-v2-4`
- chain-8 report: STATUS complete, GATE PASS (+ knip, plutil), commit `aafed2b`, 4/4 tasks. Class-A: review notes condensed to fit 4,000 (now 3,886; chain-9's age-signal sentence is 92, leaving 22), no fact dropped except the full syscall names in 4.7.2 (kept in `docs/store/review-notes.md`); `whim.anycognition.ca/a/<id>` in notes.txt reworded (the listing checker refuses release-domain text under `release/store/`); `[#64]` marker dropped (headers exist), App Attest sentence left out (#65 unshipped); stale `answers.md` lines fixed ("anonymous", 90 days, "no analytics SDK"); fastlane `ANALYTICS` purpose token unverified. Red-checks against six weaker checker variants. Carry to chain-9: the review notes' how-to-try step 2 says a fresh install opens consent first, which is wrong once the terms step ships.
- chain-8 integrity: exit 0. Merged; regate-pass.
- chain-2 report: STATUS complete, GATE PASS, commit `78373c0`, 4/4 tasks. Migration evidence: a real WAL DB written by the BASE store (4-col `usage`, 3 rows, 6 ledger rows) → first open adds `last_credited_day` once and backfills the run day, totals identical → second open (later clock) is byte-identical. The checked-in fixture replays that dump. Class-A: purge moved into `usage-store.ts` (`scheduleUsagePurge`, same cadence); per-device methods on separate `UsageRecordKeeping`/`ReportRecordKeeping` interfaces; deploy check runs `loadServerConfig` via new `server/config-check.mjs`, with a shared `server_optional_keys` list; `preflight_server_config` called after chain-1's disclosure hook; only `WHIM_USAGE_IDLE_DAYS` is an operator value (retention keys stay server-only per the existing rule); no log retention to cap (Docker rotates by size). Tables covered by export/delete: `usage`, `requests` (usage.db), `reports` (reports.db). Red-checks: purge by created day, backfill on every open, unguarded ALTER, write lock on open, delete ignoring device id, missing deploy preflight. Gap filed: #85 (VM docker logs by size, not age). Carry to final fix: `docs/deploy.md` lacks `WHIM_USAGE_IDLE_DAYS`.
- chain-2 integrity: exit 0. Merged; regate-pass. Worktrees for chains 2, 8 removed.
- dispatched chain-7: BASE `c122974dce66bd4082ee8b76655ccb8fec2c5b71`, worktree `.claude/worktrees/legal-surface-v2-7`
- owner decisions (2026-09-24): (1) no street address/phone tonight → they become optional-render on the legal pages (like the EU/UK reps); the deploy check still refuses unresolved `{{…}}`, draft markers, missing manifest categories and empty REQUIRED fields (name, contact email, officer title, effective date). (2) privacy contact = `support@anycognition.ca` (existing Zoho mailbox) instead of privacy@; 11.2 resolved. Effective date 2026-09-24. Sent to chain-7 mid-run; recorded in README decision 3 and design.md. Carry to final fix: `docs/legal/*` still say privacy@.
- chain-4: first run stopped by the API session limit mid lint-fix; resumed the same agent (worktree intact), then STATUS complete, GATE PASS (+ knip), commit `3dca09f`, 4/4 tasks + `handoff/terms-flow.md`. Class-A: English-only `COPY.termsOfUseLabel` for the Settings row; `entryDecision` replaced by `nextLegalStep(terms, consent, refused)` + `LegalFlow<S>`; `onConsentAskDecline` → `onLegalDecline`; French URL twins `RELEASE.privacyPolicyUrlFr`/`termsUrlFr` (chain-6 wires them). Send gate is now `consentedClientOptions(terms, consent, baseUrl, deviceId, appInfo)`. Red-checks: a consent-only gate fails 5; dropping the refusal override fails 7. GAP for chain-5 (orchestrator decision): Settings "Turn on AI features" must route through the terms step (today it grants consent without terms). Existing quirk left: Continue on compose silently does nothing if terms or grant vanish mid-compose.
- chain-4 integrity: exit 0. Merged; regate-pass. Worktree and branch removed.
- harness-feedback: in-report from now on (README protocol changed after 8 debrief resumes burned ~1M uncached tokens and hit the session limit). Only chain-1's lsv2 debrief was collected; the others were lost.
- chain-7 report: STATUS complete, GATE PASS (+ knip), commit `2fd19fd`, 5/5 tasks. Class-B resolved by the orchestrator: the spec scenario "A missing address blocks the deploy" is amended to the owner decision (required values block; address/phone optional and left out until set). Class-A: address+phone must be set together; the Korean section sits inside "Who handles it right now"; privacy.html quotes the consent text in a blockquote (word-for-word rule holds); age-signal paragraph and "another server" section left out; officer by title only; phone added to the terms contact line; providers gained a retention column; template language gained `<!--IF:A&B-->` and `<!--EACH:PROVIDERS:<lang>-->` (core in `server/src/site/template.ts`); forced edits in `deploy-config.suite.ts`, `deploy/smoke.sh` (now also checks /terms, /fr/privacy, /fr/terms), `docs/deploy.md`, `deploy/Caddyfile`. Retention rule reworked: the page states each category's manifest maximum exactly (12 months = 365 days), and every server default must fit; mutation runs caught each weaker variant.
  - Identity file as committed: legalName "AnyCognition Inc.", locality "Ottawa, Ontario, Canada" (sourced: draft-copy §2 and the research facts), contactEmail support@anycognition.ca, officer title en/fr, effective dates 2026-09-24, providers Google Cloud + OpenRouter (en/fr/ko). Empty and optional: streetAddress, phone, EU/UK reps, provider contacts. The check passes with this file.
  - Owner checks: provider contacts are empty (the Korean PIPA section wants a recipient contact); Google log-storage region and OpenRouter's legal name/contact are unconfirmed; provider retention wording is the agent's. French consent quote on the policy page is chain-7's translation, so chain-6's fr table should match it (or 6.3 adds a fr parity rule).
  - Note: a full deploy publishes the site too, so the server-first migration step publishes the v2 pages at the same time. That's fine now that the identity file passes.
- chain-7 integrity: exit 0. Merged; regate-pass. Worktree and branch removed. NEXT: chain-5 (plus the Settings AI-on → terms routing), then 6 (make the fr consent quote match chain-7's policy page), then 9. No agent in flight. Clean point for the orchestrator handoff.
- orchestrator handoff (2026-09-24 03:30, new orchestrator): resumed at chain-5. Owner now allows moderate parallelism (lsv2 chain-5 alongside developer-observability's independent chains).
- dispatched chain-5: BASE `432a64d3f90e09e7450f389f1c917ecd9edea097`, worktree `.claude/worktrees/legal-surface-v2-5` (@whim symlinks). Extra scope (orchestrator decision from chain-4): Settings "Turn on AI features" routes through the terms step.
- 11.10 Google Play (orchestrator, 2026-09-24 03:55): filed in Play Console → Whim → Age signals → Significant changes. Effective from **2026-09-27** (the earliest the form allows: today + 3), editable until 2026-09-25. Text (200-char cap per language), en-US: "New Terms of Use to accept before AI features. Whim now sends error details (can be turned off), keeps usage records by a random phone ID up to 12 months, and checks the store's age signal." plus fr-CA.
  - Decision: because §121.053 wants notice BEFORE the change, the v2 terms and policy take effect 2026-09-27, not 2026-09-24. `deploy/site/legal-identity.json` effective dates move to 2026-09-27 in the final fix chain. Publishing the v2 pages early with a future effective date is fine; the v2 app build ships after the 27th anyway.
  - Apple: no console form; the channel is PermissionKit's Significant Change API, called from the app. Not wired; filed as an ai-proposed issue.
- chain-5 report: STATUS complete, GATE PASS (+ knip), commit `a33ef75`, 3/3 tasks + extra scope + `handoff/privacy-settings.md`. `error-details.ts` (`whim.error-details:v1`, only `'0'` is off; throw/missing = on); `resetDeviceId(kv): string`, LauncherRoot `deviceId` is state so option memos rebuild; Settings "Turn on AI features" → `advanceLegalFlow` with continuation `{kind:'settings'}` (terms → ask-mode consent → back to Settings) unless only consent is left.
  - 5.3 build-flag table: Android debug `WHIM_INTERNAL_BUILD true` (buildConfigField, `debug`); Android offline `npm run android:release` → `true` in `offline` (overrides `initWith release`); Android store (fastlane `bundle Release`) → `false`; iOS Debug → `#if DEBUG` in `WhimAppInfoModule.mm` → true; iOS store (fastlane Release) → false; Node suites → module missing = store unless passed. JS reads `WhimAppInfo.internalBuild` via `internalBuildFrom` (only boolean `true` is internal).
  - Class-A: `ConsentContinuation` gains `{kind:'settings'}`; Settings path shows the consent text twice (review then ask mode) so terms always precede consent; "compiled out" = native flag, JS still in the bundle but never renders/reads (a bundle-level strip would need Metro/Babel config); test-seam edits (Switch a11y label, `internalBuild` in renders, Alert mock records calls); an eslint-disable with reason on the silent catch, as in ai-consent.ts.
  - Unverified: neither native build compiled (codegen only) → covered by the emulator visual pass (Android) and an iOS simulator build before closure.
  - ENV finding: `checks/test/repo/source-scans.suite.ts`'s emoji scan is Node/ICU-dependent (`\p{Extended_Pictographic}` matches ✎/⚑ on v22.20.0, not on v22.23.1), so the fast gate fails on BASE under v22.20.0. Gate with the shell default v22.23.1. Filed as an ai-proposed issue.
- chain-5 integrity: exit 0. Merged; regate-pass (node v22.23.1). Worktree and branch removed.
- dispatched chain-6: BASE `e04ec2a6dbc4dd680df96f12c9fbe9609fe8343f`, worktree `.claude/worktrees/legal-surface-v2-6`. 6.4 (emulator fr-CA) moved to the orchestrator's visual pass (the worktree can't build a native app).
- chain-5 on-device check (orchestrator, offline build of `e04ec2a` on a fresh AVD `Whim_Verify`/emulator-5556; the existing AVD keeps its store build and user data untouched): the Android native `WHIM_INTERNAL_BUILD` change compiles; Settings shows "Send error details" (on, hint), About → Privacy policy / Terms of use / Support / "This phone's ID" + "Make a new ID", and Advanced (internal build). AI features Off → tap → consent (review mode) → "Turn on AI features" → Terms step → Accept → consent → back to Settings "On since 9/24/2026". Nits for the visual pass: the terms step is ~60% empty space (draft-copy's copy is intentionally short); the date renders US-style (9/24/2026) while the app spells Canadian ("Colours").
- chain-6 report: STATUS complete, GATE PASS (+ knip), commit `e31ec916`, 6.1–6.3 + French privacy-page parity. 34 French legal keys + `CONSENT_WHATS_NEW.fr[1]` (shared `WIDENED_SINCE_V1` covers). Language resolved once in `LauncherShell` and passed as props; `activeLegalLanguage(kv, deviceLocale)`, `chooseLegalLanguage`, `preferredLocale`, `intlLocale`; new RN `device-locale.ts` (Intl first; fallback Android `I18nManager.localeIdentifier`, iOS `Settings.get('AppleLanguages')[0]`); `LegalLanguageSwitch.tsx`; new key `legalLanguageSwitch`; gate rule: every `LEGAL_COPY_KEYS` + `consent*` key non-empty in every table; `web-site.suite.ts` French parity vs `/fr/privacy` (consentWho wording aligned on both sides; one edit to chain-7's `fr/privacy.html`). Class-A: those signature changes (`handoff/legal-copy.md` now stale → final fix chain updates it); `LauncherRoot deviceLocale` prop; copy scans include `LEGAL_COPY.fr`.
  - For 11.7 (fluent reader): unsure phrases listed in the report: `consentOutdatedLine`, `permissionRequiredLine`, `termsLead` ("là où l'IA se trompe"), `consentWhoTitle` "Qui les reçoit", consentWho wording, `consentFootnote` "(Settings)", "app" vs "appli", non-breaking spaces before ":".
  - RISK (for the fix chain): on iOS, `Intl` may follow the app's declared localizations (Xcode lists only `en`/`Base`), so a French iPhone could resolve `en-CA` and get English — the Quebec case the law targets. Fix: on iOS read the preferred-languages list first (or declare `fr`), verified on the simulator with a French locale.
  - Unpinned-locale suites (fork-ui, app-link-ui, connectivity-ux, launcher-interactions, mini-app-delivery, privacy-settings restart) use the machine's LANG; harmless today (no legal screens), a latent flake on a fr_* machine → fix chain pins them.
- chain-6 integrity: exit 0. Merged; regate-pass. Worktree and branch removed.
- dispatched chain-9: BASE `59505b5f69658492e7616afe156a92fc9d4d8951`, worktree `.claude/worktrees/legal-surface-v2-9`. Extra: review-notes how-to-try step 2. 9.6 (emulator) → orchestrator's visual pass.
- 6.4 (orchestrator, Android emulator `Whim_Verify`, offline build of `59505b5`, app locale fr-CA via `cmd locale set-app-locales`): a fresh install shows the terms step in French ("Conditions d'utilisation", "Continue in English" top right), then the consent screen in French ("Avant que Whim crée des apps pour vous"). "Continue in English" switches the consent screen, and after a force-stop and relaunch the flow stays English, now offering "Continuer en français". Screenshots: `evidence/6.4-fr-terms.png`, `evidence/6.4-fr-consent.png`, `evidence/6.4-english-persists-after-restart.png` (plus the chain-5 ones `evidence/5-*.png`). iOS is unchecked: the Intl risk above needs a French simulator run in the visual pass.
- chain-9 report: STATUS complete, GATE PASS (+ knip, release check), commit `608c80ea`, 9.1–9.5 + review-notes step 2. `age-check.ts` (non-RN; `whim.age-check:v1` stores only `{outcome, checkedAt}`; re-asks when none, >30 days, blocked, or future-dated; errors = unavailable); iOS `WhimAgeSignal.swift` + `.mm` (floor iOS 26.2: `isEligibleForAgeFeatures` first so no age sheet where no law applies; single gate 18; weak-linked `DeclaredAgeRange`; entitlement `com.apple.developer.declared-age-range`; App ID capability `DECLARED_AGE_RANGE` must be enabled before a release build); Android `WhimAgeSignalModule.kt` (VERIFIED→adult, SUPERVISED→minor-approved, APPROVAL_PENDING/DENIED→minor-not-approved, else unavailable), Gradle `com.google.android.play:age-signals:0.0.1-beta01` **pinned from memory, unverified**; new `'age'` screen before terms (`AgeScreen.tsx`, blocked copy in en+fr); `nextLegalStep(age, …)`. notes.txt 3,994/4,000 chars; step 2 now terms → consent ("Agree and continue").
  - Class-A: check runs on a transient age screen (the sync refusal path also reaches terms); iOS never yields `minor-not-approved` (withdrawn parental consent arrives only as a server notification Whim doesn't receive); iOS 26.0/26.1 → unavailable.
- chain-9 integrity: exit 0. Merged (9.1–9.5 ticked); regate-pass. Next: Android build (resolves the Gradle coordinate) + iOS simulator build, 9.6, then gate-full + reviewer.
- dispatched chain-9b (Android compile fix: `WhimAgeSignalModule.kt` compares `AgeSignalsVerificationStatus` to Int, 4 errors at :42–45): BASE `8f35a9b87c43e37d18376a60ba6fbf8510bbf937`, worktree `.claude/worktrees/legal-surface-v2-9b` with the primary `node_modules` symlinked so Gradle can build there
- Apple App ID capability (orchestrator, 2026-09-24 ~06:05): enabled **Declared Age Range** on `com.anycognition.whim` (bundleId `44Z7DGGG2G`, team 2B7K4YLS34) in the developer portal. The ASC API rejects `DECLARED_AGE_RANGE` as a capabilityType on POST, but lists it after the web change. The portal warned that existing provisioning profiles for this App ID are invalidated, so the next iOS release build must regenerate its profile (the fastlane lane's signing).
- iOS simulator build of `8f35a9b` (orchestrator): FAILED in `WhimAgeSignalModule.mm`: `Whim-Swift.h:817 cannot find interface declaration for 'RCTDefaultReactNativeFactoryDelegate'` (the RN header is missing before the `Whim-Swift.h` import). Added to chain-9b's scope. `pod install` rewrote `project.pbxproj` and `PrivacyInfo.xcprivacy`, so both were reverted after the build (ops note: the checked-in PrivacyInfo is curated; never commit pod-install's rewrite).
- reviewer (whole change, `672e4e4..integration/legal-surface-v2`, before chain-9b): VERDICT findings; chain reports match the diff. High: H1 the policy (en/fr) lacks draft-copy §2's D18 age-check paragraph (chain-7 deferred it, chain-9 never picked it up); H2 the published 90-day log retention isn't enforced (Docker rotates by size only, #85); H3 the backfill keeps pre-v2 usage totals ~365 days after deploy, against "made before … deleted within 90 days". Medium: M1 the French what's-new check can't fail (shared `covers`, text never checked); M2 iOS can't hold an unapproved minor and under-13s pass on both platforms; M3 the v2 "It stays on your phone" wording ships before 11.8's network-deny device runs; M4 the provider list lacks the routed model providers, the content-safety provider and Zoho; M5 a beta Gradle dependency pinned from memory. Low: L1 the EU consent sentence vs a default-on switch; L2 the what's-new keep-period understates lifetime totals; L3 no archived v1 policy until the effective date; L4 preflight skips profile-file keys; L5 devobs files on this branch; L6 test-only exports; L7 "compiled out" is a runtime flag. Confirmed queued items 1, 2 (reaches further: the text too), 3, 4 (worse: the breach runbook sends people to a nonexistent privacy@), 6, 8.
- orchestrator decisions on the review (owner asleep):
  - H2 → enforce it: a VM daily job caps container logs at 89 days (fix-infra chain), plus a deploy-config test tying the cap to the manifest's connection-log keep days. Reason: the promise is right; the infrastructure is what's missing.
  - H3 → backfill pre-existing rows with run day − (idle days − 90) so a phone that never returns loses its pre-v2 totals within 90 days, as the policy says; active phones re-credit and stay.
  - M3 → qualify the wording instead of holding the site: drop "It stays on your phone" for saved data (consent en/fr, pages, store descriptions) and say what's true now ("Whim doesn't send it anywhere"), until 11.8 passes. Reason: v1 never made this claim, and 13.6/13.7 need sudo tcpdump and a real iPhone (owner items).
  - M2 → add `under-13` (store age range upper bound < 13 blocks, with a "13 and over" message); iOS asks with gates 13 and 18; spec amended (store-age-signals: `under-13` value + scenario; iOS never produces `minor-not-approved`). Review-notes 4.7.5: "keeps only the outcome, on the device".
  - L7 → spec amended (app-launcher: store builds "SHALL NOT render or read" the field). A bundle-level strip needs Metro/Babel config for no user-visible gain.
  - M5 → ratified: the coordinate `com.google.android.play:age-signals:0.0.1-beta01` resolved from Google Maven in the orchestrator's Gradle build (the AAR is in `~/.gradle`), so it exists; beta is the only published line.
  - L5 → accepted (docs only: devobs chain-0's handoff and the run handoff).
  - L1 → legitimate interest wins (matches the legal-basis table); the sentence is fixed and the point goes into the lawyer brief.
  - Fix chains: fix-A (text + data: items 1, 2, 3, 4, 5, 6, 7, H1, H3, M1, M3, M4, L1, L2, L3, L4, L6) ‖ fix-infra (H2) ‖ fix-B after chain-9b (native/locale/age: items 8, 9, 10, M2).
- dispatched fixA: BASE `070d732c5fbf33b9ab96b79e469c77126663d735`, worktree `.claude/worktrees/legal-surface-v2-fixA`
- dispatched fixinfra: BASE `070d732c5fbf33b9ab96b79e469c77126663d735`, worktree `.claude/worktrees/legal-surface-v2-fixinfra`
- chain-9b report: STATUS failed-gate in its worktree (ENV: a full `node_modules` symlink made `@whim/contract` resolve to the primary tree, tripping `prod-build.suite.ts:251`; zero-diff repro), fix complete, commit `c71decab`. Android: `userStatus()` returns the `@IntDef` annotation type, constants are Int → `result.userStatus() as? Int` before the `when`, mapping unchanged (`:app:compileDebugKotlin` BUILD SUCCESSFUL). iOS: `@import React_RCTAppDelegate;` (the module `AppDelegate.swift` already imports) before `Whim-Swift.h`.
- chain-9b integrity: exit 0. Merged; main-tree regate-pass. Android `assembleOffline` + iOS simulator build running from the main tree.
- dispatched fixB: BASE `fed5391979a052c226f773678082d7399495648b`, worktree `.claude/worktrees/legal-surface-v2-fixB`
- Android `assembleOffline` + iOS simulator Release build of `fed5391` (after 9b): both PASS. `pod install`'s PrivacyInfo rewrite is key reordering only (Tracking moved after Purposes), semantically identical: the shipped manifest keeps chain-8's declarations. 9.6 (orchestrator, emulator `Whim_Verify`, app locale en-CA, no Play sign-in so the signal is unavailable): tapping compose on a fresh install lands on the Terms step ("Continuer en français" offered) → PASS.
- fix-infra report: STATUS complete, GATE PASS, commit `4162ab43`. `deploy/vm/log-age-cap.sh` (cap 89 d; the suite requires cap + 1-day timer ≤ the manifest's connection-log days) removes old lines from active AND rotated json-file logs (mtime alone leaves mixed-age rotated files), in place with the inode unchanged (count old lines → copy tail + catch-up → inode check → truncate → one append-mode `dd` write; loss window ~ms); `whim-log-age-cap.{service,timer}` (daily, Persistent, systemd sandboxed) installed last by `bootstrap.sh`. 8 red-checks. Unverified on real systemd/mawk/Docker; production install commands in the report (orchestrator runs them at deploy).
- fix-A report: STATUS complete, GATE PASS (+ knip, release check), commit `1ee66a27`, 17/17. v1 usage-records purpose `[]`; roles split `ai-providers` / `hosting-providers` (the diff now emits `recipient:<cat>:<role>` for a new role; v1→v2 widenings 20 → 33); per-language `covers` phrase map (French-only deletion fails naming fr + id); backfill `run day − (idle − 90)` (never future; `usageIdleDays` required to open undated rows); age paragraph `#age-signal` on both policies, tested against the real `age-check.ts`; "It stays on your phone" → "Whim doesn't send it anywhere" (consent en/fr, pages, store descriptions; version unchanged); providers: Google Cloud, OpenRouter (+ safety check, which runs through OpenRouter), "AI model providers OpenRouter picks" (mostly US), Zoho (US, inferred); legitimate interest for error details + lawyer-brief G6; "12 months after last use"; `/privacy/v1` (main's v1 page verbatim, Caddy route, smoke); preflight checks every `deploy/profiles/*` file; `docs/legal` privacy@ → support@; change-process §8 channels + notice log; effective dates 2026-09-27; handoffs legal-copy.md and disclosure-manifest.md rewritten. Owner checks: the provider contact URLs (general knowledge), model-provider countries, Zoho's data centre, email retention wording; tasks.md 11.1 still says privacy@ (planning text).
- fix-B report: STATUS complete, GATE PASS (+ knip, release check), commit `e45bf72e`. iOS reads `AppleLanguages` before `Intl` (a French iPhone with an en-only app bundle gets French; red-check on the old order); six suites pinned to en-US; Settings date `Intl.DateTimeFormat` month-short ("Sep 24, 2026" / "24 sept. 2026", ISO fallback); `under-13` (Android: SUPERVISED* and `ageUpper()` 0–12; iOS: gates 13 and 18, upper bound < 13) stored as `blocked`, message chosen from the fresh answer (`ageUnder13Title/Body` en+fr); review notes 4.7.5 "keeps only the outcome, on the device" (3,991 chars). Class-A: `held?: AgeHold` replaces `blocked`; with French active the Settings line mixes "On since" (English) with a French date. Found: `copy.ts:659` `toLocaleString()` without a locale makes run-signals fail on a fr_* machine (pre-existing; issue filed).
- fix-infra, fix-A, fix-B integrity: exit 0 each; merged in that order with no conflicts; combined regate-pass (the three were merged back to back, then gated once).
- gate-full (orchestrator, main tree, tip after the fix chains): FULL GATE PASSED (knip, guard:metro, the three Chromium invariant suites, openspec validate 45/45).
- closure: ruleset probe OK ("Protect main"); merged `origin/main` (`3e417a5`, request-envelope archive) into the staging branch (`4a29d0e`, clean, openspec valid); pushed; draft PR #90.
- iOS check (orchestrator, simulator iPhone 18 Pro, iOS 27, AppleLanguages fr-CA, Release build of the post-fix tip): fresh install → compose → French terms step ("Conditions d'utilisation", "Continue in English"); so fix-B's AppleLanguages-first order works with the en-only app bundle, and the age check (iOS 27 sim: not eligible → unavailable) lets the flow through. Screenshot `evidence/ios-fr-terms.png`.
- Sonar round 1 (PR #90): gate OK, 26 open issues (8 shell S7679/S7688 in deploy.sh + log-age-cap.sh; 18 TS minors). Decision: fix them anyway (owner standard: no lint left behind), as two parallel workers (shell, TS) rather than the per-finding fix-loop: all are mechanical and the PR gate is the red check. `openspec/critic/sonar-ledger.md` doesn't exist (runbook reference stale); findings kept in `findings-sonar-1.md`.
- Sonar round 1 fixes: shell `afa6f87` (S1, S2, S4–S9: locals for positional params, `[[ ]]`; one planted literal in `deploy-config.suite.ts` synced), TS `96bb065` (S3, S10–S26: Set, useState naming, `serverOverride(kv, {internalBuild})`, `replaceAll`, `includes`, merged pushes, `export…from`). Integrity 0 each; merged; regate-pass; knip clean. Pushed; re-polling.
- Sonar round 2 (PR #90): gate OK, 11 S7781 left — the rule wants string patterns, not `/x/g` regexes. Orchestrator made the 11-line edit itself on `chain/legal-surface-v2-sonar2` (too small for a dispatch; order preserved: `&amp;` decoded last, `&` encoded first); gate PASS; integrity 0; merged; regate-pass.
