# Dispositions ledger — fix-design-conformance

Append-only. Every disposition is recorded AS IT HAPPENS, never batched at the end.
This ledger plus the lane branches are the resume state if orchestrator context is lost.

## Run header

| Field | Value |
|---|---|
| Batch id | `fix-design-conformance` |
| Schema | `whim-fixloop` |
| Run type | STANDALONE (attended) |
| Staging branch | `integration/design-conformance` |
| Cut from | `redesign` @ `e18c1bf1ce837461a11b2f37dad52270c4c61554` |
| Closure target | **`redesign`** (not `main`) |
| Findings source | `findings.md`, 25 findings, owner rulings R1–R8 binding |
| Lanes | 6 (L1 serial, then L2–L6 as one parallel wave) |

## Preconditions

| Check | Result |
|---|---|
| `.claude/settings.json` → `worktree.baseRef: "head"` | PASS (line 38-40) |
| `scripts/gate.sh`, `gate-full.sh`, `fixloop.sh` present + committed clean | PASS |
| `git branch --list 'integration/*'` empty at run start | **FAIL → resolved**, see D3 |

## Deviations from the runbook (recorded, owner-approved)

**D1 — lane grouping instead of per-finding dispatch.**
The runbook dispatches one `fix-worker` per finding. This batch dispatches one per *file-owned
lane*. Rationale: 9 of 25 findings are in `HistoryScreen.tsx` and 7 more consume
`design-tokens.ts`; 23 concurrent worktrees would contend on ~6 files, and the per-finding
isolation the runbook buys would be spent on merge conflicts instead. Each lane's allowlist is
disjoint from every other lane's, which preserves the property the runbook actually depends on
(no two parallel writers touch one file). Cost accepted: a lane is reverted as a unit, so a
single bad finding inside a lane takes its siblings with it.

**D2 — staging branch cut from `redesign`, closure targets `redesign`, not `main`.**
Owner-approved. `redesign` is 153 commits ahead of `main`; `main` is 0 ahead of `redesign`.
Every finding cites line numbers in code that exists only on `redesign`, so a staging branch cut
from `main` would fail the stale-check on all 25. The harness's "`main` is the published branch"
rule is suspended for this run only — `redesign` is the effective publish target while it is
unmerged. One ratified merge per run still holds; the target differs.

**D3 — two stale `integration/*` branches deleted at run start.**
`integration/fix-generate-stream-transport` (`04478c8`, 2026-08-01) and
`integration/fix-phase0-obs` (`e18c1bf`, 2026-08-02). Both verified fully merged into `redesign`
via `git merge-base --is-ancestor` before deletion; deleted with the safe `-d` form, which would
have refused otherwise. Leftovers from completed runs whose post-merge teardown never ran — not
evidence of a concurrent run. No parked `wip/` branches, no pending grants.

**D4 — Class-2 protection is NOT enforced on this branch.**
`.claude/hooks/protect-harness.sh` is present and executable, but no `PreToolUse` hook is wired
in `.claude/settings.json` or `.claude/settings.local.json`. Confirmed with the owner. Practical
effect on this run: nil — no lane declares a protected file, and `src/sdk/design-tokens.ts` is
ordinary source. Recorded because the `integrity` check's exit-3 tamper signal is the only thing
still guarding protected paths here, so it must not be waved through if it fires.

## Ledger

| # | UTC | Event | Lane | Detail |
|---|---|---|---|---|
| 1 | 2026-08-05 | `run-start` | — | staging branch cut from `redesign` @ `e18c1bf` |
| 2 | 2026-08-05 | `bookkeeping` | — | change created, `findings.md` / `plan.md` / ledger initialized |
| 3 | 2026-08-05 | `planners-dispatched` | L1–L6 | six read-only planners, no worktrees created |
| 4 | 2026-08-05 | `plan-received` | L3 | 6 findings. V4/V5/V6×3/V7/S3 actionable; **S4 = NO-OP** (both sites already on `TYPE_SCALE.body`, inherit L1's retarget). All structural → no test. Allowlist: 4 files, disjoint. Severity med/low |
| 5 | 2026-08-05 | `ruling-added` | — | **R9** (spacing literals are not R2's concern) and **R10** (a call site already on the right role needs no edit) appended to `findings.md`, prompted by L3's analysis. Binding on all lanes |
| 6 | 2026-08-05 | `plan-received` | L2 | 4 findings. V1/V2/S2 actionable; **V3 BLOCKED** — needs `size?: number` on `AppTileProps` in `app-tile.tsx`, owned by L5. Planner escalated instead of crossing the lane boundary (correct). All structural → no test |
| 7 | 2026-08-05 | `reorder` | L2 | **Wave order revised: L1 → [L3, L4, L5, L6] → L2.** L2 leaves the parallel wave and follows L5. Rationale: L5's B3 (120×120 done tile) and L2's V3 (fluid grid) need the same optional `AppTile` size prop — one piece of work, so L5 writes the contract and L2 consumes it. Cross-lane contract recorded in `plan.md`. Surfaced at planning time, read-only, before any worktree existed |
| 8 | 2026-08-05 | `plan-received` | L1 | 7 role changes + header amendment, all LIVE at HEAD. Test class: **STANDING INVARIANT** (first non-structural in the batch) — pins the four microcopy roles stay four distinct faces + every face names a shipped font asset. `body` resolved to 13.5/20.925 from a 3-vs-2-vs-1 instance count in the mockups, taking the top of R5's range on legibility grounds. Flags an unfixable asset gap: `kindBadge` wants mono 600, only Regular/Medium ship |
| 9 | 2026-08-05 | **`ruling-corrected`** | L1 | **R11 supersedes R3's `screenTitle` clause.** The planner enumerated the token's call sites and found the 26 → 22 retarget would REGRESS three currently-correct screens (clarify `:443`, plan `:475`, build `:498` all specify 26 in the mockups). Only V8/V13 were ever filed against it. Root cause is V10's defect one level up: one token serving two design roles, and a retarget picks a winner rather than splitting. Resolution: `screenTitle` → 22 scoped to history + confirm sheet; **ADD `stepTitle` 26/29.9/-0.65/700**, carrying `screenTitle`'s current numbers so every repointed site renders byte-identically. Orchestrator error caught at planning time, before any code was written |
| 10 | 2026-08-05 | `ruling-added` | L2 | **R12** — `SettingsScreen.tsx` had no mockup and no lane owner, yet consumed both retargeted tokens. It would have inherited both silently, unreviewed. Folded into L2 |
| 11 | 2026-08-05 | `ruling-added` | — | **R13** — the `body` retarget shrinks `TextInput` content and carets (`ComposeStep.tsx:81`, `SettingsScreen.tsx:72`), not just static prose. Lands per R1, added to the R4/R5 deferred list as its own question |
| 12 | 2026-08-05 | `plan-received` | L6 | 1 finding, LIVE. Test class: **BEHAVIORAL** — the batch's first. Extracts `orbRowGlyphColor` as a pure exported function so the decision logic is assertable without rendering RN, matching the suite's existing idiom for a component that embeds native modules. Clean import-boundary RED at HEAD. Severity low |
| 13 | 2026-08-05 | **`finding-split`** | L6 | **R14 — V15b (per-row direction hint) DROPPED as a non-defect.** Three converging reasons: `Orb.tsx:11-14` already lists it as a deliberate D12 negative requirement alongside the "hold to flick" caption that `findings.md` itself files under *Not flagged*; the existing green test `orb-menu.suite.ts:99-102` asserts no `up\|down\|left\|right` literal in that file, so building it breaks a standing invariant immediately; and with the wheel deferred the hint would advertise a gesture that does not exist. V15a (the glyph swatch) proceeds. The auditor had merged two items onto one line |
| 14 | 2026-08-05 | `ruling-added` | L6 | **R15** — the two Orb tints and the 10px icon radius stay local to `Orb.tsx`, scoped as `KIND_BADGE_COLORS` is. One consumer does not justify a shared token. Consistent with R9 |
| 15 | 2026-08-05 | `flag` | L6 | `⌂` (U+2302, the home glyph) needs Android glyph-coverage verification — carry it into the R8 on-device pass |
| 16 | 2026-08-05 | `plan-received` | L4 | 10 findings, all LIVE. **Severity HIGH** — the batch's only lane whose blocking finding adds structure rather than swapping a value. V8 and S4 resolve to **NO-OP** (already on the right role, inherit L1). Ten of ten structural-no-test, honestly argued: `HistoryScreen.tsx` is never rendered under Node (`history-ui.spec.md:9`), so no layout claim is behaviorally assertable, and a source-grep on the token names would encode the patch. Allowlist: one file. B2 uses per-row line segments rather than a list overlay — an overlay cannot scroll with a `FlatList` without measurement and breaks when a filter changes the row count |
| 17 | 2026-08-05 | `finding-corrected` | L4 | **R17** — V10's line list is wrong. `:365` is `caption`, not `eyebrow`, with no design counterpart; `:323` (kind badge) is an `eyebrow` site the list omits. Six sites, not five. `:349` is character-identical to `:329`'s design declaration (html:42 vs :38), so both take `metaWide` — which is why three roles cover four sites |
| 18 | 2026-08-05 | `ruling-added` | L4 | **R16** — four residuals ACCEPTED without minting roles (`:383` marker, toast face, `:449` label, sheet-title line-height). Each is within ~2px of a legitimate role and already sits in R4/R5's deferred territory; adding roles the R8 pass may overturn is churn. Consistent with R9/R15 |
| 19 | 2026-08-05 | `flag` | L4 | **R18** — B2's restructure must preserve three literal substrings that `history-logic.suite.ts:284-289` asserts against `HistoryScreen.tsx` **as source**. Pre-existing invariant (expand-not-restore, confirm-gated restore, fork-from-viewed-version), not a batch artifact. Worker checkpoints `npm run launcher:test` immediately after B2, before any other edit |
| 20 | 2026-08-05 | `flag` | L4 | No SVG library exists and a dependency add is not grantable, so V9's chevron is built from a rotated two-border view. Border line-caps are square where the design specifies round — an accepted, unavoidable fidelity gap. V9 also adds `accessibilityLabel={COPY.backLabel}`: dropping the visible word "Back" without it would silently un-name the control for a screen reader |
| 21 | 2026-08-05 | `plan-received` | L5 | 5 findings, all LIVE. **All six lane plans now in.** Mixed test classes: B4 **behavioral** (`buildProgressFraction` is a pure function in a Node-importable module), B3 + V16 **standing invariant** via the source-assertion idiom `tile-colour.suite.ts` already uses for RN-importing files, S6 + the rise animation **structural-no-test**. Severity: B3/B4 high, V16 med, S1/S6 low |
| 22 | 2026-08-05 | `finding-widened` | L5 | **R19** — V16 names only `secondary` (46), but design puts BOTH CTAs at 52 and `primary` is 54, also off-spec and unfiled. Fixing one leaves the pair mismatched, which is the visible defect. Both move to 52. No scope expansion beyond `DoneStep.tsx` |
| 23 | 2026-08-05 | **`open-question`** | L5 | **R20** — the done tile renders in `STATUS_COLORS.done`, not the app's own hue. Implemented per R1 (mockup writes a bare literal where every sibling value is a `{{ }}` binding; `RESERVED_TILE_HUES` forbids any app resolving to that hue). **NOT settled** — it means the app appears teal on done and in its own colour on the grid moments later. Carried to R8 as an explicit question, not a checkbox. `app-tile.tsx`'s header comment becomes false and is updated in the same edit |
| 24 | 2026-08-05 | `allowlist-grant` | L5 | **R21** — `src/host/launcher/prompt-flow.ts` granted to L5. Unclaimed by any lane; L3's allowlist verified not to include it. `BuildStep.tsx` imports `react-native` at module scope so it is not Node-importable (`tile-colour.suite.ts:5-7`); `prompt-flow.ts` is, making it the only home for a real test of the fraction. Derivation stays honest — 12.5/37.5/62.5/87.5, never fabricates 100% |
| 25 | 2026-08-05 | `stale-check` | L1 | `fixloop.sh stale` exit 0 — evidence present at HEAD, 6 lines. Finding live |
| 26 | 2026-08-05 | `dispatched` | L1 | fix-worker launched, `isolation: worktree`. **BASE = `ff6b208`** (staging tip). Spec carries `stepTitle` folded in, which L1's plan predates — R11 was ruled after that plan was written |
| 27 | 2026-08-05 | `fix-complete` | L1 | worker green, self-gate PASS, commit `43d8505` on `worktree-agent-aa25eb78c2094bdef`. `body` landed 13.5/20.925 (confirms L3 + L4 no-ops); `stepTitle` confirmed byte-identical to pre-edit `screenTitle` |
| 28 | 2026-08-05 | **`false-tamper`** | L1 | `integrity` first returned **exit 3** naming `.claude/settings.json`, `scripts/gate.sh`, `scripts/gate-full.sh` — while `git diff --stat` showed only the two allowlisted files. Cause: **orchestrator error, not a scope violation.** `scripts/fixloop.sh:46` defaults `INTEGRATION_BRANCH="${FIXLOOP_INTEGRATION_BRANCH:-main}"` and `base_of()` (`:78`) computes the baseline as `merge-base <branch> $INTEGRATION_BRANCH`. The var was never exported, so the baseline fell back to `main` — 153 commits behind `redesign` — sweeping every commit that ever touched those files into the "diff". Re-run with the correct value: **exit 0, INTEGRITY OK**, two files vs BASE `ff6b208` |
| 29 | 2026-08-05 | **`standing-trap`** | — | **Every `fixloop.sh` invocation in this batch MUST be prefixed `FIXLOOP_INTEGRATION_BRANCH=integration/design-conformance`.** Without it the tool silently baselines against `main` and reports false tamper. Deviation D2 (staging cut from `redesign`, not `main`) is precisely what makes the default wrong here — the two interact, and the failure mode is a scary-looking exit 3 rather than an error. Applies to `stale`, `integrity`, `redcheck`, `gatefull`, `park`, `finish` |
| 30 | 2026-08-05 | `gate-full-refused` | L1 | `gatefull` refused: primary tree dirty. Its check (`:227`) is `git diff --quiet && git diff --cached --quiet` — **tracked files only**, so the untracked `whim-design-handoff/` is irrelevant. Cleared by committing this ledger and stashing an unrelated pre-existing `.gitignore` modification (comment-line removals, not this batch's work, preserved in the stash rather than absorbed into a commit that would misattribute it) |
| 31 | 2026-08-05 | `gate-full-FAIL` | L1 | First `gatefull` failed on `synthetic-run` + `generation-e2e`. Both PASS standalone on the staging tip without L1, so not pre-existing — and a type-scale edit cannot plausibly break a Chromium/server E2E |
| 32 | 2026-08-05 | **`orchestrator-error`** | L1 | Cause found: the `reviewer` was dispatched **concurrently** with `gatefull`. `gatefull` checks the branch out **into the primary tree** (Metro cannot resolve `node_modules` from a worktree), while the reviewer re-bundled a mutated copy of `src/` and wrote `.audit-run.tmp.mjs` into that same tree. Two processes on one working tree, one swapping the checked-out branch under the other. `synthrun` and `server:e2e` are the heaviest and last-running stages, so the most exposed. Re-run with nothing else touching the tree: **FULL GATE PASSED**, `PASS: synthetic-run`, `PASS: generation-e2e` |
| 33 | 2026-08-05 | **`standing-rule`** | — | **`gatefull` requires EXCLUSIVE use of the primary working tree.** While it holds the tree, run NOTHING else against the repo — no reviewer, no second gate, no suite, no `git` operation. This is stronger than it first appears: a reviewer running during a `gatefull` reads files from a tree that has *another branch* checked out, so it would silently audit the wrong content and report a confident, wrong verdict. Per lane, VERIFY (step 5) and FULL GATE (step 6) must be **serialized**, never parallel. Merges were already serialized by the runbook; this extends the same discipline to the checks |
| 34 | 2026-08-05 | `gate-full-PASS` | L1 | Re-run clean at `43d8505` vs BASE `ff6b208`. All stages green: build, typecheck, lint, 7 Node suites, static-checks, corpus-eval, sonar ingestion, fixloop preflight, bash policy, compound unroller, Codex adapters, scaffolding tripwires, knip, metro-guard (1,995,092 bytes), invariants, synthetic-run, generation-e2e, openspec 38/38. Tree restored to `integration/design-conformance` |
| 35 | 2026-08-05 | **`verify-REJECT`** | L1 | Reviewer REJECTED. All eight numeric values independently re-derived from the design HTML and CORRECT; `stepTitle` confirmed byte-identical to pre-edit `screenTitle`. Two MED defects, both documentation-truth — the same failure class L1 was fixing. (a) The header claims `stepTitle` has no README row; `README.md:86` IS that row, so a reader would 'restore' `screenTitle` to 26 and revert R11. (b) The font assertion checks `TYPE_SCALE` against `FONT_FAMILY` — same module — while its comment promises the asset ships; proven by a mutant battery (6 mutations, 5 caught, 1 escaped: adding `IBMPlexMono-SemiBold` to `FONT_FAMILY` passes green with no `.ttf` on disk). Plus one LOW miscitation (`:41` is 13px, not 13.5). Revision 1 of 2 sent |
| 36 | 2026-08-05 | `note` | L1 | Reviewer's finding 4 (untracked `.gitkeep` in the worktree) is NOT a defect — the runbook requires a worker to pin its tree with one as its first action. Told the worker to keep it |
| 37 | 2026-08-05 | `fix-revised` | L1 | Revision 1 landed as `bf9825a`, kept as a SEPARATE commit (not amended) so the corrections diff alone. Fast gate PASS. Worker verified nothing but comments changed since the reviewed commit — `git diff` over `design-tokens.ts` matches zero lines containing `fontSize\|lineHeight\|letterSpacing\|fontWeight`. Corrected a THIRD site beyond the two filed (the `stepTitle` entry comment), so a reader who never scrolls to the header still gets the truth |
| 38 | 2026-08-05 | `integrity` | L1 | exit 0, INTEGRITY OK — two files vs BASE `ff6b208` |
| 39 | 2026-08-05 | **`verify-ACCEPT`** | L1 | Reviewer ACCEPTED. Verified by construction in an isolated scratchpad replica, not by reading claims. Six mutants: E (add `IBMPlexMono-SemiBold` to `FONT_FAMILY`) now **RED**, was GREEN at `43d8505`; F (raw-string bypass of `FONT_FAMILY`) **RED** — and `typecheck` would NOT catch it since `TypeFace.fontFamily` is `string`, so the suite is the only guard; G (empty fonts dir) **RED** via the new `onDisk.size > 0` guard; H (unrelated .ttf) **RED**; I (`kindBadge.fontWeight` 600→500) **GREEN**, correctly abstaining from the R4/R5-deferred numbers; J (no ancestor found) **RED** and diagnosable, `fail(): never` so the walk cannot spin. Worktree resolution proven empirically (K3: a marker `.ttf` in the worktree only, run from a parent lacking it → PASS). All ten `TypeFace` literals byte-identical to `43d8505`; `stepTitle` still byte-identical to `screenTitle` at `ff6b208` |
| 40 | 2026-08-05 | **`follow-up`** | L1 | LOW residual, NOT fixed — reviewer explicitly advised against spending the lane's last revision on it, and the orchestrator agrees. `findRepoRoot` requires `package.json` AND `assets/fonts` at the same level, so if the fonts directory is **absent entirely** the walk steps over the tree under test and validates against a parent tree's fonts, passing GREEN (mutant K). The `onDisk.size > 0` guard catches an *empty* directory but not an *absent* one, and git does not track empty directories — so 'delete the font assets' is exactly the escaping shape. Reachability low; no lane in this batch touches that directory. **Hardening for a later lane:** stop the walk at the first ancestor holding `package.json` and require `assets/fonts` THERE, failing loudly if missing, instead of continuing upward. Noted by the reviewer: the cruder repo idiom this code cites as precedent (`path.join(process.cwd(), …)`, `history-logic.suite.ts:285`) would have thrown ENOENT here rather than passing |
| 41 | 2026-08-05 | `gate-full-refused` | L1 | Second refusal, orchestrator error again: rows 31-36 of THIS ledger were appended but never committed, leaving the tracked tree dirty. Same class as row 30. Bookkeeping writes must be committed before every `gatefull`, not merely written |
| 42 | 2026-08-05 | `gate-full-PASS` | L1 | Clean at `bf9825a` vs BASE `ff6b208` — **217 PASS stages**. Tree restored |
| 43 | 2026-08-05 | **`merged`** | L1 | `--no-ff` onto `integration/design-conformance`. Staging tip **`3a64941`** |
| 44 | 2026-08-05 | `regate-pass` | L1 | `gate.sh` on the merged tip: **FAST GATE PASSED**. The wave (L3, L4, L5, L6) is unblocked |
| 45 | 2026-08-05 | `teardown` | L1 | Worktree removed and branch deleted, UNSANDBOXED per `docs/harness.md` §11 — sandboxed it strands a `.git/config` section nothing can prune. `git worktree list` back to the primary tree alone |
| 46 | 2026-08-05 | `false-alarm` | L1 | Post-teardown the IDE reported 8 TS errors in `theme.acceptance.ts`, including `Cannot find module '../theme'`. **Stale LSP state pointing at the deleted worktree copy.** Verified against the primary tree rather than assumed: `tsc --noEmit` clean, `sdk:test` → `SDK theme (v2) acceptance: PASS`, both files present, import resolves |
| 47 | 2026-08-05 | **`ruling-REVERSED`** | L5 | **R20 reversed by the owner: the done tile renders in THE APP'S OWN HUE, not `STATUS_COLORS.done`. SETTLED — no longer an R8 question.** Continuity of app identity across two adjacent screens beats the mockup's bare literal; a prototype hardcodes whatever colour its example app had, so the binding pattern was evidence about the prototype's plumbing, not product intent. Consequences for L5: fill stays `tileColor(name, manifest)` and needs no `STATUS_COLORS` import; the glow becomes `shadowColor: <the tile's own resolved colour>` at `shadowOpacity: 0.3`, which is what 'colour-matched' meant; **`app-tile.tsx`'s header comment is CORRECT and must be left alone** — the earlier instruction to rewrite it as false is WITHDRAWN. Rest of B3 (120×120, radius 32, rise-in, no name label) unchanged |
| 48 | 2026-08-05 | `owner-request` | — | The pre-existing `.gitignore` modification (comment-line removals) is committed alongside the batch at the owner's direction, rather than kept stashed as unrelated work |
| 49 | 2026-08-05 | `stale-check` | L3,L4,L5,L6 | All four exit 0 — evidence present at HEAD, findings live (6/5/5/2 lines respectively) |
| 50 | 2026-08-05 | `dispatched` | L3,L4,L5,L6 | Wave launched, four `fix-worker`s with `isolation: worktree`. **BASE = `f4eee30`** for all four. Allowlists verified mutually disjoint. Each spec carries the R11 `stepTitle` repoints for ITS files — L3 owns `ClarifyStep:58` + `PlanStep:79`, L5 owns `BuildStep:55` + `DoneStep:33`; missing one is a silent 4px regression, so they are named explicitly rather than left to inference |
| 51 | 2026-08-05 | `plan` | — | **Scheduling for the wave.** `integrity` is cheap and parallel-safe. Reviewers are parallel-safe with each other (all instructed to keep scratch under the session scratchpad, never in the repo) but NOT with a gate. `gatefull` is EXCLUSIVE per rule 33, so the four full gates must run one at a time — that is the wave's bottleneck, and it is a correctness constraint, not a tuning choice |
| 52 | 2026-08-05 | `fix-complete` | L3 | `4d61b3a`, self-gate PASS. Both R11 `stepTitle` repoints landed (`ClarifyStep:58`, `PlanStep:79`). S4 verified as a genuine ZERO-diff no-op at both sites. Literals 24/28/26 introduced with html line citations per R9; Compose needed none (`SPACING.xl` is exactly 34) |
| 53 | 2026-08-05 | `integrity` | L3 | exit 0 — four files vs BASE `f4eee30` |
| 54 | 2026-08-05 | `verify-dispatched` | L3 | Reviewer given one check the plan did not have: grep for any SURVIVING `TYPE_SCALE.screenTitle` in the four files. A missed repoint typechecks, lints, passes every suite and renders 4px small forever — 'did it happen everywhere' is the question that matters after a token split, and only the reviewer can answer it |
| 55 | 2026-08-05 | **`class-B-block`** | L6 | Worker STOPPED rather than improvising. The prescribed test mechanism is impossible: `run.mjs` esbuild-**bundles** the suite, so importing `orbRowGlyphColor` from `Orb.tsx` drags in the whole module graph and `react-native`'s Flow source fails to parse. **Measured, not asserted** — probe output quoted (`NativeComponentRegistry.js:11:12`, `index.js:27:7`). Exporting a pure function from a `.tsx` does not make it importable; bundling is whole-module. `prompt-flow-wiring.suite.ts:9` already records the same limitation. Orchestrator spec error: the L6 plan correctly noted the source-assertion idiom exists BECAUSE the file is not importable, then proposed importing from it anyway |
| 56 | 2026-08-05 | `unblocked` | L6 | **Option A**: `src/host/launcher/orb-actions.ts` added to the allowlist; the three symbols live there and `Orb.tsx` imports them beside `ORB_ACTIONS`. Chosen over a new `orb-row-style.ts` because the suite already imports that module, it adds no file for knip, and the symbols belong beside `ORB_ACTIONS` on the merits |
| 57 | 2026-08-05 | **`ruling-clarified`** | L6 | **R15 corrected by the worker, accepted.** R15 cited `KIND_BADGE_COLORS` as a 'stays local' comparator, but that constant actually lives in `src/sdk/design-tokens.ts` and is *scoped* to one consumer rather than physically local. So **R15 protects single-consumer scoping, not file location** — moving the Orb values into `orb-actions.ts` honours it rather than bending it. The binding part is unchanged: they must not become `SHELL_COLORS`/`RADIUS` roles, and `design-tokens.ts` stays untouched |
| 58 | 2026-08-05 | `memory-proposal` | L6 | Queued for end-of-run owner ratification: RN components under `src/host/**` can NEVER be imported by the Node acceptance suites, even for a pure exported helper, because `run.mjs` bundles the whole graph and `react-native`'s Flow source fails to parse. Pure logic must live in a non-RN sibling module (the repo idiom: `tiles.ts`, `manifest-tile-color.ts`, `whim-prose/styles.ts`). Belongs in `whim-test-classification.md` |
| 59 | 2026-08-05 | **`verify-REJECT`** | L3 | Reviewer REJECTED `4d61b3a`. **V6 overshot by 16px on all three screens** — design header is `padding:16px 22px 0` (bottom ZERO, html:412/438/470), but `flow-chrome.tsx:88` kept `paddingBottom: 16`, shipping 50/44/42 against a design of 34/28/26. The fix INVERTED the defect ('half the breathing room' became 1.5x) and recorded nothing. Everything else verified clean: all three V6 values in their correct files, all three R9 citations true, both R11 repoints landed, **zero surviving `TYPE_SCALE.screenTitle`**, S4 a genuine zero-diff no-op |
| 60 | 2026-08-05 | `ruling-added` | L3 | **R22** — header padding is part of V6 (`paddingBottom: 0`, `paddingTop: SPACING.md`), plus `primary.height` 54 → 52 folded in from the reviewer's worth-noting list, because R19 moves DoneStep's CTAs to 52 and a 54/52 split across adjacent screens is the exact mismatch R19 prevents |
| 61 | 2026-08-05 | `fix-revised` | L3 | `7173f9c` on top of `4d61b3a`, fast gate PASS. Effective gap now 34/28/26 exactly. Worker re-verified all six cited html lines and independently re-confirmed the `flow-chrome` import set — also surfacing `prompt-flow-screens.suite.ts:282`, which reads `ClarifyStep.tsx` as SOURCE and would break silently on a reformat |
| 62 | 2026-08-05 | `fix-complete` | L6 | `b683a55`, fast gate PASS, Option A landed. Worker verified its own red-check by stashing only the prod files and observed the precise import-boundary error |
| 63 | 2026-08-05 | **`false-RED`** | L6 | `redcheck` first reported RED for an orchestrator usage error. `fixloop.sh:183` is `if ( cd "$wt" && "${testcmd[@]}" )` — it EXECUTES the test argument as a command. A file path was passed, the shell tried to run a `.ts` file, got `Permission denied`, exited non-zero, and the script read that as 'test failed'. **It would have reported RED for any input whatsoever.** Re-run with `npm run -s launcher:test`: genuine RED via an esbuild import-resolution failure. **Standing trap: the `<TEST...>` argument must be a runnable COMMAND, never a path** — third `fixloop.sh` usage trap this run, after the `FIXLOOP_INTEGRATION_BRANCH` default and the clean-tree precondition |
| 64 | 2026-08-05 | `fix-complete` | L5 | `d6333ae`, fast gate PASS, `launcher:test` 3104 checks. Three test classes as specified: B4 behavioral, B3/S1/V16 standing-invariant, rise animation + S6 structural-no-test |
| 65 | 2026-08-05 | **`spec-contradiction`** | L5 | **R24** — the orchestrator's spec asserted `buildProgressFraction(null,false) → 0` while the verbatim body it also supplied yields **0.125** (`activeBuildStepIndex(null,…)` is 0, same as `'plan'`, so step 1 already reads `active`). Worker implemented the body, asserted 0.125, and argued special-casing null to 0 would render an empty bar beside an already-active first step. Accepted — it is right, and it caught a self-contradiction in the instructions rather than silently picking a side |
| 66 | 2026-08-05 | **`contract-mismatch`** | L5→L2 | **R23** — L5 delivered `size?: 'done'`, a string-literal VARIANT. L2's fluid 3-up grid needs a numeric width, which that prop cannot express; `plan.md` had predicted `size?: number`. Resolution: **two orthogonal props**, `size?: 'done'` for the variant and a new `width?: number` for the dimension. NOT `size?: number \| 'done'` — one prop meaning both 'which variant' and 'how wide' reads fine at the call site and rots at the definition. L2's allowlist gains `app-tile.tsx`; safe because the wave order already puts L2 after L5, so no two lanes hold that file at once |
| 67 | 2026-08-05 | `integrity` | L3,L5,L6 | All exit 0 against BASE `f4eee30` — 4, 6 and 3 files respectively |
| 68 | 2026-08-05 | `verify-dispatched` | L3,L4,L5,L6 | Four reviewers concurrent, all instructed to keep scratch out of the repo per rule 33. L6's reviewer additionally asked to judge whether the direction-hint invariant's scan set should widen to `orb-actions.ts` — the three symbols moved into a file that check may not cover, which would be a hole opened by the fix |
| 69 | 2026-08-05 | **`verify-ACCEPT`** | L3 | Reviewer ACCEPTED `7173f9c`. Re-derived the arithmetic with the new header values: shipped gaps now exactly 34/28/26 against html 416/442/474. All six cited html lines read directly and confirmed. Per-step `content.paddingTop` values byte-identical to the already-approved commit — no regression hid inside the fix. `prompt-flow-screens.suite.ts:282`'s source literal intact |
| 70 | 2026-08-05 | `follow-up` | — | **F1** recorded: the CTA sits 18px low on all three flow screens (design puts 16px above it with content bottom at 0; RN has `content.paddingBottom: 34` and no `marginTop` on `primary`). Pre-existing at BASE, named by no finding, NOT folded into L3. Same defect shape as V6 one element lower in the same file — the audit caught the 16px instance and missed this one |
| 71 | 2026-08-05 | **`verify-ACCEPT`** | L4 | Reviewer ACCEPTED `b979646`, zero must-fix, revision cap untouched. Geometry re-derived exactly (dot top edge 16, x-span [8,19] centre 13.5, ring 19→22 — all matching the design byte-for-byte). Hex scan over added lines returned `#fbfaf8` and `rgba(13,148,136,.3)` **both inside comments** — no second literal entered the code. Eyebrow grep: exactly one survivor, `:430`, the intended R16 residual. R18's three substrings intact character-for-character |
| 72 | 2026-08-05 | **`yoga-adjudicated`** | L4 | **The on-device risk the worker flagged is RESOLVED, not deferred.** The reviewer read the pinned `ReactCommon/yoga/yoga/algorithm/AbsoluteLayout.cpp` (RN 0.85.3): with an inset defined, `positionAbsoluteChild:185-217` computes `inset + border + margin` and **never consults padding**; the padding-adding path is `justifyAbsoluteChild`/`alignAbsoluteChild` for INSETLESS children, gated by `Errata::AbsolutePositionWithoutInsetsExcludesPadding`. So `{top:0,bottom:0}` spans `rowWrap`'s full border-box height including the 10px `paddingBottom`; segments abut deterministically. **Off the on-device list.** It also declined to demand the alternative construction — spanning a sibling's margin would be genuinely outside any node's box, i.e. strictly worse |
| 73 | 2026-08-05 | `fix-revised` | L4 | `e120b06` on top of `b979646`, comment-only (11+/4-, one block), gate PASS. Narrows the version-dependence claim to insetless children, cites the Yoga file and errata constant by name, and records honestly that `marginLeft: 26` was unnecessary (`paddingLeft: 26` renders identically) but is kept because it is not wrong. Folded back despite the ACCEPT because a comment asserting more risk than exists is the same defect class that got L1 rejected twice — and it was the sole recorded justification for a design choice |
| 74 | 2026-08-05 | **`verify-REJECT`** | L6 | Reviewer REJECTED `b683a55`. Production code correct; both revisions confined to the test file. (a) **The R14 direction-hint invariant scans ONE file.** `orbCode` is built from `read('Orb.tsx')` alone — `MiniAppView.tsx` is read elsewhere, in a different test, so the worker's characterization was wrong. `orb-actions.ts` is outside the scan, and this diff moved the per-row presentational table there, so `ORB_ROW_DIR = {change:'up',…}` would now defeat R14 with the invariant still green. (b) **The test's messages overclaim** — all three say 'from a token, never an inline hex', but returning `'#3f3d8f'`/`'#1c1917'` passes, since the tokens are byte-equal. Naming a guarantee the code does not provide is the tautology defect class again |
| 75 | 2026-08-05 | `memory-proposal` | L4 | Queued for owner ratification: RN 0.85 Yoga positions an absolute child that DEFINES its insets as `inset + containing-node border + margin` — padding never consulted, no errata gate. Padding enters only for insetless absolute children (`Errata::AbsolutePositionWithoutInsetsExcludesPadding`). So `{top:0,bottom:0}` spans a parent's full border-box height including padding. Suggested slug `rn-yoga-absolute-inset-padding.md` |
| 76 | 2026-08-05 | `fix-revised` | L6 | `3e4058a`, test-file only, gate PASS. **Hole closure verified EMPIRICALLY, not by inspection** — inserted `ORB_ROW_DIR` into `orb-actions.ts`, observed `1 failed — no literal direction-hint strings in orb-actions.ts`, reverted. Green on its own code. Necessary wrinkle found: `orb-actions.ts` documents the unbuilt gesture in JSDoc blocks, so the `//`-only stripper had to become block-aware or the widened assertion would fire on correct prose |
| 77 | 2026-08-05 | `verify-dispatched` | L6 | Re-review focused on the **shared comment stripper**, not the widening. It feeds `orbCode`, which four assertions depend on — a change made for one silently altered the input to three others, and narrowing an assertion's input never makes it fail, so the suite would go MORE green while getting LESS useful |
| 78 | 2026-08-05 | **`verify-ACCEPT`** | L5 | Reviewer ACCEPTED `d6333ae`. Default `AppTile` render verified byte-identical site by site; R20-as-reversed confirmed on all three checks; R11 repoints landed with zero surviving `TYPE_SCALE.screenTitle`. **R21 proved by construction**: `activeBuildStepIndex` returns `[0,3]` for every input, so the fraction is always `(activeIndex+0.5)/4` and **1.0 is unreachable including the terminal case**; all four values exactly representable in binary FP, so the equality cannot go flaky. The V16 test independently judged materially better than the tautology it replaced |
| 79 | 2026-08-05 | `spec-error-confirmed` | L5 | R24 independently upheld from source: `prompt-flow.ts:286-289` maps `null` and `'plan'` to the same index 0 **by design** (`:285` — 'an unstarted stream sits on the first'), and at that moment `BuildStep.tsx:76-79` renders step 1 accent-ringed at full opacity. A 0-width bar beside an already-active step would have the screen contradicting itself. My `→ 0` line was the error; the verbatim body was right |
| 80 | 2026-08-05 | **`folded-in`** | L5 | Three corrections despite the ACCEPT. **MED: the Android glow is not the design's glow** — `shadowOffset`/`Opacity`/`Radius` are iOS-only, so on the only target platform an elevation-8 tinted shadow ships instead of `0 8px 22px rgba(hue,.3)`, while `tile-colour.suite.ts:97` asserts '30% over a 22 blur' from literals Android ignores. RN 0.85 ships `boxShadow` + `processBoxShadow.js` which translates the CSS literally. **Fourth instance in this batch of a test naming a guarantee it cannot detect** (L1's font check, L6's token-vs-hex wording, this). LOW: `Easing.ease` is CSS `ease-in` (`bezier(0.42,0,1,1)`), not the design's `ease` — the tile accelerates in where the design decelerates; repo precedent translates faithfully at `Orb.tsx:58`. LOW: constant-true condition at `prompt-flow.ts:310` |
| 81 | 2026-08-05 | `recorded-only` | L5 | `Animated.View` forces `collapsable: false` unconditionally (`createAnimatedPropsHook.js:227-232`), so every grid tile now materializes a native view where it was previously flattened on Android. Yoga computes on the shadow tree regardless — no layout or pixel consequence. Logged so it is not re-derived later as a regression |
| 82 | 2026-08-05 | `handoff-fixed` | L2 | `plan.md`'s cross-lane contract was STALE — still predicted `size?: number`. Rewritten to the delivered interface per R23, plus an explicit trap: `tile-colour.suite.ts:78-86` pins JSX SOURCE TEXT that L2 will edit (four `isDone ? styles.X : null` names each required exactly once, and the name-label guard). Adding `width?: number` is safe, but any reformat of those style arrays goes red for a correct edit. Same class as R18 |
| 83 | 2026-08-05 | **`verify-ACCEPT`** | L6 | Re-review ACCEPTED `3e4058a`. Everything REPRODUCED in a scratchpad replica, not read: the `ORB_ROW_DIR` injection is CAUGHT by the widened check and MISSED by the old one; `\bcount\b` on stripped `orb-actions.ts` really does hit `COUNT_KEY_PREFIX`, so leaving it component-scoped was right; block-stripping really was required (the header JSDoc lines start with `*`, unreachable by a `//` filter). **My stripper concern was justified — behaviour DID change on `Orb.tsx`** (three `OrbProps` JSDoc lines now stripped) — but all ten dependent regexes verified identical in outcome, so it is inert. Honest strength report: the check is literal-matching; unquoted keys, concatenation, `\u` escapes, `const enum`, and numeric cardinals (`ORB_ROW_ANGLE = {change:-90,…}`) all evade. Judged the right scope for a source-grep invariant; recorded rather than assumed stronger |
| 84 | 2026-08-05 | `fix-revised` | L5 | `e38e845`, gate PASS, 3106 checks. **`boxShadow` proven to render on Android by source trace**, not assumption: `ReactNativeStyleAttributes.js:44` (processed attribute), `BaseViewManager.java:843` (`@ReactProp` on the base view manager), `BackgroundStyleApplicator.kt:390` (gates on FABRIC; `newArchEnabled=true` here), `OutsetBoxShadowDrawable.kt` (real Gaussian blur at `blurRadius × 0.5` sigma). Easing corrected to `Easing.bezier(0.25,0.1,0.25,1)` with the CSS-`ease-in` gotcha recorded; constant-true condition simplified with the by-construction proof written into the doc comment |
| 85 | 2026-08-05 | **`platform-trade`** | L5 | `OutsetBoxShadowDrawable.kt:29` sets `MIN_OUTSET_BOX_SHADOW_SDK_VERSION = 28`; the project's `minSdkVersion` is 24. **On API 24-27 the done tile has NO glow.** Worker chose graceful absence over keeping `elevation` alongside, which on API 28+ would draw a second differently-shaped shadow beneath the correct one. Accepted — a decorative glow absent on Android 8-and-older beats a wrong shadow on every modern device — and a `Platform.Version < 28` gate remains available if the owner wants full coverage. **R8 TRAP: the on-device pass MUST run on API 28+, or the screenshot reads 'no glow' as a regression** |
| 86 | 2026-08-05 | `test-corrected` | L5 | The assertions now name only what Android renders — `boxShadow` delivering 8-down over a 22 blur at `0x4d`, plus a NEGATIVE over `shadowOpacity\|shadowRadius\|shadowOffset\|elevation`. Needed the `code()` comment-stripper from `prompt-flow-screens.suite.ts`, since the source comment necessarily names the iOS-only props it forbids and would otherwise self-trip. **Closes the fourth instance of a test naming a guarantee it could not detect** |
| 87 | 2026-08-05 | `memory-proposal` | L5 | Queued for owner ratification: RN shadow props are platform-split and it is easy to ship a shadow that exists only in source. `shadowOffset`/`shadowOpacity`/`shadowRadius` are iOS-only; on Android only `elevation` (default profile) and RN 0.76+ `boxShadow` (Fabric + API 28+) render. Also `Easing.ease` is CSS `ease-in`, not `ease`. Suggested slug `whim-rn-style-platform-gaps`, or append to `whim-rn-build-gotchas` |
| 88 | 2026-08-05 | `gate-queue` | L3,L4,L6 | Three lanes accepted and gate-ready. Full gates run STRICTLY one at a time per rule 33 — extended: worktrees resolve modules against the PRIMARY tree, so a worker self-gating while `gatefull` has swapped the branch there would resolve against the wrong branch. Exclusivity covers workers, not just reviewers |
| 89 | 2026-08-05 | `gate-full-PASS` + `merged` + `regate-pass` | L3 | Gate clean vs BASE `f4eee30`; merged `--no-ff`; staging tip **`c3ccca9`**; regate FAST GATE PASSED. 4 files, 16+/12- |
| 90 | 2026-08-05 | `gate-full-PASS` + `merged` + `regate-pass` | L4 | Gate clean; merged; staging tip **`f268f1d`**; regate PASSED. 1 file, 122+/20- — the batch's largest and highest-risk lane through clean |
| 91 | 2026-08-05 | `gate-full-PASS` + `merged` + `regate-pass` | L6 | Gate clean; merged; staging tip **`a5a735f`**; regate PASSED. 3 files, 103+/13- |
| 92 | 2026-08-05 | **`verify-ACCEPT`** | L5 | Re-review ACCEPTED `e38e845`. **Every RN source citation verified by reading**, and the reviewer checked one the worker never addressed: `ReactViewGroup.kt:168` sets `clipChildren = false` and `overflow:'hidden'` clips children in `dispatchDraw`, not the view's own background drawable — so the glow is not silently clipped at the tile bounds. Blur confirmed a genuine Gaussian at σ = blurRadius/2 (`BLUR_RADIUS_SIGMA_SCALE = 0.5f` with the W3C citation inline), not a profile approximation |
| 93 | 2026-08-05 | `colour-string-proven` | L5 | The `${bg}4d` concatenation — the diff's highest-risk line — proven safe on EVERY path. `tileColor` has exactly two returns: the declared one gated by an anchored `/^#[0-9a-f]{6}$/i` (named colours, `rgba()`, `#fff`, `''` all fail it), and `appColor(name)` indexing a 10-entry palette of 7-char literals via `hashName() >>> 0 % 10`, which cannot be out of range or `undefined`. `#rrggbbaa` is always well-formed; `normalize-colors` `hex8` parses it |
| 94 | 2026-08-05 | `trade-endorsed` | L5 | The no-`elevation` decision independently endorsed, on a sharper reason than double-draw: `elevation` renders a neutral platform-profile shadow, **never the tile's hue at 30%**, so a `Platform.Version` fallback would ship a *different wrong* decoration to old devices rather than the design's. Absence is the more honest degradation for a decorative element |
| 95 | 2026-08-05 | **`regression-from-my-correction`** | L5 | MED, folded into revision 2. The reachability pin at `tile-colour.suite.ts:175-176` claims every done-variant style sits behind the same `isDone` gate, enumerating four style names. The glow used to live in `styles.tileDone` and was covered; **my folded-in `boxShadow` correction moved it to a call-site const outside the enumeration.** Constructed failure: `const glow = true` ships the glow on every home-grid tile with all six new assertions AND the whole reachability loop green. Fifth instance in this batch of an assertion claiming coverage it does not have — and the first caused by an orchestrator-requested change |
| 96 | 2026-08-05 | `follow-up` | — | **F2** recorded: `Orb.tsx:139-143` still carries `shadowOpacity`/`shadowRadius`/`shadowOffset` + `elevation: 8` — the exact defect L5 was corrected for, on the orb. Unfiled, outside L5's allowlist, left alone per the F1 discipline. **Second instance of the same platform gap suggests the pattern was copied** — the later pass should grep the whole launcher rather than fix two known sites, and a lint rule is warranted since these props typecheck fine and are invisible to every harness check |
