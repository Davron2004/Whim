# Progress ledger: developer-observability

- run-start (2026-09-24 03:35 EDT): staging `integration/developer-observability`, cut from `integration/legal-surface-v2` tip `432a64d3f90e09e7450f389f1c917ecd9edea097` (not from `main`), MAIN_TIP ancestor `672e4e4a2978c8b3b5282ca66f75fd9bf81258ee`. Orchestrator worktree `.claude/worktrees/devobs-staging` (merges and `gate.sh` regates run there; `gate-full.sh` runs in the main tree after legal-surface-v2 closes).
  - Decision (orchestrator, owner asleep, owner asked for moderate parallelism): run this change concurrently with legal-surface-v2 instead of "one staging branch at a time". Reason: lsv2's remaining chains (5, 6, 9) are launcher-only while this change's chains 1, 2, 3, 6, 7 are server/deploy/runtime. Basing on the lsv2 tip picks up lsv2's server edits (`usage-store.ts`, `deploy.sh`, `smoke.sh`) and chain-0's handoff, avoiding conflicts on the same files. After lsv2 merges into `main`, `main` is merged into this branch before chains 4 and 5 (they need lsv2's `handoff/privacy-settings.md` and the rewritten disclosures, "#63").
- chain-0: done by the previous orchestrator (Ops Agent live on the VM; `handoff/log-shipping.md`).
- HUMAN-BOOTSTRAP 3.4 / 7.1: owner says HUMAN-BOOTSTRAP doesn't apply. The orchestrator commits them into the base: 3.4's `invariants/` cases written by a separate agent (never the chain-3 implementer); 7.1 `package.json` before chain-7 gates.
- dispatched chain-1: BASE `ef2e06331ecd7fd8f7850c2c4347ffe69bf84c9c`, worktree `.claude/worktrees/developer-observability-1` (@whim symlinks)
- dispatched chain-2: BASE `ef2e06331ecd7fd8f7850c2c4347ffe69bf84c9c`, worktree `.claude/worktrees/developer-observability-2` (@whim symlinks)
- dispatched invariants author (task 3.4, separate agent, not the chain-3 implementer): worktree `.claude/worktrees/developer-observability-inv`, branch `chain/developer-observability-inv`
- invariants author report: commit `8c659f1` on `chain/developer-observability-inv`, only `invariants/sandbox-isolation/bridge/{runner.mjs,fixtures/error-raiser.app.tsx}`. Cases `INV-ERRFRAME runtime|rejection|forged`: runtime and rejection RED on the current build, forged GREEN, all existing cases green, `npm run invariants` 8/8. Checked against in-memory loader variants: correct 13/13; message leak, rejection-as-runtime and constant `name:'Error'` each fail. Merged into staging as `ebe5e1b` (orchestrator, Class-2 by design: HUMAN-BOOTSTRAP done by owner-proxy; the chain-3 implementer never edits `invariants/`). Branch/worktree removed.
- dispatched chain-3: BASE `ebe5e1b2957c6dc5c6df33fe46b0e3e63a5dbdc9` (includes the 3.4 cases), worktree `.claude/worktrees/developer-observability-3`
- chain-1 report: STATUS complete, GATE PASS, commit `8ca5f2e`, 3/3 tasks. pino `formatters.level` → `{level, severity}` by threshold (numeric `level` stays first, the agent regex relies on it); `deploy/vm/ops-agent.yaml` byte-for-byte from the handoff (sha256 prefix c9482216 matches live); `bootstrap.sh` `install_ops_agent` after `install_docker` (fingerprint-checked key, HTTPS only); compose services gain the `com.docker.compose.service` label, keep `json-file`; deploy-config suite runs the committed config's regex + map over real pino lines; `docs/deploy.md` §Operating → Logs saved queries + `gcloud logging read` + on-VM fallback. Class-A: pre-existing emoji-scan failure fixed (U+FE0E after ✎/⚑ in `orb-actions.ts`; only fails under Node v22.20.0's ICU, see #87); a one-file `time_key` exemption in the secret scan with two red cases. Note: next deploy recreates Caddy once (compose label).
- chain-1 integrity: exit 0. Merged; regate-pass (node v22.23.1).
- chain-3 report: STATUS complete, GATE PASS, commit `ecbb801`, 3/3 tasks. 3.1 finding (scratch Playwright against the built runtime): a handler throw, an unhandled rejection AND an uncaught React 19 render error all fire on the realm window with `name` set → no SDK-root error boundary added. `loader.js` registers `error`/`unhandledrejection` listeners before any bundle runs (private, unremovable by the bundle), posting `{where, name}` via the existing nonce-authenticated `post('error')`; non-Error throws → `name:'NonError'`. `useMiniAppHost.ts`: `log.error(page, 'mini-app failed', {where, errorClass, appId})` in the fatal branch, watchdog (`where:'paint-timeout'`, `errorClass:'StartupDeadline'`), refused launch (`where:'launch'`, refusal kind) and `deliverBySourceJs` throw; runtime/rejection frames log `'mini-app error'`. `handleErrorFrame` now ignores `trusted !== true`. Class-A: those naming choices; same emoji fix as chain-1 (merged clean). Notes: a post-paint render error unmounts the tree → blank app with no recovery screen until exit (UX gap, filed as ai-proposed issue); a top-level bundle throw yields two records (runtime, then paint-timeout); a throw loop relies on chain-4's dedup/caps.
- chain-3 integrity: exit 0 (the `invariants/` cases were in its BASE). Merged; regate-pass; after `npm run build`: `invariants` 8/8, `bridge:invariants` 13/13 (INV-ERRFRAME runtime/rejection/forged PASS).
- (chain-6 worktree created at `9f76d5a` but not dispatched; recreated after chain-2)
- chain-2 report: STATUS blocked (Class B on 2.6 only), GATE PASS (+ knip), commit `45d09ed`, 2.1/2.4/2.5 + `handoff/server-diagnostics.md`. Strict `DiagnosticsBatch` (128 chars/string, 4096 `stack`, path-only `route`, 1–50 records); nullable `failure_reason` with a new closed `server/src/generation/failure-codes.ts` + `RunTrace.failureCode`; `whim-admin usage` prints per-reason counts; `POST /v1/diagnostics` (413 >32768 B, 400 unknown key or >50 records, 429 per-device 200/day + global 20000/day in memory, 204; `scope:"device"` lines via the root logger; no DB rows, device id on no line). Class-A: the route requires the `error-details` consent practice (consent <2 → 403 `consent_required`; chain-4 must send ≥2); clarify/rewrite 502 or admission throw keeps a null reason; >50 records is 400 not 413; same emoji fix; lsv2 `device-records.suite.ts` migration check updated for the new column. New operator keys `WHIM_LIMIT_DIAGNOSTICS_PER_DEVICE_DAY`/`_PER_DAY` aren't in `deploy.sh`'s optional keys (production runs on defaults).
  - 2.6 Class B: `server/test/deploy-config.suite.ts` (chains 1/6 own it) bans `--build-arg` in cloudbuild, pins `DEFAULT_HEALTH` to the real `/healthz` body, and runs smoke against a server reporting `commit:"unknown"`. **Decision (orchestrator): fold 2.6 into chain-6**, which owns the suite and hadn't been dispatched yet; chain-6 also registers the two diagnostics limit keys. Chain-6's worktree is recreated from the post-chain-2 tip.
- chain-2 integrity: exit 0. Merged (2.1, 2.4, 2.5 ticked); regate-pass.
- dispatched chain-6 (+ task 2.6 + the two diagnostics limit keys): BASE `22a74842d9f80c4b25cc07c8351295f189cdd9eb`, worktree `.claude/worktrees/developer-observability-6`
- 7.1 (HUMAN-BOOTSTRAP, done by the orchestrator per the owner): `metro-symbolicate` `0.84.4` (the version RN 0.85.3 already resolves, lock entry `node_modules/metro-symbolicate` unchanged) added to root devDependencies in `package.json` and the lock's root entry, by hand (`npm install` is permission-denied in this session). Committed into the base before chain-7.
- chain-6 report: STATUS blocked (Class B on one alert), GATE PASS (+ knip), commit `739fab03`. Done: 2.6 (`ARG/ENV WHIM_COMMIT` in the Dockerfile runtime stage, `--build-arg=WHIM_COMMIT=$COMMIT_SHA` in cloudbuild; config accepts a 40-hex SHA or `unknown`, else boot fails; `/healthz` = `{ok, service, commit, minBuild}`, boot line carries `commit`; `smoke.sh --commit <sha>` exact match; `deploy.sh` passes it incl. `--tag` rollbacks; a suite rule keeps `WHIM_COMMIT` out of every deploy file but the Dockerfile/cloudbuild), 6.2 (provision.sh finds by display name + fingerprint of the filled definition → create/update/`unchanged`; budget 50/90/100 %; private bucket `gs://$project-sourcemaps`; `WHIM_ALERT_EMAIL`/`WHIM_BILLING_ACCOUNT`/`WHIM_MONTHLY_BUDGET_USD` required), 6.3 (runbook Alerts table), 6.4 (`whim-data-daily` snapshot schedule, 07:00, keep 14, attached; restore steps). 6.1 partial: channel, uptime (`uptime-healthz.env`, flags not JSON; host from `WHIM_API_HOST`), metric `whim-terminal-failures`, policies api-down / report / terminal-failures / device-error.
  - Class B: no `budget_exhausted` refusal alert — the 503 refusal logs only a `request` line with a status shared with `policy_unavailable`. **Decision (orchestrator): option 1** — add the refusal code to the per-request log line for `/v1` JSON responses ≥400 (`error: <ApiError.error>`), then `policy-credit-exhausted.json` matching that OR the existing `provider credit exhausted` ERROR line. Dispatched as chain-6b.
  - Class-A: channel via `gcloud beta` (needs `gcloud components install beta`); diagnostics limit keys NOT registered (the runbook's rule: daily limits are code defaults, profiles forbid `WHIM_LIMIT_*`) — accepted; provision.sh also requires `WHIM_API_HOST`/`WHIM_WEB_HOST`. Terminal-failures metric filter not checked against a real line (verify in 8.1).
  - Production steps after merge: `gcloud components install beta`; add the 3 keys to `~/.config/whim/deploy.env`; run provision.sh twice (second prints only `unchanged`); deploy. A rollback to a pre-change image now fails smoke on `commit` though it goes live (runbook notes it).
- chain-6 integrity: exit 0. Merged (2.6, 6.2, 6.3, 6.4 ticked; 6.1 stays open for 6b); regate-pass.
- dispatched chain-6b (the credit-exhausted alert): BASE `cd7407bedf334c0051c294961bb4092c78e79962`, worktree `.claude/worktrees/developer-observability-6b`
- chain-6b report: STATUS complete, GATE PASS, commit `b38a0be6`. `app.ts` per-request middleware captures `ApiError.error` where a refusal is written (per-request `c.json` override) and logs `error:"<code>"` on `/v1` JSON responses ≥400 (closed code only; 2xx lines carry none); `deploy/monitoring/policy-credit-exhausted.json` ("Whim: credit exhausted": the `budget_exhausted` request line OR `scope="run" msg="provider credit exhausted"`, 1 h rate limit); runbook row; deploy-config tests drive real refusals/402 through `createApp` + a red-check that a `status=503`-only filter also catches `policy_unavailable`. The agent (sonnet) omitted the HARNESS FEEDBACK section; not resumed per protocol.
- chain-6b integrity: exit 0. Merged (6.1 ticked); regate-pass.
- dispatched chain-7: BASE `d1aa3673987e85ac0a0e47a8811dc11bbde65985` (includes 7.1), worktree `.claude/worktrees/developer-observability-7`
- chain-7 report: STATUS blocked (Class B: knip), GATE PASS, commit `47105315`, 7.2 + 7.3. Fastlane `ios testflight` passes `SOURCEMAP_FILE=ios/build/sourcemaps/main.jsbundle.map` via xcargs and uploads before `upload_to_testflight`; `android closed` uploads `generated/sourcemaps/react/release/index.android.bundle.map` after `verify-aab`, before the Play upload (also with `upload:false`). `upload-source-map` (release CLI) exits 1 on a missing/non-v3 map or a failed `gcloud storage cp`, naming the key → the lane stops before any store upload. Bucket `gs://<WHIM_GCP_PROJECT>-sourcemaps` read like `deploy/lib.sh`. `scripts/symbolicate.mjs <platform> <version> <build> [--maps-dir]` runs metro-symbolicate; a missing map exits 1 naming `<platform>/<version>+<build>.map`, never falls back. Fixture: 658 B trimmed from the real offline-59505b5 map, byte-identical output vs the full map (frames `redact.ts:65 isSensitiveField` … `report-send.ts:51`). 5 red-checks. Unverified: no lane run (attended, nothing to GCS), iOS `SOURCEMAP_FILE` via build setting relies on documented Xcode behaviour (fails safe).
  - Class B: knip can't see `metro-symbolicate` (`knip.json` `project` excludes `scripts/`). **Decision (orchestrator): option 1** — `"metro-symbolicate"` added to `ignoreDependencies`, committed into the base (CONFIG_SET file). Option 2 (scan `scripts/**`) would surface 6 unused exports and 3 unlisted binaries and still miss this dependency: filed as a follow-up in the harness feedback.
- chain-7 integrity: exit 0. Merged (7.2, 7.3 ticked); regate-pass; `npx knip` clean.
- NEXT: chains 4 and 5 wait on legal-surface-v2 landing on `main` (then merge `main` into this branch); chain-8 is attended (orchestrator).
- re-apply onto main (legal-surface-v2 landed as `8af431ee` with rewritten history, so `git merge origin/main` conflicted on 60+ files through the stale merge-base `672e4e4`): the branch was reset to `origin/main` and this change's own diff (`432a64d..backup/devobs-pre-rebase`, 76 files) applied with `git apply --3way`; 3 hand-resolved files (`bootstrap.sh` header, `usage-store.ts` both migrations in order, `deploy-config.suite.ts` union of blocks/stubs) by an agent; one commit `56fe480a`. gate PASS, invariants 8/8, bridge 13/13 (INV-ERRFRAME ×3), knip clean; per-file reverse-patch check: nothing dropped or duplicated. Old tip kept as `backup/devobs-pre-rebase`.
  - FOUND (cross-change): lsv2's `log-age-cap.sh` trims the ACTIVE Docker log by truncate + rewrite; the Ops Agent (fluent-bit tail, live on the VM since chain-0) resets its offset on truncation → re-ships the kept tail (up to ~10 MB) to Cloud Logging and can false-fire the generation-failures alert. Can't trigger before the active log holds an 89-day-old line (VM logs are ~10 days old; every deploy recreates containers). **Decision:** fix in this change's fix chain: when the active log needs trimming, recreate the container (new container id → new log file, old one removed) instead of truncating; rotated files keep line-trimming (the agent doesn't tail rotated files? verify). Owner: devobs fix chain.
- chains 4 and 5 unblocked (#63 = legal-surface-v2 merged): dispatched in parallel (disjoint files: 4 = device logging seam/transport/index.js/checks pass; 5 = release checks + declarations).
- dispatched chain-4: BASE `6045c392080aec3c03fe9cceaa3900cb13ff8bbe`, worktree `.claude/worktrees/developer-observability-4`
- dispatched chain-5: BASE `6045c392080aec3c03fe9cceaa3900cb13ff8bbe`, worktree `.claude/worktrees/developer-observability-5`
- production prerequisites (orchestrator): `gcloud components install beta` done (notification channels are beta-only). Billing account `0123A0-49680F-D9240A` ("My Billing Account", linked to anycognition-whim) bills in **CAD**, but chain-6's provision.sh passes `<N>USD` → the budget create would fail. → fix chain: take the currency from the billing account (`gcloud beta billing accounts describe --format='value(currencyCode)'`) or an explicit `WHIM_MONTHLY_BUDGET` + currency; rename `WHIM_MONTHLY_BUDGET_USD` accordingly. Operator values to set at deploy (orchestrator decisions): `WHIM_ALERT_EMAIL=support@anycognition.ca` (the business mailbox the owner already reads, the published privacy contact; no personal address in GCP), `WHIM_BILLING_ACCOUNT=0123A0-49680F-D9240A`, monthly budget 100 CAD (a budget only alerts, it never caps or charges; the VM + disk + logging run well under it).
- chain-5 report: STATUS complete, GATE PASS (+ knip, release check), commit `77879e5c`. 5.1: every declaration already covers D11 + D9 (consent en/fr, both privacy pages incl. "(including any error code)", Play `crash_logs`/`diagnostics`, iOS `CrashData`/`OtherDiagnosticData` Linked + not tracking, App Store `CRASH_DATA`/`OTHER_DIAGNOSTIC_DATA`) → no text added, consent version stays 2. 5.2: `checkDiagnosticsDisclosure` in `scripts/release/lib/store-listing.ts` (within the existing checker): a build "contains the transport" when non-test `src/**` or `index.js` holds `/v1/diagnostics` or `diagnosticsTransport`; then the manifest, the four store declarations, the consent screen (every language) and both privacy pages must declare error details; 6 tests, discriminating red-check = manifest + all declarations dropping diagnostics together (the task's named red-check already failed on base via lsv2's manifest comparison). 5.3: #63 already declares both Analytics and App functionality (definitions from general knowledge, no web access); left as is, narrowing to Analytics is an owner call at the next manifest version.
- chain-5 integrity: exit 0. Merged (5.1–5.3 ticked); regate-pass. Re-run the gate after chain-4 so the live path of "the real repo passes with zero findings" is exercised.
- chain-4 report: STATUS complete, GATE PASS (+ knip), commit `cd894881`, 4.1–4.5. `toDiagnostic` (`src/host/logging/diagnostic.ts`: allowlist tested against the contract schema's keys; strings 128, stack 4096, stack keeps frame lines only; route = path; mini-app records keep only `where` + built-in `errorClass`/`Other`); `DiagnosticsTransport` as `seam.diagnostics` (error only, dedup `(channel, message, errorClass, where)` with count deltas, 50/session, flush 20/30 s/background, one POST ≤ 32 KB, failure on `whim:sink` without re-entry; gate `diagnostics-target.ts` = "Send error details" on + `liveClientOptions` (terms + current grant), asked per record and before each POST; refused records discarded); `crash-capture.ts` (chained global handler + Hermes rejection hook), `fatal-slot.ts` (`whim.fatal-error:v1`, written only if allowed at crash, sent at launch only if still allowed, deleted either way), `install-diagnostics.ts` imported from `index.js` after the polyfills; request ids on `logMappedError` (`route`), `generation step failed`, `failure screen shown`, `stream ended with no terminal event`; `checks/test/repo/log-egress.suite.ts` (third-path scan). Class-A: `path`→`route`, `ctor`→`errorClass` in log fields; one `no-deep-imports` disable (RN rejection tracking options). Unexecuted on a device: RN wiring, background flush, Hermes tracker → chain-8. Notes: dedup merges records that differ only by stage/status/requestId (first request id wins per session); a non-terminating fatal (dev redbox) can upload twice.
- chain-4 integrity: exit 0. Merged (4.1–4.5 ticked); regate-pass; knip clean; release check passes with the transport present (chain-5's live path).
- gate-full dispatched on `3004e75fe0060549907003b52acb9bc1fd8e9be5` (main tree, via fixloop.sh gatefull); reviewer dispatched on `8af431ee..3004e75fe0060549907003b52acb9bc1fd8e9be5`; fix-1 dispatched (log-age-cap × Ops Agent, CAD budget): BASE `3004e75fe0060549907003b52acb9bc1fd8e9be5`, worktree `.claude/worktrees/developer-observability-fix1`
- gate-full on `3004e75`: FAILED synthetic-run (2 checks): chain-3's new loader `error` frames (`where:runtime|rejection`, message-free) reach synthrun's diagnostics: extra `runtime_throw` kinds and 'runtime_throw names the thrown error (got [rejection] runtime error (no message))'. A real regression for the generation repair loop. All other gate-full parts PASS. Dispatched fix-2 (synthrun): BASE `3004e75fe0060549907003b52acb9bc1fd8e9be5`, worktree `.claude/worktrees/developer-observability-fix2`
- reviewer (whole change, `8af431ee..3004e75`): VERDICT findings; reports match the diff (3 minor discrepancies). Confirms Q1 (the agent's glob `*-json.log` doesn't match rotated `*.log.N`, so recreate-instead-of-truncate suffices) and Q2 (bare `--budget-amount N` uses the account currency). HIGH: F1 model-written plan text (screen names from `plan.ts:136-150`) reaches `/v1/diagnostics` via the allowlisted `reason` (`LauncherRoot.tsx:313,1608`) and Cloud Logging via the server's `terminal failure` line (`machine.ts:675`); F5 knip ignore + hand-edited lock need owner ratification. MEDIUM: F2 Caddy error lines carry `X-Whim-Device` + client IP to Cloud Logging (**confirmed live**: `warn "aborting with incomplete response"` entries with the header since 2026-09-15; the Ops Agent is live since chain-0); F3 fatal React render crashes bypass `ErrorUtils` (RN 0.85 `onUncaughtError` → `ExceptionsManager`), no record/slot; F4 runbook query `jsonPayload.reason="<code>"` never matches; F6 owner-proxy `invariants/` need owner sign-off; F7 clarify/rewrite 502 and internal failures settle with a null `failure_reason`. LOW: F8 release Hermes tracker absence is silent; F9 429/`Retry-After` ignored; F10 dead `'diagnosticsTransport'` marker; F11 test-only counters/caps copy; F12 14-day snapshots vs deletion wording. `terminal failure` lines: none in Cloud Logging in 30 d (F1 not yet realised).
- orchestrator decisions: F1 → the server's terminal line logs the closed `code` (no sentence); the device logs the code in `reason` and keeps the sentence out of every projected field; `plan.ts` reasons keep their wording for the user but are never logged. F2 → Caddy global log filter deleting request headers and client/remote IP + `ops-agent.yaml` drops `jsonPayload.request`; after the fix is live, purge the ingested `docker` log (`gcloud logging logs delete docker`; all pre-launch; VM originals stay under the age cap). F3 → root error boundary in `App.tsx` (log + `keepFatalRecord` + rethrow). F4 → with F1 (`jsonPayload.code`). F5/F6 → kept (owner told me HUMAN-BOOTSTRAP doesn't apply and to decide protected-file edits); listed in the handoff for the owner to see. F7 → closed codes `model_failure`, `internal_error`. F8 → one-time `warn` when the tracker is absent. F9 → stop uploads for the session (or until `Retry-After`) after a 429. F10 → key detection on the route constant only. F11 → accepted. F12 → policy (en/fr) gains one sentence: deleted data may remain in disk backups for up to 14 days. Fix chains: fix-3b (device: F1-device, F3, F8, F9, F10) now; fix-3a (server/infra: F1-server, F2, F4, F7, F12) after fix-1 (shared `deploy/**`, `docs/deploy.md`).
- dispatched fix-3b: BASE `4590f9e857372a45ddc31a298adbf5cc6043fa1c`, worktree `.claude/worktrees/developer-observability-fix3b`
- fix-2 report: STATUS complete, commit `614bf4fe`. Root cause: `synthrun/observe.ts` `attachObserversEarly` routed every trusted `error` frame to `recordMountError`; the loader's new `{where:'runtime'|'rejection'}` frames became message-free `runtime_throw`s next to CDP's message-bearing capture. Fix: `recordMountError` ignores `REALM_LISTENER_WHERES` (still kept in `state.events`); bundle/mount/deliver/probes frames unchanged. New test "realm errors: each is reported once, with its message…" (red on the old code, and a runtime-only filter would still fail it). synthrun 316/316. Integrity 0; merged; regate-pass (orchestrator re-ran synthrun: 316 pass).
- fix-1 report: STATUS complete, commit `39203942`. log-age-cap never edits an active log: over-cap containers (project label `whim`) are recreated once per service via `docker compose --project-directory /opt/whim --file /opt/whim/compose.yaml up -d --force-recreate --no-deps <svc>`; unparsable/unknown containers fail the run (exit 1), never recreated; rotated files still trimmed (a test proves the agent's glob doesn't match them). Budget: key renamed `WHIM_MONTHLY_BUDGET`; provision reads the account currency (`gcloud beta billing accounts describe`) → `<N><CUR>`, refuses if unreadable; suite on a CAD stub. 7 named red-checks + the real BASE script swapped in. Unverified: `docker compose` under the unit's `ProtectSystem=strict`/`ProtectHome` (check command in the report; run at deploy). Integrity 0; merged; regate-pass.
- dispatched fix-3a: BASE `0e2e923da7301f5d686cbf91e184389e34f995e5`, worktree `.claude/worktrees/developer-observability-fix3a`
- fix-3b report: STATUS complete, commit `0d0d091b`. F1: closed `DIAGNOSTIC_REASONS` (`terminal_failure`, `no_terminal_event`, `server_refused`, `empty_bundle`, `unexpected_error`); the projection drops any other `reason`, fatal-slot records with a foreign reason are never sent; LauncherRoot logs the code in `reason`, the sentence in the device-local `detail`; end-to-end test builds the real `plan_failed` sentence with the server's `parsePlan`/`validatePlan` and asserts no "Alice"/"Lisbon" in the POST body (red on old code); audit of 10 projected error call sites in the report. F3: `src/host/RootErrorBoundary.tsx` wraps `App.tsx` (records + keeps the fatal record, rethrows from `componentDidCatch` so React's `onUncaughtError` → `ExceptionsManager` fatal path is unchanged; one extra non-fatal log line). F8: one `warn` on the sink channel when the Hermes tracker is absent. F9: 429 pauses uploads until delta-seconds `Retry-After` or the session end; records discarded. F10: detection keyed on `DIAGNOSTICS_PATH` only; suite plants the real transport file. Class-A: device-side codes (the wire failure event has no `code`; join to the server line's `code` by `requestId`); `message`→`detail` on the stream-failure branch. `App.tsx` wiring typechecked only (device check in chain-8). Integrity 0; merged; regate-pass.
- spec amendment (orchestrator): device-diagnostics gains scenarios "A failure sentence built from model output travels only as a closed code" and "A refused upload pauses the session".
- fix-3a report: STATUS complete, commit `bf94c04d`. F1: `terminal failure` logs the closed code in `reason` (the logger redacts any field named `code`, so `jsonPayload.code` would be empty); real `plan_failed` test, no "Alice"/"Lisbon" on any line; alert metric + runbook query tested against a real failure. F4: runbook row notes `reason` holds the code. F2: Caddy global `log default { format filter { … request>headers/remote_ip/remote_port/client_ip, resp_headers delete } }`, validated with a from-source Caddy 2.11.4 and a live reverse-proxy abort (no headers/IP); Ops Agent `client_data` processor drops `jsonPayload.request` (when it has method/client IP) and top-level `remote_ip`/`client_ip` (same `move_from`+`omit_if` pattern proven live; untested against the real agent). F7: `REQUEST_FAILURE_CODES` `model_failure`/`internal_error` on every clarify/rewrite/generate/report failure path (+ expired-then-aborted keeps `expired`); spec line updated. F12: backup sentence (en/fr), tied by test to `SNAPSHOT_KEEP_DAYS`. Open (audit): `policy/cache.ts:50` logs the classifier's free-text `category` → fix-4. Integrity 0; merged; regate-pass.
- dispatched fix-4 (policy category → closed set): BASE `dead183bc40aa6f9b45f0e3060a57c10086ed3c4`
- gate-full on `dead183` (after fixes 1, 2, 3a, 3b): FULL GATE PASSED (synthrun 316, invariants, bridge invariants, knip, guard:metro, openspec).
- fix-4 report: STATUS complete, commit `30fe710`. `parseCategoryList` reads `docs/content-policy.md`'s `## Categories`; `cachedPolicy({knownCategories})` logs a category only when it case-insensitively matches the list, else `other` (fresh + cache-hit paths); verdict logic unchanged; refusal response and ledger already carry no category (verified). Test with `{"verdict":"refuse","category":"Alice owes 40 for Lisbon"}` → `other`, no "Alice" on any line; red-checks named. Side finding: `docs/capabilities.md`'s `content-policy` pointer targets a nonexistent live spec (filed). Integrity 0; merged; regate-pass.
- PR #91 round 1: CI 3/3 pass, Sonar gate OK, 39 open issues (33 shell in provision.sh/smoke.sh/bootstrap.sh, 6 TS) → two Sonar workers (shell, TS); findings in `findings-sonar-1.md`.
- VM check (orchestrator, read-only): `docker compose --project-directory /opt/whim ps` runs under `systemd-run -p ProtectSystem=strict -p ProtectHome=yes -p PrivateTmp=yes -p NoNewPrivileges=yes` (both services listed, exit 0); containers carry `com.docker.compose.project=whim` + service labels (`whim-server`, `caddy`) → fix-1's recreate path is sound on the real VM.
- Sonar round 1 fixes: shell `8ce21e9` (S2, S3, S11–S38: `[[ ]]`, positional → locals, a no-op `*)` default for the placeholder check, `HTTPS_ONLY` constant), TS `97c007b` (S1/S8 no array copy, S4–S6 Sets, S7 `codePointAt` byte count (fixes a lone-surrogate edge), S9 `String.raw`, S10 default built in-body, S39 comment on the loader's deliberate empty catch, no logic change). Integrity 0 each; merged; regate-pass; invariants 8/8, bridge 13/13.
- Sonar round 2 (PR #91): 1 left (S2486 on the loader's `catch (e)` with an unused binding; a comment wasn't enough). Orchestrator changed it to `catch {` (the loader already uses optional catch binding at line 164); rebuilt; invariants 8/8, bridge 13/13, synthrun 316, gate PASS; integrity 0; merged.
