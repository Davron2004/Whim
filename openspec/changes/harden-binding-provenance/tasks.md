## 1. Workstream A — binding-provenance detector (Class-1, subagent-executable)

- [ ] 1.1 Create `checks/passes/binding-provenance.ts`: for every `.ts`/`.mjs` file under `scripts/`, `invariants/`, `build/`, `src/`, `server/`, `contract/`, `synthrun/`, `checks/` (excluding `node_modules` and `openspec/changes/archive/**`), parse with `ts.createSourceFile` directly (never `checks/internal/parse.ts`), choosing `ts.ScriptKind.JS` for `.mjs`/`.js` and `ts.ScriptKind.TS` for `.ts` by extension. Walk each file's AST for `exposeFunction(` / `exposeBinding(` call expressions. Ban `exposeFunction` outright wherever the file imports `playwright` — no allowlist. For every `exposeBinding` call in such a file, require the callback's FIRST statement to match one of the two guard shapes from design.md D2: page-level (`source.frame !== page.mainFrame()`) or context-level (`source.frame !== source.page.mainFrame()`). Structural/AST matching only — no string-grep. Use `SyntaxKind.ImportKeyword` if dynamic-import detection is ever needed; `ts.isImportCall` is `@internal`.
- [ ] 1.2 Wire the pass into the existing `checks:test` seam: add a new `test()` call in `checks/test/acceptance.ts` (or a sibling module it imports) that invokes the pass and asserts zero violations across the repo scan. Do not touch `checks/index.ts`'s `PASSES` array or `runStaticChecks` — that pipeline is scoped to one mini-app bundle string and is the wrong domain (design.md D1). No edits to `scripts/gate.sh` or `checks/test/run.mjs` are needed; confirm `external: ['typescript']` (`checks/test/run.mjs:32`) is inherited for free.
- [ ] 1.3 Add the synthetic weakened-case fixture required for non-vacuity (design.md D1/D2, Non-Vacuity): two inline source strings parsed by the detector in the same `test()` — (a) an `exposeBinding` callback whose main-frame guard sits AFTER a `JSON.parse` call, which MUST be flagged; (b) a correctly-guarded control (guard as first statement) which MUST NOT be flagged. Do not add a new fixture directory — inline strings only, per the read-budget correction that `checks/test/hostile/` does not transfer to repo-harness auditing.
- [ ] 1.4 Capture the pre-fix red-check against the PINNED COMMIT `754e2f7` — not against the live working tree. In a scratch worktree checked out at `754e2f7` (the last commit before workstream B's guard), run the detector and confirm it flags `invariants/sandbox-isolation/bridge/runner.mjs:72`. Capture the verbatim output into `openspec/changes/harden-binding-provenance/evidence-red-check.md`, recording the SHA. Pinning to a commit rather than to tree state is deliberate (see the orchestrator decision on chain-1 in `chains.md`): the evidence stays reproducible by anyone at any time, instead of being a transient property of one worktree that vanishes the moment the guard lands.
- [ ] 1.5 Run `npm run checks:test` against the current tree — with workstream B already applied, it MUST pass with zero live violations, and the fixture assertions from 1.3 must pass alongside it. This chain self-gates green; if it does not, the detector is over-matching and the fixture set in 1.3 needs a control case, not a suppression.

## 2. Workstream B — runner.mjs guard + refusal counter (HUMAN-BOOTSTRAP)

- [ ] 2.1 [HUMAN-BOOTSTRAP — subagents are hard-blocked from `invariants/` by `.claude/hooks/protect-harness.sh:103-134`] Apply the chain B handoff contract's exact patch text (delivered separately as a `handoff/*.md` file — see `chains.md` for the filename) to `invariants/sandbox-isolation/bridge/runner.mjs`: convert the `exposeFunction('whimHostDispatch', …)` call at line 72 to `exposeBinding`, add a first-statement main-frame guard (page-level or context-level form per design.md D2) that returns `null` before any `JSON.parse` or `dispatcher.handle` call, add a refusal counter surfaced on the object `scenario()` returns (mirroring `hostProvenanceRefusals` in `synthrun/observe.ts`), and add the adversarial scenario described in the handoff contract: from `appFrame(page)`, assert the non-vacuity triad first (`g.top !== g`; the `__whimSyscall` shim installed; `typeof whimHostDispatch === 'function'`), then call `whimHostDispatch` with a `storage.kv.set` frame and assert `sysret === null`, the write is absent when read back from the storage engine (not merely absent from a trace), and the refusal counter incremented — closed by a positive control that a legitimate main-frame dispatch still writes. Include the scenario's own red-check (decision #28): neuter the guard to `if (false)`, confirm the hostile write lands, then restore the guard.
- [ ] 2.2 [HUMAN-BOOTSTRAP] Run the guard's own red-check before calling this task done (decision #28 — the suite's non-vacuity is load-bearing): neuter the guard to `if (false)`, confirm the hostile write LANDS (the adversarial scenario goes red), then restore the guard and confirm it goes green. Record both outcomes. Note that this chain runs BEFORE the workstream-A detector exists, so `checks:test` is not the check here — the adversarial scenario is. Watch the trap the handoff contract flags: scenario 5's pass condition is `stale === null`, the same shape the guard returns on refusal, so a scenario that drifted into `appFrame` would go green for the wrong reason.
- [ ] 2.3 [HUMAN-BOOTSTRAP] Run `npm run bridge:invariants` and confirm scenarios 1,2,3,4,5,6,7,8 (`runner.mjs:88-101,103-109,111-151,179-198,200-213,215-224,226-235,237-271`) all still pass unmodified, per design.md D6's per-scenario trace, plus the new adversarial scenario passes green with the guard restored.

## 3. Workstream C — research-discipline process amendments (HUMAN-BOOTSTRAP)

- [ ] 3.1 [HUMAN-BOOTSTRAP — `.claude/**` is Class-2 protected] Apply the chain C handoff contract's exact patch text to `.claude/agents/researcher.md`: precedent found in the codebase is a suspect to verify, not an exemplar to copy (design.md D5).
- [ ] 3.2 [HUMAN-BOOTSTRAP] Apply the chain C handoff contract's patch to the `whim-harness` schema's `research` artifact instruction (`openspec/schemas/whim-harness/schema.yaml`): require a pattern census — enumerate every in-repo occurrence of the pattern under research, classify each SAFE / UNSAFE / NOT-CHECKED, and name the test applied per row. Ban "find an exemplar to copy" as a research primitive.
- [ ] 3.3 [HUMAN-BOOTSTRAP] Apply the chain C handoff contract's patch to `.claude/commands/opsx/apply.md` near its step-13 duties (`:57`) and to the schema: mandate that any UNSAFE out-of-scope census row is written to `openspec/critic/open-follow-ups.md` with file:line and vulnerability class, not left only as prose in `research.md`.
- [ ] 3.4 [HUMAN-BOOTSTRAP] Run `node scripts/sync-codex.mjs --write` to regenerate the Codex mirror for the `researcher` agent change from 3.1, then `node scripts/sync-codex.mjs --check` to confirm the mirror is in sync (`gate-full.sh` enforces this check).

## 4. Dogfooding and closure

- [ ] 4.1 Write this change's own out-of-scope observation to `openspec/critic/open-follow-ups.md` (Class-1 — `openspec/` is not in `.claude/hooks/protect-harness.sh`'s protected-path list, verified against the script's case patterns): `scripts/gate.sh` is Class-2 protected but the suites it invokes — including `checks:test`, which houses this change's own detector (`checks/passes/binding-provenance.ts`, wired at `scripts/gate.sh:63`) — are not, so an agent could weaken the detector after the fact without triggering a Class-2 gate. Vulnerability class: verification-integrity / detector-weakening gap. Cite this file:line and design.md's Risks/Trade-offs entry recording the same gap as explicitly out of scope for this change.
- [ ] 4.2 [HUMAN-BOOTSTRAP] After workstreams B and C have both landed, run `scripts/gate-full.sh` on the integration branch and confirm it is fully green (build, typecheck, lint, all Node suites including `checks:test` and `bridge:test`, knip, `guard:metro`, the Chromium invariant suites, `openspec validate`, and `sync-codex.mjs --check`).
- [ ] 4.3 [HUMAN-BOOTSTRAP] Confirm via the desktop Chromium invariants run (fast pre-check) and, if available, the on-device Android System WebView run (`RUN_BRIDGE_PROBE`) that containment is unaffected — this change touches no runtime/product code path and decision #41's syscall authority rule (`ev.source === window.parent`) is untouched.
