# Progress ledger: store-launch-compliance

Staging branch: integration/store-launch (shared launch run; MAIN_TIP 3a66cca)

- 17:53 dispatched chain-1 BASE 51f95bc (merge waits for the server-connectivity gate-full run in the primary tree)
- 18:05 chain-1 report: complete, GATE PASS, class-A x3 (grantConsent now is an ISO string value; 1.2/1.5 cases in prompt-flow-wiring suite; domain scan keyed on WHIM_DOMAIN to avoid KV-key false positives, red-checked) · integrity OK · merged · regate-pass
- 18:05 dispatched chain-2 BASE 9b537eb
- 18:28 chain-2 report: complete, GATE PASS, class-A x5 (contract-mirror.ts until server chain-1 lands; fake-xhr headers; optional headers read; 3-line LauncherRoot bridge cast replaced by chain-3; logging example renamed) · integrity OK · merged · regate-pass. RECONCILE when public-generation-server chain-1 merges: delete contract-mirror.ts, repoint imports to @whim/contract.
- 18:28 dispatched chain-3 BASE cba972c
- 19:00 chain-3 report: complete, GATE PASS (launcher 9030), class-A x4 (returnTo attached in LauncherRoot; ConsentScreenForShell for cognitive complexity; retired unconfigured-server copy and updated 4 source-pinned suites; server section label kept inside Advanced) · integrity OK · merged · regate-pass. Watch: brittle source-text assertions recur in later chains.
- 19:00 dispatched chain-4 BASE 78eba26
- 22:55 RESUME (fresh orchestrator after the usage cutoff): chain-4 worktree held ~250 lines of uncommitted work (ServiceNotice, refusal-landing, step wiring; pinned-assertion updates half done) → re-dispatched into the same worktree (BASE 78eba26) to continue from the diff.
- 23:18 chain-4 report: complete, GATE PASS, class-A x2 (complexity-driven extractions rewriteRefusalTarget/handleGenerateRefusal/refusedGenerateOutcome; contract authored fresh). Inherited draft verified correct for 4.1-4.5 and kept; added the missing suites (refusal-landing, D10 split, notice carry/clear); 3 source pins re-pinned, none loosened · integrity OK · merged · regate-pass
- 23:54 chain-5 report: complete, GATE PASS (launcher 9782), class-A x2 (orb report sheet state local to MiniAppView, done/history share one LauncherRoot reportTarget; cited literal monospace preview size) · integrity OK · merged · regate-pass. SheetModal is a real RN Modal (a sibling View is not reliably above a live Android WebView). back-policy: overlayOpen → close-overlay first. For chain-12: verify the ⚑ orb glyph renders on device.
- 23:54 dispatched chain-6 BASE 8f05186
- 00:21 chain-6 report: complete, GATE PASS, class-A x3 (linkExitFor takes Screen kind | "sheet"; LauncherRoot screen switch extracted to renderScreenContent for complexity, no behavior change; link open/building/failed reuse the tile-tap handlers, reading index.list()/pending.list() live) · integrity OK · merged · regate-pass. contract-mirror.ts still has importers outside this chain (ReportSheet, service-refusal, report-payload), so it goes to the fix list.
- 00:21 dispatched chain-7 BASE 70fdfcb
- 00:56 USAGE CUTOFF (session limit) killed every running agent; resumed at low priority by messaging the same agents (transcripts intact). Worktree state at resume: server-15 substantial uncommitted site work; platform-10 Fastfile only; server-9b and compliance-7 empty; iOS planner partial drafts.
- 01:02 chain-7 report: complete, GATE PASS, class-A x2 (the bridge registers TEN syscalls, not the nine design D18 says, so the review notes list all ten; review-notes.md headed as later condensed by platform chain-11) · integrity OK · merged · regate-pass. Every "tap X" string verified against copy.ts; the network-deny line is an explicit [TODO]; no personal contact details; decision #65 appended. ALL 37 TASKS DONE; next: gate-full + reviewer.
- 01:20 fix chain contract-mirror-removal (dispatcher-created, to clear the knip finding): complete, GATE PASS, class-A (report-payload.suite.ts was also an importer) · integrity OK · merged 075005e. Shapes checked byte-identical vs contract/src/index.ts before deletion; every production launcher @whim/contract import is type-only. Post-merge in the primary tree: npx knip exit 0 (no findings), guard:metro OK (2092826 bytes, unchanged), regate PASS. gate-full rerun started.
- 01:25 gate-full on e753d96: FULL GATE PASSED (knip clean, guard:metro OK, invariants + bridge-invariants + synthrun Chromium suites PASS, openspec 40/40).
- 01:25 reviewer (Opus) on 51f95bc..staging: CHANGES-REQUESTED. Privacy confirmed: nothing reaches the server before consent; report note text never logged. Findings: H1 ConsentScreen review-off has no non-granting exit on iOS; M1 consent Agree on a Retry continuation is a no-op the first time (stale clientOptions memo); M2 transport text becomes the failure reason; M3 rewrite refused from compose lands on an empty clarify; M4 contract-mirror drift guard vacuous (ALREADY FIXED by 075005e); M5 app-link parser strict only under Node URL, not the RN URL polyfill (evil host via ?@); M6 task 3.5 probe-gate suite missing, D10 refusal tests tautological, ReportSheet states untested; lows L1 link race, L2 NUL bytes in a suite, L3 double hint, L4 raw reason enum in preview, L5 consent copy omits app name, L6 three false review-notes claims, L7 report outcome logged as network, L8 probe gate by convention (deferred), L9 static retry line + pluralization + Number(raw) Retry-After, L10 brittle source pins (deferred). Report-honesty gaps in chains 3 and 4 noted.
- 01:25 fix chains dispatched at BASE e753d96: fix-launcher-wiring (M1 M2 M3 M6 L1 L2 L3 L4 L7), fix-consent-retry (H1 L5 L9, with privacy.html parity), fix-app-link-parser (M5). L6 sent to platform chain-11 (it owns review-notes.md). Deferred with reason: L8 (structural probe brand; convention holds, reviewer found no leak) and L10 (pin brittleness, a post-launch test cleanup). Re-review after the fixes merge.
- 01:30 fix-app-link-parser: complete, GATE PASS (launcher 9991), class-A (added a port-bearing host case) · integrity OK · merged · regate-pass on the real domain. parseAppLink uses an anchored regex + exact lowercase host compare vs RELEASE.webHost, decodes the id once; 13 new cases including ?@/#@ smuggling; a hostile globalThis.URL stub test proves URL independence (red-check: reinserting new URL failed it).
- 01:39 fix-consent-retry: complete, GATE PASS (launcher 10008, server 1683 incl. consent parity) · integrity OK · merged · regate-pass. H1 FIXED: consent-screen-actions.ts table, every mode has a non-granting exit (review-off gains COPY.consentDecline "Not now", which avoids a new consent* key tripping the privacy parity check), red-checked. L5 FIXED: consentWhatSentEdit + privacy.html now name the app; the orchestrator applied the same to docs/store/review-notes.md §4 and notes.txt (3998/4000 chars). L9 PARTIAL (class-B, accepted): plurals + strict Retry-After (digits or HTTP-date) + retryLine elapsed branch done; the on-screen caption still goes stale because callers precompute it, and design D12 "a sender refusal clears when its window ends" is NOT implemented → follow-up chain notice-window (ServiceNotice.tsx + ComposeStep/PlanStep/ClarifyStep/FailureScreen/ReportSheet) after fix-launcher-wiring merges.
- 02:21 fix-launcher-wiring (a fresh agent finished it after the first stalled twice): complete, GATE PASS, class-A x2 (L2 diff still shows binary because the base blob had NULs; content fixed. Re-pinned one prompt-flow-wiring source string for the resolveClientOptions rename) · integrity OK · merged 8f28226 (acceptance.ts auto-merged with fix-consent-retry's registration, both present) · regate-pass · knip clean. M1 M2 M3 M6(a,b,c) L1 L2 L3 L4 L7 FIXED; red-checks M1 M2 M3 M6b. Found: the five new suites call harness eq(a,b) without its msg argument (the IDE flags TS2554; the gate does not typecheck launcher suites) → launcher-polish-fixes F10.
- 02:21 dispatched follow-up notice-window BASE 8f28226
- 02:39 fix-notice-window: complete, GATE PASS (launcher 10102), no deviations · integrity OK · merged 784557e · regate-pass. FlowNotice/ReportNotice carry retryAt only; ServiceNotice computes the caption at render time (formatLocalTime single definition); D12 implemented: noticeExpiredAt (pure) + useNoticeWindowClear clears neutral-tone (sender) notices at window end, text-tone notices unaffected; red-check (drop the tone guard) failed exactly the danger-tone case. All CHANGES-REQUESTED findings are now addressed except the deferred L8/L10 → re-review dispatched over e753d96..784557e.
- 02:51 re-review (Opus) over e753d96..staging: APPROVE-WITH-FINDINGS. Verified: H1 fixed; consent gating intact (every server call needs the branded options, liveClientOptions gated); D10 settlement matches; D12 correct (tone rule, wired on all surfaces, no timer leak or re-render reset); the app-link parser accepts https://whim.anycognition.ca/a/<id> and rejects port, userinfo, trailing dot and extra segments; report logging content-free; privacy.html parity. New findings → fix chain review-2 (queued until iOS chain-3 merges, since it shares LauncherRoot.tsx): N1 MEDIUM, errorReason shows client-generated http hints ("Unexpected rewrite response shape"; require http status>=400 or device_id); N2 lax Retry-After HTTP-date branch contradicts both specs (delete it); N3 M1/M3 call sites unguarded (pins or a pure resolveOptions); N4 probeGateFor should take ConsentedClientOptions|null; N5 schemeAndHostOf uses the RN URL polyfill (a query can become the host in logs); N6 review-notes healthz parenthetical implies a pre-consent send; N7 leftovers (stale "line 418" comment, device_id 400 logged as network). Report-honesty nits recorded.
