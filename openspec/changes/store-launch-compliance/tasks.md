## 1. Launcher foundations (RN-free modules)

- [x] 1.1 Create `src/host/launcher/release-config.ts` per design D6: `WHIM_DOMAIN = 'example.com'`, a frozen `RELEASE` with `serverUrl`, `webHost`, `webOrigin`, `privacyPolicyUrl`, `supportUrl`, `appLinkBase`, and `AI_CONSENT_VERSION = 1`. No React Native import.
- [x] 1.2 In `server-address.ts` add `effectiveServerUrl(kv)` (saved override, else `RELEASE.serverUrl`; blank or whitespace counts as no override) and `clearServerUrl(kv)`, leaving `loadServerUrl`/`saveServerUrl` unchanged. Node suite: default on a fresh store, override wins, whitespace falls back, clear restores the default.
- [x] 1.3 Create `ai-consent.ts` per design D4: key `whim.ai-consent:v1`, `consentStatus(kv)` returning `granted` (with `grantedAt`) / `absent` / `outdated` against `AI_CONSENT_VERSION`, `grantConsent(kv, now)`, `revokeConsent(kv)`. Node suite covers ai-data-consent §"Consent grants are versioned", including malformed JSON reading as `absent`.
- [x] 1.4 Create `app-link.ts` per design D15: `appLinkFor(id)` and `parseAppLink(url): string | null`. Node suite covers app-links §"App links have one grammar": round trip over a `freshAppId()` value, a seed fixture id, and a `repo__lineage` fork id; the four rejection cases; an uppercase host accepted; query and fragment ignored.
- [x] 1.5 Add a source-scan suite for release-config §"One domain constant derives every Whim URL": fail, naming the file, when any launcher source other than `release-config.ts` contains the `WHIM_DOMAIN` value or a `whim.` URL literal. Red-check it by planting a literal in a scratch copy, not in the repo. Register every new suite from this group in `test/acceptance.ts`.

## 2. Client transport, refusal rules, consent-gated options, report call

- [x] 2.1 Add `code?: string` and `retryAfterSeconds?: number` to `GenerationClientError` (`transport-shared.ts`). Fill them in `httpErrorFrom`: `code` only when the body is structurally an `ApiError`, `retryAfterSeconds` only for a positive integer `Retry-After`. Have `xhr-transport.ts#finishHttpError` expose `xhr.getResponseHeader('Retry-After')` on its fake response. Node suite covers the generation-stream-transport delta: identical fields over fetch and XHR for `429` + `Retry-After: 120` + `server_busy`, a malformed header dropped, a non-`ApiError` body leaving `code` absent.
- [x] 2.2 Create `service-refusal.ts` per design D8 and D11: type-only `ServiceRefusalCode` import, the mapped-type `REFUSAL_RULES` table, `serviceRefusalOf(err)`, `retryAtOf(refusal, receivedAt)`, and `retryLine(retryAt, now, formatTime)` with the `Intl`-missing fallback. Node suite: the table's keys equal the real zod `ServiceRefusalCode.options` exactly (all seven, `budget_exhausted` included); `rate_limited` and hint-less bodies are not refusals; retry-line thresholds for seconds, minutes, same day and tomorrow; zero, negative and non-integer values count as absent.
- [x] 2.3 Introduce `ConsentedClientOptions` (branded `ClientOptions`) and its only constructor, `consentedClientOptions(status, baseUrl, deviceId)`, which returns `null` unless consent is granted (design D2). Change `clarifyPrompt`, `rewritePrompt` and `generateApp` to require it, and update the existing client suites' fixtures. Node suite: `null` for `absent` and `outdated`, options for `granted`.
- [x] 2.4 Add `sendReport(options: ClientOptions, body: ReportRequest, signal?)` to `generation-client.ts` (design D14): `POST /v1/report` with `requestHeaders`, a structural guard on the `202` body (non-empty `reportId`), errors through `httpErrorFrom`, thrown fetch errors as `network`. Node suite with an injected fetch: `202`, a `413 payload_too_large` carrying `code`, a network error, a malformed `202` body.
- [x] 2.5 Create `report-payload.ts` per design D13 and D14: the `ReportDraft` type, `buildReportRequest(draft)`, `reportPreview(request)`, `reportLogFields(request, outcome)`, and `reportDraftFor(entry, access)` over `InstalledApp.name`, `StoreAccess.activeDescription` and `StoreAccess.activeSource`. Node suite: built requests parse with the real zod `ReportRequest`; the note is cut at 1000 and the app name at 200; switched-off or absent prompt and source are omitted; preview rows equal the request's fields; log fields contain no note, prompt, source or app name.
- [x] 2.6 Add report note text to the sensitive keys in `src/host/logging/redact.ts` (host-observability delta), with a case in `src/host/logging/test/logging.suite.ts` showing a note field arrives redacted in the ring buffer.

## 3. Consent screen, consent gate, Settings

- [x] 3.1 Add to `copy.ts`: the consent screen strings (design D5: ask, outdated line, review-on, review-off, privacy link), the Settings section titles and rows (AI features on with date and off, `Privacy policy`, `Support`, `Advanced`, `Use Whim's server`, `Checked once AI features are on`). Follow the voice rules; the product-verbs guard passes.
- [x] 3.2 Create `ConsentScreen.tsx` per design D5: full screen, its own `BackHandler` declines, `SHELL_PALETTE` and SDK tokens only, and the privacy link through `Linking.openURL(RELEASE.privacyPolicyUrl)`. Ask mode offers `Agree and continue` and `Not now`; review mode puts the safe action as the large button.
- [x] 3.3 Create RN-free `consent-flow.ts`: `entryDecision(status, continuation)` (open the target, or the consent screen with continuation and `returnTo`), `declineTarget(returnTo)` (Home for an app screen), and the continuation union. Node suite covers ai-data-consent §"The first action that would send data asks for consent at that moment" and §"Permission is explicit", for every entry point.
- [x] 3.4 In `LauncherRoot.tsx`: add the `consent` `Screen` kind; route the composer row, long-press "Prompt again", the orb change action, history "Change it from here" and ghost Retry through `entryDecision`; derive `ConsentedClientOptions` from `consentStatus`, `effectiveServerUrl` and the device id in place of the old `clientOptions` memo; on grant run the continuation, on decline go to `declineTarget`. Remove the compose "set an address in Settings" branch, which no longer has any way to happen (prompt-flow delta).
- [x] 3.5 Wire `server-connectivity` to the gate (its `server-connectivity` delta): key the startup/retry effect on the consented options so nothing probes before consent, granting starts a probe, and revoking cancels the scheduled probe and resets the state to `unknown`; call `markOnline()` whenever `serviceRefusalOf` classifies a clarify, rewrite or generate error. Node suite for the pure pieces: no probe without consent, a probe on grant, cancellation on revoke.
- [x] 3.6 Rework `SettingsScreen.tsx` into AI features / Highlighting / About / Advanced per design D7 and app-launcher §"Settings groups its controls": Advanced collapsed unless an override is saved (`advancedInitiallyOpen(override)` in an RN-free sibling with a Node case), the placeholder showing the default host, `Use Whim's server` calling `clearServerUrl`, the save-time probe only with `canProbe`, and otherwise the neutral line. Review mode opens from the AI features row, and turning off calls `revokeConsent`.

## 4. Refusal UX in the prompt flow

- [x] 4.1 Create `ServiceNotice.tsx` per design D12 (plain-text hint, optional retry line, `danger` or neutral tone, tokens only), and add the retry-line copy (`in about {n} seconds`, `in about {n} minutes`, `in about {n} hours`, `after {time}`, `tomorrow after {time}`) to `copy.ts`.
- [x] 4.2 Create RN-free `refusal-landing.ts`: `refusalLanding(request, sentFrom, code)` per design D9, and `retryWindowState(retryAt, now)` returning `{ disabled, msUntilEnable }`. Node suite: the landing table for all seven codes against clarify-from-compose, rewrite-from-compose, rewrite-from-clarify and generate-from-plan.
- [x] 4.3 In `LauncherRoot.tsx` `onComposeContinue` and `openPlan`: on `serviceRefusalOf(e)`, route to the landing step with a `notice` on that screen's state (the compose, clarify and plan screen types gain `notice?`), log the code, status and request through the logging seam, and never open the failure screen. `isClarifySkip` stays 502-only.
- [x] 4.4 In `runAttempt` and `onRetryPending` (design D10): a fresh attempt refused while `ctl.detached` is false deletes its record and journal the way cancel does and returns to plan with the notice; a detached attempt or a Retry settles `failed` with the hint as its reason; a refused Retry keeps the failure screen with Retry gated. Confirm `errorReason` maps hint-less errors to the generic sentence (prompt-flow delta).
- [x] 4.5 Render `ServiceNotice` above the primary action in `ComposeStep.tsx`, `ClarifyStep.tsx`, `PlanStep.tsx` and `FailureScreen.tsx`. Disable the primary action (label unchanged) while `retryWindowState` says so, with one timer to re-enable it and no automatic request. Clear text-landing notices on a compose text change and on a saved plan row edit.
- [x] 4.6 Node suites for pending-builds and service-refusals: fresh-on-screen refusal deletes record and journal, detached refusal leaves a failed record whose reason is the hint, a refused Retry keeps the record failed with the hint, and the prompt-flow machine transitions carry and clear notices under an injected clock.

## 5. Report sheet

- [ ] 5.1 Create `SheetModal.tsx` per design D14: a transparent RN `Modal`, bottom rise on `MOTION.sheetRise`, scrim tap and `onRequestClose` both close, keyboard-aware, bottom safe-area inset, tokens only.
- [ ] 5.2 Create `ReportSheet.tsx` per content-reporting §§"The report sheet collects a reason", "previews exactly the body", "Sending a report", and "doesn't go through". It holds reason pills, the note (`maxLength` 1000), the include switches, a preview rendered from `reportPreview(buildReportRequest(draft))` with expandable prompt and monospace code, the AnyCognition line plus privacy link, `Send report` (busy `One moment`) and `Cancel`, the thanks state, and inline failures through `ServiceNotice` and the retry window. It sends with plain `ClientOptions` from `effectiveServerUrl` and the device id, and logs only `reportLogFields`. Add its copy to `copy.ts`.
- [ ] 5.3 Add `report` to `ORB_ACTIONS`, `ORB_ROW_TINT`, `ORB_ROW_GLYPH` and copy. Open `ReportSheet` over the running app from `MiniAppView`/`LauncherRoot` with `reportDraftFor(entry, access)`, keeping the sheet inside the boundary-covered subtree.
- [ ] 5.4 Add a plain-text `Report this app` under `DoneStep.tsx`'s two destinations, and a `Report` action in `HistoryScreen.tsx`'s header for the version the user is on. Both open `ReportSheet` through `reportDraftFor`, with no change to history row actions.
- [ ] 5.5 Add an `overlayOpen` input to the `back-policy.ts` reducer that yields `close-overlay` without forwarding or counting (mini-app-back-navigation delta), passed from `useMiniAppHost` while the report sheet is open. Node suite: the delta's "A host sheet takes back first" scenario, and an unchanged guaranteed exit once the overlay closes.
- [ ] 5.6 Node suites: the orb action table includes `report` and its tap count persists like the others; `reportDraftFor` over an app with and without stored source. Register the suites in `test/acceptance.ts`.

## 6. App links

- [ ] 6.1 Create RN-free `link-routing.ts` per design D15: `resolveAppLink(id, apps, pending)`, `linkExitFor(screen)` covering every `Screen` kind plus an open sheet, and a last-wins pending-link holder released on ready. Node suite covers app-links §§"opens the app", "pending build", "isn't on this phone", "leaves the current screen", and "waits".
- [ ] 6.2 In `LauncherRoot.tsx`: read `Linking.getInitialURL()` once ready and subscribe to `Linking.addEventListener('url')`; parse with `parseAppLink`; log a rejected URL with its scheme and host only; apply `linkExitFor`, then `resolveAppLink`; make a link to the app already open a no-op; add the `link-missing` `Screen` kind.
- [ ] 6.3 Create `AppLinkMissingScreen.tsx` (title, body and `Back to your apps` from design D15; its own `BackHandler` goes Home; tokens only) and add its copy.
- [ ] 6.4 Add an `App link` row to the installed-tile long-press sheet in `HomeScreen.tsx` (never on ghost tiles), opening `AppLinkSheet.tsx` on `SheetModal` with the app name, the link as `<Text selectable>`, and the copy line from design D16.
- [ ] 6.5 Write `handoff/app-link-platform.md` from design D17: the JS guarantees, the iOS entitlement and `AppDelegate` forwarding, the Android intent filter and `singleTask`, the AASA and assetlinks bodies, the fallback, privacy and support pages, and the three-place domain lockstep.

## 7. Store docs

- [ ] 7.1 Write `docs/store/review-notes.md` sections 1 to 4 per design D18 (how to try Whim, the guideline 4.7 mapping with the nine syscalls named, 2.5.2 context, what leaves the phone), quoting the real `COPY` labels as landed. Apply the `unslop` skill.
- [ ] 7.2 Add D18 sections 5 and 6 to `docs/store/review-notes.md`: Apple App Privacy and age-rating drafts, Google Play Data safety, the AI-generated content declaration, content rating, target audience, the webview JavaScript exemption (each marked as a draft to confirm in the console), and the pre-submission checklist including the D17 domain lockstep.
- [ ] 7.3 Append one entry to `docs/decisions.md` (read the tail for the next number) recording design D1, D2, D3, D6, D7, D10 and D16, with one line of why each.
