# Progress ledger: generation-loop

## Run metadata
- run-start: 2026-07-31 (overnight orchestrator, attended=false); staging branch v1-sprint (sprint-wide)
- chain DAG: {1 ∥ 2} → {3 ∥ 4} → 5 → 6 → 7; HUMAN-BOOTSTRAP touchpoint task 6.5 (pending-class2.md record only); task 7.6 attended on-device.
- PRECONDITION SCOPING (dispatcher decision): chains.md's "synthetic-run-harness fully merged before ANY chain" precondition is scoped to chains 5–7 per its own stated rationale (RunReport/harness interface consumption); chains 1–4 touch no synthrun interface and are dispatched against their declared file scopes. Chains 5–7 remain hard-blocked until synthetic-run-harness chain-5 merges.

## Dispositions (append-only)
- chain-1 (contract-wire-shapes) DISPATCHED: BASE=3e52c17, worktree=.claude/worktrees/generation-loop-1, implementer=sonnet.
- chain-2 (model-client-and-prompts) DISPATCHED: BASE=3e52c17, worktree=.claude/worktrees/generation-loop-2, implementer=sonnet.
- chain-1 REPORT: complete 4/4, GATE PASS, commit fd7f8d6, contract handoff/wire-shapes.md. Deviation (A, HARNESS-CRITICAL finding): fresh worktrees resolve @whim/contract|server to the PRIMARY tree via node_modules walk-up — worktree must create local @whim symlinks or self-gate validates stale code. Chain-1 fixed locally; dispatcher warned chain-2 mid-flight; all future contract/server-editing chains get symlink setup up front. MEMORY + docs/harness.md gap noted for closure.
- chain-1 INTEGRITY: exit 0. MERGED onto v1-sprint, tasks 1.1-1.4 ticked, REGATE PASS (primary tree regate resolves @whim/* to itself — merged code validated correctly here).
- chain-3 (device-seam) DISPATCHED: BASE=c81306c (includes chain-1 wire shapes), worktree=.claude/worktrees/generation-loop-3, implementer=sonnet; @whim symlinks pre-created by dispatcher per chain-1's walk-up finding.
- chain-2 REPORT: complete 7/7, GATE PASS, commit 241946d, contract handoff/model-client.md (109 lines). Deviations: A — symlink warning received pre-gate, all verification post-fix; A — prompts.suite covers adapter/no-network scenarios beyond literal 2.5 list (in-scope spec excerpt); A — per-turn context types split from PromptInputs (I/O boundary); B flagged forward — server/test/run.mjs needs external:['typescript'] when chain-7 wires prompts.suite into acceptance (suite verified standalone 84/84, documented in handoff for chain-7).
- chain-2 INTEGRITY: exit 0. MERGED onto v1-sprint, tasks 2.1-2.7 ticked, REGATE PASS.
- chain-4 (plan-and-state-machine) DISPATCHED next (after chain-2).
- chain-3 REPORT: complete 7/7, GATE PASS x2, commit d9a32bd, contract handoff/device-seam.md (97 lines). Deviations (A): buildGenerateRequest extracted to generation-request.ts (LauncherRoot un-importable under Node; mirrors history-logic split — behavioral tests over source-grep); activeSource now Promise<string|undefined> per spec (absence is honest, not an error); op-sqlite device path cannot guarantee no-file-created-on-peek (no fs-existence API; Node path fully honors it; documented, no DDL either way).
- chain-3 INTEGRITY: exit 0. MERGED onto v1-sprint, tasks 3.1-3.7 ticked, REGATE PASS.
- INTERRUPTION (monthly API spend limit): chain-4 implementer killed near completion. Partial work preserved as WIP commit ac32c70 on chain/generation-loop-4 (worktree retained): machine.ts, plan.ts, machine.suite.ts, handoff/pipeline-machine.md — NOT gated, NOT mergeable as-is. Resume per HANDOFF-v1-sprint.md: finish+gate in worktree, integrity vs BASE 80328a9, merge, regate; then chains 5→6→7. Knip's modelRosterFromEnv deferral clears when its consumer merges.
- RESUME RUN (2026-07-31, attended): chain-4 RE-DISPATCHED into the retained worktree .claude/worktrees/generation-loop-4 from WIP ac32c70, BASE=80328a9, implementer=opus; brief = finish/verify + self-gate + commit. @whim symlinks already present in that worktree.
- DISPATCHER CONSTRAINT (recorded 2026-07-31, resume run): generation-loop chain-7 (task 7.7) and eval-harness chain-F BOTH append to docs/capabilities.md; chain-7 also appends docs/decisions.md. The two lanes are otherwise file-disjoint, but these two chains MUST NOT run concurrently — the second of the pair is cut from the staging tip only AFTER the first has merged, so it appends below the other's row instead of conflicting at the table tail. A merge conflict here would be a HALT per apply.md step 9, not an auto-resolve.
- chain-4 REPORT: complete 6/6, GATE PASS (x2, determinism), commit afa9ddb, contract handoff/pipeline-machine.md (104 lines, under cap). Resuming implementer verified rather than rewrote: the interrupted session's uncommitted diff held a genuine correctness fix (roundDiagnostics accumulates CHECK + RUN diagnostics of one round for the repair decision, not RUN alone) plus an emitCompletion/settle() refactor closing an unhandled-rejection hazard on the usage promise. machine.suite.ts = 176 assertions, all passing standalone. Deviations: none.
- chain-4 CARRY-FORWARD (dispatcher, HARD REQUIREMENT for chain-7 task 7.5): neither chain-2's prompts.suite.ts nor chain-4's machine.suite.ts is registered in server/test/acceptance.ts, so `npm run server:test` type-checks them (server/tsconfig.json includes test/**/*.ts) but does NOT execute them. Both suites are currently gate-invisible. Chain-7 task 7.5 must register both; the dispatcher verifies at chain-7 merge that the server suite's assertion count actually rises, not just that the gate stays green.
- chain-4 INTEGRITY: exit 0 vs pinned BASE 80328a9, scope exactly as declared (4 files, no protected file). MERGED onto v1-sprint --no-ff, tasks 4.1-4.6 ticked, REGATE PASS.
- chain-5 (checks-build-and-record) DISPATCHED: BASE=377b0df07aec102b80afc4a3887500346276c8e1, worktree=.claude/worktrees/generation-loop-5, branch=chain/generation-loop-5, implementer=opus; @whim symlinks + npm run build pre-created. Preconditions verified: chain-3 + chain-4 merged (device-seam.md + pipeline-machine.md contracts available), synthetic-run-harness chains 1-5 all merged (its closing summary confirms), so the change-wide synthrun precondition is satisfied for chains 5-7.
- chain-5 REPORT: complete 7/7, GATE PASS (plus independently: typecheck, checks:test 65/65, server:test 152/152), commit 03b08ad, contract handoff/stage-contracts.md (95 lines, under cap). Deviations all Class A: (1) preflightSource's "default-exported defineApp" test approximated as `report.manifest !== undefined || diagnostics.some(kind==='manifest_not_static')` because public runStaticChecks only exposes {diagnostics, manifest?} — documented in stage-contracts.md §3, verified it rejects compiled-bundle text and accepts a diagnostic-bearing real defineApp; (2) preflightSource exported from check.ts but NOT wired into prompt assembly — that needs prompts/index.ts or the composition root, both chain-7 task 7.1 scope; flagged in the contract; (3) sonarjs/no-nested-template-literals forced extracting a schemaFieldEntry helper in checks/test/acceptance.ts, no behavior change.
- chain-5 checks/contract.ts AUDIT (dispatcher read the diff directly, not the claim): PURELY ADDITIVE — two string literals ('id_below_floor', 'build_failure') appended at the END of DIAGNOSTIC_KINDS plus two doc-comment bullets. No existing entry reordered, renumbered, or removed; no exported type's shape changed. Safe for the already-merged synthetic-run-harness and for eval-harness's type-only import. Verified post-merge: evals suite still 163/163.
- chain-5 RED-CHECKED (evidence, not claim): (1) disabling the allocation-floor call made both new floor scenarios fail; (2) removing the two kinds from DIAGNOSTIC_KINDS was caught by BOTH checks:test's B0 self-test AND a hard TS2322 in schema-check.ts. Both restored and re-verified green before commit.
- chain-5 INTEGRITY: exit 0 vs BASE 377b0df, 8 files, scope as declared, no protected file. MERGED onto v1-sprint --no-ff, tasks 5.1-5.7 ticked, REGATE PASS + evals 163/163.
- chain-5 CARRY-FORWARD (chain-7 task 7.5, now THREE unregistered suites): server/test/{prompts,machine,stages}.suite.ts all export their run*Tests() entry points but NONE is imported into server/test/acceptance.ts, so none executes under `npm run server:test` — they only type-check. Chain-7 must register all three. ADDITIONALLY: once stages.suite.ts is registered, server/test/run.mjs's esbuild call needs `external: ['typescript', 'esbuild']` — without it the bundle throws "Dynamic require of \"fs\" is not supported" at import time (stages/build.ts pulls esbuild in transitively via synthrun/builder.ts; prompts.suite.ts already pulls typescript). server/test/run.mjs was outside chain-5's scope. DISPATCHER VERIFIES AT CHAIN-7 MERGE that the server suite's assertion count actually RISES from 152 — a green gate alone would not distinguish "registered and passing" from "silently still not running".

## Dispatcher probe: suite-registration risk (2026-07-31, resume run)
- MOTIVATION: three suites (prompts/machine/stages) were merged but unregistered in server/test/acceptance.ts, so none executed — each author had verified theirs only via a throwaway standalone esbuild bundle, never through the real runner. Whether they PASS under server/test/run.mjs was unknown, and finding out only at chain-7 would have been late. Ran a disposable probe worktree off tip facb08e (no merge, no commit, torn down after) to answer it before chain-7 is briefed.
- VERDICT: all three pass. `npm run server:test` goes 152 -> **452 passed, 0 failed**. Per-suite: prompts 89, machine 176, stages 35 (152+89+176+35=452 exactly). No product bug, no test bug — a pure runner/registration gap. Ran twice, logs identical apart from a PID and a temp-dir suffix inside the intentionally-triggered esbuild resolve-failure test (`build_failure` diagnostic path — expected stderr noise, followed by ok lines). No flakiness.
- ESBUILD EXTERNALS: `external: ['typescript', 'esbuild']` is necessary as a PAIR; neither alone suffices. With none: `Dynamic require of "fs" is not supported` thrown from the bundled `typescript` package (frame `getNodeSystem`) via prompts.suite.ts's direct `import ts from 'typescript'`. With only `['typescript']`: same error class, now from node_modules/esbuild/lib/main.js — confirming stages.suite.ts -> stages/build.ts -> synthrun/builder.ts transitively bundles the esbuild package, which has the identical CJS-require problem. With both: fully green. No new npm deps needed; both already resolve from node_modules.
- VERIFIED RECIPE for chain-7 task 7.5 saved to scratchpad/chain-blocks/PROBE-suite-registration.patch (38 lines): three imports + three awaited calls appended in acceptance.ts before `report()`, and the external pair added to run.mjs's build() call. Nothing else was required — report() still runs once, the check/eq/section harness idiom was already shared.
- CHAIN-7 RECONCILIATION NOTE: chain-6 may independently edit server/test/run.mjs (its browser-backed runner needs externals too, incl. playwright). Chain-7 must MERGE with whatever chain-6 landed there rather than overwrite it — the externals array may already have entries.
- DISPATCHER VERIFICATION AT CHAIN-7 MERGE: require `npm run server:test` to report 452 (or higher, if chain-6/7 add assertions), NOT merely a green gate. A green gate cannot distinguish "registered and passing" from "still silently not running" — which is the exact failure mode this probe was run to retire.
- chain-6 REPORT: complete 6/6, GATE PASS (x2), commit a00450a, contract handoff/run-stage.md (117 lines). Browser-backed suite `node server/test/e2e.run.mjs` = 31 checks, 0 failed. Post-merge dispatcher verification: fast gate PASS, synthrun suite 82 checks (up from 75 — chain-6 added 7), e2e 31/31.
- chain-6 RED-CHECKED (edit-run-revert, observed): (1) containment short-circuit — made run.ts's contained:false branch leak report.diagnostics instead of [] => "nothing is fed back" went red (30/31) => reverted => 31/31; (2) post-abort reconciliation — neutered reconcile.ts's usageStore.credit with `if (foundAny && false)` => 3 assertions red (28/31, zeros vs credited) => reverted => 31/31.
- chain-6 task 6.5 RECORDED (applied neither) in openspec/changes/generation-loop/pending-class2.md: (1) package.json, after "server:test": `"server:e2e": "node server/test/e2e.run.mjs",`; (2) scripts/gate-full.sh, after `check "bridge-invariants" npm run -s bridge:invariants`: `check "generation-e2e"    npm run -s server:e2e`.
- chain-6 deviations, all Class A: (1) server/test/e2e.ts is the first server-space importer of synthrun/session.ts, which transitively pulled synthrun/page.ts's `import from '../build/assemble.mjs'` into server/tsconfig.json's program and surfaced a PRE-EXISTING latent TS7016 (that import had only ever been reached by the root RN tsconfig, which has allowJs). Fixed with a new synthrun/assemble.d.ts using a WILDCARD specifier — TS never matches an ambient `declare module` for a RELATIVE specifier — kept deliberately separate from env.d.ts because merging them let env.d.ts's process/crypto polyfills shadow server/'s real @types/node globals project-wide (both facts confirmed empirically). No build/* file touched. (2) Task 6.4's literal "byte-identical to the production build" is unreachable: createBuildStage (chain-5) always builds from TEXT via a synthesized temp filename, so esbuild's embedded source comment and derived identifiers can never match a file-path build; the test asserts byte-identity after normalizing exactly that one documented difference, PLUS a red-check proving the normalizer does not launder a genuine content difference. (3) createRunStage takes an already-bound RunCandidate rather than a SynthRunSession — containment cannot be forced false by any real candidate (probes.js's verdict comes from the harness's own fixed self-tests, never candidate code), so that path is only exercisable via an injected report.
- chain-6 INTEGRITY: exit 0 vs BASE facb08e, 12 files, scope as declared, no protected file. MERGED onto v1-sprint --no-ff, tasks 6.1-6.6 ticked, REGATE PASS.
- chain-6 CONFIRMED NO CONFLICT with the suite-registration probe recipe: chain-6 did NOT touch server/test/run.mjs or server/test/acceptance.ts (it added its own server/test/e2e.run.mjs runner instead), so the recorded 38-line probe patch applies to chain-7 unchanged.
- chain-7 REPORT: complete 6/7 (7.6 left PENDING/unticked, attended on-device, not attempted), GATE PASS, commit e80eaa1. **npm run server:test = 458 passed, 0 failed** (152 baseline + prompts 89 + machine 176 + stages 35 = 452, plus 6 new rewrite assertions = 458).
- chain-7 SUITE-REGISTRATION RISK CLOSED — verified by the dispatcher on the MERGED tip, by assertion count and not by gate colour: server:test 152 -> 458. The three suites that had been merged-but-never-executed since chains 2/4/5 now actually run. Probe recipe applied as recorded (three imports + three awaited calls before report(); `external: ['typescript','esbuild']` in run.mjs).
- chain-7 knip: `modelRosterFromEnv` no longer reported — its consumer is the new composition root's buildModelDepsFromEnv. This should clear the gate-full knip deferral outstanding since synthetic-run-harness.
- chain-7 preflightSource: WIRED. Pipeline.run applies preflightSource(request.app.source) before machine.run, so a supplied source failing the parse-and-defineApp check is treated as absent (closes chain-5's forwarded item).
- chain-7 RED-CHECKED (edit-run-revert, observed): (1) commenting out the `if (!apiKey) throw new MissingApiKeyError()` guard made a throwaway probe fail (expected MissingApiKeyError, got undefined) => reverted => green; (2) forcing rewrittenPrompt = parsed.data.prompt made the new "rewrite never echoes the input prompt verbatim" check fail => reverted => 458/458.
- chain-7 DOCS: docs/capabilities.md = pure one-row append below chain-F's corpus-eval row (dispatcher verified the diff). docs/decisions.md = 76 insertions, **0 deletions** (dispatcher verified append-only); tail was #55, took **decision #56**, status tag [BUILT — desktop gates green; on-device acceptance (task 7.6) PENDING].
- chain-7 deviations, all Class A: (1) Pipeline.run's real impl takes an optional 3rd `trace?: RunTrace`, but server/src/pipeline.ts is left verbatim per design D1, so routes/generate.ts widens the call type LOCALLY rather than editing that file — a stub ignoring the 3rd arg stays runtime-conforming; (2) **edited server/test/server-core.suite.ts, OUTSIDE its declared file scope** — task 7.2 retires the canned rewrite that suite's §5.5 test was written against, so leaving it would have broken the gate; (3) AppOptions.model/.roster/.reconcile made optional so out-of-scope call sites keep compiling, with unconfigured rewrite => 502 rewrite_not_configured; (4) added a real openRouterGenerationStatsTransport since no production implementation existed and D2 puts real wiring in the composition root; (5) main.ts stub mode still boots without model env vars (rewrite 502 until configured) since stub mode exists for zero-setup LAN UI work, while non-stub misconfiguration fail-fasts at startup.
- chain-7 OUT-OF-SCOPE EDIT ADJUDICATED (dispatcher read the diff directly, since the implementer flagged it for review): server/test/server-core.suite.ts is purely additive/adaptive — NO coverage removed. The §5.5 determinism assertion is retained (renamed, now over two identically-scripted apps), the invalid-body-400 case is untouched, and two genuinely strong assertions are ADDED (never-echoes-input, unconfigured=>502). ACCEPTED. Honest caveat on the record: with a ScriptedModelClient the "deterministic" assertion is now near-tautological — two clients scripted with the same string trivially return it — so its real content is "the plumbing adds no nondeterminism", not "the model is deterministic". The previous canned-echo version was equally weak; the two NEW assertions are where this block's value now lives.
- chain-7 INTEGRITY: exit 0 vs BASE 3b28f72, 10 files, no protected file. pending-class2.md NOT modified (no new Class-2 need; chain-6's two lines remain the only pending items). MERGED onto v1-sprint --no-ff, tasks 7.1-7.5 + 7.7 ticked, 7.6 left PENDING, REGATE PASS.
- POST-MERGE BATTERY on the merged tip (dispatcher-run, all green): fast gate PASS; server:test **458**; evals **196**; synthrun **82**; generation e2e **31**.
