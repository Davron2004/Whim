
## Lanes

- [ ] L1 (F1+F2, high): bundle-error recovery surface in MiniAppView.tsx (lastError branch, COPY.appError* keys, Retry via WebView key bump = fresh realm, Home via onExit) + paint watchdog in useMiniAppHost.ts (PAINT_WATCHDOG_MS=6000, paintTimer ref armed on 'delivery', cleared on 'paint'/bind()/exit()/unmount, fires setS lastError). Test: src/host/launcher/test/bundle-error-watchdog.suite.ts (source-assertion pattern per launch-failure-ui.suite.ts, registered in acceptance.ts). RED-without: MiniAppView has zero lastError refs; 'delivery' case is bare `return;`. Allowlist: MiniAppView.tsx, useMiniAppHost.ts, copy.ts, test/bundle-error-watchdog.suite.ts, test/acceptance.ts.
- [x] L2 (F3, med): tighten isWireAppRecord bundle conjunct to isRunnableBundle (non-empty + __WHIM_APP_MODULE__ substring assertion); rejection surfaces via existing stream_parse -> failure path. Tests in generation-client.suite.ts (empty bundle rejected, binding-less rejected, valid passes) + fixture repairs at generation-client.suite.ts:166 and xhr-transport.suite.ts:341. Allowlist: generation-client.ts + those two suites.
- [x] L3 (F4+F5, med): stub bundle built once through synthrun buildCandidateSource (memoized promise; no new deps — esbuild already exercised by this workspace), record renamed stub-app -> Hello App; /v1/rewrite gains stub short-circuit mirroring clarify (RewriteRouteOptions.stub, raw prompt with [[fail]] returned marker-intact, zero model calls), wired in app.ts. Tests in server-core.suite.ts: vm-eval of delivered bundle yields __WHIM_APP_MODULE__; rewrite preserves [[fail]] with no model call and generate then terminates failure. Allowlist: pipeline.ts, routes/rewrite.ts, app.ts, server-core.suite.ts, pipeline.suite.ts.
