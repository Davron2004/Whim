# Dispatch ledger: failure-dismiss-honesty

- run-start 2026-08-21 — staging branch `integration/failure-dismiss-honesty` cut from `v1-sprint` tip eb51762 (sprint precedent; main receives the sprint PR). Change proposed same-day from the live dismiss-button bug report; user approved shape "relabel + true no-op back".
- DAG: single chain (honest-exits, tasks 1.1–2.1).
- chain-1 dispatched — BASE eb51762, worktree .claude/worktrees/failure-dismiss-honesty-1.
- chain-1 report: complete, GATE PASS (6674 checks), commit f95ea6d. Hardware back rewired onDismiss→onBack with locks; discriminating red-check (injected journal.delete fails the no-store-calls lock). Deviation A: Discard border uses p.danger full-strength (no destructive-border token exists). Integrity exit 0 (5 files). Merged; tasks ticked; fast + FULL GATE PASS at 20231f4.
- reviewer: REJECTED. F1 HIGH LauncherRoot:917 — ALL five live failure sites keep onDismiss: goHome; three of them (showStreamFailure ×2, build-threw) HAVE a settled record, so the danger-styled "Discard this attempt" performs pure navigation — label/effect mismatch, new dishonesty introduced by this very change; the other two (clarify/rewrite, pre-attempt) offer discarding nothing. F2 HIGH report-mismatch — task 1.3 ticked but its live-failure half unimplemented; implementer's rationale contradicted by the code's own comment (:882-885). F3 MED wiring suite asserts nothing about live-branch onDismiss (exactly the introduced asymmetry). F4 LOW behavioural test half is store-poking, source-grep half carries the load — overclaimed. F5 LOW failureBack missing from flow-screen copy-key list. Clean: Back-touches-nothing, hardware back, copy keys, scope/styling/RN hygiene.
- fix chain R dispatched (same implementer continued) — BASE 20231f4, worktree .claude/worktrees/failure-dismiss-honesty-R. Prescription: recordId threaded distinct from pendingId; live Discard routes dropAttempt when record exists; NO Discard button pre-attempt; suite assertions per F3/F5 + no-Discard render branch.
- fix chain R report: complete, GATE PASS (6693 checks), commit cce0208. recordId on the failure variant (distinct from pendingId, primary stays Rephrase); live branch `pending.get(recordId)` → onDismissPending(settled) spread; onDismiss optional on FailureScreen, Discard button guarded; failureBack in copy-key list; overclaiming test header re-described. Red-checked both ways. Deviations A ×2 (journalId param renamed settledAttemptId setting both keys; orphaned doc comment re-homed). Dispatcher added the missing spec surface: ADDED requirement "Live failure screens offer Discard only when an attempt was settled" (2 scenarios) — validate green.
- fix chain R integrity exit 0 (5 files). Merged; FULL GATE PASSED at dd2bbe2.
- reviewer re-verify: VERDICT CLEAN at dd2bbe2 — all five findings closed, walked all five live construction sites independently (3 settled → working Discard; 2 pre-attempt → no button); locks re-counted directly (dropPendingBuild=1, journal.get=2); ADDED requirement's scenarios verified incl. the `{true && (` weaker-variant discrimination; no residual findings.
- CLOSURE: merged staging → v1-sprint (sprint precedent), specs synced, change archived.
