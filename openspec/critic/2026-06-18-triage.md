# Critic Triage — Ratification Cut-List (2026-06-18)

> Adjudication of the 89 baseline findings by 9 read-only `reviewer` agents, each re-opening the cited code. **Read-only — no code changed.** Severity here is the *adjudicator's* recalibration (flagged where it differs from the critic). Full original problem text for any finding lives in `openspec/critic/2026-06-18.md`.

**Status:** 87 confirmed · 2 partial · 0 false-positive.  **Severity:** 10 high · 33 medium · 46 low.

**Lanes:** mechanical-fix 52 · openspec-behavioral 7 · protected-file-human 13 · defer 15 · wontfix 2.

## Coordinated clusters (fix together)

- **A1 + A2** — top severity `high`; lane(s): mechanical-fix
- **C1 + C3 + C5** — top severity `high`; lane(s): mechanical-fix
- **E1 + E2** — top severity `high`; lane(s): mechanical-fix
- **G1 + G7** — top severity `high`; lane(s): protected-file-human
- **G2 + G5** — top severity `high`; lane(s): protected-file-human
- **H1 + H3 + H4** — top severity `high`; lane(s): protected-file-human
- **A3 + A4** — top severity `medium`; lane(s): mechanical-fix, openspec-behavioral
- **B1 + B2** — top severity `medium`; lane(s): mechanical-fix
- **C6 + C7** — top severity `medium`; lane(s): openspec-behavioral
- **G3 + G6** — top severity `medium`; lane(s): mechanical-fix
- **D11 + D8** — top severity `low`; lane(s): defer
- **I3 + I7** — top severity `low`; lane(s): defer, mechanical-fix

## The cut — not fixed now (ratify)

**C2** (C) · `medium` · wontfix · effort L

- **Status — confirmed.** The critic is correct that orphaned loose blobs from a partial snapshot() (killed between git.add and git.commit) are never freed by compactRepo because the deletion loop at compaction.ts:91 guards `if (reachable.has(oid))`. However: (a) the scenario requires process-kill mid-snapshot; (b) the compaction design in compaction.ts was explicitly scoped to reachable objects only, never a full GC; (c) the leak is bounded to a few blobs per crash event, not a per-normal-snapshot accumulation; (d) decision #36's justification is loose-object pressure from normal snapshot cycles. Severity is medium not high because it is crash-only. Downgrading to wontfix: documenting the known gap in the compaction.ts header is sufficient.

**H9** (H) · `low` · wontfix · effort S

- **Status — confirmed.** Confirmed that the four sed rules in settings.local.json lines 4-7 are stale (they target spike-2 on-device acceptance strings that have no current use). However, .gitignore explicitly excludes settings.local.json from version control (confirmed: `.claude/settings.local.json` is in .gitignore). This file is local-machine state only — it cannot be committed, it cannot drift into CI, and it has no effect on other developers. The stale allow-rules only expand the permission surface on the developer's local machine where they were presumably added intentionally. The 'audit surface' concern is real but negligible given the file's gitignored status and the write-protection provided by protect-harness.sh and bash-policy.sh for .claude/ paths. The human can clean it up at their discretion without a tracked change.

**A5** (A) · `low` · defer · effort S

- **Status — confirmed.** Confirmed: lines 43-54 in loader.js are a forward-declared nav-depth contract anchor comment added by the launcher-shell WIP commit (git diff shows it as new). The comment explicitly states it adds NO runtime behavior. The critic's concern that this adds content a security reviewer must filter is valid but minor: the comment is clearly labeled 'contract anchor only' and is not in any security-sensitive code path (it sits between the transport shim and the post() function). The byte-stability note in the comment is self-aware about the tradeoff. The launcher-shell change is already in progress (wip commit exists), so moving this comment will happen when that change lands. Forcing its relocation now creates a separate churn with no security value. Defer until launcher-shell change is implemented.

**B8** (B) · `low` · defer · effort M

- **Status — confirmed.** Confirmed at build/build.mjs:297-304: the full `bundles` map (all 11 entries including adversarial fixtures) is written to app-bundles.ts as APP_BUNDLES. The critic's concern is valid in principle: any future feature iterating APP_BUNDLES would silently include attack bundles. However, this is a future-hazard, not a present defect. Currently the seeding code explicitly names tip-splitter and water-counter (as the comment at line 296 states), and no existing code iterates APP_BUNDLES to build a picker or pre-warm list. The RUNTIME_HTML bundles map at line 251 already demonstrates correct explicit-allowlisting for the delivery surface. Deferring keeps the change list tight; it can be addressed when the launcher-shell / picker UI (D7) is built and would actually iterate APP_BUNDLES. At that point the split is a prerequisite, not a nice-to-have.

**C11** (C) · `low` · defer · effort M

- **Status — confirmed.** Confirmed at engine.ts:71-72, 77-78. VersionStoreOptions.backend is typed as the concrete MemoryFs class rather than an interface. The three methods used beyond FsBackend (countLooseObjects, listLooseObjects, removeTree) are defined on MemoryFs. The public factory functions in index.ts hide this type from all callers, and KvBackedFs extends MemoryFs, so there is no practical breakage today. This is a type-system cleanliness issue. Defer to a future internals-cleanup pass.

**C12** (C) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at compaction.ts:57-66. startOids is a Set so duplicate oids (multiple snap tags at the same commit) are deduplicated before the walk. Within each start oid, git.log walks to the root, so shared ancestors of multiple branches are traversed multiple times. The reachable Set deduplicates correctly for packing. This is a performance issue only, and the cost is negligible at current scale (O(10-100) snapshots, O(1-2) forks per mini-app). Defer.

**D8** (D) · `low` · defer · effort S · cluster: D11

- **Status — confirmed.** Confirmed. Both spec files have the TBD Purpose placeholder at lines 3-4. The specs are normative (cited by decisions.md #40 and #41) and the empty Purpose sections are a genuine doc-drift issue. However, this is documentation-only and has no correctness or security impact. The behavior and requirements sections that follow the Purpose are populated. Severity downgraded to low from medium — the spec bodies contain full requirements and scenarios; the missing Purpose is an orientation problem for a new reader, not a functional gap. Worth doing as a housekeeping task but does not block anything and poses no risk of a regression. Deferred to backlog rather than the active fix queue.

**D9** (D) · `low` · defer · effort M

- **Status — confirmed.** Confirmed. engine.ts:68 throws invalid[0] and engine.ts:71 throws diff.errors[0] when multiple errors are present. The validateArtifact() function comment says 'Returns every problem found' but only the first error surfaces through open(). This is a developer-experience problem rather than a correctness or security issue — the engine is still correct (it rejects the artifact), just not maximally helpful. Changing the surface to throw multiple errors would require a contract change to StorageEngineError or open()'s declared behavior, which is an openspec-level decision. At low severity with minimal real-world impact (most artifacts have one problem at a time, and the fix-resubmit cycle is tolerable), this does not warrant pursuing in the near term. The comment on validateArtifact is the main artifact; the logging-before-throwing option mentioned in the critic's rec could be done mechanically but the caller-facing behavior stays unchanged regardless.

**D11** (D) · `low` · defer · effort S · cluster: D8

- **Status — confirmed.** Confirmed. The spec at openspec/specs/storage-schema-evolution/spec.md:29-31 says 'Deleting a field SHALL retire its burned ID forever (tombstone)' and 'identities are never reused'. schema.ts:220-227 explicitly implements the rollback-across-tombstone resurrection path — moveColumn(nextColl, 'retired', 'active', f.id) — which decisions.md:392 documents as intentional. The spec and the code are in direct contradiction. This is real but low-severity doc-drift (the code behavior is the intended behavior; the spec language is imprecise). The fix is a spec wording update: clarify that 'forever' means an ID cannot be repurposed for a different field type, but rollback to the same field with the same type is an idempotent reinstatement. Deferred as a backlog spec-cleanup task; cluster with D8 since both are spec doc fixes.

**D12** (D) · `low` · defer · effort M

- **Status — confirmed.** Confirmed. validateArtifact() in schema.ts:126-134 validates tombstone IDs for form (BURNED_ID_RE) and checks that a tombstone ID is not simultaneously active, but does not cross-reference tombstones against the applied schema — it has no applied context. diffSchemas() at schema.ts:241-245 silently skips a tombstone whose ID is not in the current active list (the for loop over incomingTombstones does nothing if the ID is absent from nextColl.active). A phantom tombstone ('f99' never declared) would be silently stored in _meta as a phantom entry. This could confuse the diff logic if the same ID were later legitimately allocated and then tombstoned, but that sequence is unlikely given burned IDs are minted sequentially and the misfire would surface as unexpected 'older-subset' classification rather than data loss. The critic acknowledges this is low priority and the check cannot be in validateArtifact alone. Adding it to diffSchemas would require a design choice about warning vs. error. Not worth pursuing now given the low real-world risk.

**E4** (E) · `low` · defer · effort M

- **Status — confirmed.** Confirmed: the three ProbeScreens (BridgeProbeScreen.tsx:60-70, StorageProbeScreen.tsx:64-74, VersionStoreProbeScreen.tsx:79-89) have byte-identical StyleSheet.create blocks. However, all three screens are dev/probe-only surfaces — none are reachable in a production build (gated by RUN_VSTORE_PROBE, RUN_STORAGE_PROBE, RUN_BRIDGE_PROBE flags in App.tsx, or __DEV__). The duplication is real but the maintenance cost is low: these screens change rarely and independently (each has a different runner function and a different async pattern — BridgeProbeScreen uses setTimeout+promise, StorageProbeScreen uses setTimeout+sync, VersionStoreProbeScreen uses async IIFE). Extracting a shared ProbeScreen component is legitimate refactoring but the cost of drifting styles here is negligible. Routing to defer so it can be batched with future UI work.

**E8** (E) · `low` · defer · effort L

- **Status — confirmed.** Confirmed: app-bundles.ts and app-records.ts contain adversarial fixtures (evil, poison, victim, cap-intruder, sql-injector). LauncherRoot imports both unconditionally. The fixtures are unreachable to the end user (defaultSeeds() filters, DevProbeScreen is __DEV__-only) but they ARE bundled in the release JS. The bundle-split fix would require changes to the build pipeline (build/build.mjs) to produce separate dev/prod outputs and a conditional import in LauncherRoot — non-trivial and touching the build pipeline. The security impact is negligible: the adversarial payloads are WebView sandbox tests that only escape to the JS bundle, not to any native surface, and they cannot execute without explicit host delivery. This is backlog material; the dev/prod split is a good future improvement when the build pipeline is next touched.

**E9** (E) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at FloatingExit.tsx:24-26. `Dimensions.get('window')` is called at render time; the resulting `width` and `height` values are captured in the useMemo PanResponder dependency array (line 90). On device rotation the component will not re-render and snap math will use stale dimensions. However: (1) the app is Android-first and the current build is arm64 single-orientation; (2) FloatingExit is a dev/UX convenience affordance, not a security or data-integrity path; (3) the snap would be cosmetically wrong but the button remains tappable and still calls onPress = host.exit correctly. The fix (useWindowDimensions hook) is straightforward but low-urgency given the orientation constraints of the current target platform.

**G9** (G) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at sandbox-isolation-probe.html:149: the message handler has no ev.source or ev.origin check. However, this is a standalone manual artifact (a preserved spike probe page, not part of any CI pipeline). The live host in assemble.mjs does use nonce authentication. The probe page is loaded from file:// URLs in an isolated browser context for manual or CI use. In that context the threat model for postMessage spoofing is academic — there is no adversarial page that can reach this window in CI. The fix would be cosmetic alignment with the live loader's defense-in-depth pattern. The page is explicitly described as a 'preserved from Spike 1' artifact. The wontfix rationale is that fixing it in isolation (without updating the inlined script it builds) is incomplete, and the page has no test role in any automated gate. Low urgency is confirmed by the critic's own assessment.

**H7** (H) · `low` · defer · effort S

- **Status — partial.** The finding is partially correct but overstated. critic.md line 22 instructs the critic to write to `openspec/critic/<YYYY-MM-DD>.md`, and critic-run.md says 'Give it the ref and the report path openspec/critic/<today>.md'. These are consistent with each other — both describe file writing. The adjudicator (this role) uses StructuredOutput, but the adjudicator and the critic are different agents invoked differently. The /critic-run command dispatches the critic subagent with a file path; the critic writes the file. The human-ratifier (adjudicator) receives the structured output. The mismatch the critic identified — that the adjudicator uses StructuredOutput instead of reading the file — is a property of the ADJUDICATOR workflow, not the critic workflow. critic.md is correct for its own role. No fix needed.

**I7** (I) · `low` · defer · effort S · cluster: I3

- **Status — confirmed.** decisions.md #43 (line 512) and v1-roadmap.md #5 (line 185-186) both confirmed to say on-device acceptance PENDING / NOT YET RUN. The fix-launcher-shell-bugs progress.md records all chains GATE PASS but explicitly preserves the B1 on-device caveat. The decisions.md entry promises 'This entry is updated to [BUILT + ACCEPTED] once that run is recorded.' The actual on-device run has not been recorded anywhere. This is a real documentation gap but it is not an agent-fixable mechanical issue — updating decisions.md to [BUILT + ACCEPTED] requires a human to first perform or confirm the on-device run. Routing to defer: the right action is for the human to run the acceptance spec (acceptance.spec.md), then update the log; an agent writing [BUILT + ACCEPTED] without that run being done would be fabricating the acceptance verdict. Severity downgraded to low from medium: the emulator acceptance is done and recorded; the outstanding item is a real-hardware smoke check documented in backlog.md.

**I10** (I) · `low` · defer · effort S

- **Status — confirmed.** docs/harness-build-guide.md confirmed: 'Fable, high thinking' appears in the role table and narrative (lines 11, 517, 558). 'Fable' does not appear in CLAUDE.md, decisions.md, or v1-roadmap.md. From context, 'Fable' appears to be an internal code-name for an extended-thinking model used as the proposer — the guide treats it as a model identity distinct from Opus (dispatcher) and Sonnet (implementer). This is cosmetically inconsistent with how the rest of the repo names models, but it does not cause an agent to fail a task — the proposer role is the main-thread user session, not an invoked subagent; the 'model' column in the role table is informational. Routing wontfix: the guide is authored by the human and the 'Fable' name may be intentional (it is the human's own session identity). If updated, the human should do it. Marking worth_pursuing false — no correctness or cost impact.

## Severity overrides (adjudicator ≠ critic)

- **B5 → `low`** (openspec-behavioral). Confirmed at fixtures/latency-probe.app.tsx:41-43. The direct globalThis.__whimSyscall access is present. However, the in-code comment at lines 39-40 explicitly documents the intent: 'the SDK exposes storage; diag is the bare round-trip' — the author was aware this bypasses the SDK facade. The fixture is a diagnostic tool, not a user-facing production app, and the comment at lines 4-8 describes it as measuring 'the syscall round-trip over the REAL transport'. The underlying question is a product/spec decision: should a `diag` SDK facade be added (fixing the mini-app contract violation properly), or should this be formally excepted as a diagnostic tool with documented access to runtime internals? Neither outcome is mechanically obvious — it requires a spec/contract call. Severity is lowered to low because the risk is refactor-brittleness, not correctness or security: if __whimSyscall is renamed, the fixture breaks at runtime but only this diagnostic fixture is affected, and it falls back gracefully to Promise.reject.

- **C2 → `medium`** (wontfix). The critic is correct that orphaned loose blobs from a partial snapshot() (killed between git.add and git.commit) are never freed by compactRepo because the deletion loop at compaction.ts:91 guards `if (reachable.has(oid))`. However: (a) the scenario requires process-kill mid-snapshot; (b) the compaction design in compaction.ts was explicitly scoped to reachable objects only, never a full GC; (c) the leak is bounded to a few blobs per crash event, not a per-normal-snapshot accumulation; (d) decision #36's justification is loose-object pressure from normal snapshot cycles. Severity is medium not high because it is crash-only. Downgrading to wontfix: documenting the known gap in the compaction.ts header is sufficient.

- **C6 → `low`** (openspec-behavioral). Confirmed at openspec/specs/mini-app-versioning/spec.md:3-4 and mini-app-forking/spec.md:3-4. Both Purpose sections read 'TBD'. This is a doc gap only; severity drops to low from the critic's medium. Routes to openspec-behavioral because Purpose text is a product-level statement a human must ratify, not something an agent should fill silently.

- **D8 → `low`** (defer). Confirmed. Both spec files have the TBD Purpose placeholder at lines 3-4. The specs are normative (cited by decisions.md #40 and #41) and the empty Purpose sections are a genuine doc-drift issue. However, this is documentation-only and has no correctness or security impact. The behavior and requirements sections that follow the Purpose are populated. Severity downgraded to low from medium — the spec bodies contain full requirements and scenarios; the missing Purpose is an orientation problem for a new reader, not a functional gap. Worth doing as a housekeeping task but does not block anything and poses no risk of a regression. Deferred to backlog rather than the active fix queue.

- **E2 → `medium`** (mechanical-fix). Confirmed at useMiniAppHost.ts:235-252. The only useEffect is the BackHandler registration, whose cleanup (lines 248-251) only removes the listener and clears popTimer — no teardown of the realm. The `exit` function at lines 222-230 already has the correct teardown logic; an unmount cleanup effect calling it (minus the onExit callback) would make the invariant self-enforcing. Severity downgraded from high to medium: the E1 DevProbeScreen bug is the concrete breakage today; this finding is structural fragility (a missing defensive layer) that would be triggered by any future caller that forgets to route all exits through host.exit(). Not high because no second caller currently fails — but the architectural gap is real and worth fixing before new screens are added.

- **E3 → `low`** (mechanical-fix). Confirmed at VersionStoreProbeScreen.tsx:21-22. `USE_MMKV = true` with a stale 'Flip to true once MMKV is installed' comment. The `if (!USE_MMKV) return undefined` early-return in tryMmkv() is dead code with the flag hardcoded true. The fix is bounded: remove the constant, the dead branch, and update the comment. Severity is low (not medium) because this is a dev-only probe screen never reached in production, and the functional behavior is correct — MMKV is wired and working.

- **E4 → `low`** (defer). Confirmed: the three ProbeScreens (BridgeProbeScreen.tsx:60-70, StorageProbeScreen.tsx:64-74, VersionStoreProbeScreen.tsx:79-89) have byte-identical StyleSheet.create blocks. However, all three screens are dev/probe-only surfaces — none are reachable in a production build (gated by RUN_VSTORE_PROBE, RUN_STORAGE_PROBE, RUN_BRIDGE_PROBE flags in App.tsx, or __DEV__). The duplication is real but the maintenance cost is low: these screens change rarely and independently (each has a different runner function and a different async pattern — BridgeProbeScreen uses setTimeout+promise, StorageProbeScreen uses setTimeout+sync, VersionStoreProbeScreen uses async IIFE). Extracting a shared ProbeScreen component is legitimate refactoring but the cost of drifting styles here is negligible. Routing to defer so it can be batched with future UI work.

- **H2 → `medium`** (protected-file-human). Confirmed but with a material refinement that changes severity. .gitignore explicitly excludes `.claude/settings.local.json` (found in .gitignore: `.claude/settings.local.json`). Since the file is untracked, the git-diff tripwire in gate.sh (lines 22-33) can NEVER see it — not just because it isn't listed, but because git diff HEAD doesn't track untracked files. protect-harness.sh does cover `.claude/*` which would block agent edits. The actual threat model is: a committed change to the file is impossible (it's gitignored), but the existing allow-rules were placed there at machine-setup time outside git. An agent cannot expand these rules via Edit (protect-harness.sh blocks it) and cannot bash-write them (bash-policy.sh blocks `>` redirects to .claude/). The residual risk is lower than the critic assessed — the file cannot enter a commit, so the git-diff tripwire's gap is moot. Severity downgraded to medium because the write-protection layers (protect-harness.sh + bash-policy.sh) still cover the file path, even though the tripwire doesn't. The critic's recommendation to add a separate existence/content check is valid as a defense-in-depth measure.

- **I1 → `medium`** (mechanical-fix). All 11 cited specs confirmed to have verbatim TBD Purpose text. The critic grades this high/broken-invariant, but the requirements body of each spec is intact — the missing Purpose raises proposal cost and increases misattribution risk, but does not break a runtime invariant. The capability index in docs/capabilities.md provides a one-line substitute for orientation; Purpose is a second-level lookup. Downgrading to medium: real defect but not correctness-blocking. The fix is bounded: read the requirements body for each spec, write 2–3 sentences. No behavioral choices required.

- **I2 → `medium`** (mechanical-fix). All six specific stale passages confirmed. spec.md:100 says 'thin REST or tRPC'; spec.md:206 says 'schemaless and forgiving. JSON soup'; spec.md:289 says 'floating button' as primary; spec.md:316 says 'leaning DeepSeek [OPEN]'; spec.md:434-439 lists multiple [OPEN] items decisions.md #42 resolves. The spec itself states 'the log supersedes, the spec stays a thinking document' — so the design intent is acknowledged. However, with no banner warning readers, every new agent session or human reads stale decisions as live. The fix is a non-behavioral doc annotation: a clearly dated SUPERSEDED callout at the top of spec.md with per-section cross-references to decisions.md. No product behavior changes, no spec contract changes — purely adding reader guidance. Critic rates this high; downgrading to medium because the spec itself acknowledges it may be superseded, the decisions log is always consulted per CLAUDE.md, and no runtime code consumes spec.md directly.

- **I3 → `low`** (mechanical-fix). docs/bugs-2026-06-13.md confirmed: no status markers anywhere in the file. progress.md for fix-launcher-shell-bugs confirms B1–B9 all reached GATE PASS. B1 has an explicit caveat: 'B1 still needs an on-device smoke check' (the RN injectJavaScript timing race cannot be reproduced in Chromium). Reading the bug file today gives no signal on resolution status. Downgrading to low from medium: this is a closed archive doc — the bugs aren't being triaged from here in the normal workflow; the decisions log and progress.md are the live source. But it is the only durable bug log and a researcher could be misled.

- **I5 → `low`** (mechanical-fix). docs/capabilities.md confirmed: 13 rows, no static-checks or harness-diagnostics entries. Both specs exist at openspec/changes/static-check-pipeline/specs/harness-diagnostics/spec.md and openspec/changes/static-check-pipeline/specs/static-checks/spec.md. The change has no progress.md and chain-A is blocked on a human bootstrap action — so these specs are in-flight, not archived, and their canonical home is still the change folder, not openspec/specs/. Adding them to capabilities.md now with a note pointing to the change-folder location is valid and low-risk. Chain-G's note already anticipates this. Downgrading to low from medium because static-check-pipeline is not yet dispatched; these capabilities are not yet live, so a researcher orienting for current work will not miss them in practice. The fix is one maintenance update.

- **I7 → `low`** (defer). decisions.md #43 (line 512) and v1-roadmap.md #5 (line 185-186) both confirmed to say on-device acceptance PENDING / NOT YET RUN. The fix-launcher-shell-bugs progress.md records all chains GATE PASS but explicitly preserves the B1 on-device caveat. The decisions.md entry promises 'This entry is updated to [BUILT + ACCEPTED] once that run is recorded.' The actual on-device run has not been recorded anywhere. This is a real documentation gap but it is not an agent-fixable mechanical issue — updating decisions.md to [BUILT + ACCEPTED] requires a human to first perform or confirm the on-device run. Routing to defer: the right action is for the human to run the acceptance spec (acceptance.spec.md), then update the log; an agent writing [BUILT + ACCEPTED] without that run being done would be fabricating the acceptance verdict. Severity downgraded to low from medium: the emulator acceptance is done and recorded; the outstanding item is a real-hardware smoke check documented in backlog.md.

- **I8 → `low`** (mechanical-fix). Confirmed: static-check-pipeline has no progress.md; checks/ directory does not exist; chains.md marks chain-A as HUMAN-BOOTSTRAP — not dispatchable; no record of the human bootstrap being done appears in backlog.md or v1-roadmap.md. This is a planning-visibility gap rather than a broken invariant — the change is fully specified and waiting for a known, documented human action. Downgrading to low from medium: the state is 'limbo' only in the sense that a busy reader might not know the next action; adding one sentence to the roadmap or backlog entry fully resolves it. The critic's wording ('inconsistency') is apt.

## Full routing (by lane, then severity)

### Mechanical fix — automated loop — 52

**A1** (A) · `high` · mechanical-fix · effort S · ⚠ owner-authored test · cluster: A2

- **Status — confirmed.** Confirmed at loader.js line 110: the window.addEventListener('message', ...) handler has no ev.source guard. syscall.js line 69 explicitly has `if (ev.source !== window.parent) return;` with a comment calling it host-channel-only acceptance. The asymmetry is stark and looks unintentional. The critic's attack path is valid: after deliveryBusy resets at line 185 (synchronous, end of the handler), a bundle can fire window.postMessage with __whimDeliver:true on a setTimeout tick. This causes loader.js to increment __whimGeneration, wrap and inject the bundle's message payload as a DOM inline script (still under CSP + neutralize — no containment escape), and post a delivery frame to the host. The host's generation state machine is corrupted. Critically: the forged delivery frame uses the closure-captured hostNonce, which the bundle never saw — so the forged frame carries the real nonce, making it indistinguishable to the host from a legitimate delivery acknowledgement. This is a host-state integrity violation even though no containment escape occurs. The paired invariant probe (T6 already tests eavesdrop/spoof direction; a new owner-authored probe for self-posted __whimDeliver being ignored is needed to gate this fix).
- **Acceptance:** A postMessage handler in loader.js must return immediately (without processing __whimDeliver or __whimHostInit) when ev.source !== window.parent. A regression test (invariant probe) must: (1) from bundle scope, call window.postMessage(JSON.stringify({__whimDeliver:true, bundle:'window.__WHIM_SELFPOST_RAN=true'}), '*') and then window.postMessage(JSON.stringify({__whimHostInit:true, nonce:'evil'}), '*'); (2) assert that __whimGeneration is unchanged and __WHIM_SELFPOST_RAN is not set; (3) assert the host receives no spurious 'delivery' or 'ready' frame attributable to the self-post.

**C1** (C) · `high` · mechanical-fix · effort S · cluster: C3, C5

- **Status — confirmed.** Confirmed at engine.ts:218-221. compactRepo() is awaited bare with no try/catch inside snapshot(). The commit and tag are already durable at line 216 before the compaction call at line 219. Any throw from packObjects or indexPack propagates out of snapshot() so the caller sees a rejection even though the snapshot data is fully committed and tagged. On KvBackedFs the KV keys are written; the caller has no way to distinguish 'not saved' from 'saved, compaction failed', and a retry creates a duplicate g{n+1} for the same generation.
- **Acceptance:** Given autoCompact is enabled and compactRepo is made to throw: snapshot() resolves (not rejects) and the returned Snapshot has a valid id; a subsequent history() call lists the new snapshot; looseObjectCount is unchanged (not zero). Equivalently: snapshot's returned Promise never rejects due to a compaction failure.

**D1** (D) · `high` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed in engine.ts:145-146. The list() implementation builds out = { id: Number(row[RECORD_ID]) } and then iterates all active fields writing out[name] = fromStorage(...). BURNED_ID_RE requires [a-z][0-9]+ so the burned field ID can never be 'id' (no digit), meaning quoteIdent(RECORD_ID) is SQL-safe. However, the display name (fieldName) is the key of coll.fields in SchemaArtifact — which is user-supplied — and validateArtifact() in schema.ts:96-165 never checks display names against the reserved string 'id'. A field declared as fields: { id: { id: 'f1', type: 'int' } } passes all validation, open() succeeds, and list() will overwrite the engine-assigned row id with that field's value. The result is that every record's .id property holds a user-supplied int rather than the primary key, breaking update/remove callers. The fix is a single guard in validateArtifact: reject a field whose display name equals 'id' (and similarly 'id' as a collection display name to prevent StorageRecord key confusion). This is a single-obvious-correct-outcome fix with no behavioral choice required.
- **Acceptance:** Given an artifact with a field display name equal to 'id' (e.g. { schemaVersion:1, collections:{ Items:{ id:'c1', tombstones:[], fields:{ id:{ id:'f1', type:'int' } } } } }), validateArtifact() must return a non-empty error array (kind 'invalid_artifact' or equivalent) and engine.open() must throw a StorageEngineError — the artifact must NOT be accepted. Additionally, records.list() on a correctly-opened schema must never return a record where the .id property holds a non-integer or a value different from the engine-assigned primary key.

**E1** (E) · `high` · mechanical-fix · effort S · cluster: E2

- **Status — confirmed.** Confirmed at DevProbeScreen.tsx:40. The back button uses `onPress={onExit}` which calls `LauncherRoot.goHome` directly, bypassing `host.exit()`. The `exit` callback in useMiniAppHost (lines 222-230) is the only path that calls `tearDownRealm(live.current.realm)` and `live.current.realm.engine?.close()`. Skipping it leaves the live realm's DB handle open and the realm's alive flag set to true. MiniAppView.tsx:50 correctly uses `onPress={host.exit}` for FloatingExit — the fix pattern is already established in the same codebase. This is a concrete resource-leak bug with a single obvious correct fix.
- **Acceptance:** After the fix: a DevProbeScreen navigation-away via the back button must result in `tearDownRealm` being called for the live realm and `engine.close()` being called if a storage engine was open. Verified by: (a) the host state after exit shows no live.current reference remaining, and (b) no op-sqlite handle-closed-while-open error on subsequent launch.

**A2** (A) · `medium` · mechanical-fix · effort S · ⚠ owner-authored test · cluster: A1

- **Status — confirmed.** Confirmed by cross-reading neutralize.js and probes.js. neutralize.js lines 66-79 strip EventSource (line 66), RTCPeerConnection (line 67), SharedWorker (line 79), and caches (line 77). probes.js has zero mentions of any of these four globals (grep returns empty). neutralize.js itself at line 62 flags RTCPeerConnection as load-bearing: 'WebRTC is NOT covered by connect-src none, so the value-strip is the only thing that closes it.' A regression that drops RTCPeerConnection from neutralize.js would pass the full probe suite green. The never-regress invariant (probes.js) is hollow for these four. This is a missing-test finding on existing behavior — the tests are straightforward expectThrow additions with no structural changes needed.
- **Acceptance:** probes.js must include expectThrow('network', 'EventSource', ...) asserting new EventSource('https://evil.example') throws; expectThrow('network', 'RTCPeerConnection', ...) asserting new RTCPeerConnection() throws; expectThrow('ambient', 'SharedWorker', ...) asserting new SharedWorker('data:...') throws; expectThrow('ambient', 'caches.open', ...) asserting caches.open('test') throws. Each probe must appear in the invariant suite's probe result set with ok:true when run against a correctly built runtime.

**B1** (B) · `medium` · mechanical-fix · effort S · cluster: B2

- **Status — confirmed.** Confirmed at build/build.mjs:162: `mappedSource.includes('tip-splitter')` is hardcoded. The function signature accepts a generic `appName` parameter and uses it correctly in the `okSources` branch (line 150, though with a redundant OR guard), but the `roundTrips` predicate ignores `appName` entirely. Any call with a different appName produces `roundTrips = false`, making the gate at line 234 always throw. Currently only called once (for tip-splitter), so this is latent but will break immediately if B2 is acted on (adding more verifySourceMap calls). The redundant `s.includes('tip-splitter') ||` in okSources is a fossil of the same issue. Fix is a two-line substitution with a single obvious correct outcome.
- **Acceptance:** Given a call to verifySourceMap('water-counter', ..., 'fixtures/water-counter.app.tsx', <needle>) where the bundle contains the needle string and the source map correctly maps it back to the .tsx file, the function returns roundTrips=true and does not throw.

**B2** (B) · `medium` · mechanical-fix · effort M · cluster: B1

- **Status — confirmed.** Confirmed at build/build.mjs:229: verifySourceMap is called exactly once, for tip-splitter. The APPS map has 11 entries; water-counter and pour-over-timer are production fixtures used in RUNTIME_HTML. If esbuild regresses sourcesContent on TypeScript entries, the build passes silently for 10 of 11 bundles. Must be fixed after B1 (once appName is parameterized correctly), then called for at minimum the production fixtures. Adversarial bundles need only version+sources checks since they have no meaningful user-readable needle.
- **Acceptance:** After npm run build completes without error: verifySourceMap is called for at minimum tip-splitter, water-counter, and pour-over-timer; each call asserts okVersion=true and okSources=true; the production fixtures additionally assert okContent=true and roundTrips=true; a deliberate esbuild misconfiguration (e.g. removing sourcesContent:true) causes the build to throw.

**B3** (B) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at build/build.mjs:110-113: the catch block logs via console.log and returns fallback with capabilities=[]. The comment at lines 213-216 explicitly says 'best-effort: the throwaway sandbox-escape fixtures run module-scope attacks that throw under Node — they declare no capabilities, so a failed extract defaults to [] correctly.' This logic is sound for SKIP_EXTRACT entries, but the same silent fallback applies to production fixtures (water-counter, pour-over-timer, latency-probe, etc.). If a production fixture's extraction silently fails (e.g. due to a new SDK import triggering the REACT_STUB gap), app-records.ts ships with capabilities=[], and the capability gate denies every bridge call at runtime with no build signal. A post-extraction cross-check for non-SKIP_EXTRACT apps is the correct fix.
- **Acceptance:** If extractAppRecord returns capabilities=[] for any fixture not in SKIP_EXTRACT that declares non-empty capabilities in its defineApp call, the build process exits with a non-zero code and prints an error identifying the mismatched app. A correctly extracted production fixture (e.g. latency-probe with capabilities=['diag','storage']) does not trigger the error.

**B4** (B) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at fixtures/pour-over-timer.app.tsx:80-91 and 95-100. start() is async; after all three await delay(1000) calls resolve, it unconditionally calls setStageIdx(0), setRemaining(STAGES[0].secs), and setPhase('brewing'). reset() (lines 95-100) sets phase to 'idle' synchronously, but has no mechanism to interrupt the pending start() coroutine. The rendering at lines 132-136 confirms Reset is reachable during the 'ready' phase (while start() is suspended). The ghost-restart is a real UX correctness defect. The fix (a useRef cancelled flag checked after each await) has a single obvious correct implementation pattern. This is a mini-app fixture bug, not a spec-level behavioral choice.
- **Acceptance:** After pressing Reset during the 3-2-1 countdown, the app reaches phase='idle' and stays there indefinitely (does not transition to 'brewing' within 5 seconds). Pressing Pause during the countdown similarly leaves the app in 'ready' (or transitions to 'idle' if the start coroutine exits cleanly) without auto-advancing to 'brewing'.

**C3** (C) · `medium` · mechanical-fix · effort S · cluster: C1, C5

- **Status — confirmed.** Confirmed at compaction.ts:84-94. Each call to compactRepo writes a new .pack + .idx file via git.packObjects(write:true) but never enumerates or removes the previous pack files in objects/pack/. After N compaction cycles there are 2N dead KV keys for obsolete pack files. The deletion loop only targets loose objects via listLooseObjects(), which explicitly skips the pack/ shard. The design claim that 'compaction reduces KV key count' is undermined over time by pack file accumulation.
- **Acceptance:** After triggering compaction N >= 2 times on the same repo, the number of FS entries (or KV keys) whose path contains '/objects/pack/' equals exactly 2 (one .pack and one .idx for the single current pack). kvKeyCount() after the second compaction is not higher than after the first.

**C5** (C) · `medium` · mechanical-fix · effort M · cluster: C1, C3

- **Status — confirmed.** Confirmed by reading the acceptance suite. Section 4 tests (lines 191-215) exercise compaction via a MemoryFs-backed store and only check looseObjectCount. Section 5 creates a KvBackedFs store but never triggers compaction. The kvKeyCount() method on KvBackedFs is the metric the design claims compaction reduces (kv-fs.ts header: 'compaction therefore genuinely collapses KV keys'), and this claim has no test.
- **Acceptance:** The acceptance suite includes a test that: (1) creates a KvBackedFs store (MapKVBackend) with a low compaction threshold; (2) takes enough snapshots to trigger auto-compaction at least once; (3) asserts kvKeyCount() after compaction is strictly less than kvKeyCount() before compaction; (4) asserts looseObjectCount === 0 after compaction; (5) asserts history() still returns all snapshots.

**D3** (D) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. engine.ts:44 defines a local const BURNED_ID_RE = /^[a-z][0-9]+$/; which is byte-identical to the exported BURNED_ID_RE in contract.ts:72. The engine.ts import from './contract' at line 15-27 does not include BURNED_ID_RE. The local copy is used by quoteIdent() at engine.ts:292, which is the injection backstop that prevents any non-burned identifier from reaching SQL text. This is the most security-sensitive regex in the codebase, and drift between it and the canonical definition in contract.ts would be silent. The fix is one-line: add BURNED_ID_RE to the named imports from './contract' and delete line 44. No behavioral choice required; the correct definition is unambiguously in contract.ts.
- **Acceptance:** engine.ts must import BURNED_ID_RE from './contract' (verified by grep), must have no local definition of BURNED_ID_RE, and the quoteIdent() function must use the imported constant. TypeScript compilation must pass. The §C injection invariant tests must remain green.

**D4** (D) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. The test file (acceptance.ts:76-466) was read in full. No test stores and reads back a bool, float, or json field value. The type_change test at line 227 uses float in the artifact only as a rejected type change — no float value is ever written to or read from SQLite. bool and json field types appear nowhere in the test. The marshal paths are non-trivial: bool uses 0/1 integers; json serializes through JSON.stringify/parse; float uses SQLite REAL. A bug in any of these paths (e.g., fromStorage('bool') returning 0 instead of false, or fromStorage('json') silently corrupting nested structures) would not be caught by the blocking CI gate. These are existing behaviors that need test coverage; no behavioral choice required.
- **Acceptance:** The Node acceptance suite must include tests that: (1) store bool true and bool false and read them back, asserting the JS type is boolean (not 0/1); (2) store a float with a fractional part (e.g. 3.14) and read it back, asserting equality; (3) store a json field with a nested object and an array and read it back via JSON.stringify equality; (4) store null for each of these three types and read it back as null. All six assertions must be in the §A or a new §A field-types section and must pass under npm run storage:test.

**D5** (D) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. The acceptance suite §B tests (lines 202-330) cover: rename (line 202), type change rejection (line 221), new field without default (line 234), additive diff DDL (line 244), tombstone + fresh-ID reuse (line 268), rollback then roll-forward (line 289), and rollback-shaped DDL (line 315). The rollback-across-tombstone path — where a previously-retired field ID re-appears in a new artifact with the same type (schema.ts:220-227, the moveColumn(nextColl, 'retired', 'active', f.id) path) — is not exercised. schema.ts:224-227 has explicit logic for this case documented in decisions.md:392. The absence of a test means a regression in this path (e.g., incorrectly routing to tombstone_violation) would not be caught by CI.
- **Acceptance:** The Node acceptance suite must include a §B rollback-across-tombstone test that: (1) applies v1 with a field (e.g. f2); (2) applies v2 that tombstones f2 and verifies the tombstone was accepted; (3) applies v3 that re-declares f2 with the same type (no new default needed since the column exists). The test must assert: open(v3) does not throw, records.list returns data for f2's column (the physical column was never dropped), and the RecordingExecutor's DDL log for step (3) is empty (no ALTER TABLE emitted for the resurrected column).

**D6** (D) · `medium` · mechanical-fix · effort M

- **Status — confirmed.** Confirmed. Three JSON.parse calls without try/catch: marshal.ts:95 (fromStorage for 'json' type), engine.ts:92 (kv.get), and engine.ts:198 (loadApplied). All read from SQLite rows and call JSON.parse directly on the string. The loadApplied case (engine.ts:198) is the most severe: if the _meta row is corrupted the Engine constructor throws an uncaught SyntaxError, permanently bricking the engine with no actionable structured error. The kv.get case means a corrupted KV row throws a raw SyntaxError out of the public verb surface. The fromStorage('json') case means list() throws unexpectedly on a corrupted record row. The fix for each has a single obvious correct outcome: wrap in try/catch, throw a StorageEngineError. The loadApplied fallback to emptyApplied() is the deliberate recovery path (the engine can still open, triggering additive DDL to rebuild). No behavioral choice required for json/kv — these are clear bugs.
- **Acceptance:** Given a kv table row with non-JSON text as its v value (injected directly via the RecordingExecutor), engine.kv.get() must throw a StorageEngineError (not a SyntaxError), with kind in the StorageErrorKind union. Given a _meta row with non-JSON text as applied_schema, the Engine constructor must not throw — it must open successfully with an empty applied schema (triggering additive DDL on the subsequent open()). Given a record row with non-JSON text in a json-typed column, records.list() must throw a StorageEngineError (not a SyntaxError).

**D7** (D) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. op-sqlite.ts:34 reads: typeof db.executeSync === 'function' ? db.executeSync(sql, params) : db.execute(sql, params). In op-sqlite v16.2.0 (JSI-only, pinned version), executeSync is always present and the fallback is dead code. If the fallback fired, db.execute returns a Promise, and the subsequent res?.rows / res?.insertId / res?.rowsAffected accesses would all be undefined — every INSERT/UPDATE/DELETE would appear to silently succeed and every SELECT would return empty. The same pattern is repeated at device-acceptance.ts:99 and 116 in the resetAppDb and dbSizeBytes helpers. This is dead code whose backward-compat reason expired at the pinned version. The fix is mechanical: remove the ternary and call executeSync directly. Adding a startup assertion (throw if executeSync is not present) is the single obvious correct backstop.
- **Acceptance:** op-sqlite.ts must call db.executeSync(sql, params) directly with no typeof guard. A startup assertion must be present that throws an Error (not a StorageEngineError) if typeof db.executeSync !== 'function'. The same direct call must be used in device-acceptance.ts resetAppDb and dbSizeBytes helpers. TypeScript compilation must pass.

**E2** (E) · `medium` · mechanical-fix · effort S · cluster: E1

- **Status — confirmed.** Confirmed at useMiniAppHost.ts:235-252. The only useEffect is the BackHandler registration, whose cleanup (lines 248-251) only removes the listener and clears popTimer — no teardown of the realm. The `exit` function at lines 222-230 already has the correct teardown logic; an unmount cleanup effect calling it (minus the onExit callback) would make the invariant self-enforcing. Severity downgraded from high to medium: the E1 DevProbeScreen bug is the concrete breakage today; this finding is structural fragility (a missing defensive layer) that would be triggered by any future caller that forgets to route all exits through host.exit(). Not high because no second caller currently fails — but the architectural gap is real and worth fixing before new screens are added.
- **Acceptance:** After the fix: unmounting a component that uses useMiniAppHost without calling host.exit() must still result in tearDownRealm and engine.close() being called on the live realm. Verified by: a test that mounts the hook, delivers a bundle, unmounts the component tree without calling exit(), and asserts the realm's teardown was triggered.

**E5** (E) · `medium` · mechanical-fix · effort S

- **Status — partial.** The deliver-by-source.desktop.mjs Chromium test covers end-to-end source delivery but does NOT cover: (a) deliverBySourceJs throwing BundleTooLargeError for a source over the 512 KiB limit, (b) the byteLength fallback when TextEncoder is unavailable, or (c) useMiniAppHost catching the error and setting lastError state. The guard at deliver.ts:48-50 is the only host-side protection before a pathological source reaches injectJavaScript. The Node launcher test suite (acceptance.ts) has no deliver.ts coverage at all. The three missing cases are deterministic pure-JS behavior that can be tested in the Node launcher suite without a browser.
- **Acceptance:** After the fix: the launcher Node test suite includes tests for deliver.ts covering: (1) deliverBySourceJs returns a string for a source within MAX_BUNDLE_SOURCE_BYTES, (2) deliverBySourceJs throws BundleTooLargeError (with correct .bytes value) for a source of MAX_BUNDLE_SOURCE_BYTES+1 bytes, (3) byteLength returns s.length when TextEncoder is not defined in the environment. All three pass under `npm run launcher:test`.

**E6** (E) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at build/assemble.mjs:131. The __whimUiEvent branch relays any postMessage with __whimUiEvent:true to RN without checking ev.source against iframe.contentWindow. Lines 136 (__whimNavDepth) and 139 (syscall) both guard with `if(ev.source !== (iframe && iframe.contentWindow)) return;` before acting. The inconsistency breaks uniform source-verification. While the consequence today is only cosmetic (ui-events carry `trusted:false`) and no second frame currently exists, adding the source check is the correct pattern and this is an agent-writable file. Note: assemble.mjs is in build/ not in the protected list.
- **Acceptance:** After the fix: the __whimUiEvent branch in assemble.mjs orchestrationScript checks `if(ev.source !== (iframe && iframe.contentWindow)) return;` before calling toRN. The desktop deliver-by-source verification and invariant suite both pass. A message with __whimUiEvent:true posted from a non-iframe source (e.g. the outer page itself) is not relayed to RN.

**F1** (F) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/src/sse.ts:37-65. The ReadableStream is constructed with only a start() method — there is no cancel() handler. Both clearInterval calls (lines 57 and 62) are inside the start() async closure and only run when the for-await loop ends naturally or throws. The try/catch on enqueue (lines 43-47) silently swallows the post-close error, so the interval continues to fire on client disconnect until the pipeline generator finishes. With the real OpenRouter pipeline (30–60 s) this is a real resource leak. The fix has a single obvious correct outcome: add a cancel() method that calls clearInterval(keepaliveInterval).
- **Acceptance:** When the client disconnects mid-stream (ReadableStream cancel() is invoked), the keepalive interval is cleared within the same tick. A test that creates a stream with a long-lived source, cancels the reader, and asserts the interval fires 0 times after cancellation would pass.

**F3** (F) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/src/openrouter.ts:11 ('import type { Usage }' — no runtime schema object imported) and server/test/contract.test.ts:88-95 (the comment punts: 'identity is asserted in the modules that reuse it' — but no test or module does so). server/test/openrouter.test.ts:118 calls Usage.safeParse(capturedUsage) which is a shape check, not a reference-identity check. SPEC §1.5 and SPEC §7.2 both require the same Usage schema by identity. The fix is mechanical: change 'import type' to a value import in openrouter.ts and use Usage.parse() or assert identity in the test. The fix outcome is unambiguous.
- **Acceptance:** openrouter.ts imports Usage as a value (not type-only). The contract test (or openrouter test) contains a reference-equality assertion: the Usage object imported by openrouter.ts, the Usage event schema in GenerationEvent, and the Usage export from @whim/contract are all the same object reference (e.g. assert capturedUsage validated by Usage.safeParse). A test passes that would fail if openrouter.ts declared its own inline Usage-equivalent object.

**F4** (F) · `medium` · mechanical-fix · effort M

- **Status — confirmed.** Verified in server/test/metering.test.ts:77-93. The §6.2 test reads usage via store.read() and checks Object.keys(usage).length === 3 and type-checks the returned fields. This tests the public read API output shape, not the underlying table schema. The comment at line 90 explicitly says 'verified by the store's CREATE TABLE' — that is not a test assertion. A prompt_text column added to CREATE TABLE would not be detected. The fix is mechanical: open a second DatabaseSync(':memory:') is not possible since ':memory:' databases don't share state across connections. The correct fix is to either expose the db handle via a test-only accessor or use a temp file and run PRAGMA table_info(usage) from a second connection, then assert the column names are exactly {device_id, prompt_tokens, completion_tokens, total_tokens}.
- **Acceptance:** The §6.2 test block opens the SQLite database (via a temp file + second DatabaseSync, or a test-only handle accessor) and runs PRAGMA table_info(usage). It asserts that the returned column names are exactly ['device_id', 'prompt_tokens', 'completion_tokens', 'total_tokens'] — any extra column (e.g. 'prompt_text') causes the test to fail.

**G3** (G) · `medium` · mechanical-fix · effort S · cluster: G6

- **Status — confirmed.** Confirmed at invariants/sandbox-isolation/run-against-build.mjs:193: the waitForFunction predicate is '() => /Timer Ticker|gen 1/.test(status) || true'. The '|| true' makes the predicate unconditionally resolve in the first Playwright polling tick, rendering the wait a no-op. The comment says this waits for the gen-1 ticker to run, but the actual synchronization is the subsequent 500ms waitForTimeout (line 194). If torn.before === 0 (no ticks arrived yet), the test self-diagnoses as VACUOUS and records a FAIL. On a loaded CI runner this is a real flakiness risk. The fix is trivially removing '|| true' from the predicate — a single-obvious-correct-outcome code change.
- **Acceptance:** After removing '|| true': the waitForFunction on line 193 must block until document.getElementById('status').textContent matches /Timer Ticker|gen 1/, or time out at 8000ms. In a run where the ticker starts normally, torn.before must be > 0 before the boundary is set. The INV-TIMER test must not record 'VACUOUS' on any run where the ticker fixture loaded correctly.

**G4** (G) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at run-against-build.mjs:128-130: rejectedSpoof is computed correctly on line 128 via 'r.console.some((l) => /REJECTED-FORGERY kind=spoof-probe/.test(l))'. But the ok condition on line 130 is 'contained === true && rejectedForgery && honestBlocked && !r.errors.length' — rejectedSpoof is absent. The diagnostic string on line 131 includes forgedSpoofRejected=${rejectedSpoof} for humans to read, but the gate does not fail if it is false. The T6b spoof-probe constraint (the host must authenticate/ignore bundle-origin control messages) is therefore enforced by comment and diagnostic string only. A nonce-auth regression that lets kind=spoof-probe frames through would not be caught by this test. The fix is adding rejectedSpoof to the ok condition — a single mechanical change.
- **Acceptance:** After the fix: if the host's message handler accepts a kind=spoof-probe frame without rejection (i.e., the REJECTED-FORGERY log line is absent for kind=spoof-probe), the b-evil check must exit with FAIL and the run-against-build.mjs suite must exit 1.

**G6** (G) · `medium` · mechanical-fix · effort S · cluster: G3

- **Status — confirmed.** Confirmed at run-against-build.mjs:141: the waitForFunction condition is '() => /gen 1/.test(status) && /victim/i.test(document.title + "") === false'. The second clause checks that document.title does NOT contain 'victim'. document.title is set by the host to 'WHIM:pending', 'WHIM:CONTAINED', or 'WHIM:LEAK' — the word 'victim' never appears there. The second clause is always true, making the effective wait '() => /gen 1/.test(status)'. This means the test does not wait for the victim's probes to actually complete; the real synchronization is the 1200ms waitForTimeout on line 142. Compare to test 5 (line 162) which correctly waits for /gen 2/.test(status) as a deterministic signal. The dead condition should be replaced with something that signals the new realm's probe completion.
- **Acceptance:** After the fix: the waitForFunction at the reset re-injection step must not have a clause that is always-true. A correct condition should prevent advancing past the drive block until the new realm has delivered its probes (e.g., the status element shows gen-1 activity from the victim, or the title transitions away from WHIM:pending). The fixed test should not rely solely on a fixed-duration sleep for correctness.

**I1** (I) · `medium` · mechanical-fix · effort M

- **Status — confirmed.** All 11 cited specs confirmed to have verbatim TBD Purpose text. The critic grades this high/broken-invariant, but the requirements body of each spec is intact — the missing Purpose raises proposal cost and increases misattribution risk, but does not break a runtime invariant. The capability index in docs/capabilities.md provides a one-line substitute for orientation; Purpose is a second-level lookup. Downgrading to medium: real defect but not correctness-blocking. The fix is bounded: read the requirements body for each spec, write 2–3 sentences. No behavioral choices required.
- **Acceptance:** For each of the 11 specs, the ## Purpose section contains at least 2 non-placeholder sentences describing what the capability covers, which source path implements it, and what invariant or user property it protects. The literal string 'TBD - created by archiving' must not appear in any openspec/specs/*/spec.md file.

**I2** (I) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** All six specific stale passages confirmed. spec.md:100 says 'thin REST or tRPC'; spec.md:206 says 'schemaless and forgiving. JSON soup'; spec.md:289 says 'floating button' as primary; spec.md:316 says 'leaning DeepSeek [OPEN]'; spec.md:434-439 lists multiple [OPEN] items decisions.md #42 resolves. The spec itself states 'the log supersedes, the spec stays a thinking document' — so the design intent is acknowledged. However, with no banner warning readers, every new agent session or human reads stale decisions as live. The fix is a non-behavioral doc annotation: a clearly dated SUPERSEDED callout at the top of spec.md with per-section cross-references to decisions.md. No product behavior changes, no spec contract changes — purely adding reader guidance. Critic rates this high; downgrading to medium because the spec itself acknowledges it may be superseded, the decisions log is always consulted per CLAUDE.md, and no runtime code consumes spec.md directly.
- **Acceptance:** docs/spec.md must contain, before §1, a clearly delimited banner (e.g., blockquote or NOTE callout) stating the document may be superseded, directing readers to docs/decisions.md as authoritative, and listing at minimum the six reversed sections (§4.4 WebSocket, §4.7 tRPC, §5.6 schemaless storage, §10 floating button, §13 DeepSeek, §17 OPEN items) with their decision numbers. The literal text 'TBD' or stale [OPEN] labels on those sections must either be annotated with a reversal note inline or referenced from the banner.

**I4** (I) · `medium` · mechanical-fix · effort S

- **Status — confirmed.** CLAUDE.md Commands section confirmed: lists only npm run build, invariants, invariants:spike2, vstore:test, lint — and states 'CI runs build + invariants as a blocking gate.' The actual invariants.yml (confirmed read) has 8 blocking steps: server:test, guard:metro, storage:test, bridge:test, launcher:test, npx playwright install, build, invariants, bridge:invariants. An agent or new developer following CLAUDE.md would not run storage:test, bridge:test, launcher:test, server:test, guard:metro, or bridge:invariants before pushing. CLAUDE.md is not inside .claude/ — it is at the repo root and is not hook-protected. The fix is a doc update only.
- **Acceptance:** CLAUDE.md Commands section must list all blocking CI steps that invariants.yml runs, including storage:test, bridge:test, launcher:test, server:test, guard:metro, and bridge:invariants, each with a one-line description of what it tests. The sentence 'CI runs build + invariants as a blocking gate' must be updated to enumerate all blocking steps or reference the full list.

**A4** (A) · `low` · mechanical-fix · effort S · cluster: A3

- **Status — confirmed.** Confirmed: resolver.js line 36 says 'CLOSED allowlist — exact-name match only. No prefix/subpath/relative resolution, ever.' and lines 12-14 list 'react-dom/server, fs, child_process, relative paths, SDK subpaths, any 3rd-party' as things that throw. But line 32 also resolves 'react-dom/client', which is a subpath — directly contradicting both comments. This is a comment/code contradiction that creates an incorrect mental model of the security surface for anyone reading the resolver. The fix is purely textual and follows whatever decision A3 produces: update the comments to accurately reflect the allowlist. Clusters with A3 because A3's resolution determines the correct comment content.
- **Acceptance:** After A3 is resolved: resolver.js line 36 comment and lines 12-14 comment accurately list all entries that resolve (and explicitly state whether react-dom/client is included or excluded). There must be no contradiction between the comment describing 'no subpath' and an actual subpath in the allowlist.

**A6** (A) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed: syscall.js line 90 does a plain writable property assignment `window.__whimSyscall = { call: call }`. neutralize.js uses Object.defineProperty with writable:false/configurable:false for all security-relevant globals. The critic's cross-generation scenario requires a host violation of constraint #5 (realm reset), which the design mandates never occurs. Within a single realm, self-sabotage is self-inflicted and harmless. However, making __whimSyscall non-configurable is cheap, consistent with the existing pattern in neutralize.js, and closes the concern independent of any host-side slip. The fix is a single Object.defineProperty call and has a single correct implementation. No security-critical probes needed — the property protection is an implementation consistency fix.
- **Acceptance:** syscall.js must install window.__whimSyscall using Object.defineProperty with writable:false and configurable:false (matching the pattern in neutralize.js). A probe or invariant test must assert that attempting window.__whimSyscall = null (or similar reassignment) either throws in strict mode or leaves window.__whimSyscall unchanged, confirming the property is non-writable.

**A7** (A) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed: src/sdk/index.tsx line 74 `const d = Number.isFinite(ms) && ms > 0 ? ms : 0`. Number.isFinite(Infinity) === false, so delay(Infinity) resolves immediately via setTimeout(resolve, 0). The JSDoc at line 69 says 'Resolve after at least ms' with no caveat about non-finite input. The sibling interval() at line 108 correctly bails with `if (!running || !Number.isFinite(ms) || ms < 0) return undefined;`. The inconsistency is real and the bug is a plausible footgun: a mini-app author writing `await delay(Infinity)` to mean 'never proceed until realm teardown' would proceed immediately. The fix is a simple early-return guard before line 74. Low severity because Infinity is an unusual argument, the JSDoc does not explicitly promise it works, and no existing fixture uses it.
- **Acceptance:** delay(Infinity) must return a Promise that does not resolve within at least 10 seconds (i.e., effectively never resolves until the realm is torn down). delay(0) must continue to resolve on the next event loop tick. delay(-1) must also return a never-resolving promise (or throw a TypeError — either is acceptable; the JSDoc must document the choice). The behavior must match interval()'s !Number.isFinite(ms) handling.

**B6** (B) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. Line 11 header says 'diagnostics on'; lines 243-244 comment says 'tip splitter, diagnostics on'; line 270 generated file comment says 'on-screen probe diagnostics'. The actual call at line 262 is showDiagnostics: false. These are doc comments in build/build.mjs and the string emitted into the generated file header — both are misleading to any future maintainer. The fix is purely textual with no behavioral change.
- **Acceptance:** Lines 11, 243-244, and 270 of build/build.mjs (and the regenerated src/runtime/generated/runtime-html.ts header) all describe the production build as diagnostics-off / full-screen iframe and do not say 'diagnostics on' or 'on-screen probe diagnostics'. After npm run build, the generated runtime-html.ts header reflects the corrected description.

**B7** (B) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at build/build.mjs:136 and 139. `let gcol = 0` is declared and `gcol += f[0] || 0` is executed per segment, but gcol is never used in any return value, condition, or log. It is genuinely dead code in a hand-rolled VLQ consumer. Removing it does not change any behavior — the function returns only source index and source line. Leaving dead tracking state in subtle parsing code is a low-grade maintenance hazard (future readers may wonder if gcol is supposed to gate something).
- **Acceptance:** build/build.mjs contains neither `let gcol` nor any assignment to `gcol`; the build and all source-map verification checks pass identically after the removal.

**C4** (C) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at kv-fs.ts:51 and 81. `private ready = false` is declared and set to true at the end of hydrate(), but is never read anywhere in the file. The actual construction-time guard is `if (!this.kv)` at lines 86 and 97. The `ready` field is dead scaffolding. Removal is purely mechanical.
- **Acceptance:** The `ready` field does not appear in kv-fs.ts; TypeScript compilation passes; the acceptance suite passes.

**C8** (C) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at engine.ts:237 and engine.ts:406. Both sites use `?? ''` to silently emit id:'' when a commit has no snap tag. In normal operation this cannot occur, but if the invariant breaks (compaction drops a tag, future code adds untagged commits) the caller gets a structurally valid Snapshot with id:'' that will throw 'unknown snapshot' on any subsequent use. Silent corruption rather than a loud invariant failure.
- **Acceptance:** history() and getSnapshot()/snapshotContent() throw a descriptive internal error (e.g. 'invariant: commit <oid> has no snap tag') rather than returning id:'' when the oidToId map contains no entry for a commit oid.

**C9** (C) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at index.ts:47-72. The HEX40 regex tests every string value recursively including artifact content. A mini-app bundle.js containing a 40-character hex string (content hash, stripped UUID) triggers a false-positive throw in the test helper. The FORBIDDEN_KEYS check on object keys is the meaningful guard for 'git never exposed'; the hex check on arbitrary string values is fragile.
- **Acceptance:** assertNoGitLeak({ artifacts: { 'bundle.js': 'a'.repeat(40) } }, 'snap') does not throw. assertNoGitLeak({ oid: 'a'.repeat(40) }, 'snap') still throws. assertNoGitLeak({ id: 'a'.repeat(40) }, 'snap') still throws (top-level string value outside artifacts).

**C10** (C) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at engine.ts:287. The regex `/^[A-Za-z0-9 ._-]+$/` permits space. PIN_TAG constructs `whim/pin/<label>`, so a label like 'known good' creates a tag ref with a space. MemoryFs and KvBackedFs handle this without error (Map keys are arbitrary strings) but the stored state is not portable to real git and any future code that shell-escapes or URL-encodes ref names would break.
- **Acceptance:** pin('app', 'g1', 'known good') throws an error matching /invalid pin label/. pin('app', 'g1', 'known-good') and pin('app', 'g1', 'known_good') succeed.

**D10** (D) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed. engine.ts:207-216 (resolveCollection) correctly throws { kind: 'not_open' } when this.active is null. The test suite (acceptance.ts reviewed in full) never calls any records verb before open() and never asserts on 'not_open'. This is a low-severity missing test for an existing guard — not a bug, but a regression risk if the guard is accidentally removed. The fix is a single small test: create an engine, call records.list (or append) before open(), assert kind === 'not_open'. Mechanical, low effort.
- **Acceptance:** The Node acceptance suite must include a test that creates an engine without calling open(), calls records.append (or records.list) on any collection, and asserts that a StorageEngineError is thrown with detail.kind === 'not_open'. The test must run as part of npm run storage:test.

**E3** (E) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at VersionStoreProbeScreen.tsx:21-22. `USE_MMKV = true` with a stale 'Flip to true once MMKV is installed' comment. The `if (!USE_MMKV) return undefined` early-return in tryMmkv() is dead code with the flag hardcoded true. The fix is bounded: remove the constant, the dead branch, and update the comment. Severity is low (not medium) because this is a dev-only probe screen never reached in production, and the functional behavior is correct — MMKV is wired and working.
- **Acceptance:** After the fix: VersionStoreProbeScreen.tsx has no USE_MMKV constant, tryMmkv() always attempts the lazy require without an early-return guard, and the comment accurately states MMKV is wired. TypeScript compiler and lint pass with no new errors.

**E7** (E) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at LauncherRoot.tsx:132. `onCreate={() => {}}` is a no-op stub. HomeScreen's showCreate (line 52-55) calls `onCreate()` then always shows the Alert regardless — so the empty function adds no behavior. The stub is not annotated as a placeholder. The fix is a one-line comment addition: `// TODO(#7): navigate to prompt screen` to mark it as intentional scaffolding. This is worth pursuing because it prevents a future developer from wondering whether onCreate was accidentally omitted.
- **Acceptance:** After the fix: the LauncherRoot.tsx onCreate prop has a TODO comment referencing the future prompt screen (e.g. the change that will implement it). Lint and TypeScript pass.

**F5** (F) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/src/openrouter.ts:192-213. The flush block only looks for parsed.usage; the choices[0].delta.content path is absent. A final chunk carrying text content without a trailing newline would silently drop that delta. The critic is correct that in practice OpenRouter sends complete frames per TCP chunk, so this is unlikely to trigger. The fix is small and has one obvious correct outcome: apply the same line-splitting loop to the remaining buffer as the main loop does. Worth pursuing because it closes a correctness gap that makes the code wrong as documented, even if unlikely in practice.
- **Acceptance:** A test injects a final SSE frame whose last line has a text delta with no trailing newline (buffer contains 'data: {choices:[{delta:{content:"X"}}]}' with no \n). After draining, the collected deltas include 'X'. The test passes after the fix and would fail before it.

**F6** (F) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/test/contract.test.ts:32-60. The samples array covers stage plan/start and generate/done+attempt, but the stage enum has 5 values: plan, generate, check, run, repair. Neither check, run, nor repair appears in any round-trip sample. SPEC §1.1 explicitly requires 'one literal example of each union member'. If the enum were accidentally narrowed to omit 'repair', this test would not catch it. The fix is purely additive (3 new sample entries).
- **Acceptance:** The contract round-trip test includes at minimum one sample for each of: stage check/start, stage run/done, stage repair/start. Each sample is serialized via JSON.parse(JSON.stringify()) and re-parsed with GenerationEvent.parse(), and the result deep-equals the input. If the stage enum in the contract schema were narrowed to exclude 'repair', at least one of the new samples would fail.

**F7** (F) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Verified: server/test/run.mjs:43 uses target: 'node20' while server/dev.mjs:24 correctly uses target: 'node22'. The test suite imports DatabaseSync from node:sqlite which requires Node 22+. The target mismatch does not break runtime (esbuild target only controls syntax lowering, not native module availability) but is misleading. server/package.json has no 'engines' field. Neither file is in the protected list (only root package.json is protected). The fix is two one-line changes.
- **Acceptance:** server/test/run.mjs has target: 'node22'. server/package.json has an engines field with { node: '>=22.11.0' }. A developer reading either file would not be misled into believing Node 20 is sufficient.

**F8** (F) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/src/openrouter.ts:218-229. The three typed error classes (OpenRouterAuthError, OpenRouterRateLimitError, OpenRouterNetworkError) are only thrown in lines 117-143 (before the for-await loop on response.body). The catch block at line 218 wraps only the body-loop code. Those typed errors cannot propagate into the for-await body loop since by that point the response is confirmed 2xx with non-null body. The instanceof guards at lines 220-226 are dead — any error reaching line 218 always takes the else branch at line 227. Removing the dead guard clarifies the invariant and reduces cognitive noise. The fix is a pure deletion with no behavior change.
- **Acceptance:** After the fix, the catch block in makeDeltas() contains only the else branch: any error from the body loop is wrapped as a new OpenRouterNetworkError and rejectUsage is called. The three instanceof checks and their re-throw branches are absent. Existing tests still pass (typed error behavior for 401/429 is unchanged since those throw before the loop).

**F10** (F) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Verified in server/test/sse-reader.ts:77-79. The parseSseText function silently skips blocks where eventType, dataStr, or idStr is undefined, with no count returned in SseReadResult. Comment-only keepalive blocks are counted (keepaliveCount), but incomplete data blocks are silently dropped. The SseReadResult interface has no 'skippedFrames' field. If the server emitted a malformed frame (missing id: or event:), downstream assertions ('exactly one terminal event', 'ids strictly increasing') in server-core.test.ts could pass vacuously on a truncated event list. The fix is to either add a skippedFrames counter to SseReadResult and assert skippedFrames === 0 in tests, or add a comment documenting the skip as intentional for comment-only blocks and asserting that only comment blocks hit the skip path.
- **Acceptance:** parseSseText returns a skippedFrames count (or equivalent). Tests that call readSseResponse assert skippedFrames === 0. A server that emits a frame with a missing id: field would cause the skippedFrames count to be > 0, making the assertion fail rather than silently passing on a truncated event list.

**G8** (G) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed at invariants/sandbox-isolation/README.md:94: the text reads 'spike1-android-result.png — the on-device result (26/26 pass, contained=true, paint 11.8 ms)'. README line 66 states the promoted suite runs 42 probes. The 26/26 is the frozen spike-1 reference/probes.js count which predates the T1-T7 adversarial probes added in spike-2 productionization. This is documentation drift only — no assertion is wrong, no gate is broken. Worth a one-line annotation to avoid misleading readers about what the Android result proved.
- **Acceptance:** README.md reference section for spike1-android-result.png is updated to clarify that the 26/26 result predates the T1-T7 pen-test probes and that the current production suite runs 42 probes. No code change required.

**I3** (I) · `low` · mechanical-fix · effort S · cluster: I7

- **Status — confirmed.** docs/bugs-2026-06-13.md confirmed: no status markers anywhere in the file. progress.md for fix-launcher-shell-bugs confirms B1–B9 all reached GATE PASS. B1 has an explicit caveat: 'B1 still needs an on-device smoke check' (the RN injectJavaScript timing race cannot be reproduced in Chromium). Reading the bug file today gives no signal on resolution status. Downgrading to low from medium: this is a closed archive doc — the bugs aren't being triaged from here in the normal workflow; the decisions log and progress.md are the live source. But it is the only durable bug log and a researcher could be misled.
- **Acceptance:** docs/bugs-2026-06-13.md must contain a status block (header, front matter, or per-item marker) indicating: B2–B9 fixed in archive/2026-06-13-fix-launcher-shell-bugs; B1 fixed at the code level (on-device delivery timing) but on-device smoke check outstanding. The literal text of the file must not imply all 9 bugs are open.

**I5** (I) · `low` · mechanical-fix · effort S

- **Status — confirmed.** docs/capabilities.md confirmed: 13 rows, no static-checks or harness-diagnostics entries. Both specs exist at openspec/changes/static-check-pipeline/specs/harness-diagnostics/spec.md and openspec/changes/static-check-pipeline/specs/static-checks/spec.md. The change has no progress.md and chain-A is blocked on a human bootstrap action — so these specs are in-flight, not archived, and their canonical home is still the change folder, not openspec/specs/. Adding them to capabilities.md now with a note pointing to the change-folder location is valid and low-risk. Chain-G's note already anticipates this. Downgrading to low from medium because static-check-pipeline is not yet dispatched; these capabilities are not yet live, so a researcher orienting for current work will not miss them in practice. The fix is one maintenance update.
- **Acceptance:** docs/capabilities.md must contain rows for 'static-checks' and 'harness-diagnostics' pointing to their current spec paths (openspec/changes/static-check-pipeline/specs/*/spec.md, or openspec/specs/ if the change is archived by fix time), each with a one-line description of what the capability covers.

**I8** (I) · `low` · mechanical-fix · effort S

- **Status — confirmed.** Confirmed: static-check-pipeline has no progress.md; checks/ directory does not exist; chains.md marks chain-A as HUMAN-BOOTSTRAP — not dispatchable; no record of the human bootstrap being done appears in backlog.md or v1-roadmap.md. This is a planning-visibility gap rather than a broken invariant — the change is fully specified and waiting for a known, documented human action. Downgrading to low from medium: the state is 'limbo' only in the sense that a busy reader might not know the next action; adding one sentence to the roadmap or backlog entry fully resolves it. The critic's wording ('inconsistency') is apt.
- **Acceptance:** docs/backlog.md or docs/v1-roadmap.md change #9 entry must contain an explicit note that static-check-pipeline chain-A requires a human to (a) add 'checks:test' script to root package.json and (b) add 'checks/test' to root tsconfig.json exclude before any chain can be dispatched, and that this is the current blocker.

**I9** (I) · `low` · mechanical-fix · effort S

- **Status — confirmed.** docs/spec.md §5.2 (lines 135-163) confirmed: the code sketch uses 'vc-sdk/ui', 'vc-sdk/state', 'vc-sdk/navigation', 'vc-sdk/net', 'vc-sdk/storage', 'vc-sdk/effects', 'vc-sdk/capabilities', 'vc-sdk/ai' — eight distinct subpath specifiers. The actual contract (confirmed in CLAUDE.md and spike2-findings.md) is a single 'vc-sdk' import; the allowlist enforces exactly one specifier. The static-check-pipeline will explicitly reject subpath imports. The spec sketch reads as if subpath imports are valid — a generating agent that reads this sketch first would produce failing bundles. Fix is bounded: collapse the imports to a single 'vc-sdk' import and add a comment noting the single-specifier enforcement.
- **Acceptance:** docs/spec.md §5.2 code sketch must import only from 'vc-sdk' (single specifier). All current multi-path import lines ('vc-sdk/ui', 'vc-sdk/state', etc.) must be replaced or commented out. The sketch must include a comment noting that only 'vc-sdk' is an allowed import specifier and subpath imports are rejected by the module allowlist.

**I11** (I) · `low` · mechanical-fix · effort S

- **Status — confirmed.** backlog.md confirmed: open item at lines 49-55 documents INV-TIMER and INV-CUEGATE as outstanding. v1-roadmap.md change #2 (lines 115-138) confirmed: says 'Not yet archived' with 'Pending: the runtime-owner invariants (INV-TIMER, INV-CUEGATE).' However the roadmap entry does NOT explicitly say 'blocked on INV-TIMER + INV-CUEGATE (backlog item)' — a reader sees 'Not yet archived' with the pending items described in prose, but no explicit 'BLOCKED' link to the backlog entry. The fix is a one-line annotation on the roadmap entry. This is genuinely low: the information IS present in the roadmap entry's prose; the critic is correct that a BLOCKED marker improves planning-visibility, but it is not missing information.
- **Acceptance:** docs/v1-roadmap.md change #2 (effects-and-cues) status line must contain an explicit blocker note referencing INV-TIMER and INV-CUEGATE from backlog.md as the archive blockers, so the dependency is visible at a glance without reading the prose.

**I12** (I) · `low` · mechanical-fix · effort S

- **Status — confirmed.** docs/v1-roadmap.md lines 370-377 confirmed: post-v1 backlog ordered list has items numbered 1,2,3,4,5,6 then resets to 2,3 — the blank line before item 7 broke the Markdown list continuation and GitHub/renderers reset the counter. Item 7 ('Direct provider API + prompt caching') is numbered 2 and item 8 ('Sharing track; iOS') is numbered 3. This is a genuine Markdown rendering bug that makes the list appear to have only 6 items.
- **Acceptance:** docs/v1-roadmap.md post-v1 backlog section must render as a flat ordered list numbered 1 through 8 with no counter resets. The items currently numbered 2 and 3 at the bottom must become 7 and 8 respectively. Verified by inspecting raw Markdown (no blank line breaks in the middle of the list, or explicit list restart markers removed).

### OpenSpec / behavioral — you decide — 7

**D2** (D) · `high` · openspec-behavioral · effort M

- **Status — confirmed.** Confirmed. 'unknown_record' is declared in StorageErrorKind at contract.ts:161. engine.ts:151-163 (update) runs the UPDATE with no rowcount check; engine.ts:166-169 (remove) runs DELETE with no rowcount check. Neither throws 'unknown_record'. The test suite has zero assertions for this kind. This is an openspec-behavioral issue because the fix requires a product decision: either (a) implement the guard using res.changes === 0 after update/remove, which also means the SqlResult interface must actually surface 'changes' from the binding — which node-sqlite does but the interface needs to guarantee it — or (b) remove 'unknown_record' from StorageErrorKind and document silent-no-op as deliberate. Option (b) would also affect the capability-bridge layer (decision #41) which dispatches engine errors verbatim. A fixer must not silently pick; this needs a human decision via the OpenSpec flow.
- **Acceptance:** If the chosen behavior is (a): after engine.update(coll, nonExistentId, patch) and engine.remove(coll, nonExistentId), a StorageEngineError with kind 'unknown_record' must be thrown and the test suite must include assertions for both update and remove on a non-existent id. If the chosen behavior is (b): 'unknown_record' must be removed from StorageErrorKind in contract.ts, update/remove must be documented as silent no-ops for missing ids, and the spec must describe this behavior explicitly.

**A3** (A) · `medium` · openspec-behavioral · effort S · cluster: A4

- **Status — confirmed.** Confirmed: resolver.js line 32 resolves both 'react-dom' and 'react-dom/client' to window.ReactDOM, but docs/spike2-findings.md line 31 states the resolvable runtime surface is 'exactly {vc-sdk, react, react-dom}' and lists react-dom/server as something that throws. The critic's security concern about independent createRoot usage is real but mitigated in practice: build/react-inject.js line 10 sets `globalThis.ReactDOM = ReactDOMClient`, so window.ReactDOM IS the react-dom/client object. The resolver alias means require('react-dom/client') returns the same object as require('react-dom') — a bundle that uses createRoot gets the same instance the trusted loader already uses at loader.js line 86. No additional DOM capability is unlocked. However, the spec/implementation gap is genuine and requires a human decision: either (a) document the alias in spike2-findings.md and add an expectResolves probe for it (making it a deliberate part of the surface), or (b) remove the alias from the resolver (bundles should use 'react-dom' which already carries createRoot via the injected global). A feature agent must not silently pick.
- **Acceptance:** Either: (a) docs/spike2-findings.md and resolver.js header comment list 'react-dom/client' as a permitted resolver alias, AND probes.js includes expectResolves('module', "require('react-dom/client')", ...) confirming it resolves to a non-null value; OR (b) resolver.js line 32 resolves only 'react-dom' (no 'react-dom/client' branch), AND probes.js includes expectThrow('module', "require('react-dom/client') [subpath NOT exposed]", ...) confirming it throws. The two documents (spec + resolver comment) and the probe must agree.

**C7** (C) · `medium` · openspec-behavioral · effort S · cluster: C6

- **Status — confirmed.** Confirmed. remove() is fully implemented at engine.ts:374-378 and tested at acceptance.ts:278-319 with four cases including the KV-key-zero guarantee. Neither spec mentions it. The versioning spec covers snapshot/rollback/pin/history/diff but not removal. A developer reading the spec would not know this verb exists or what its promise is.
- **Acceptance:** mini-app-versioning/spec.md contains a Requirement stating that all snapshots and history for an app can be permanently removed with no storage residue. The requirement has a scenario: after remove(), history() returns empty and on a KvBackedFs store, kvKeyCount() for the app's prefix is 0.

**F2** (F) · `medium` · openspec-behavioral · effort S

- **Status — confirmed.** Verified in server/src/routes/generate.ts:27, server/src/routes/rewrite.ts:19, and contract/src/index.ts:104-108. Both route handlers return { error: 'invalid_request' } which is not in the DeviceIdError enum (only 'missing_device_id' and 'invalid_device_id' are declared). This is a real contract gap — clients matching against DeviceIdError will fall through to an unhandled branch on 400 from an invalid body. The fix requires a product decision: define a separate RequestValidationError in the contract OR reuse a new error literal in DeviceIdError OR document that 'invalid_request' is intentionally out-of-contract for this use case. A human must choose via OpenSpec flow.
- **Acceptance:** After the fix, the contract exports a schema (e.g. RequestValidationError) that covers { error: 'invalid_request', hint: string }. The server route handlers import and use that type. A contract round-trip test serializes and re-parses { error: 'invalid_request', hint: 'x' } through the schema and the result deep-equals the input.

**B5** (B) · `low` · openspec-behavioral · effort M

- **Status — confirmed.** Confirmed at fixtures/latency-probe.app.tsx:41-43. The direct globalThis.__whimSyscall access is present. However, the in-code comment at lines 39-40 explicitly documents the intent: 'the SDK exposes storage; diag is the bare round-trip' — the author was aware this bypasses the SDK facade. The fixture is a diagnostic tool, not a user-facing production app, and the comment at lines 4-8 describes it as measuring 'the syscall round-trip over the REAL transport'. The underlying question is a product/spec decision: should a `diag` SDK facade be added (fixing the mini-app contract violation properly), or should this be formally excepted as a diagnostic tool with documented access to runtime internals? Neither outcome is mechanically obvious — it requires a spec/contract call. Severity is lowered to low because the risk is refactor-brittleness, not correctness or security: if __whimSyscall is renamed, the fixture breaks at runtime but only this diagnostic fixture is affected, and it falls back gracefully to Promise.reject.
- **Acceptance:** Either: (a) a `diag.echo` facade is exported from vc-sdk with the same call signature, latency-probe uses it instead of globalThis.__whimSyscall, and the fixture passes all existing build and type checks; OR (b) a comment in docs/spec.md or fixtures/latency-probe.app.tsx explicitly documents the accepted exception and names the runtime internal it depends on, and a knip/lint rule or build-time assertion enforces that no other fixture touches __whimSyscall directly.

**C6** (C) · `low` · openspec-behavioral · effort S · cluster: C7

- **Status — confirmed.** Confirmed at openspec/specs/mini-app-versioning/spec.md:3-4 and mini-app-forking/spec.md:3-4. Both Purpose sections read 'TBD'. This is a doc gap only; severity drops to low from the critic's medium. Routes to openspec-behavioral because Purpose text is a product-level statement a human must ratify, not something an agent should fill silently.
- **Acceptance:** Both spec files have a non-stub Purpose section describing the capability scope and rationale. Neither Purpose reads 'TBD'.

**F9** (F) · `low` · openspec-behavioral · effort S

- **Status — confirmed.** Verified in server/src/usage-store.ts:66-74 and contract/src/index.ts:21-25. Both UsageStore implementations accumulate all three fields verbatim. The contract schema has no refine() enforcing totalTokens == promptTokens + completionTokens. The critic is right that OpenRouter can send inconsistent values from non-standard models. However, this is a pure policy decision with no obvious 'correct' answer: accumulating verbatim preserves fidelity to what OpenRouter reports (which may itself be authoritative even when inconsistent); re-deriving would mask OpenRouter bugs but diverge from the source. This requires an OpenSpec behavioral choice, but the value is low enough (edge case from buggy model providers) and the change set is already large enough that deferring is appropriate. The finding is real but the risk surface is negligible for the current implementation scope (LAN dev, stub pipeline).

### Protected-file — you apply the patch — 13

**G1** (G) · `high` · protected-file-human · effort S · protected: scripts/gate.sh · cluster: G7

- **Status — confirmed.** Confirmed at scripts/gate.sh:44-55: gate.sh lists typecheck, lint, knip, build, invariants, vstore:test, storage:test, bridge:test, launcher:test, launcher:deliver-verify, server:test, openspec. There is no bridge:invariants step. Meanwhile .github/workflows/invariants.yml:69 has 'npm run bridge:invariants' as a blocking step. The gap is real: a commit that breaks the browser-level capability security checks (undeclared-capability denial, forged-sysret inertness, stale-generation drop, etc.) will pass gate.sh, pass the SubagentStop hook, and only fail after landing on CI. The fix is a one-line addition to gate.sh, which is a protected file (scripts/gate.sh is explicitly in the protect-harness.sh blocklist).
- **Acceptance:** Running ./scripts/gate.sh on a tree where bridge:invariants fails must exit with a non-zero code and print 'FAIL: bridge-invariants'. The step must appear in the GATE FAILED output alongside the other failed checks.

**G2** (G) · `high` · protected-file-human · effort M · ⚠ owner-authored test · cluster: G5

- **Status — confirmed.** Confirmed at src/runtime/web/neutralize.js:61-67: the comment explicitly states RTCPeerConnection is load-bearing because WebRTC is NOT covered by connect-src none. RTCPeerConnection is neutralized (line 67). However, src/runtime/web/probes.js (lines 48-52) only probes fetch, XMLHttpRequest, WebSocket, window.fetch, and navigator.sendBeacon in the 'network' category. There is no expectThrow for RTCPeerConnection, EventSource, caches, or SharedWorker. If RTCPeerConnection neutralization regresses, no probe catches it. The T1 self-injected script (probes.js:103-131) also probes only fetch, XHR, WebSocket, localStorage, eval — not RTCPeerConnection. Adding probes to src/runtime/web/probes.js is a change to the invariant suite, which per CLAUDE.md §16.4 must be authored by runtime owners, not feature agents.
- **Acceptance:** After the fix: the promoted run-against-build.mjs suite must include a passing expectThrow('network', 'RTCPeerConnection', ...) assertion. If RTCPeerConnection neutralization is removed from neutralize.js, the b-tip check must report contained=false or a failed probe named 'RTCPeerConnection'. The T1 self-injected script block must also probe RTCPeerConnection and record 'threw:' for it.

**H1** (H) · `high` · protected-file-human · effort S · protected: scripts/gate.sh · cluster: H3

- **Status — confirmed.** Confirmed by reading gate.sh (lines 44-54) and invariants.yml (lines 41, 69). gate.sh has `bridge:test` (line 51) but not `bridge:invariants`; it has no `guard:metro` entry at all. CI runs both (invariants.yml lines 41 and 69). The comment in invariants.yml line 68 explicitly calls bridge:invariants 'a security invariant' covering forged-sysret inertness, SQL-injection, and stale-generation drop — all are security-critical paths that a local GATE: PASS does not exercise. The disparity between the local gate and CI is real and dangerous: an implementer can ship a security regression with a green gate.
- **Acceptance:** gate.sh contains a `check "bridge-invariants"` line that runs `npm run -s bridge:invariants` after the `build` check, and a `check "metro-guard"` line that runs `npm run -s guard:metro` before the browser steps. Running gate.sh on a clean tree with both suites passing produces GATE PASSED.

**H3** (H) · `high` · protected-file-human · effort M · protected: .github/workflows/invariants.yml · cluster: H1

- **Status — confirmed.** Confirmed by reading invariants.yml (all 70 lines). CI runs: server:test, guard:metro, storage:test, bridge:test, launcher:test, build, invariants, bridge:invariants. It does NOT run typecheck, lint, knip, openspec validate, or scaffolding tripwires. A PR introducing type errors, dead exports, spec drift, or TEMP:/HACK: markers gets a green CI badge. The local gate (gate.sh) covers all of these but CI does not — so CI is not a backstop for any quality gate beyond the runtime suites.
- **Acceptance:** The CI YAML contains steps that run `npm run typecheck`, `npm run lint -- --max-warnings 0`, `npx knip`, and the scaffolding tripwire grep. All four steps block the build on non-zero exit. These steps run in a job that does not require Playwright/Chromium installation.

**H4** (H) · `high` · protected-file-human · effort S · protected: package.json, scripts/gate.sh · cluster: H3

- **Status — confirmed.** Confirmed. gate.sh line 55 calls `openspec validate --all --strict` directly. package.json devDependencies (read in full) has no openspec entry. `which openspec` returns `/opt/homebrew/bin/openspec` — globally installed only. CI does not run openspec (H3 confirms), so this is a latent failure for gate.sh in any CI context or clean-machine run. Currently masked because CI doesn't call gate.sh at all, but any future CI-gate integration would fail on this step silently. The fix requires package.json edit (protected file).
- **Acceptance:** openspec appears in package.json devDependencies. gate.sh invokes it via `npx openspec validate --all --strict` or an npm script. `npm ci` in a fresh checkout installs the binary and the gate step succeeds without a global install.

**G5** (G) · `medium` · protected-file-human · effort M · ⚠ owner-authored test · cluster: G2

- **Status — confirmed.** Confirmed: neutralize.js lines 66 (EventSource), 77 (caches via inertStore), 79 (SharedWorker) neutralize these globals. src/runtime/web/probes.js has no expectThrow for any of them in the 'ambient' category (lines 60-63) or elsewhere. The spike2-bundle-contract/reference/probes.js also lacks them. EventSource is a network exfil channel; SharedWorker is a cross-tab compute/comms surface; caches (Cache API) is a persistence store — all meaningful if their neutralization regresses. This is clustered with G2 because both require new assertions in src/runtime/web/probes.js, which per CLAUDE.md §16.4 must be authored by runtime owners.
- **Acceptance:** After the fix: expectThrow assertions for EventSource (network category), caches.open (ambient category), and SharedWorker (ambient category) must be present in src/runtime/web/probes.js. If any neutralization is removed from neutralize.js, the b-tip containment check must report the corresponding probe as failed.

**H2** (H) · `medium` · protected-file-human · effort S · protected: scripts/gate.sh

- **Status — confirmed.** Confirmed but with a material refinement that changes severity. .gitignore explicitly excludes `.claude/settings.local.json` (found in .gitignore: `.claude/settings.local.json`). Since the file is untracked, the git-diff tripwire in gate.sh (lines 22-33) can NEVER see it — not just because it isn't listed, but because git diff HEAD doesn't track untracked files. protect-harness.sh does cover `.claude/*` which would block agent edits. The actual threat model is: a committed change to the file is impossible (it's gitignored), but the existing allow-rules were placed there at machine-setup time outside git. An agent cannot expand these rules via Edit (protect-harness.sh blocks it) and cannot bash-write them (bash-policy.sh blocks `>` redirects to .claude/). The residual risk is lower than the critic assessed — the file cannot enter a commit, so the git-diff tripwire's gap is moot. Severity downgraded to medium because the write-protection layers (protect-harness.sh + bash-policy.sh) still cover the file path, even though the tripwire doesn't. The critic's recommendation to add a separate existence/content check is valid as a defense-in-depth measure.
- **Acceptance:** gate.sh includes a check that reads .claude/settings.local.json (if it exists) and fails if it contains any `Bash(...)` allow-rule that is not on an approved allowlist; OR the gate documents that settings.local.json is intentionally out-of-scope with a comment explaining why (gitignored, write-blocked by hooks).

**H5** (H) · `medium` · protected-file-human · effort S · protected: .claude/hooks/gate-on-subagent-stop.sh

- **Status — confirmed.** Confirmed. gate-on-subagent-stop.sh line 21 calls `./scripts/gate.sh` with a relative path. gate.sh itself does `cd "$(dirname "$0")/.."` at line 14, but that only resolves if the shell can first locate the script. The hook process cwd is not guaranteed to be the project root. The failure mode is silent: if the script is not found, the `$?` from the failed subcommand is non-zero, the counter increments, and after 2 attempts the subagent is let through (line 19). The fix is straightforward: `$CLAUDE_PROJECT_DIR/scripts/gate.sh` as the critic recommends. This touches a protected file.
- **Acceptance:** gate-on-subagent-stop.sh line 21 invokes gate.sh using an absolute path (e.g. `"$CLAUDE_PROJECT_DIR/scripts/gate.sh"` or equivalent `$(dirname "$0")/../scripts/gate.sh` resolved from the hook's own location). A hook run from a non-root cwd correctly finds and executes the gate.

**H6** (H) · `medium` · protected-file-human · effort S · protected: .claude/hooks/protect-harness.sh, scripts/gate.sh

- **Status — confirmed.** Confirmed. protect-harness.sh lines 21-43 list: gate.sh, .claude/*, eslint.config.*, .eslintrc*, .eslintignore, knip*.json, tsconfig*.json, package.json, package-lock.json. babel.config.js and metro.config.js are absent from this list and from gate.sh's tamper tripwire (lines 22-31). CLAUDE.md explicitly calls out `tsconfigRaw: '{}'` as 'load-bearing' and explains that `babel.config.js` controls JSX transform. A subagent could edit babel.config.js freely (only the main thread gets an 'ask' prompt for protected files; all other files get no protection). The risk is real but the exploit path requires a non-obvious chain (edit babel → break JSX transform → break esbuild build). The gate would catch it via the `build` check, but not via the tamper tripwire.
- **Acceptance:** protect-harness.sh includes `babel.config.js` and `metro.config.js` in its hard-block pattern for subagents. gate.sh's git-diff tamper tripwire includes both files. A subagent attempting to edit babel.config.js receives the BLOCKED message and cannot proceed.

**I6** (I) · `medium` · protected-file-human · effort S · protected: .claude/agents/researcher.md

- **Status — confirmed.** Confirmed at two levels. (1) docs/harness-build-guide.md:313 says 'Write the digest to the path the caller gives you'; harness-build-guide.md:304 (researcher agent block) shows 'tools: Read, Grep, Glob'. (2) .claude/agents/researcher.md (confirmed from backlog.md reference) also declares tools: Read, Grep, Glob with no Write. The fix requires editing .claude/agents/researcher.md — inside .claude/** which is hook-blocked. The harness-build-guide.md edit (clarifying the wording) is not protected. Both must land together to avoid a half-fix where the guide says 'return as message' but the agent file still has ambiguous instruction. Routing protected-file-human because the agent definition file is the load-bearing half.
- **Acceptance:** Either (a) .claude/agents/researcher.md grants a scoped Write tool (change-folder path) AND docs/harness-build-guide.md step 3 reads 'Write the digest to the path the caller gives you (default: print as final message)' with clear intent, OR (b) .claude/agents/researcher.md makes no mention of writing AND docs/harness-build-guide.md step 3 reads 'Return the digest as your final message; the proposer saves it to research.md.' The parenthetical 'default: print it' that makes intent ambiguous must be resolved in whichever direction is chosen.

**G7** (G) · `low` · protected-file-human · effort S · protected: package.json · cluster: G1

- **Status — confirmed.** Confirmed: package.json line 24 has 'invariants:spike2': 'node invariants/sandbox-isolation/spike2-bundle-contract/runner.mjs'. The spike2 README calls it the 'Spike-6 CI seed' and 'regression oracle'. Neither gate.sh nor invariants.yml runs it. The promoted run-against-build.mjs suite exercises the same T1-T7/F4 attack vectors against the current build artifacts, so the spike2 suite is a frozen standalone reference. The ambiguity about its status (oracle vs. archived) should be resolved. If archived, the package.json script should be removed (protected file); if still needed, it should be added to CI. Severity is low because the substantive coverage is in run-against-build.mjs.
- **Acceptance:** Either: (a) invariants:spike2 is removed from package.json AND the spike2/README.md is updated to say 'archived reference — superseded by run-against-build.mjs'; or (b) 'npm run -s bridge:invariants' is added to gate.sh AND invariants.yml already has it. The dead script must not remain as an unreachable entry point.

**H8** (H) · `low` · protected-file-human · effort S · protected: .claude/commands/opsx/apply.md

- **Status — confirmed.** Confirmed. apply.md line 44 says 'suggest using `/opsx:continue`'. No `/opsx:continue` exists in .claude/commands/opsx/ (directory listing shows: apply.md, archive.md, explore.md, propose.md, sync.md). SKILL.md line 48 correctly says 'suggest using openspec-continue-change'. This is a stale reference that gives users a ghost command. Low severity because it only affects the blocked state path which is uncommon, but it's a clear doc-drift that should be fixed. Touches a protected file (.claude/).
- **Acceptance:** apply.md line 44 references the correct recovery path (openspec-continue-change or the actual recovery command). No reference to `/opsx:continue` exists in apply.md.

**H10** (H) · `low` · protected-file-human · effort S · protected: knip.json

- **Status — confirmed.** Confirmed. knip.json lines 44-46 show the contract workspace has only `"project": "src/**/*.ts"` with no `entry` key. contract/package.json has `"main": "./src/index.ts"` and `"exports": { ".": "./src/index.ts" }` — so knip may auto-detect the entry via package exports. The risk is low: knip's export-resolution is well-tested and the package.json exports field is the canonical way to declare entries. The inconsistency is real but the consequence (missed dead-export detection) is unlikely given the exports field. Worth fixing for consistency and to make the intent explicit, but not urgent.
- **Acceptance:** knip.json contract workspace block includes `"entry": ["src/index.ts"]`. Running `npx knip` on a clean tree produces the same results as before the change (no new unused-export errors introduced by making the entry explicit).

### Defer — backlog — 15

**A5** (A) · `low` · defer · effort S

- **Status — confirmed.** Confirmed: lines 43-54 in loader.js are a forward-declared nav-depth contract anchor comment added by the launcher-shell WIP commit (git diff shows it as new). The comment explicitly states it adds NO runtime behavior. The critic's concern that this adds content a security reviewer must filter is valid but minor: the comment is clearly labeled 'contract anchor only' and is not in any security-sensitive code path (it sits between the transport shim and the post() function). The byte-stability note in the comment is self-aware about the tradeoff. The launcher-shell change is already in progress (wip commit exists), so moving this comment will happen when that change lands. Forcing its relocation now creates a separate churn with no security value. Defer until launcher-shell change is implemented.

**B8** (B) · `low` · defer · effort M

- **Status — confirmed.** Confirmed at build/build.mjs:297-304: the full `bundles` map (all 11 entries including adversarial fixtures) is written to app-bundles.ts as APP_BUNDLES. The critic's concern is valid in principle: any future feature iterating APP_BUNDLES would silently include attack bundles. However, this is a future-hazard, not a present defect. Currently the seeding code explicitly names tip-splitter and water-counter (as the comment at line 296 states), and no existing code iterates APP_BUNDLES to build a picker or pre-warm list. The RUNTIME_HTML bundles map at line 251 already demonstrates correct explicit-allowlisting for the delivery surface. Deferring keeps the change list tight; it can be addressed when the launcher-shell / picker UI (D7) is built and would actually iterate APP_BUNDLES. At that point the split is a prerequisite, not a nice-to-have.

**C11** (C) · `low` · defer · effort M

- **Status — confirmed.** Confirmed at engine.ts:71-72, 77-78. VersionStoreOptions.backend is typed as the concrete MemoryFs class rather than an interface. The three methods used beyond FsBackend (countLooseObjects, listLooseObjects, removeTree) are defined on MemoryFs. The public factory functions in index.ts hide this type from all callers, and KvBackedFs extends MemoryFs, so there is no practical breakage today. This is a type-system cleanliness issue. Defer to a future internals-cleanup pass.

**C12** (C) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at compaction.ts:57-66. startOids is a Set so duplicate oids (multiple snap tags at the same commit) are deduplicated before the walk. Within each start oid, git.log walks to the root, so shared ancestors of multiple branches are traversed multiple times. The reachable Set deduplicates correctly for packing. This is a performance issue only, and the cost is negligible at current scale (O(10-100) snapshots, O(1-2) forks per mini-app). Defer.

**D8** (D) · `low` · defer · effort S · cluster: D11

- **Status — confirmed.** Confirmed. Both spec files have the TBD Purpose placeholder at lines 3-4. The specs are normative (cited by decisions.md #40 and #41) and the empty Purpose sections are a genuine doc-drift issue. However, this is documentation-only and has no correctness or security impact. The behavior and requirements sections that follow the Purpose are populated. Severity downgraded to low from medium — the spec bodies contain full requirements and scenarios; the missing Purpose is an orientation problem for a new reader, not a functional gap. Worth doing as a housekeeping task but does not block anything and poses no risk of a regression. Deferred to backlog rather than the active fix queue.

**D9** (D) · `low` · defer · effort M

- **Status — confirmed.** Confirmed. engine.ts:68 throws invalid[0] and engine.ts:71 throws diff.errors[0] when multiple errors are present. The validateArtifact() function comment says 'Returns every problem found' but only the first error surfaces through open(). This is a developer-experience problem rather than a correctness or security issue — the engine is still correct (it rejects the artifact), just not maximally helpful. Changing the surface to throw multiple errors would require a contract change to StorageEngineError or open()'s declared behavior, which is an openspec-level decision. At low severity with minimal real-world impact (most artifacts have one problem at a time, and the fix-resubmit cycle is tolerable), this does not warrant pursuing in the near term. The comment on validateArtifact is the main artifact; the logging-before-throwing option mentioned in the critic's rec could be done mechanically but the caller-facing behavior stays unchanged regardless.

**D11** (D) · `low` · defer · effort S · cluster: D8

- **Status — confirmed.** Confirmed. The spec at openspec/specs/storage-schema-evolution/spec.md:29-31 says 'Deleting a field SHALL retire its burned ID forever (tombstone)' and 'identities are never reused'. schema.ts:220-227 explicitly implements the rollback-across-tombstone resurrection path — moveColumn(nextColl, 'retired', 'active', f.id) — which decisions.md:392 documents as intentional. The spec and the code are in direct contradiction. This is real but low-severity doc-drift (the code behavior is the intended behavior; the spec language is imprecise). The fix is a spec wording update: clarify that 'forever' means an ID cannot be repurposed for a different field type, but rollback to the same field with the same type is an idempotent reinstatement. Deferred as a backlog spec-cleanup task; cluster with D8 since both are spec doc fixes.

**D12** (D) · `low` · defer · effort M

- **Status — confirmed.** Confirmed. validateArtifact() in schema.ts:126-134 validates tombstone IDs for form (BURNED_ID_RE) and checks that a tombstone ID is not simultaneously active, but does not cross-reference tombstones against the applied schema — it has no applied context. diffSchemas() at schema.ts:241-245 silently skips a tombstone whose ID is not in the current active list (the for loop over incomingTombstones does nothing if the ID is absent from nextColl.active). A phantom tombstone ('f99' never declared) would be silently stored in _meta as a phantom entry. This could confuse the diff logic if the same ID were later legitimately allocated and then tombstoned, but that sequence is unlikely given burned IDs are minted sequentially and the misfire would surface as unexpected 'older-subset' classification rather than data loss. The critic acknowledges this is low priority and the check cannot be in validateArtifact alone. Adding it to diffSchemas would require a design choice about warning vs. error. Not worth pursuing now given the low real-world risk.

**E4** (E) · `low` · defer · effort M

- **Status — confirmed.** Confirmed: the three ProbeScreens (BridgeProbeScreen.tsx:60-70, StorageProbeScreen.tsx:64-74, VersionStoreProbeScreen.tsx:79-89) have byte-identical StyleSheet.create blocks. However, all three screens are dev/probe-only surfaces — none are reachable in a production build (gated by RUN_VSTORE_PROBE, RUN_STORAGE_PROBE, RUN_BRIDGE_PROBE flags in App.tsx, or __DEV__). The duplication is real but the maintenance cost is low: these screens change rarely and independently (each has a different runner function and a different async pattern — BridgeProbeScreen uses setTimeout+promise, StorageProbeScreen uses setTimeout+sync, VersionStoreProbeScreen uses async IIFE). Extracting a shared ProbeScreen component is legitimate refactoring but the cost of drifting styles here is negligible. Routing to defer so it can be batched with future UI work.

**E8** (E) · `low` · defer · effort L

- **Status — confirmed.** Confirmed: app-bundles.ts and app-records.ts contain adversarial fixtures (evil, poison, victim, cap-intruder, sql-injector). LauncherRoot imports both unconditionally. The fixtures are unreachable to the end user (defaultSeeds() filters, DevProbeScreen is __DEV__-only) but they ARE bundled in the release JS. The bundle-split fix would require changes to the build pipeline (build/build.mjs) to produce separate dev/prod outputs and a conditional import in LauncherRoot — non-trivial and touching the build pipeline. The security impact is negligible: the adversarial payloads are WebView sandbox tests that only escape to the JS bundle, not to any native surface, and they cannot execute without explicit host delivery. This is backlog material; the dev/prod split is a good future improvement when the build pipeline is next touched.

**E9** (E) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at FloatingExit.tsx:24-26. `Dimensions.get('window')` is called at render time; the resulting `width` and `height` values are captured in the useMemo PanResponder dependency array (line 90). On device rotation the component will not re-render and snap math will use stale dimensions. However: (1) the app is Android-first and the current build is arm64 single-orientation; (2) FloatingExit is a dev/UX convenience affordance, not a security or data-integrity path; (3) the snap would be cosmetically wrong but the button remains tappable and still calls onPress = host.exit correctly. The fix (useWindowDimensions hook) is straightforward but low-urgency given the orientation constraints of the current target platform.

**G9** (G) · `low` · defer · effort S

- **Status — confirmed.** Confirmed at sandbox-isolation-probe.html:149: the message handler has no ev.source or ev.origin check. However, this is a standalone manual artifact (a preserved spike probe page, not part of any CI pipeline). The live host in assemble.mjs does use nonce authentication. The probe page is loaded from file:// URLs in an isolated browser context for manual or CI use. In that context the threat model for postMessage spoofing is academic — there is no adversarial page that can reach this window in CI. The fix would be cosmetic alignment with the live loader's defense-in-depth pattern. The page is explicitly described as a 'preserved from Spike 1' artifact. The wontfix rationale is that fixing it in isolation (without updating the inlined script it builds) is incomplete, and the page has no test role in any automated gate. Low urgency is confirmed by the critic's own assessment.

**H7** (H) · `low` · defer · effort S

- **Status — partial.** The finding is partially correct but overstated. critic.md line 22 instructs the critic to write to `openspec/critic/<YYYY-MM-DD>.md`, and critic-run.md says 'Give it the ref and the report path openspec/critic/<today>.md'. These are consistent with each other — both describe file writing. The adjudicator (this role) uses StructuredOutput, but the adjudicator and the critic are different agents invoked differently. The /critic-run command dispatches the critic subagent with a file path; the critic writes the file. The human-ratifier (adjudicator) receives the structured output. The mismatch the critic identified — that the adjudicator uses StructuredOutput instead of reading the file — is a property of the ADJUDICATOR workflow, not the critic workflow. critic.md is correct for its own role. No fix needed.

**I7** (I) · `low` · defer · effort S · cluster: I3

- **Status — confirmed.** decisions.md #43 (line 512) and v1-roadmap.md #5 (line 185-186) both confirmed to say on-device acceptance PENDING / NOT YET RUN. The fix-launcher-shell-bugs progress.md records all chains GATE PASS but explicitly preserves the B1 on-device caveat. The decisions.md entry promises 'This entry is updated to [BUILT + ACCEPTED] once that run is recorded.' The actual on-device run has not been recorded anywhere. This is a real documentation gap but it is not an agent-fixable mechanical issue — updating decisions.md to [BUILT + ACCEPTED] requires a human to first perform or confirm the on-device run. Routing to defer: the right action is for the human to run the acceptance spec (acceptance.spec.md), then update the log; an agent writing [BUILT + ACCEPTED] without that run being done would be fabricating the acceptance verdict. Severity downgraded to low from medium: the emulator acceptance is done and recorded; the outstanding item is a real-hardware smoke check documented in backlog.md.

**I10** (I) · `low` · defer · effort S

- **Status — confirmed.** docs/harness-build-guide.md confirmed: 'Fable, high thinking' appears in the role table and narrative (lines 11, 517, 558). 'Fable' does not appear in CLAUDE.md, decisions.md, or v1-roadmap.md. From context, 'Fable' appears to be an internal code-name for an extended-thinking model used as the proposer — the guide treats it as a model identity distinct from Opus (dispatcher) and Sonnet (implementer). This is cosmetically inconsistent with how the rest of the repo names models, but it does not cause an agent to fail a task — the proposer role is the main-thread user session, not an invoked subagent; the 'model' column in the role table is informational. Routing wontfix: the guide is authored by the human and the 'Fable' name may be intentional (it is the human's own session identity). If updated, the human should do it. Marking worth_pursuing false — no correctness or cost impact.

### Wontfix — 2

**C2** (C) · `medium` · wontfix · effort L

- **Status — confirmed.** The critic is correct that orphaned loose blobs from a partial snapshot() (killed between git.add and git.commit) are never freed by compactRepo because the deletion loop at compaction.ts:91 guards `if (reachable.has(oid))`. However: (a) the scenario requires process-kill mid-snapshot; (b) the compaction design in compaction.ts was explicitly scoped to reachable objects only, never a full GC; (c) the leak is bounded to a few blobs per crash event, not a per-normal-snapshot accumulation; (d) decision #36's justification is loose-object pressure from normal snapshot cycles. Severity is medium not high because it is crash-only. Downgrading to wontfix: documenting the known gap in the compaction.ts header is sufficient.

**H9** (H) · `low` · wontfix · effort S

- **Status — confirmed.** Confirmed that the four sed rules in settings.local.json lines 4-7 are stale (they target spike-2 on-device acceptance strings that have no current use). However, .gitignore explicitly excludes settings.local.json from version control (confirmed: `.claude/settings.local.json` is in .gitignore). This file is local-machine state only — it cannot be committed, it cannot drift into CI, and it has no effect on other developers. The stale allow-rules only expand the permission surface on the developer's local machine where they were presumably added intentionally. The 'audit surface' concern is real but negligible given the file's gitignored status and the write-protection provided by protect-harness.sh and bash-policy.sh for .claude/ paths. The human can clean it up at their discretion without a tracked change.

## Adjudicator notes per area

- **A** — VERDICT: findings — code is real but not a report-mismatch. The critic report matches what is in the code.

REPORT HONESTY: matches diff. All cited file:line locations are accurate. The critic's description of loader.js missing ev.source (A1), the four missing probes (A2), the resolver/spec gap (A3/A4), the nav-depth comment (A5), __whimSyscall writability (A6), and delay(Infinity) (A7) all check out against current code.

Two nuances the critic underweighted:

1. A1 severity is high but the containment escape is NOT achieved via this path — the bundle cannot forge the hostNonce (it is closure-scoped in loader.js and never placed on window). However the host state machine DOES receive a spurious authenticated delivery frame (with the real nonce), which corrupts its generation accounting. This is a host-integrity violation, not a sandbox escape. High severity is still correct because the design invariant 'host controls all bundle delivery' is violated.

2. A3 is less of a security gap than the critic implies. build/react-inject.js line 10 sets `globalThis.ReactDOM = ReactDOMClient` — the react-dom/client resolver alias returns the SAME object the trusted loader uses. A bundle resolving require('react-dom/client') gets no additional DOM capability vs require('react-dom'). The real problem is spec/code disagreement, not a new attack surface. Severity medium is retained because the spec is a security document and any gap in it is a medium concern.

A5 is routed to defer (wontfix-equivalent) because the launcher-shell change is already in flight and the comment is explicitly self-describing. Forcing relocation now creates churn with no security value.

Owner-authored tests: A1 and A2 both require new assertions in probes.js, which is the invariant oracle. CLAUDE.md mandates invariant suite entries are authored by runtime owners, not feature agents. The mechanical code fixes for A1 (ev.source guard) and A2 (probe additions) can be drafted by an agent, but the invariant harness assertions need owner sign-off before the probe file is considered done.
- **B** — 1. B1 and B2 form a strict dependency chain: B1 is a prerequisite for B2. Any change that adds verifySourceMap calls for additional fixtures (B2) before fixing the appName hardcoding (B1) will cause false-positive build failures on any non-tip-splitter app. The two should be committed together.

2. B5 severity is downgraded from medium to low. The direct __whimSyscall access is clearly intentional (documented in the fixture's own comment), the fallback is graceful, and the affected surface is a diagnostic tool not a user-facing app. The real question is whether to formalize the exception or add a diag facade to vc-sdk — that is a spec/contract decision, not a mechanical fix.

3. B8 is deferred rather than wontfix: the critic's concern about attack bundles leaking into a future app-picker is structurally correct, but no present code iterates APP_BUNDLES for listing/pre-warm. The split should be a prerequisite when the launcher-shell feature (D7) is built.

4. No finding in this area touches protected files (scripts/gate.sh, .github/**, package.json, tsconfig*.json, .eslintrc*, knip.json, .claude/**). All mechanical-fix findings are agent-editable.

5. No finding requires an owner-authored invariant-suite assertion. None of these are security-critical sandbox/bridge behaviors; they are build correctness, a fixture UX bug, and dead code.

6. The ghost-restart in B4 (pour-over-timer) is a real correctness defect but is contained to a fixture app. It will become visible the first time a user tests the reset-during-countdown path on device.
- **C** — Key rationale calls for the ratifier:

C1 is the most important finding. The fix is small (try/catch around compactRepo in snapshot()) but requires a product decision: should compaction failure be silently swallowed, logged, or surfaced as a separate signal? The acceptance criterion requires snapshot() to resolve regardless of compaction outcome, which is the obvious correct behavior. Confirm that a compaction failure is never surfaced to the UI.

C2 is downgraded to wontfix. The orphaned-blob scenario is crash-only and bounded. The compaction design was always scoped to reachable objects. If device acceptance ever shows unexpected KV growth, re-open.

C9 fix requires a scope judgment: the simplest path is to skip HEX40 recursion into values of keys named 'artifacts'. The ratifier should confirm this scope before the fixer proceeds.

C11 and C12 are deferred, not wontfix. C11 is a real coupling with no current impact because factory functions in index.ts hide the type. C12 is a valid optimization that does not matter at current mini-app scale.

No findings in this area touch protected files or require runtime-owner invariant-suite changes. The acceptance suite (test/acceptance.ts) is a product test, not a security invariant.
- **D** — Overall the storage engine implementation is solid — the injection invariant (§C), the schema evolution logic, and the additive-only DDL constraint are all well-implemented. The six active findings (D1–D7 minus D9) are real defects or important gaps, none of them are false positives.

Key observations for the ratifier:

D2 (unknown_record) is the most consequential finding from a product-contract perspective. The 'unknown_record' kind in StorageErrorKind is a promise the engine does not keep. Because this touches the capability-bridge layer (decision #41 dispatches engine errors verbatim to mini-apps), the behavioral choice — implement the guard or delete the kind — must be made by a human via the OpenSpec flow before any implementer touches it. Do not let an agent silently pick option (b) (deletion) since that weakens the error contract the bridge relies on.

D1 (display name 'id' collision) is a real data-corruption bug, but it requires a mini-app artifact author to name a field 'id' — which would be an unusual choice. The bug is silent and would be hard to diagnose. The fix (one guard in validateArtifact) is trivial and has no backward-compat concern since no correctly-authored artifact would use 'id' as a field name.

D3 (BURNED_ID_RE local copy) is low-effort and high-importance. It is the security-sensitive regex that backs the SQL injection backstop. The one-line import fix should be bundled with D1 since both are in the same schema/engine file pair.

D6 (unguarded JSON.parse) has two tiers: the loadApplied() case (constructor bricking) is medium-high concern; the kv.get() and fromStorage() cases are more theoretical. The acceptance criterion for the loadApplied fallback-to-emptyApplied behavior is the key design decision baked into the mechanical fix — make sure the fixer understands the intent is recovery, not re-throw.

D7 (op-sqlite fallback) is dead code removal. The device-acceptance.ts helpers (resetAppDb, dbSizeBytes) also carry the same pattern and should be updated in the same change.

D9 (first-error-only throw) is wontfix. The code is correct; the multi-error collection is a developer-experience improvement, not a correctness issue, and changing the contract is disproportionate.

D12 (phantom tombstone) is wontfix now. The scenario requires minting a burned ID, tombstoning it, then in a phantom-tombstone artifact declaring it tombstoned again — low probability, low severity consequence.
- **E** — VERDICT: findings\nREPORT HONESTY: matches diff\n\nKey observations for the ratifier:\n\n1. E1+E2 are the most important items. E1 is a concrete resource-leak (open DB handle + live realm on DevProbeScreen back-button press). E2 is the structural safety net that closes the class of bugs. These must land together — fixing E1 alone is a patch, fixing E2 alone might miss the existing DevProbeScreen bug if the unmount cleanup is not exercised through the same code path.\n\n2. E6 (assemble.mjs source-verification gap) is the only finding that touches a build-pipeline file. build/assemble.mjs is NOT in the agent-protected list (protected files are scripts/gate.sh, .claude/**, package.json, tsconfig*.json, .eslintrc*, .eslintignore, knip.json, .github/**), so a feature agent can fix it. The fix is a one-line addition consistent with the existing pattern on lines 136 and 139.\n\n3. E5 is real — the BundleTooLargeError guard at deliver.ts:48-50 has zero unit test coverage in the Node launcher suite. The desktop deliver-by-source.desktop.mjs test covers render parity but does not exercise the size-guard error path at all. The byteLength TextEncoder fallback is also untested.\n\n4. E4, E8, E9 are deferred as low-value-now. E8 (adversarial fixtures in release bundle) is the most architecturally interesting but requires build pipeline work and has negligible runtime security impact today.\n\n5. No finding touches invariants/ or any agent-protected file, so no protected-file-human routing was required."
- **F** — 1. F9 is the only wontfix/defer ruling. It is real but a pure policy question (accumulate-verbatim vs. re-derive totalTokens) with no obvious correct answer at this development stage (LAN dev, stub pipeline). File in backlog for when OpenRouter integration is live-wired (#11).

2. F2 is the most consequential finding: the 'invalid_request' error code is emitted by both /v1/generate and /v1/rewrite but is not in the contract. Any device client that pattern-matches on DeviceIdError will silently fall through. This must go through OpenSpec before F3 and F6 are finalized, because a new error type addition changes the contract surface.

3. F1 (SSE cancel) is the highest-consequence bug in production use: with a real 30–60 s OpenRouter pipeline, an interval leaks for the full generation duration on every client disconnect. Although the current pipeline is a stub, the server infrastructure is meant to be production-ready per the design. Priority: fix before wiring the real pipeline (#11).

4. F3 is currently a spec lie: contract.test.ts:88 says 'identity is asserted in the modules that reuse it' but no module does so and openrouter.ts uses 'import type' which provides zero runtime presence. The comment should not survive ratification.

5. No findings touch agent-protected files (scripts/gate.sh, .claude/**, root package.json, tsconfig*.json, .eslintrc*, .eslintignore, knip.json, .github/**). F7 touches server/package.json (workspace package, not root) and server/test/run.mjs — both are writable by agents.

6. No findings require invariants/ edits. No finding is a boundary violation of the invariants suite ownership rule.
- **G** — Key observations for the human ratifier:

1. G1 is the most operationally urgent finding: bridge:invariants is a blocking CI check (invariants.yml:69) covering capability-denial and nonce-auth invariants, but gate.sh — which is the implementer's 'done' signal enforced by the SubagentStop hook — does not run it. A regression in the bridge's browser-level security can land without any local signal. The fix is a one-liner in gate.sh but requires human application (protected file).

2. G2 and G5 are security-coverage gaps in the invariant suite itself. RTCPeerConnection (G2) is documented in neutralize.js as the ONLY defense against WebRTC exfil (CSP connect-src does not cover WebRTC), yet no probe asserts its neutralization. Per CLAUDE.md §16.4, additions to src/runtime/web/probes.js are runtime-owner work — the agent may draft the probe text but a human must review and apply it.

3. G4 is a mechanical fix that closes a genuine assertion gap: the b-evil test computes rejectedSpoof but does not gate on it. The T6b constraint (host must reject bundle-origin spoof-probe frames) is currently enforced only by the diagnostic string. This is a single-line change to the ok condition and is agent-executable.

4. G3 and G6 are both latent flakiness bugs in run-against-build.mjs: dead waitForFunction predicates that cause the test to rely on fixed-duration sleeps for correctness. G3's '|| true' was presumably left during debugging. G6's /victim/ title check never matches. Both are agent-executable.

5. G9 is deferred: sandbox-isolation-probe.html is a manual artifact not in any automated gate. The live host uses nonce auth; the probe page does not. The design divergence is real but the exploit surface in practice (file:// CI context) is nil. Worth noting for future if the page is ever served from a non-isolated context, but not worth a fix cycle now.

6. G7 and G1 are clustered because both involve deciding what scripts/gate.sh and package.json should contain for the invariants:spike2 entry. The human applying G1's patch should simultaneously decide whether to remove or promote the spike2 script.
- **H** — Key ratifier decisions:

1. H2 SEVERITY DOWNGRADE (high -> medium): The critic assessed this as high because settings.local.json is not in the gate's tamper tripwire. On reading .gitignore, the file is explicitly gitignored — so it can never appear in `git diff HEAD` regardless. The real protection is protect-harness.sh blocking agent edits to .claude/* and bash-policy.sh blocking bash writes. The tripwire gap is real but the consequence is lower than claimed. A defense-in-depth check (does the file contain unexpected allow-rules?) may still be worth adding, but it is not a broken-invariant.

2. H7 FALSE-POSITIVE (wontfix / defer): The critic conflated two different agents. critic.md correctly describes file-writing output for the critic subagent. The adjudicator (this triage role) uses StructuredOutput, but these are separate workflows with separate invocation paths. No fix needed; I marked it 'partial' in finding_status and 'defer' with worth_pursuing=false.

3. H9 WONTFIX: settings.local.json is gitignored — it is local-machine state only, cannot be committed, and cannot affect CI or other developers. The stale allow-rules are a local hygiene concern the human can address informally.

4. The CI parity gap (H1 + H3 + H4) is the most urgent cluster. Currently the only gate that covers typecheck, lint, knip, openspec, and scaffolding tripwires is the local gate.sh — which agents may or may not run. CI has no backstop for any quality check beyond the runtime suites. This means a PR with type errors or dead code gets a green CI badge.

5. H4 requires a package.json edit to add openspec to devDependencies — this is a protected file and must be a human-applied change. Until then, gate.sh line 55 will fail on any machine without a global openspec install (currently masked because CI does not run gate.sh).
- **I** — Key judgment calls the ratifier should review:

1. I1 severity downgrade (high → medium): the TBD Purpose stubs are genuinely annoying and raise proposal cost, but the capabilities.md one-liner is a usable substitute and the requirements body is intact. If the team is onboarding new agents rapidly, the ratifier may want to restore high.

2. I2 severity downgrade (high → medium): spec.md explicitly says it stays as a thinking document and may be superseded. The decisions log is always the canonical read per CLAUDE.md. If agents are confirmed to be reading spec.md as authoritative (rather than as context), restore high.

3. I7 routed to defer, not mechanical-fix: the decisions log entry promises '[BUILT + ACCEPTED] once that run is recorded.' An agent cannot manufacture that acceptance — it requires a human to perform the on-device walk first. Clustered with I3 to signal the connection.

4. I10 marked wontfix: 'Fable' appears to be the human's own session identity for the proposer role. The guide is a human-authored document; the ratifier may want to update it directly in the editor if they know the current model name for their high-thinking session.

5. I6 is the only finding that requires a protected-file edit (.claude/agents/researcher.md). The harness-build-guide.md half is a normal doc edit. Recommend the human apply both in one editor session to keep them in sync.

6. static-check-pipeline (I5, I8): the change is fully specified but stuck on a human bootstrap. Both findings are low because the limbo state is temporary and documented in chains.md — but the backlog/roadmap lack an explicit 'BLOCKED' callout that would surface this in a planning review."

