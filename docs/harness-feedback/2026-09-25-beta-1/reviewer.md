# reviewer (whole change f847b1cd..integration/beta-1), Opus

1. **Terminal client errors never close the socket.** **What:** the M1 open request. **Mechanism:** the new `fallback` error kind took over the old `stream_parse` handling. No chain block or spec asked what happens to the connection after a client-side terminal error. **Verdict:** CAUGHT-REAL-MISTAKE (reviewer). **Cost:** about 10 minutes of tracing. **Evidence:** `generation-client.ts:393,669`, `LauncherRoot.tsx:1463,1759`.
2. **Contract correction without code cleanup.** **What:** `queue_timeout`. **Mechanism:** chain-2 corrected the handoff text ("no row, no code") but didn't remove chain-1's code or test. Handoff edits aren't checked against the code. **Verdict:** CAUGHT-REAL-MISTAKE. **Cost:** low. **Evidence:** `failure-codes.ts:17`, `ledger.suite.ts:652`, handoff line 71.
3. **A design sentence no one tested.** **What:** the "harmless rollback" claim in design.md. **Mechanism:** the chains made the wire stricter (new required fields) and nothing checked the design's rollback sentence against the old server's schemas. **Verdict:** CAUGHT-REAL-MISTAKE. **Cost:** low. **Evidence:** `design.md:251`, the contract diff, `generation-client.ts:108`.
4. **The recurring IDE error.** **What:** the IDE error at `contract.suite.ts:153` keeps coming up. **Mechanism:** the ledger records "IDE error" without the message or the IDE's TypeScript version, so nobody can check it against the repo's TypeScript. **Verdict:** DRAWBACK. **Cost:** about 5 minutes of probing, no reproduction. **Evidence:** progress.md chain-1/chain-2 lines.
5. **Launcher suites aren't typechecked.** **What:** type errors in launcher test files are invisible. **Mechanism:** `src/host/launcher/test` is excluded from the root tsconfig, so no gate typechecks these files (e.g. `generation-client.suite.ts:625`). This is older than beta-1. **Verdict:** DRAWBACK. **Cost:** none this run. **Evidence:** `tsconfig.json` excludes.
6. **The primary tree wasn't clean.** **What:** an untracked `openspec/changes/beta-1/acceptance/section-10-plan.md` sat in the tree during the review. **Mechanism:** another writer during an exclusive review. It didn't affect the gate, which only checks its config files. **Verdict:** ENV. **Cost:** none. **Evidence:** `git status`.

What helped: the priority list with concrete checks, and the ledger's per-chain Class A notes. Those notes made every intended deviation checkable, so none were mistaken for findings. The server-side emitter reused in the device tests (`wire-future-frames`, `stubFutureFrame`) also meant the fixtures came from the real code that sends them.

Proposals:
1. Add to the D16 rule on where messages are sent and received: "a client that ends a flow on a fallback aborts its request", with a transport-abort test in the fixture suite for future frames.
2. Make `forwardEvents` pass every event through `eventForLevel`, so layer 2 is enforced by structure rather than discipline.
3. Ledger rule: an "IDE error" note must quote the message and the IDE's TypeScript version. Also add `src/host/launcher/test` (and the similar excluded folders) to a typechecked project.
