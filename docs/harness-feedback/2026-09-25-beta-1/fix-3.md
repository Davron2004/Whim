# fix-3 (stub that runs the whole acceptance; candidate caps), implementer, Opus

1. **What:** Item 4 conflicted with a rule the dispatch block did not list. **Mechanism:** the block listed the places that "document or pin" the caps, which a search for the literal numbers finds. A relational rule (synthrun ≤ vCPUs) never contains those numbers, so that search misses it. **Verdict:** DRAWBACK. **Cost:** one of four tasks blocked, plus about 10 minutes probing. **Evidence:** `server/test/deploy-config.suite.ts:869-871`. **Proposal:** before dispatching a change to a default, the planner loads the new value through the suites once, as a one-line probe.
2. **What:** Red-checking meant rerunning server suites; the full `server:test` takes about 6–7 minutes (~4200 checks). **Mechanism:** `server/test/run.mjs` has no way to run a single suite, so I wrote scratch esbuild entries (in the scratchpad) that import one suite each. **Verdict:** DRAWBACK. **Cost:** about 15 minutes of setup and waiting. **Evidence:** four focused red/green runs of about 30 s each, against 7-minute full runs. **Proposal:** add a `SERVER_SUITES=<names>` filter to `server/test/acceptance.ts`. Separately: on failure the suite runners exit before their `finally` runs, which leaves `.server-e2e.<pid>.tmp.mjs` behind.
3. **What:** The block said "red-check against the old source, naming the failing test". **Verdict:** NEUTRAL (worked). It was unambiguous and led straight to the two red-checks.
