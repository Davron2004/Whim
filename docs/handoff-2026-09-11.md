# Handoff, 2026-09-11 (PR 15 merge, Sonar scope, first jig run)

Written at the end of the session. The harness described in CLAUDE.md was off for this run, as for the previous ones; nothing below went through it.

## Where things stand

`main` holds the whole v1 sprint (PR 15, rebase-merged as 86 linear commits), the Sonar scope change (PR 27) and the sweep (PR 28, seven commits, CI green on all three checks including SonarCloud's PR analysis). SonarCloud on `main` at the end of the session: quality gate OK, bugs 0, vulnerabilities 0, reliability A, security A, maintainability A, duplication 0.7%, 294 smells. Every by-design finding was accepted with a written reason through the scripts under `~/.cache/whim-sonar/` (outside the repo); see "Sonar" below. The smell count still includes the 156 assertion findings; the profile change that removes them applies at the next analysis of `main`.

The user's checkout at `~/Work/other/Whim` still carries its own uncommitted `.gitignore` and `README.md` edits and untracked `demo/` and `docs/demo/` material from before this session; nothing here touched them.

## What happened, in order

1. **The isolation-suite flake.** CI failed on four of the last eight runs, always the same assertion in the launcher suite's StoreAccess concurrency test (added in the sprint's per-repo serialization commit). The test gave a version-store read three timer turns and asserted it had finished; one `activeBundle` read is about ten WebCrypto SHA-1 round-trips in isomorphic-git, each of which can slip past a timer under load. Reproduced locally at roughly one in five under CPU spinners, zero of sixty after the fix. The test now awaits beta's read while alpha's gate is held, so a global lock deadlocks by name instead of losing a race, and the suite's `within()` helper keeps its timer referenced so a deadlock exits with a test name instead of code 13. Commit `test(launcher): store-access concurrency test no longer races the event loop`.
2. **PR 15 could not rebase-merge.** A ruleset on `main` requires linear history, so the merge-commit button is hidden, and the branch's 39 integration merges cannot be replayed linearly. The branch was rewritten from its 120 first-parent steps into 86 topic commits with a byte-identical tree (no patch replay, so no conflicts), force-pushed, and rebase-merged. The script is at `~/.cache/whim-sonar/linearize.sh`. The same ruleset lists SonarCloud as a required check, contrary to the 09-10 handoff; the owner bypasses it.
3. **Sonar scope** (`.sonarcloud.properties`, PR 27). Sources and tests share the repo root; `**/test/**` and the live `invariants/` runners are tests; the archived spike-2 reference copies, adversarial fixtures, generated output, design mockups, demo tooling and the lockfile are excluded. Before this, 245 of 396 findings sat in the frozen reference copies and the invariant runners, and the org's line count had tripped the free-tier cap (also fixed by deleting the stale `bs-draft-engine` project from the org).
4. **PR 28**, the sweep. See its description. The `var` conversion in `src/runtime/web/` ran through jig end to end; the rest was direct dispatch.

## Sonar: decisions and the scripts that apply them

Every rule profile is the stock "Sonar way"; nothing was ever removed. The decisions taken this session, each with a written reason on the issue or profile:

- `S2819` (postMessage origin) in `src/runtime/web/` and `src/sdk/navigation.tsx`: accepted. Opaque-origin sandbox, wildcard target is the only valid one, authority is nonce plus `ev.source` (decisions 35, 41). `accept-postmessage.sh`.
- `S6505` on the two `npm ci` steps and the Claude Code CLI install: accepted, lifecycle scripts are required. `accept-install-scripts.sh`, run only after PR 28 is on `main`.
- `S2699` (tests should include assertions), 156 findings: a "Whim way" TypeScript profile inheriting Sonar way with the rule off, because the suites use their own `h.eq` harness, which the rule cannot see; eslint already does the same for `checks/test`. `S2187` on `evals/test/*.test.ts`: false positive. `S8569` Gradle locking: accepted until someone does it properly. `S6479` on chart marks: accepted, positional data. All four in `sonar-tune.sh`.
- `fetch-sonar.sh` pulls measures, gate, branches, every open issue, profiles and the org line count into that folder. Network calls from the agent are refused by the auto-mode screen (a `curl` carrying a token), so the user runs these with the `!` prefix.

All three acceptance scripts and the tuning script ran after PR 28 merged; the gate went green on that analysis. Rerun any of them after a re-analysis only if a finding of the same kind reappears; each lists what it would touch with `--dry`.

## Jig, honestly

Setup was the expensive part: a clean clone at `~/.cache/whim-jig/Whim` with the primary `node_modules` symlinked (the symlink needs a bare `node_modules` line in `.git/info/exclude`; `guard:metro` cannot run there), a plan file outside the repo, two `jig check` passes. The loop itself was six CLI calls and two agents for one task. The verifier passed first try, so by the measure agreed beforehand (did it catch something direct dispatch would have merged?) it did not earn its overhead on a mechanical job. What it proved is that the tool works on a foreign repo with the workspace quirk. One real lesson: a rubric that sounds precise can be wrong at the language level ("let only if reassigned" versus a bare declaration assigned once in a `try`); the builder flagged it and the clarification went to the verifier by hand, which a lowered Workflow run would not have had. Use jig for structural refactors where an independent verifier matters; do sweeps by direct dispatch.

## Known, not done

- Remaining smells after the profile change lands, roughly 140: optional chaining (`S6582`, 29 across `src/runtime/web/` and `src/host/`), `S7773`, `S7780`, a few unused-variable findings, and 48 shell-quoting findings in `scripts/` and `.codex/hooks/` (harness files, the owner's call). None affects the gate, which judges new code only. Rerun `fetch-sonar.sh` and read `sonar-issues-main-p1.json` for the live list; the July `digest.md` is stale.
- Remote branches `sonar-scope` and `sonar-sweep` are merged and can be deleted. The jig clone at `~/.cache/whim-jig/Whim` and the worktree at `~/.cache/whim-sonar/wt-sonar-scope` are disposable.
- `src/runtime/web/` is covered by nothing but the two Chromium invariant suites: eslint and knip both ignore it. Any future edit there needs those suites, not the fast gate alone.
- CI caches the npm store and the Playwright browser directory (PR 28's last commit). The browser cache key carries the pinned Playwright version and must be bumped with it.
- The isolation-suite's `S6505` acceptances cover the two `npm ci` steps at their current lines; if the workflow is reshuffled, Sonar may reopen them as new issues.
- Gradle dependency locking (`S8569`) needs a real Android resolution run to generate and verify the lockfile.
- **Animation is researched, not built.** `docs/research/animation-options-2026-09.md` (commit 37a780d, a fan-out of research sweeps reviewed against primary sources) settles the stack and the plan: Reanimated 4.6 plus worklets 0.12 pinned exact on the host, a `ReducedMotionConfig` at the launcher root as the single reduce-motion switch, a WAAPI-only motion surface for the SDK behind tokens, no three.js, Rive, Lottie or Skia, a proposal to make the orb the mascot (whether Whim has a mascot at all is undecided), twelve named motion moments, three adoption phases, and a list of open questions. Two owner-authored suites currently assert the absence of motion and will need deliberate updating. Next step is an OpenSpec proposal built from that document; nothing in the app animates yet beyond what shipped before it.
- The logging suite's `settle()` uses the same single-tick pattern the flaky test did, at lower risk.
- The `prompt-flow` spec still describes cancel-on-navigate-away, superseded by the 09-10 back-button change.
- Two builds at once show the first attempt's clock on the second build screen (from the 09-10 note).

## Memory notes saved

`node-suite-fixed-tick-budget-flake`, `knip-npx-ignore-scripts-parsing`, `jig-on-whim-setup`, `instruction-files-minimal-tokens`, `whim-sonar-toolkit`.

## Process notes from the user, applied this session

- Small mechanical diffs are done by the orchestrator, not delegated; the orchestrator skill's delegation threshold now reads "do it yourself when the brief would be longer than the diff".
- Skills and CLAUDE.md edits must not grow the file: replace the sentence that misfired, check for repetition and lost emphasis.
- The harness in CLAUDE.md stayed off; Class-2 files (`scripts/`, `.codex/`, `.github/`) were edited by ordinary PR, ratified by the owner's merge.
