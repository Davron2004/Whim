# Handoff 2026-09-25: beta-1 (the first build testers get)

Written by the planning session (2026-09-25) for the orchestrator that applies `beta-1`. Keep it updated
at every merge, dispatch and blocker; it replaces `docs/handoff-2026-09-24.md` as the file to resume from.

## State

- **Demo done (2026-09-24).** Three `/beta` signups (2 iOS, 1 Android). Nobody outside the team has an
  install. Only the owner's and his mom's phones run 381237/382511. Evidence and the issue ranking:
  `docs/beta-readiness-2026-09-24.md`.
- **Production** runs image `0fb65d51…` (beta-waitlist), healthy. `WHIM_BETA_LIMIT_PER_CLIENT_HOUR=200`
  is still set from the venue; lower it back to the default when convenient.
- **Stores:** TestFlight external group `Public beta` (public link `https://testflight.apple.com/join/tM69UkDf`)
  holds 382511, which is Beta-App-Review approved. Play closed track (`alpha`) holds 382511. **Don't invite
  anyone to either until beta-1 is there.**
- **Branch `proposal/beta-1`** holds the approved change (`openspec/changes/beta-1/`), the readiness doc
  and this file. It's based on `main` `06ab2007` and isn't pushed.
- Filed during planning: #118 (capacity). A comment on #75 (the server runs one Chromium, not one per
  generation).

## Owner decisions that shape beta-1 (don't re-litigate)

- **The installed pre-beta builds are not a constraint** (2026-09-25). Break the wire. They're retired by
  the protocol header's 426 and then the minimum builds (design D16/D17).
- **Forward compatibility is layered:** tolerant reader, then protocol-level negotiation
  (`x-whim-protocol`), then an in-band `compat` fallback from the frozen set `skip | fail | update`, then
  forced update. It's declarative, never server-sent code. It must ship in beta-1 because the first
  tester build is the oldest reader forever (design D16).
- A real queue on the stream (`queued` events), the clarify `limit` arm, a mid-stream retry with `restart`,
  and clarify answer modes (one or many, "Other", "Decide for me"): all in beta-1.

## Standing instructions (carried over)

- Run attended console and account steps yourself (ego-browser where a CLI falls short). Don't ask permission.
- Never pay for anything or sign a binding agreement. Prepare it and leave the final click to the owner.
- Merge the closure PR yourself (plain `--rebase` after checks pass, never `--auto`), then deploy.
- Low-priority capacity: run chains one at a time, and don't fan out.
- Never resume a finished subagent. Ask for `HARNESS FEEDBACK` in each dispatch prompt, and write the run's
  feedback to `docs/harness-feedback/<date>-beta-1/`.
- Deploy only from `../Whim-deploy`: smoke, one real generation, roll back by `--tag` on failure.
  Pre-launch production has no users, so deploys need no per-step ask.
- Owner rule for devices: a build goes on the demo phone (iPhone 15 Plus; the 16 Pro Max is off limits)
  only after it passes on a **newly created** simulator and a fresh emulator.
- Keep replies short. File side findings as `ai-proposed` issues.

## Prompt for the orchestrator

> You're the build orchestrator for Whim. Read `docs/handoff-2026-09-25.md` first, then
> `openspec/changes/beta-1/` (proposal, design, chains, tasks). The owner approved `beta-1` on 2026-09-25.
> Its approved artifacts are on branch `proposal/beta-1`. Cut the staging branch from `main` and merge
> `proposal/beta-1` onto it (the beta-waitlist pattern), then run `/opsx:apply beta-1`: nine chains, one at a
> time, in `chains.md` order (chain-1, the wire protocol, first; it writes `handoff/wire-protocol.md`). Section
> 10 is yours, attended: load test and caps, flowbench before/after, simulator/emulator acceptance, the
> upgrade check 382511 → beta-1, deploy, TestFlight `Public beta` and the Play closed track, then raise the
> minimum builds. The owner does the demo-phone check. Don't send beta invites: the owner does that after
> seeing the build.
>
> Decide yourself and write decisions to `openspec/changes/beta-1/progress.md`. Only paying and signing wait
> for the owner. Keep this handoff updated. Follow its standing instructions.
