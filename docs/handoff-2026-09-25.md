# Handoff 2026-09-25: beta-1 (the first build testers get)

Written by the planning session (2026-09-25) for the orchestrator that applies `beta-1`. Keep it updated
at every merge, dispatch and blocker; it replaces `docs/handoff-2026-09-24.md` as the file to resume from.

## State

- **Demo done (2026-09-24).** Three `/beta` signups (2 iOS, 1 Android). Nobody outside the team has an
  install. Only the owner's and his mom's phones run 381237/382511. Evidence and the issue ranking:
  `docs/beta-readiness-2026-09-24.md`.
- **Production** runs image `0fb65d51…` (beta-waitlist), healthy. `WHIM_BETA_LIMIT_PER_CLIENT_HOUR=200`
  is still set from the venue; lower it back to the default when convenient.
- **Stores:** TestFlight external group `Public beta` (public link `https://testflight.apple.com/join/tM69UkDf`)
  holds 382511, which is Beta-App-Review approved. Play closed track (`alpha`) holds 382511. **Don't invite
  anyone to either until beta-1 is there.**
- **Branch `proposal/beta-1`** holds the approved change (`openspec/changes/beta-1/`), the readiness doc
  and this file. It's based on `main` `06ab2007` and isn't pushed.
- Filed during planning: #118 (capacity). A comment on #75 (the server runs one Chromium, not one per
  generation).

## Run status (orchestrator; newest first)

- **Superseded: resume from `docs/handoff-2026-09-26.md`.**

- **PAUSED (usage limit), 2026-09-26 early. Resume here.**
  - Merged on `integration/beta-1` (pushed only up to the 10.6 deploy): all 9 chains, plus fixes 1, 2, 3, 3b, 5, 6 and 8b.
  - Production runs the staging tip `78e59059`: smoke green, the 426 proven by the new smoke check, one real generation OK.
  - In flight: fix-4 and fix-5b (worktrees/branches in progress.md). When they report, adjudicate, integrity, merge, regate. If one died mid-run, commit its worktree as WIP and redispatch from `dispatch/fix-4.md` / `dispatch/fix-5b.md` plus "continue from WIP".
  - Then dispatch fix-7 (`dispatch/fix-7.md`).
  - Then gate-full, a scoped reviewer on everything after fix-1, rebuild both apps, and redeploy the tip.
  - Then: load test at the 5/2 caps (R13; revert = `config.ts:302` + `docs/deploy.md:405` if the normalized peak is ≥ 70%); flowbench `--repeat 10 --stop-after clarify` on the limits set (target ≥ 9/10 per case) plus the visible set (0 false limits) to re-close 10.3; re-run device acceptance on emulator-5560 and simulator `Whim-beta1-2156` (line via `WHIM_STUB_DELAY_MS`, 4th plan row, keyboard on both platforms, stub markers).
  - Then 10.5 (upgrade check, own headless AVD/port), closure PR, merge, redeploy from main, 10.7 stores, 10.8 min builds.
  - The owner wants harness feedback at the end: `docs/harness-feedback/2026-09-25-beta-1/orchestrator.md` + README tally.

- 22:30 fix-1 merged; gate-full green; scoped re-review MERGE-READY (10.1 done). **Production now runs the beta-1
  server** (staging tip `78e59059`, smoke green, a real generation OK), so 381237/382511 get 426 by design.
  In progress: Android device acceptance (emulator-5560, agent), flowbench AFTER, fix-2 (smoke asserts the
  426). The iOS Release simulator build compiled (chain-5 native code OK) and is next on simulator `Whim-beta1-2156`.

- All nine chains merged (37/45 tasks); FULL GATE PASSED on the staging tip. Reviewer (Opus): SHIP WITH FIXES:
  M1 (a fallback mid-build never aborts the request) and M2 (a server rollback below beta-1 strands beta-1
  apps) plus L1–L5. fix-1 dispatched (Opus, `dispatch/fix-1.md`, R11). Next: merge fix-1, gate-full, a scoped
  reviewer re-check, then section 10 per `acceptance/section-10-plan.md`.

- chain-8 + fix-8b merged. Orchestrator bootstrap `9a79c9c9`: `build/build.mjs` forwards a fixture's `tileColor`
  (chain-8's distinct example colours were silently dropped). chain-9 (upgrade-check tooling, the last chain)
  dispatched (Opus). Next: gate-full, then the reviewer (Opus, whole-change diff from `9a7a69d9`), then section 10 per R4/R6/R10.

- 19:10 chains 6 and 7 merged (keyboard wrapper; realm focus, `render` frame, orb inset). All Chromium suites are
  green from the main tree. R10: two device risks from chain-6 are checked first in 10.4
  (`acceptance/keyboard-checklist.md`). chain-8 dispatched (Sonnet; capacity is normal after the reset).

- chain-5 merged (age deadline, the guardian acknowledgment split into two native calls, and Settings through the one
  legal gate). Filed #124; archive-order note on #69. chain-6 dispatched (Opus). The `.mm`/`.kt` for the
  acknowledgment are compiled first in 10.4 (iOS needs `pod install`).

- chain-4 merged (the line UI, restart, limit screen, fallbacks, answer modes; stub markers `[[limit]]` and
  `[[future:skip|fail|update]]` for device acceptance). Filed #123 (heartbeat spec 8 s vs code 40 s). chain-5
  dispatched (Opus).

- 16:55 chain-3 merged (Opus, after two Sonnet stalls; #106's root cause found). chain-4 dispatched (Opus). At
  low-priority capacity, dispatch on Opus: Sonnet subagents stalled at the 600 s watchdog (R9; the owner has a bug
  report draft).

- 15:33 chain-3 stalled twice at low-priority capacity with nothing written (R9). Paused until the 17:00 reset,
  then it gets the same redispatch: worktree `.claude/worktrees/beta-1-3` is clean at BASE `74573b7b`, block
  `dispatch/chain-3.md`.

- 15:12 chain-2 merged (the line; regate green). R8: the load-tested caps go into the `config.ts` defaults (the
  standard profile may not set server limits). Filed #120. chain-3 dispatched (Sonnet,
  `dispatch/chain-3.md`).

- Every remaining chain's dispatch block is pre-written in `openspec/changes/beta-1/dispatch/chain-<n>.md`
  (tensions resolved: R7 moved the iOS acknowledgment to 26.4+ with its own 60 s deadline; chain-9 builds
  both sides of the upgrade check from source). Dispatch = the tasks verbatim + that file + the worktree
  facts + the HARNESS FEEDBACK request. chain-6's block needs chain-4's "Other" field component filled in.

- Unfrozen after the limit reset: chain-2 redispatched fresh into `.claude/worktrees/beta-1-2`, continuing from WIP
  `6eb0e3d3` (block `openspec/changes/beta-1/dispatch/chain-2.md`). The freeze notes below are history.

- **FROZEN by the owner (usage limit), 2026-09-25 ~14:30.** No new chains get dispatched. chain-2 (the line) was
  in flight: worktree `.claude/worktrees/beta-1-2`, branch `chain/beta-1-2`, BASE `e2fe33a9`. **Resume:**
  0. **Actual state at freeze:** chain-2 was killed by the session limit before reporting. Its uncommitted work is
     saved as WIP `6eb0e3d3` on `chain/beta-1-2` (worktree kept): `slots.ts`, `routes/generate.ts`,
     `usage-store.ts`, `config.ts`, `openrouter.ts` plus tests. Gate state unknown. Missing: the `docs/deploy.md`
     rows (2.3) and the load-test driver update (chain block decision 11). Resume = dispatch a fresh implementer
     into that worktree with `openspec/changes/beta-1/dispatch/chain-2.md` plus
     "continue from WIP `6eb0e3d3`: run the gate, review what exists against 2.1–2.3, finish the gaps".
  1. If `progress.md` doesn't log chain-2 as merged, check `git log e2fe33a9..chain/beta-1-2`. If there's a
     commit, run `./scripts/gate.sh` in that worktree, then
     `FIXLOOP_INTEGRATION_BRANCH=integration/beta-1 scripts/fixloop.sh integrity chain/beta-1-2`, merge
     `--no-ff` onto `integration/beta-1`, tick 2.1–2.3, regate, and tear down the worktree and branch. With no
     commit, redispatch chain-2 fresh from the staging tip (chain block: ledger 14:05 entry; decisions R5/R6).
  2. Then `/opsx:apply beta-1` continues at chain-3, one chain at a time. Resolve each chain's tensions in its
     block (harness-feedback T1).
  3. Section 10 follows `progress.md` R4 (flowbench on production) and R6 (deploy the staging tip, then the load
     test, and the caps land in the same PR).

- 14:05 chain-1 merged (`1efd3fcd`; regate, `guard:metro` and `server:e2e` green). Contract:
  `openspec/changes/beta-1/handoff/wire-protocol.md`. chain-2 (the line) dispatched. Flowbench "before" is
  recorded in `progress.md` (weather-p2 built a fake forecast; clarify never says "can't build"). Filed #119
  (`policy_unavailable` on clarify in production).

- 2026-09-25: apply run started. Staging `integration/beta-1` cut from `origin/main` `06ab2007`,
  proposal merged. Local `main` moved back to `origin/main` (its extra readiness commit is on the proposal
  branch). Ledger and decisions: `openspec/changes/beta-1/progress.md`. chain-1 (wire protocol) is
  dispatched. The flowbench "before" run is going against production.

## Owner decisions that shape beta-1 (don't re-litigate)

- **The installed pre-beta builds are not a constraint** (2026-09-25). Break the wire. They're retired by
  the protocol header's 426 and then the minimum builds (design D16/D17).
- **Forward compatibility is layered:** tolerant reader, then protocol-level negotiation
  (`x-whim-protocol`), then an in-band `compat` fallback from the frozen set `skip | fail | update`, then
  forced update. It's declarative, never server-sent code. It must ship in beta-1 because the first
  tester build is the oldest reader forever (design D16).
- A real queue on the stream (`queued` events), the clarify `limit` arm, a mid-stream retry with `restart`,
  and clarify answer modes (one or many, "Other", "Decide for me"): all in beta-1.

## Standing instructions (carried over)

- Run attended console and account steps yourself (ego-browser where a CLI falls short). Don't ask permission.
- Never pay for anything or sign a binding agreement. Prepare it and leave the final click to the owner.
- Merge the closure PR yourself (plain `--rebase` after checks pass, never `--auto`), then deploy.
- Low-priority capacity: run chains one at a time, and don't fan out.
- Never resume a finished subagent. Ask for `HARNESS FEEDBACK` in each dispatch prompt, and write the run's
  feedback to `docs/harness-feedback/<date>-beta-1/`.
- Deploy only from `../Whim-deploy`: smoke, one real generation, roll back by `--tag` on failure.
  Pre-launch production has no users, so deploys need no per-step ask.
- Owner rule for devices: a build goes on the demo phone (iPhone 15 Plus; the 16 Pro Max is off limits)
  only after it passes on a **newly created** simulator and a fresh emulator.
- Keep replies short. File side findings as `ai-proposed` issues.

## Prompt for the orchestrator

> You're the build orchestrator for Whim. Read `docs/handoff-2026-09-25.md` first, then
> `openspec/changes/beta-1/` (proposal, design, chains, tasks). The owner approved `beta-1` on 2026-09-25.
> Its approved artifacts are on branch `proposal/beta-1`. Cut the staging branch from `main` and merge
> `proposal/beta-1` onto it (the beta-waitlist pattern), then run `/opsx:apply beta-1`: nine chains, one at a
> time, in `chains.md` order (chain-1, the wire protocol, first; it writes `handoff/wire-protocol.md`). Section
> 10 is yours, attended: load test and caps, flowbench before/after, simulator/emulator acceptance, the
> upgrade check 382511 → beta-1, deploy, TestFlight `Public beta` and the Play closed track, then raise the
> minimum builds. The owner does the demo-phone check. Don't send beta invites: the owner does that after
> seeing the build.
>
> Decide yourself and write decisions to `openspec/changes/beta-1/progress.md`. Only paying and signing wait
> for the owner. Keep this handoff updated. Follow its standing instructions.
