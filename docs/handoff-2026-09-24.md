# Handoff 2026-09-24: build orchestrator, mid legal-surface-v2

Written by the outgoing orchestrator (session started 2026-09-23 21:00 EDT) so a fresh one can take over from files. Demo tonight (2026-09-24 evening) against production. Production has no real users (closed beta).

## Owner standing instructions for this build push (not for memory)

- Run attended console and account steps yourself, using the ego-browser where a CLI falls short. Don't ask permission.
- Never pay for anything or sign a binding agreement: prepare it, and leave the final click to the owner.
- Merge closure PRs yourself, then deploy, for `legal-surface-v2` and `developer-observability`. Publishing the new consent, terms and policy text is not "signing".
- We're on low-priority capacity: run chains one at a time, and don't fan out.
- Never resume a finished subagent (5-minute cache TTL, full-transcript reload). Ask for `HARNESS FEEDBACK` inside each dispatch prompt's final report (`docs/harness-feedback/README.md`).
- Freeze production about 2 hours before the demo unless something is broken.

## Live status (orchestrator #2, from 03:30 EDT; newest first)

- 03:55 Play Texas §121.053 notice filed (effective 2026-09-27, the form's earliest date). v2 legal effective dates move to 2026-09-27 in the lsv2 fix chain. Apple channel: #86.
- 03:40 developer-observability started in parallel (owner allowed moderate parallelism): staging `integration/developer-observability` cut from the lsv2 tip `432a64d`, orchestrator worktree `.claude/worktrees/devobs-staging` (merges + `gate.sh` there). In flight: chain-1, chain-2, and the 3.4 `invariants/` author (separate agent). Chain-3 after the invariants commit; chains 4/5 after lsv2 lands on `main` (then merge `main` into the dev-obs branch).
- 03:30 lsv2 chain-5 dispatched (worktree `.claude/worktrees/legal-surface-v2-5`).

## State

- `main` = `3e417a5`. `request-envelope` is applied (PR #80), archived (PR #84) and deployed.
- Production runs image `672e4e4a2978c8b3b5282ca66f75fd9bf81258ee`, with both minimum builds at 0, verified tonight.
  - Rollback target: the same image with `deploy/deploy.sh --tag 672e4e4a2978c8b3b5282ca66f75fd9bf81258ee`.
  - Pre-request-envelope image: `a9b03c47eef3ae33948aedf9ec38c2c56fe2147f`.
- **Deploy only from the deploy worktree** `/Users/davrondjabborov/Work/other/Whim-deploy`, detached at `origin/main` with `node_modules` symlinked:
  - first `git -C ../Whim-deploy fetch origin && git -C ../Whim-deploy checkout --detach origin/main`;
  - then `PATH=~/.nvm/versions/node/v22.20.0/bin:$PATH deploy/deploy.sh`;
  - after it: `deploy/smoke.sh`, one real generation with `node server/flowbench.mjs --url https://api.whim.anycognition.ca --eval-set evals/sets/visible --cases tip-splitter-p1`, and roll back on failure.
- The main tree belongs to the apply run.
- **Active run: `legal-surface-v2`**, staging branch `integration/legal-surface-v2`, checked out in the main tree. The ledger is `openspec/changes/legal-surface-v2/progress.md`; read it fully, it's the resume state.
  - Merged: chains 10, 1, 3, 8, 2, 4 (and 7, if the outgoing orchestrator merged it before handing off; check the ledger).
  - Next: **chain-5**, then chain-6, then chain-9, one at a time. Then gate-full, the reviewer on `main...integration/legal-surface-v2`, a fix chain, and a visual pass on the emulator (consent v2, terms step, Settings, French). Then closure (PR, Sonar, `/git-cleanup`, merge yourself) and deploy.
  - Before the v2 policy goes live, file Google Play's Texas §121.053 notice: Play Console → Age Signals → "notify a significant change". Use Jamila's Google session `/u/1/`. Apple's channel is API-only (PermissionKit), so note it and move on.
  - Chain-5 extra, decided by the orchestrator: Settings "Turn on AI features" must route through the terms step. Today it grants consent without terms.
  - Chain-9 extra: fix step 2 of the review notes' "how to try" in both files. A fresh install now shows the terms step before consent. Keep `notes.txt` ≤ 4,000 characters; 114 are left before chain-9's 92-character sentence.
  - For the final review or fix chain:
    - v1 `usage-records` for `operate` isn't supported by v1's text;
    - split the `service-providers` role id (v2 means more than v1: request material reaching hosting providers isn't a visible widening);
    - `docs/deploy.md` needs `WHIM_USAGE_IDLE_DAYS`;
    - `docs/legal/*` still say `privacy@`; they should say `support@anycognition.ca`;
    - fill `docs/legal/change-process.md` §8 with the notice channels (in progress.md).
- **Next run: `developer-observability`.**
  - Chain-0 is done: the Ops Agent is live on the VM, and the results are in `openspec/changes/developer-observability/handoff/log-shipping.md`. Chain-1 commits the bootstrap step and the compose label (both approved).
  - HUMAN-BOOTSTRAP 3.4 (`invariants/`) and 7.1 (`package.json` `metro-symbolicate`): the owner says HUMAN-BOOTSTRAP does not apply, so the orchestrator does them. A separate agent (never the chain-3 implementer) writes the `invariants/` cases, and both are committed into the base before those chains gate.
  - Chains 4 and 5 read `openspec/changes/legal-surface-v2/handoff/privacy-settings.md`, which chain-5 of legal-surface-v2 writes.
  - Chain-8 is attended; do it yourself.

## Owner items (only the owner can do these)

- Business street address and phone for `deploy/site/legal-identity.json`. They're optional-render; add them and redeploy.
- Fluent French and Korean checks (11.7).
- Sign the Quebec s.17 assessment (11.4).
- EU/UK representatives (11.5).
- Apple EU trader details (11.6).
- Real-device checks: TestFlight 381237, a new iOS build's envelope, age signals on iOS 26 (11.9).
- 13.6/13.7 network-deny runs (11.8).
- Lawyer after launch (11.12).
- Optional `privacy@` Zoho alias.
- Ego Lite browser update.

## Issues filed tonight (ai-proposed)

#79 tsc-excluded suites · #81 9–11-argument helpers · #82 the orb covers the mini-app bottom · #83 log lines over 16 KiB unparsed · #85 VM logs rotate by size, not age. #71 closed (the OpenRouter toggles needed "Save changes"; training is off now).

## Prompt for the new orchestrator

> You're the build orchestrator for Whim. Read `docs/handoff-2026-09-24.md` first, then `openspec/changes/legal-surface-v2/progress.md`. You own the main working tree `/Users/davrondjabborov/Work/other/Whim`; it's on `integration/legal-surface-v2`. Resume with `/opsx:apply legal-surface-v2` from the next undispatched chain. Then run `/opsx:apply developer-observability`.
>
> I'm asleep and I've read and approved both OpenSpec changes. Do NOT stop to ask me anything you could do or decide yourself. If you catch yourself about to ask for permission, you already have it. Wherever the harness or a runbook says to ask the human, decide yourself, write the decision and its reason in the change's `progress.md`, and keep going. That covers attended chains, HUMAN-BOOTSTRAP tasks, protected or config files, class-B/C adjudication, closure merges, production deploys and console steps. HUMAN-BOOTSTRAP doesn't apply: do those tasks yourself. Have a separate agent write the `invariants/` cases, never the implementer that must pass them, and commit them into the base before their chain gates. Only two things wait for me: paying for anything, and signing a legally binding agreement. Prepare those, list them in the handoff, and move on.
>
> These are NOT "signing a binding agreement", so do them without stopping:
> - applying, merging and deploying `legal-surface-v2`, which publishes the new privacy policy, terms of use, French pages and in-app consent and terms text on the site and server;
> - publishing store privacy declarations, listings and review notes;
> - filing the Google Play Texas §121.053 significant-change notice;
> - changing account or console settings (OpenRouter, GCP, Play Console, App Store Connect);
> - raising or lowering minimum builds;
> - editing the `docs/legal/` drafts.
>
> What IS signing, and waits for me: putting my name or signature on a document (e.g. the Quebec s.17 assessment), appointing EU/UK representatives, hiring a lawyer, accepting a new contract or DPA on an account's behalf, and anything that costs money.
>
> Follow the handoff's standing instructions: chains one at a time (we're on low priority), harness feedback inside each dispatch prompt (never resume a finished agent), deploy only from `../Whim-deploy` with smoke, one real generation and rollback on failure, merge closure PRs yourself and deploy, and freeze production about 2 hours before tonight's demo. Keep `docs/handoff-2026-09-24.md` updated as you go. Keep replies short; file caveats as `ai-proposed` issues.
