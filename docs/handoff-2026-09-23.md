# Handoff 2026-09-23: observability, request envelope, legal surface

Written at the end of a long planning session so new sessions can start from files, not the chat.

## State

Everything below is on branch `plan/2026-09-23-observability-legal`, opened as a draft PR into `main`. `main` doesn't have it until that PR is merged.

| Commit | What |
|---|---|
| `859c5f1` | Working operator commands in `docs/deploy.md`; `NODE_OPTIONS` drops node:sqlite's warning from the JSON log (Dockerfile) |
| `9d5acc7` … `ec5c929` | OpenSpec change `developer-observability` (proposal, design, specs, tasks, chains) |
| `09f6706` | `provider.data_collection: 'deny'` on every OpenRouter request |
| `aa4f701` | #63 research and draft copy: `docs/research/legal-surface-2026-09/` |
| `b35cd30` | OpenSpec change `request-envelope`; the request id moved into it from `developer-observability` |

**Not deployed:** `859c5f1`'s Dockerfile change and `09f6706`. They go out with the next backend deploy. Nothing in production changed today.

## Order of work

1. **Merge the PR for `plan/2026-09-23-observability-legal` into `main`** once SonarCloud passes. The apply harness cuts `integration/<change-id>` from `main`, so the two OpenSpec changes have to be on `main` before the first `/opsx:apply`. The branch was renamed off `integration/*` because the harness refuses to start a run while any `integration/*` branch exists.
2. **`/opsx:apply request-envelope`.** Safe before the demo: with both minimum builds at `0`, build 381237 is served as before. Chain-6 (live check) is attended.
3. **`/opsx:apply developer-observability`.** Chains 0, 1, 2, 3, 6 and 7. Chain-0 installs the Ops Agent on the production VM and needs the owner present. Chains 4 and 5 wait for #63's new text.
4. **#63.** The owner answers the seven decisions at the top of `docs/research/legal-surface-2026-09/README.md`. Then an OpenSpec change lands the new copy (consent v2, policy, terms, store declarations, French) plus the README's agent tasks (B6, B8, B9). Then `developer-observability` chains 4 and 5.
5. **One deploy** for all server work, with a live check. It includes `09f6706`.

## Open issues from this session

- #63: rewrite the consent, policy and store text (research done; owner decisions pending)
- #64: minimum app version (covered by `request-envelope`)
- #65: scripted client can exhaust the global limits → device attestation (per-IP limits were already rejected)
- #66: say so when the AI provider is down; circuit breaker
- #67: generated apps all look the same
- #68 `ai-proposed`: throughput routing picks a slow fp4 provider
- #69 `ai-proposed`: archive `public-generation-server` and `store-launch-compliance`
- #71 `ai-proposed`: OpenRouter account training toggles won't stay off (retry 2026-09-24; per-request deny covers it once deployed)
- #72 `ai-proposed`: app updates must never lose data; decide what OS backups include

## Not started

A full pre-launch pre-mortem. This session covered secrets (git history clean), abuse and lockout (#65), forced updates (#64), provider loss (#66), backups (#72), snapshots (in `developer-observability`) and legal (#63). Not yet covered: abuse and content moderation beyond reports, and store-review risks beyond #63. The method: one read-only agent per failure category, a skeptic pass, then `ai-proposed` issues.

## Run order for parallel sessions

The main working tree (`/Users/davrondjabborov/Work/other/Whim`) belongs to the build orchestrator. Its full gate checks branches out there, refuses when tracked files are modified, and validates every OpenSpec change folder it finds (#74). Every other session works in a sibling worktree.

1. **Demo session:** send it prompt D. It moves its uncommitted `docs/demo/stage/` work into its own worktree.
2. **You:** deal with `.codex/config.toml`. It's modified in the main tree, and `gate.sh`'s CONFIG_SET tripwire refuses while any `.codex/**` file differs from the base. Commit it to `main` if it's a change you want, or `git checkout -- .codex/config.toml` if not.
3. **You:** when PR #73's checks are green, mark it ready and merge it.
4. **Start sessions B and L in parallel.**
   - **B**, the build orchestrator, works in the main tree (prompt B).
   - **L**, launch readiness, works in its own worktree (prompt L), and walks you through #63's decisions.
5. **B** applies `request-envelope`. When that run closes (you merge its PR), B takes whichever is ready:
   - L's `legal-surface-v2`, if its PR is merged;
   - otherwise `developer-observability`, parking the run when only chains 4 and 5 are left, and applying `legal-surface-v2` when it lands.
6. **B** then finishes `developer-observability` chains 4 and 5, and does the one production deploy with a live check.

Never in parallel: two apply runs; any session editing tracked files in the main tree; two production deploys; two sessions on the Android emulator.

### Prompt D (demo session)

> Another session is taking over the main working tree `/Users/davrondjabborov/Work/other/Whim` for build runs, and its gate refuses while tracked files there are modified. Move your work out of it now. In the main tree: `git stash push -u -m demo -- docs/demo/stage` and then `git worktree add ../Whim-demo -b demo/stage-2026-09-24 origin/main`. In `../Whim-demo`: `git stash pop`. Confirm `git status` in the main tree shows nothing under `docs/demo/stage`. From now on, edit and commit only in `/Users/davrondjabborov/Work/other/Whim-demo`, and never switch branches in the main tree. Don't touch `.codex/config.toml`.

### Prompt B (build orchestrator)

> You're the build orchestrator for Whim. Read `docs/handoff-2026-09-23.md` first. You own the main working tree `/Users/davrondjabborov/Work/other/Whim`; no other session edits files there.
> Preflight: `git status` shows no modified tracked files (if it does, stop and tell me what). Switch to `main` and `git pull --ff-only`. Confirm no `integration/*` branch exists and that `openspec/changes/request-envelope/` is on `main`.
> Then run `/opsx:apply request-envelope`. After I merge its closure PR, follow step 5 of the handoff's run order: apply `legal-surface-v2` if its PR is merged, otherwise `developer-observability`, parking the run when only chains 4 and 5 are left.
> Stop and ask me before every attended chain and before every production deploy. I'm demoing against production on the evening of 2026-09-24. For each deploy: record the currently deployed image SHA first, run `deploy/smoke.sh` and one real generation after, and on any failure roll back with `deploy/deploy.sh --tag <previous sha>`.
> I merge every closure PR myself. Keep replies short, and file caveats as GitHub issues labeled `ai-proposed`.

### Prompt L (launch readiness)

> You're the launch-readiness orchestrator for Whim. Read `docs/handoff-2026-09-23.md` and `docs/research/legal-surface-2026-09/README.md`.
> Work only in your own worktree: `git fetch origin`, then `git worktree add ../Whim-legal -b proposal/legal-surface-v2 origin/main`. Never edit files in `/Users/davrondjabborov/Work/other/Whim`; the build orchestrator runs gates there.
> 1. Walk me through the README's seven "Decide before public launch" decisions, one at a time: the question in plain words, your recommendation, and why. Record each answer in the README as we go.
> 2. Run `/opsx:propose legal-surface-v2`. It should land the new text (consent screen v2, privacy policy, terms and the terms step, store declarations, review notes, listings, French if decided) and the README's agent tasks. Leave out B10, which `request-envelope` already covers. Build the v2 consent categories on `request-envelope`'s consent-practices table. Make the re-consent rule a spec requirement. Point `research.md` at the README instead of researching again. Store-console steps and the lawyer brief are attended tasks. Run `openspec validate legal-surface-v2 --strict` in your worktree.
> 3. Commit, push `proposal/legal-surface-v2`, and open a draft PR titled "Propose legal-surface-v2 (#63)".
> Don't apply, deploy, or touch any console. Keep replies short, and file caveats as GitHub issues labeled `ai-proposed`.
