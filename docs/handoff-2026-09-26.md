# Handoff 2026-09-26: beta-1 apply run, paused for context before closure

Replaces `docs/handoff-2026-09-25.md` as the file to resume from. Branch **`integration/beta-1`** (pushed to origin
often; the local tip is newer, so push again). Full ledger with every decision (R1–R19) and chain report:
`openspec/changes/beta-1/progress.md`. Chain blocks: `openspec/changes/beta-1/dispatch/*.md`.

## Where it stands

- **All 9 chains and 16 fix chains are merged** on `integration/beta-1`: fix-1…fix-12, plus 3b, 5b, 7b, 8b and the tile-colour fix-8b.
  - gate-full passed at `63b10764` (before fix-12).
  - The fast gate passes at every later merge, including fix-12.
  - **Run gate-full once more at the tip before closure.**
- **Tasks:** 1.1–9.3, 10.1, 10.2, 10.3 and 10.5 are ticked. **Open: 10.4** (device acceptance, nearly done), **10.6–10.8** (post-merge rollout).
- **Production** runs staging commit `29af5041` (smoke green, including the new 426 check).
  - It has cap 5, because fix-3b's default was deployed before the revert.
  - The tip has cap **3** (R19). The post-merge redeploy from `main` applies it.
- **Final candidate build 386656** (tip `63b10764`, before fix-12):
  - Android offline APK at `android/app/build/outputs/apk/offline/`; iOS Release simulator app at `ios/build/sim/...`.
  - **Rebuild after fix-13**, below.

## Evidence and results

- **Upgrade check (10.5): PASS on both platforms**, 382511 → 386656: "4 apps, 5 versions, 2 saved values, the consent grant and the device id are unchanged", and `before.json == after.json`. Evidence in `openspec/changes/beta-1/upgrade-check/{android,ios}/`.
- **Limit rate (10.3):** 40/40 `limit` on the impossible prompts, and 0 false limits in 66 visible-set runs (`flowbench/rate-*.json`).
- **Load test (10.2):** caps stay 3 generations / 2 synthetic runs. Cap 5 reached a 98 % normalized p95 at 7 devices. The record is in `docs/deploy.md`.
- **Device passes:** evidence in `openspec/changes/beta-1/acceptance/`:
  - `android`: pass 1.
  - `android-2`: pass 2.
  - `android-3`: final pass, **all green** except the items under "Next".
  - `ios`: pass 1.
  - `ios-2`: an aborted run, one screenshot.
  - **`ios-3`: the final iOS pass on 386656 was STILL RUNNING when paused.** Read its folder and results first. If the agent's report was lost, rerun it with the same brief (it is in this session's history; the key checks are listed under "Next").

## Next steps, in order

1. **fix-13** (not dispatched yet):
   - (a) The Home action sheet (`HomeScreen.tsx:344` Modal) and `ConfirmSheet.tsx:47` lack `statusBarTranslucent` and `navigationBarTranslucent`. On Android their dim doesn't cover the status bar, and the card stops above the gesture bar (`acceptance/android-3/08a`, `03f`). Match `SheetModal.tsx:70` and pad the card for the bottom inset.
   - (b) The update-ghost tile caption says "Didn't finish" in red; it should say an update is needed (`android-3/10m`).
   - (c) Anything the iOS final pass (`ios-3`) finds.
2. After fix-13: gate-full, a scoped reviewer on `feabe62f..tip`, then rebuild both apps.
3. A quick final device check on both platforms:
   - the fix-12 VoiceOver/TalkBack sheets (the checklist is in fix-12's ledger entry);
   - the fix-13 sheets;
   - the Settings helper line on the first focus after install (1 miss in 11).
   - Devices: emulator `emulator-5560` (Whim_Verify, headless) and simulator `469C2821-24D0-4FF4-B72D-DCDB9184048D`. **Never touch `emulator-5554`**, which is another app's.
4. **Closure** (runbook `.claude/commands/opsx/apply.md` step 12):
   - The ruleset probe already passed.
   - Push, then a draft PR `integration/beta-1` → `main`, then poll checks with `checkverdict`, then Sonar rounds (`scripts/sonar-pr-issues.mjs`).
   - `/git-cleanup`: fold the WIP commits and the ledger churn into semantic commits.
   - Mark the PR ready, then merge it yourself with plain `--rebase` after the checks pass (never `--auto`).
   - Teardown, then fast-forward `main`.
5. **10.6:** redeploy from `../Whim-deploy` at the `main` commit. Run smoke (it includes the 426 check), one real generation (flowbench `tip-splitter-p1`) and a line check. Roll back by `--tag` on failure. **Never roll back below the beta-1 server once beta-1 builds are installed** (see `docs/deploy.md`).
6. **10.7:** `fastlane ios testflight`, then `fastlane android closed build:<n>`. Add the build to the TestFlight **Public beta** group and fix NOT_INVITED testers through the ASC API (memory `testflight-not-invited-api-fix`). **Don't invite anyone**; the owner does that after the demo-phone check.
7. **10.8:** set `WHIM_MIN_BUILD_IOS/_ANDROID` to beta-1's build in `~/.config/whim/deploy.env`, redeploy, and confirm on `/healthz`. Close the fixed issues with evidence: #100, #50, #49, #86, #88, #82, #104, #101, #118, #70, #62, #57, #58, #68, #106, #48, #52, #105, #89, #72, #103, #135.
8. **Harness feedback:** finish `docs/harness-feedback/2026-09-25-beta-1/orchestrator.md` (drafted), update `docs/harness-feedback/README.md`'s run table and themes, and **give the owner the harness summary in chat**. They asked for it at the end.
9. Then `/opsx:archive beta-1`. Archive order: `store-launch-compliance` first (the ai-data-consent note is on #69), and the spec deltas include R17's null rules and the History-back MODIFIED requirement.

## Background processes to stop or keep

- **Keep running for now:**
  - the local stub server on :8787 (at the tip, `WHIM_STUB_DELAY_MS=4000`, cap 1);
  - the 382511 server on :8790 (worktree `~/.cache/whim-upgrade/382511`);
  - emulator-5560.
- Stop them after closure. Remove `~/.cache/whim-upgrade/382511` with `git worktree remove`.
- The `Whim_Upgrade` AVD exists for future upgrade checks.

## What went wrong (all recorded in progress.md)

- **Usage limits killed 5 agents mid-run:** chain-2, fix-4, fix-5b, fix-11, and the iOS re-verification. Each was rescued by committing a WIP and redispatching. fix-11 was merged on the orchestrator's gate with no implementer report; the reviewer later checked its tests.
- **Sonnet subagents stalled at the 600 s watchdog during low-priority capacity.** Opus didn't. A `/feedback` draft exists.
- **I accepted the limit feature on single samples ("4/4").** Its real rate was ~27 % until fix-6. Corrected and re-measured as a rate.
- **Plan premises were wrong several times:** chain-5's two deadlines on one promise, chain-9's stub facts at the old commit, fix-3's relational rule, fix-5's spec-mandated items, fix-8b's screenshot diagnosis. The implementers caught each one.
- **Two deploy failures:** an SSH exit 255, and an adb "device offline" during an upgrade check. Both succeeded on retry.
- **A merge conflict of my own making:** `git add -A` swept an agent's evidence into a commit, and a `| tail` pipe hid a merge failure. The fix-8b feedback file was overwritten because the name "fix-8b" was reused; it's restored as `fix-8b-tile-colour.md`.
- **Filed issues:** #119–#136 (all `ai-proposed`).
