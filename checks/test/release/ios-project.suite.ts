/**
 * Stub — owned by chain-3 (ios-project-identity-and-links, openspec/changes/
 * platform-release-readiness/chains.md). chain-1 only pre-creates this file so
 * `checks/test/release/index.ts` and `checks/test/acceptance.ts` are edited once. chain-3 fills
 * `run()` per task 4.6, driving `checkIosProject` from `scripts/release/lib/ios-project.ts` —
 * never touching the two files above.
 */

export async function run(): Promise<void> {
  // Filled by chain-3.
}
