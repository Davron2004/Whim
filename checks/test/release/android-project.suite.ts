/**
 * Stub — owned by chain-4 (android-build-types-signing-links, openspec/changes/
 * platform-release-readiness/chains.md). chain-1 only pre-creates this file so
 * `checks/test/release/index.ts` and `checks/test/acceptance.ts` are edited once. chain-4 fills
 * `run()` per task 5.6, driving `checkAndroidProject` from
 * `scripts/release/lib/android-project.ts` — never touching the two files above.
 */

export async function run(): Promise<void> {
  // Filled by chain-4.
}
