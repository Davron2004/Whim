/**
 * Stub — owned by chain-2 (hermes-runtime-prerequisites, openspec/changes/
 * platform-release-readiness/chains.md). chain-1 only pre-creates this file so
 * `checks/test/release/index.ts` and `checks/test/acceptance.ts` are edited once. chain-2 fills
 * `run()` per specs/hermes-runtime-prerequisites/spec.md and task 3.4 — never touching the two
 * files above.
 */

export async function run(): Promise<void> {
  // Filled by chain-2.
}
