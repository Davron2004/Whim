/**
 * Stub — owned by chain-6 (brand-asset-generator, openspec/changes/platform-release-readiness/
 * chains.md). chain-1 only pre-creates this file so `checks/test/release/index.ts` and
 * `checks/test/acceptance.ts` are edited once. chain-6 fills `run()` per task 7.5, driving
 * `checkAssets`/`readPngInfo` from `scripts/release/lib/{assets,png}.ts` — never touching the
 * two files above.
 */

export async function run(): Promise<void> {
  // Filled by chain-6.
}
