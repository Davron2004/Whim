/**
 * Content policy acceptance (public-generation-server chain-4). Scaffolded here by chain-1
 * (task 2.5, pre-registered in acceptance.ts); chain-4 fills it in — this module is
 * chain-4's alone to edit.
 */
import { section } from './harness';

export async function runPolicyTests(): Promise<void> {
  section('Content policy');
}
