{
  "displayName": "Whim: generation failures",
  "combiner": "OR",
  "conditions": [
    {
      "displayName": "More than 5 terminal generation failures in an hour",
      "conditionThreshold": {
        "filter": "metric.type=\"logging.googleapis.com/user/whim-terminal-failures\" AND resource.type=\"gce_instance\"",
        "aggregations": [
          {
            "alignmentPeriod": "3600s",
            "perSeriesAligner": "ALIGN_SUM",
            "crossSeriesReducer": "REDUCE_SUM"
          }
        ],
        "comparison": "COMPARISON_GT",
        "thresholdValue": 5,
        "duration": "0s",
        "trigger": {
          "count": 1
        }
      }
    }
  ],
  "alertStrategy": {
    "autoClose": "3600s"
  },
  "notificationChannels": ["{{CHANNEL}}"],
  "documentation": {
    "mimeType": "text/markdown",
    "content": "More than 5 generations ended in a terminal failure in the last hour. Count them by reason on the VM with `whim-admin usage --days 1`, then read the lines in Logs Explorer: `log_id(\"docker\") jsonPayload.msg=\"terminal failure\"` (docs/deploy.md, Operating: Alerts)."
  },
  "userLabels": {
    "whim_spec": "{{SPEC}}"
  }
}
