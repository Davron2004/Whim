# Names only. Copy to ~/.config/whim/deploy.env and fill in (design D24). None of these has a
# committed default. The two store URLs are optional.
WHIM_SUPPORT_EMAIL=
WHIM_ENGINEER_MODEL=
WHIM_REWRITE_MODEL=
WHIM_APP_STORE_URL=
WHIM_PLAY_STORE_URL=
